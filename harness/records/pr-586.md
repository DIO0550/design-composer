# PR #586 DrawnBounds.measure の doc から消費側の列挙を落とす

- マージ日: 2026-09-18
- 関連 Issue: #581
- 差分規模: 1 ファイル / +0 -4

## ゴールと結果

ゴールは「`DrawnBounds.measure` の doc が、消費側が増えるたびに人手で伸びる形でなくなり、
どちらの案を採ったかとその理由が Issue に残っている」。Issue #581 の案 1（列挙を落とす）を
採り、doc 4 行の削除で達成した。ずれは無し。

判断軸を当てる回（案 1 と案 2 のどちらかを決める）だったので、差分そのものより
**採用・却下の理由が事実と合っているか**が中心になった。指摘もそこへ集中している。

## 指摘

### 1. ゴールの後半（Issue への記録）が、計画に作業として存在しなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: ゴール文は「決定と理由が Issue #581 に残っている」を完了条件に含むのに、計画の
  ファイル表は `DrawnBounds.ts` 1 件だけで、Issue へ書く手順がどこにも無かった。コード差分が
  正しくてもゴールの半分が未達で終わる形。セッション URL コメントと `Closes #581` も同様に
  計画の外にあった（実際には両方とも行っていたが、計画が数えていなかった）
- 既存ルール: AGENTS.md「計画・却下した案・その理由は、すべて Issue に追記する」
- 次にどう防ぐか: 今回は記録のみ

### 2. 採用理由が #397 / #569 のコミットメッセージと食い違っていた

- 分類: `plan-rejection-reasoning`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画に「#397 の狙いは列挙を持ち続けることではなかった」と書いたが、`54c870d` の
  採用理由は「module doc は列挙を持たなくなるので、消費側が増えても drift しない」で、
  drift しないと言っているのは module doc の側。`measure` 側の列挙は**人手で保つ前提**だった。
  さらに `c24c387` は「`DrawnBounds.measure` の doc を据え置く」を名指しで却下しており、
  計画は前回やめた理由を読み違えたまま採用理由に使っていた。理由を「#397 の見込み自体が
  翌日崩れた」に書き直し、#569 が却下した「据え置く」と今回の「落とす」が別物であることも
  Issue に明記した
- 既存ルール: `implementation-flow` フェーズ 3 手順 5
- 次にどう防ぐか: 今回は記録のみ

### 3. 手本として引いた `CanvasPointer` の doc が、実物では既に古かった

- 分類: `plan-scope-premise-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 採用理由 3 は「`CanvasPointer` は消費側を役割で 1 文書いている」を手本として引いたが、
  `grep -rn "CanvasPointer" src/` の実測では消費側は 5 フックで、`CanvasPointer.ts:6` の
  「ズーム / パン・移動・リサイズ」に `use-range-select` が入っていない。**手本にした doc 自体が
  drift している**ので、根拠として成立しない。ファイルを開いて 1 文を読んだだけで、
  その 1 文が指す実体を数えていなかった
- 既存ルール: `implementation-flow` フェーズ 3 手順 3 / `claim-verification` 手順 1〜3
- 次にどう防ぐか: 今回は記録のみ

### 4. 採用理由と却下理由が、同じ形を根拠と反証の両方に使っていた

- 分類: `plan-rejection-reasoning`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 採用理由 3（役割で 1 文書く形が正しい）と、案 3 の却下理由（役割でも消費側が増えれば
  伸びる）が互いに逆を向いていた。指摘 3 の事実（`CanvasPointer` が実際に drift している）を
  入れると却下側で一貫するので、理由 3 を取り下げて案 3 の却下理由の補強へ回した
- 既存ルール: `implementation-flow` フェーズ 3 手順 5
- 次にどう防ぐか: 今回は記録のみ

### 5. 「未決の判断: 無し」の裏に、未決が 1 つ残っていた

- 分類: `plan-open-decision-declaration`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: スコープ外にした `canvas-measure.ts:11-12` を「**必要なら**マージ後に別 Issue」と
  書いており、「分けて起票する」でも「分けない理由を書く」でもない状態で計画を締めていた。
  実装中・PR 作成時に手が止まる判断。plan-reviewer から「`pr-556.md` 指摘 9・`pr-570.md`
  指摘 4 と同形で 3 回目」と示された。その場で #585 を起票し、#581 からリンクして解消した
- 既存ルール: AGENTS.md「タスクの分割」
- 次にどう防ぐか: 今回は記録のみ（3 回目なので `harness-growth` の集計対象）

### 6. ゴール達成を確かめる手段が計画に無かった

- 分類: `plan-goal-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: doc だけの変更なので typecheck / lint / テスト / VRT はすべて緑のままで、「列挙が
  残っていない」を機械的に確かめる手段が計画に 1 つも無かった。
  `grep -n "use-" src/features/canvas/utils/DrawnBounds.ts` → 0 件、と
  `git diff --stat` が 1 ファイル・削除のみ、を完了条件に足した
- 既存ルール: `.claude/agents/plan-reviewer.md`「ゴールの達成を確かめる手段」
- 次にどう防ぐか: 今回は記録のみ

### 7. いちばん近い代替案が 2 つ、却下案の一覧から漏れていた

- 分類: `plan-rejection-coverage`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: (a)「列挙を落としたうえで、列挙しない理由の Why not を 1 行だけ残す」案が漏れていた。
  計画が挙げていた「grep で引ける、と doc に書く」案は一般論を書く案で別物。
  (b)「列挙を維持したまま drift を機械で検出する（層を上げる）」案も挙がっていなかった。
  どちらも採らないと決めたが、理由を #581 に残して却下の検討を閉じた
- 既存ルール: `implementation-flow` フェーズ 3 手順 6
- 次にどう防ぐか: 今回は記録のみ

### 8. スコープ外の根拠を、Issue 本文の引き写しで締めていた

- 分類: `plan-scope-premise-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `canvas-measure.ts` を据え置く根拠を「Issue 本文のスコープ外の定義に従い」で
  締めており、実物に当てたかが計画から読めなかった。`pr-570.md` に「Issue のスコープ外の
  根拠をそのまま信じず実物に当てた」ことが効いた前例がある。実際にファイルを開いて
  4-13 行を読み、列挙が**置き場所の根拠**として書かれている（`measure` の doc とは当てる
  判断軸が変わる）ことを確かめてから、確かめた旨ごと Issue に書き直した
- 既存ルール: `implementation-flow` フェーズ 3 手順 3
- 次にどう防ぐか: 今回は記録のみ

### 9. 先例を覆す差分なのに、覆したことの書き残し先が決まっていなかった

- 分類: `comment-missing-divergence`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「消費側をここに列挙する」は #397 と #569 で 2 回、明示的な理由付きで下された判断で、
  どちらも「列挙を人手で保ち続ける」前提に立っている。その前提を捨てる差分なのに、覆した
  ことが PR 本文にもコミット本文にも無い状態でレビューへ出した。`rules/coding.md` は
  コメントへの Issue 番号を禁じているので、置き場所は Issue とコミットメッセージしか無い。
  書かないと、次に消費側を増やす人が `c24c387` と同じ手順（doc に 1 項目足す）を再実行する。
  PR 本文に表で、コミット本文の「影響とトレードオフ」に文章で明記した
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」/ AGENTS.md（Issue と
  PR の分担）。ただし語彙としては `comment-missing-issue-only` の**逆向き**（コードではなく
  PR / コミットへの転記が抜けている形）で、既存の分類にちょうど合う行が無い
- 次にどう防ぐか: 今回は記録のみ

### 10. 昇格させない理由が、コード上から読めない状態になっている

- 分類: `comment-missing-precedent-echo`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `harness/records/pr-462.md:45` は「`src/domains/` へ昇格させない理由」が
  `DrawnBounds.ts` の Why not として残っていたと記録しているが、現在のファイルに該当記述は
  無い。`git log --follow` で追うと、この差分ではなくファイル作成時点（`f6cd942`「コメントから
  Issue 番号を落とす」）で既に落ちている。**この差分の退行ではない**が、doc を最小化した結果
  として「なぜ `features/canvas/utils/` に在るのか」に答える記述がコード上に 1 つも無くなる。
  書き戻さないと決めた（置き場所は #394 で決着済みで、Issue と `harness/records/` が持っている。
  書き戻すと同じ判断根拠が 3 箇所に増える）
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ

### 11. `.claude/hooks/` が発火しない実行環境だった

- 分類: `hook-environment`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されずに通り、検証エージェントのマーカー
  （`/tmp/design-composer-verification-agents-<session>`）も発火ログの見出し
  （`/tmp/design-composer-firings-<session>.log`）も無かった。`.claude/hooks/README.md`
  「発火しているかを確かめる（カナリア）」の表では**本当に不発**の枝。`plan-reviewer` /
  `implementation-reviewer` をどちらも通した後の実測なので、マーカー無しは取りこぼしでは
  ない。ゲートは git hooks と CI が担っており（`core.hooksPath = harness/githooks` が
  `pnpm install` の prepare で設定され、push 時に 7 検査が実際に走った）、素通りした操作・
  欠陥は無い。禁止コマンド（`git add -A` / `npx`）もこのセッションでは実行していない
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

- **計画の書き直しが 1 回。** `plan-reviewer` の 8 件を受けて、採用理由 2 つ（指摘 2・3）を
  事実に合わせて差し替え、却下案を 5 → 7 に増やし、未決（指摘 5）を #585 の起票で解消した。
  決定そのもの（案 1）は変わっていない。指摘 2・3・8 はいずれも「書く前に実物へ当てる」で
  防げた形で、`claim-verification` を計画フェーズで呼ぶ手順が入っている位置の指摘に当たる
- **実装の手戻りは無し。** doc 4 行の削除で、`implementation-reviewer` もブロッカー無し
- **検証エージェントの完了待ちで手順が詰まった。** `implementation-flow`「実行中に git add /
  commit / push を挟まない」を守って待つ間、セッションの stop フックが「未コミットの変更が
  ある」と繰り返し警告し、待機のために空の往復が数回発生した。規約どおり待ってから
  コミットしたので差分への影響は無い
- **`node_modules` が無い状態から始まった。** push 前検査の前に `pnpm install` が要る
  （この install が git hooks の `core.hooksPath` も設定する）

## 発火

- 発火: 計測対象外(フック不発)

## うまくいったこと

- **`plan-reviewer` が採用理由の事実誤りを 2 件（指摘 2・3）捕まえた。** どちらも結論
  （案 1 を採る）は合っていたが理由が事実と食い違っており、実装の前に直せた
- **却下案を 7 つ書いて Issue に残した。** 特に #569 が名指しで却下した「据え置く」と
  今回の「落とす」が別物であることを書いたので、次に同じ案が再浮上したときに読み分けられる
- **未決を「必要なら」で残さず、その場で #585 を起票した。** スコープ外 2 件を 1 つの Issue に
  まとめ、#581 と双方向にリンクした
- **push 前検査 10 件を `&&` で連ねず 1 つずつ実行した。** フック不発環境だったが CI は一発で
  緑（VRT も差分 0 件 / 180 passed）
- 人のレビュー指摘は 0 件（`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも
  確認。`get_comments` は自分の投稿と bot のみ）。CI の失敗も 0 件

## 規約への反映

無し。このスキルの管轄は記録を 1 ファイル足すところまでで、集計と介入の判断は
`harness-growth` が持つ。指摘 5（`plan-open-decision-declaration`）は plan-reviewer から
「`pr-556.md` 指摘 9・`pr-570.md` 指摘 4 と同形で 3 回目」と示されており、指摘 9 は既存の
分類にちょうど合う行が無い（`comment-missing-issue-only` の逆向き）。どちらも数えるのは
次の棚卸しの仕事なので、ここでは記録に留める。
