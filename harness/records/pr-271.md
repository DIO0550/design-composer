# PR #271 [architecture] features/assets を切り出す（部品パレット・部品化）

- マージ日: 2026-08-21
- 関連 Issue: #251
- 差分規模: 46 ファイル / +476 -226

## ゴールと結果

ゴールは「部品（Assets）のパレット一式が `features/assets/` に独立し、`EditorState` ではなく値を受け取る形になっている」。達成した。#174 の feature 分割の 1 つ。

移したのは 6 モジュール（`assets-panel` / `component-list` / `primitive-list` / `asset-row` / `create-component` / `componentization`）、`src/domains/` へ昇格したのは 1 つ（`node-template`。消費 feature が editor と assets の 2 つになったため）、`src/components/` へ上げたのが 1 つ（型アイコン）。公開 API は `AssetsPanel` / `CreateComponent` / `AssetGrab`（型）の 3 つに絞った。

human レビューは 0 件（reviews / review threads とも 0）。CI は 6 チェックすべて 1 回目で success、視覚差分も 0 changed。**指摘はすべてサブエージェント（`plan-reviewer` 8 件 / `implementation-reviewer` 4 件）と自己修正・フックから出た。**

テストは 343 ファイル / 2338 テスト。

**この回の中心は、Issue 本文の指示が規約に反していたこと（指摘 1）。** Issue は `AssetGrab` を `src/types/` へ置くよう名指ししていたが、この型は `NodeTemplate` を包んだ `Option` を抱えるため `src/types/` → `src/domains/` の import になる。`plan-reviewer` がこれを検出し、`features/assets/types/` に置いて公開 API から type export する形へ計画を差し替えた。

## 指摘

### 1. Issue 本文が名指しした置き場所が、依存方向のルールに反していた

- 分類: `layer-dependency`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: Issue #251 は「掴む側と落とす側の**両方が要るなら `src/types/` へ**」と書いており、計画はそのまま `src/types/AssetGrab.ts` を採った。しかし `AssetGrab` は `dragged: Option<NodeTemplate>` を持ち、`NodeTemplate` は同じ計画で `src/domains/` へ昇格する。既存の `src/types/Brand.ts` / `ValueOf.ts` はどちらも import 0 件で、この層に依存を持ち込む前例が無かった。さらに前提の「両方が要る」自体も成り立たず、`opened-document-editor:301` は `grab={{ dragged, onGrab }}` をインラインで組んで**構造的に代入**しており型名を参照していない。名前の消費者は assets 配下の 5 コンポーネントと `LeftPane` の計 6 箇所だけだった
- 既存ルール: rules/architecture.md「依存方向のルール」（`types/` は domains / services / features を import してはならない）
- 次にどう防ぐか: 今回は記録のみ（`features/assets/types/` に置き公開 API から type export する形へ差し替え、却下案 6 として「なぜ Issue の指示を採らなかったか」を Issue へ追記した。**Issue 本文の指示も規約に照らして検算する対象**で、名指しされた置き場所をそのまま計画に写さない）

### 2. 公開 API の一覧に、消費側が名前を要する型が入っていなかった

- 分類: `module-api`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画は `features/assets/index.ts` の公開 API を `AssetsPanel` / `CreateComponent` の 2 つと書いていた。指摘 1 の差し替えで `AssetGrab` を feature 内に置く以上、`LeftPane` が props の型として名前を要するため type export が要る。計画の 2 箇所（置き場所の節と公開 API の節）で `AssetGrab` の扱いが食い違っていた
- 既存ルール: rules/architecture.md「モジュールの公開API」
- 次にどう防ぐか: 今回は記録のみ（公開 API の表を「export / 誰が使うか」の 2 列で書き直し、3 つに揃えた）

### 3. ストーリーの fixture を差し替える手順が計画に無かった

- 分類: `plan-scope-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `create-component/index.stories.tsx` は `SampleEditorState`（`features/editor/__stories__/`）を使っている。計画どおり assets 側へ移すと **assets → editor の `__stories__/` への deep import**（feature の公開 API ではない）になるが、計画のファイル表にこのストーリーの fixture をどう用意するかの行が無かった。移すファイルは数えていたのに、移した先から見て参照が成立するかを当てていなかった
- 既存ルール: implementation-flow フェーズ 3「立てた収集条件を実際にコードへ当てて検算する」/ rules/architecture.md「依存方向のルール」
- 次にどう防ぐか: 今回は記録のみ（`features/assets/__stories__/sample-assets-document.ts` を新設して `DesignDocument` を直接組む形にし、`features/tokens/__stories__/sample-token-document.ts` の先例に揃えた。**feature をまたぐ移動では、移すファイルの参照先が移動後の層から引けるかを 1 本ずつ当てる**）

### 4. 同じヘルパーを使う 2 箇所のうち片方しか計画に挙がっていなかった

- 分類: `plan-scope-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `left-pane/index.stories.tsx` は `setupAssetGrab()` と `grabbingComponent("primary-button")` の**両方**を使っているが、計画は前者を直書きに変える行しか書いていなかった。ヘルパー名 1 つで grep して、同じファイルが使っているもう 1 つの export を数え落とした形
- 既存ルール: implementation-flow フェーズ 3「呼び出し側を grep で数える」
- 次にどう防ぐか: 今回は記録のみ（ヘルパーを移すときは**モジュールの export を列挙してから**それぞれの呼び出し側を数える。ヘルパー名 1 つで grep しない）

### 5. 却下の根拠が、規約の判断軸と違う軸で書かれていた

- 分類: `ownership-reasoning`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `Componentization.forSelection(document, singleName)` について、対型（`NodeSingleSelection` 等）を作らない理由を「1 度呼ぶだけの入口で対型を新設するほどではない。既存 API（`EditorState.select(state, name)`）と揃わない」と書いた。しかし rules/architecture.md の判断軸は**呼び出し回数ではなく「対で意味を持つか」**で、`document` 単体でも `singleName` 単体でも componentization は決まらない以上、定義上まさに「対」に当たる。比較先の `EditorState.select` も「対で意味を持つ 2 値」ではなくミューテータで、比較として成立していなかった。**結論（2 引数のまま渡す）は変わらないが根拠が別だった**
- 既存ルール: rules/architecture.md「ロジックの帰属先」（2つの値が常に対で意味を持つなら、対を表す型を作る）
- 次にどう防ぐか: 今回は記録のみ（根拠を「対を持ち回って複数のメソッドが引き直す関係が無い（`forSelection` が 1 度受けて変換して終わる）」へ載せ替え、`TokenSelection` を型にした先例との違い — あちらは同じ対から `token` / `isSelected` / `collectReferrers` が引き直す — を併記した）

### 6. 既存の型を流用すると、到達しない値まで型上通せてしまう

- 分類: `illegal-state`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `AssetRow` の `kind` prop から editor 側の `SelectionKind` 依存を外すため、計画は `TypeGlyphKind`（`"artboard" | "Box" | "Text" | "component"`）へ付け替えるとしていた。しかしパレットの行に `"artboard"` は到達しない（`PrimitiveList` は `PrimitiveType`、`ComponentList` は `"component"` を渡す）。既にある型へ付け替えるのは楽だが、値域が広がる方向の流用だった
- 既存ルール: rules/coding.md「不正な状態を型で表現できなくする」
- 次にどう防ぐか: 今回は記録のみ（`PrimitiveType | "component"` に絞った。`PrimitiveType` は `@/domains/primitive-schema` から取れて features から import でき、`TypeGlyph` の `kind` へはそのまま代入できるので、新しい型を立てるコストもかからなかった。**依存を外すための付け替え先は、値域が広がらない側を先に探す**）

### 7. 「既存テストをすべて残す」の件数が実数と合っていなかった

- 分類: `plan-scope-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画は移設対象の既存テストを「33 件」と書いたが、`grep -c "^test("` で数え直すと **67 件**だった（assets-panel 14 / component-list 20 / primitive-list 7 / create-component 20 / componentization 6）。どの数え方で 33 になったのかは本文から復元できなかった。**pr-267 の指摘 5・6 と同じ「実測していない数を実測と銘打った」系統**
- 既存ルール: rules/testing.md「API を作り替えるときは、既存テストの観点を先に数える」/ implementation-flow フェーズ 3
- 次にどう防ぐか: 今回は記録のみ（ファイル別の内訳表として 67 件を貼り直し、API が変わる 26 件（componentization 6 + create-component 20）と import 経路だけが変わる 41 件に分けて Issue へ追記した）

### 8. 既存の通しテストが引き続き保護することの明示が無かった

- 分類: `plan-integration-coverage`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: この回は「配線を保ったまま feature を切り出す」ので通しテストを新設しないが、計画にその判断が書かれていなかった。書かれていないと、挙げ忘れなのか要らないと確定したのかが区別できない。実際には `opened-document-editor.componentize.test.tsx`（7 件）と `.asset-drag.test.tsx`（9 件）がパレットからキャンバスまでの経路を画面越しに確かめている。**pr-267 の指摘 8 と同じ形**
- 既存ルール: `plan-review.md`「単体しか無いなら、通しが要らないと確定したのか挙げ忘れなのかを 1 行で書かせる」
- 次にどう防ぐか: 今回は記録のみ（「既存の通しがそのまま生きる」の 1 行と、その 2 ファイル・16 件を計画へ明記した）

### 9. ストーリーの grab 値が、テストヘルパーの中身を書き写した形になっていた

- 分類: `duplication-test`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 指摘 4 の対応で `left-pane/index.stories.tsx` に `IdleGrab` / `GrabbingPrimaryButton` を直書きしたが、これは `features/assets/__tests__/asset-grab.ts` の `setupAssetGrab()` / `grabbingComponent()` の中身をそのまま写したもので、コメント自身が「helper の中身と同じ形」と自認していた。`AssetGrab` に 3 つ目のフィールドを足したときに片方だけ直る形になる。直書きにした理由は「features 間で `__tests__/` を deep import しない」だったが、`features/assets/__stories__/sample-assets-document.ts`（指摘 3 で新設）という共有場所の先例が同じ PR 内にあった
- 既存ルール: rules/testing.md「テスト用ヘルパーの置き場所」（同じヘルパーを 2 つ以上に書いたらその時点で共通化）
- 次にどう防ぐか: 今回は記録のみ（features/assets 側へ集約した。**deep import を避けるための直書きは重複を作る**ので、避けた先に共有場所を作れないかを先に見る）

### 10. 新設した配線を、どのテストも守っていなかった

- 分類: `test-coverage-wiring`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: レビュアーが実際に壊して確認した。`left-pane/index.tsx` の `isFrozen={EditorState.isFileInvalid(state)}`（マージ後は 112 行目）を `false` 固定にしても `true` 固定にしても、テストが 1 件も落ちない。`create-component.edge.test.tsx` の凍結ケースは `rerender` で `isFrozen` を直接切り替えており**この配線を通っていない**、`opened-document-editor.frozen.test.tsx` は `inert` と見出しの「凍結中」しか見ておらず `Create component` の `disabled` を見ていない、`componentize.test.tsx` には凍結を触るテストが無い。**props 分割で単体テストを値渡しにした回に出る形**
- 既存ルール: rules/testing.md「古典学派のテスト」/ `implementation-review.md`「テストが守っているかの観点」
- 次にどう防ぐか: 今回は記録のみ（`opened-document-editor.frozen.test.tsx` へ「Assets を開いて下書きを入れているとき、ファイルが壊れると Create component が押せなくなる」を 1 件足した。対照として壊す前に押せることを確かめてから外部から壊す形。**`state` を値の props へ割ったら、割った先ごとに配線を通すテストが要るか数える**）

### 11. 移設で成り立たなくなった名指しが doc に残った

- 分類: `comment-stale-edit`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `Componentization` を `features/assets/domains/` へ移し、シグネチャを `(document, singleName)` に変えたあとも、doc に「選択できるもののうちノードでないのは artboard だけ（`EditorState` の線引き）」が残っていた。`features/assets` は `features/editor` を import できないので、**assets のドメインが editor の型を根拠にしている**形になる。pr-267 の指摘 10 と同じ「層が変わって参照できなくなる名指し」
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ（指摘 12 の対応でコメントごと書き直し、`EditorState` の名指しを外した）

### 12. 前提をコメントで持っていたため、想定外の入力が黙って別の状態になった

- 分類: `type-vocabulary`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 指摘 11 のコメントは単に古いだけでなく、**実際の穴を隠していた**。`Componentization.forSelection` は `DesignDocument.findNode` が外れたら無条件で `{ kind: "artboard" }` を返しており、ドキュメントに存在しない名前（消えたノードを指したままの選択など）が黙って artboard として扱われる。現在の呼び出し口は `EditorState.singleName(state)` だけなので実害は無いが、それを成立させているのはコメントに書いた前提であって型ではなかった
- 既存ルール: rules/coding.md「前提をコメントで書くのは、型で閉じたことにならない」
- 次にどう防ぐか: 今回は記録のみ（`DesignDocument.findArtboard` を引き直し、ノードでも artboard でもない名前は `{ kind: "unselected" }` に落とす形へ変えた。新しい状態を union に足さずに済み、UI は既存の「何も選んでいない」表示をそのまま使える。テストも 1 件足した（実在しない名前 `home-removed` → `unselected`。同じドキュメントの実在 artboard `home` → `artboard` を隣のファイルが固定しているので、決め打ちの実装でも判定を消した実装でも落ちる））

### 13. 視覚差分が出ると予告したが、実際は 0 件だった

- 分類: `なし`
- 出どころ: CI
- 内容: assets-panel のストーリーから `LeftPanePanel`（`Assets` のタイトル帯）を外して div に置き換えたため、Issue と PR 本文に「VRT に差分が出るので `visual-approved` ラベルを付ける」と予告した。実際の VRT は **0 changed / 11 new / 11 deleted / 94 passed** で、ストーリー ID が `features/editor/` 配下から `features/assets/` 配下へ移ったことで差分ではなく new + deleted のペアになり、ラベルは不要だった。**ストーリーを別 feature へ移す回では、見た目を変えても VRT は「変更」として出ない**（ID が変わるため比較相手が居ない）
- 既存ルール: なし（rules/ui-verification.md「Storybook の視覚差分」は閾値未満の差が passed になることは書いているが、ID が変わったときの扱いは書いていない）
- 次にどう防ぐか: 今回は記録のみ（PR 本文の予告を実測に合わせて訂正した。**予告を PR 本文へ書くなら、外れたときに訂正する**。この 1 件では語彙に昇格させない）

### 14. `.claude/hooks/` が発火しない実行環境だった

- 分類: `hook-environment`
- 出どころ: フック
- 内容: push 前の手順どおり `echo hook-canary` を実行したが **deny されずに通った**。`.claude/settings.json` の配線が読まれないリモート実行環境だった。**pr-267 の指摘 19 と同じ形で連続 2 回目**
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ（`pnpm install` の `prepare` が `core.hooksPath = harness/githooks` を配線しており、`git push` では pre-push が発火して typecheck / oxlint / biome / doc コメント / テスト規約すべて通過した。CI も 6 チェック全 success。ゲートは効いているので手順は変えず、PR 本文と Discord 通知に記録した）

## 手戻り

**PR 本文が MCP 経由の更新で HTML エスケープされ、書き直した。** `mcp__github__update_pull_request` に渡した本文が保存時にエスケープされ、コードスパン内の `"` が `&#34;` のまま残り、`Option<NodeTemplate>` の `<NodeTemplate>` が HTML タグとみなされて欠落した。GitHub Flavored Markdown はコードスパン内の実体参照を復号しないため、表のセルに `&#34;` が literal で表示され、かつ `AssetGrab` を `src/types/` へ置かなかった理由（NodeTemplate を抱えること）が本文から消えていた。`<` `>` `"` を避けた表記へ書き直し、再取得して修復を確認した。**リポジトリのコードには影響しないが、レビュアーが読む唯一の説明が壊れていた**ので、MCP 経由で本文を更新したら取得し直して確認する。

計画から外れた判断が 1 つあり、それは **Issue 本文が名指しした `AssetGrab` の置き場所を採らなかったこと**（指摘 1）。理由と却下案を Issue #251 へ追記したうえで進めた。

指摘 4 の対応（直書き）が指摘 9 を生んで再度直した、という連鎖が 1 件。

上記以外は計画どおり。CI の失敗は 0 回。

## うまくいったこと

- **Issue 本文の指示を規約に照らして検算した。** `plan-reviewer` が Issue の名指し（`src/types/`）を規約違反として検出し、実装前に差し替えられた。Issue の指示をそのまま計画へ写していたら、依存方向の違反が main に入っていた
- **却下案を「結論」と「根拠」に分けて扱った。** 指摘 5・6 はどちらも結論の妥当性ではなく根拠の誤りで、結論を変えずに根拠だけ載せ替えた。Issue には載せ替え前後の両方を残したので、次に同じ検討が来たときに「回数で判断してはいけない」も辿れる
- **レビュアーがミューテーションで実測した指摘（10）をそのまま受けた。** 「配線を壊しても 1 件も落ちない」は頭で判断できる形ではなく、実測されたからこそ争点にならなかった
- **コメントの誤りを型の穴として扱った**（指摘 11 → 12）。コメントを直すだけで終えれば、存在しない名前が artboard になる挙動は残っていた

## 規約への反映

**無し。** 今回の 14 件は、次の理由でいずれもこの回では介入しない。

- **`plan-scope-verification`（指摘 3・4・7）** は pr-227 で観点層へ介入済みで、pr-267 でも 2 件出ている。3 件出たので層を上げる候補だが、**数えるのは `harness-growth` の管轄**なので集計はそちらへ回す
- **`plan-integration-coverage`（指摘 8）** は pr-267 の指摘 8 と同じ形。同上
- **`layer-dependency`（指摘 1）** は #257「依存方向を CI / lint で強制する」が引き取る前提のスコープ（`types/` → `domains/` の辺は機械判定できる形なので、そちらの検査項目に足せる）。この PR では強制を足さない
- **`hook-environment`（指摘 14）** は pr-267 に続き 2 回目だが、これは実行環境の性質であって規約の抜けではない。git hooks / CI がゲートとして効いていることは両回とも確認できている
- **`分類: なし`（指摘 13）** は 1 件。語彙への昇格は 10 件溜まってから（`harness-growth` の逃し弁）
- 残る `module-api` / `ownership-reasoning` / `illegal-state` / `duplication-test` / `test-coverage-wiring` / `comment-stale-edit` / `type-vocabulary` は各 1 件

集計と介入の判断は `harness-growth` の管轄なので、ここでは記録を残すまでとする。

**この回の介入（`harness-growth` / pr-284）:** `layer-dependency` が未介入のまま
6 件再発した（pr-124/267×2/269×2/271）ため、Issue #257 が引き取る前提だった
`.oxlintrc.json` の `no-restricted-imports` で強制した。`domains/ → libs/` は
既存 5 件の債務があるため `warn` に留め、他 3 方向は `error`。詳細は #284。

- 対策済: `layer-dependency` 層=hook at pr-284
