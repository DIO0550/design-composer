# PR #734 src/utils の前提・等しさの基準・try/catch を直す

- マージ日: 2026-09-23
- 関連 Issue: #729
- 差分規模: 14 ファイル / +167 -49

## ゴールと結果

ゴール（`src/utils/` の中で、doc に書いて規律に頼っていた前提・同じモジュールで割れていた「等しい」の
基準・`utils/` の try/catch が無くなり、どの入力に対しても doc どおりの答えを返す）は達成した。
`src/utils` の try/catch 0 件・`parseText` の参照 0 件・doc の前提 0 件を grep で確かめた。
Issue の 5 件のうち `Json.optional` の `Option` 化は呼び出し側が `src/domains/dcmp/` の 5 モジュールに
及ぶので #733 に分けた。CI は 1 回目で全チェック通過、VRT の差分 0 件、人のレビュー指摘 0 件でマージされた。

## 指摘

### 1. コメントの中だけで `Json.parseText` を名指ししている箇所がファイル表に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `app-state-json` の `@param` と `implementation-reviewer.md` の 1 文を、呼び出し 2 件とは別に直す
  必要があった。後者は参照先を差し替えるだけだと `JsonText` が `src/utils/Json.ts` にあるように読める。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「計画を渡す前に踏む 8 つの手順」

### 2. `distinct` の doc「`===` で見るので `NaN` は残らない」が逆の意味になることを計画に書いていなかった

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 等しさの基準を SameValueZero に変えると既存の `@returns` が嘘になる。3 メソッドの doc の
  書き換えを行単位でファイル表に足した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 3. ゴールを確かめる手段が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: try/catch 0 件・`parseText` 0 件・前提の文言 0 件はどれも grep で確かめられるのに、確認コマンドが
  計画に無かった。実装後に 3 本の grep を走らせて PR に残した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3

### 4. 引数を広げた `toCapitalCase` が camelCase 以外で何を返すかが未決のまま「未決: 無し」と書いていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 引数名を広げるなら `width-mode` / `position_x` の答えを決める必要があった。doc を流儀ではなく
  操作（先頭以外の大文字の前に空白、`-` `_` は区切らない）で書くことにした。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「未決の判断」

### 5. `prependIfAbsent` も挙動が変わるように読める計画だった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `prependIfAbsent` は元から `includes`（SameValueZero）で、挙動が変わるのは `distinct` と
  `findEqual` だけ。「3 つとも統一」を「割れていたのを揃える、変わるのは 2 つ」に書き直した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 5

### 6. 今の実装でも通るテストを、修正を守るテストと区別せずに並べていた

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `isDigit("")`・空白 2 文字・`prependIfAbsent` の NaN は旧実装でも通る。退行を捕まえる目的で
  残すことを計画に書き、旧実装で落ちる 5 件と分けて PR に書いた。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 7. `isWhitespace` に空文字のケースが無かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `isDigit` には空文字のケースを置いたのに、同じ「1 文字でなければ false」の約束を持つ
  `isWhitespace` には無かった。両方に揃えた。
- 既存ルール: rules/testing.md「振る舞い(仕様)をテストする」

### 8. `json-text` の失敗を normal に入れ、成否だけを見る計画だった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 失敗系は edge に分ける規約。成否だけでは `err("")` を返す実装でも通るので、文言が空でないことを
  1 つの assert で見る形にした。
- 既存ルール: rules/testing.md「配置と命名」

### 9. `findEqual` が `null` / `undefined` の要素を見つけても `none` を返し、doc と食い違っていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `Option.fromNullable(array.find(...))` なので「見つからない」と「見つかった値が null」を区別
  できない。`Option.some` は `NonNullable` しか受けないので型は据え置き、`@returns` に書いた。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 10. 同じ述語で探す走査が `some` / `findIndex` / `find` の 3 通りに割れていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `isSameValueZero` で揃えたのに走査は 3 通りだった。非公開の `indexOfEqual` を 1 つ置いて
  3 メソッドをその上に乗せた。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 11. doc の「`0` と `-0` も等しい」を守るテストが無かった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `isSameValueZero` を `Object.is` に替えるミューテーションで 3384 件がすべて通った。
  `distinct([0, -0])` が `[0]` になるテストを足した。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 12. doc の「`-` `_` は区切りとして扱わない」を守るテストが無かった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `toCapitalCase` に `-` を区切る変更を入れても全件通った。計画の回答で doc に書くと決めた挙動
  なので、`CaseStyle.edge.test.ts` に `width-mode` → `Width-mode` を足した。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 13. フックが発火しない環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されず、検証エージェントのマーカーもこのセッションの発火ログも
  無かった（README の表で「本当に不発」）。起動時の作業ディレクトリがリポジトリの親（`/home/user`）。
  `npx` / `git add -A` の実行と、検証エージェント実行中の git 操作が無いことを履歴で確認した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- **Issue を分けた**: `Json.optional` を #733 に分離した（分割の理由は #729 と #733 に書いた）
- **計画を plan-reviewer の指摘 8 件で直した**（指摘 1〜8）。`JsonText.parse` の `: String(error)` の枝は
  指摘を受けたが残した（`catch` の束縛は `unknown` で、`as Error` は境界以外の `as` 禁止に当たる）
- **実装を implementation-reviewer の指摘 4 件で直した**（指摘 9〜12）。走査を `indexOfEqual` へ寄せたのは
  計画に無かった変更で、Issue に追記した
- 旧実装に戻して新テストを走らせ、修正を守る 5 件が落ちることを確かめた
- push 前検査（typecheck / lint / biome / test / ハーネスの検査 17 本）と Storybook の撮影範囲、CI は
  1 回目で通過した

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。この回は Issue #729 の実装で、記録を数えた結果の介入ではない。`対策済` を書けるのは
`harness-growth` の棚卸しだけ。

- 対策済: `comment` 層=case-law at pr-743
