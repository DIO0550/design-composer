# PR #500 [ui] レイヤーの絞り込みの見せ方を UI 案と docs/06-ui.md に載せる

- マージ日: 2026-09-12
- 関連 Issue: #498（#423 から割った）
- 差分規模: 2 ファイル / +37 -6

## ゴールと結果

レイヤーを名前で絞り込む機能の**見せ方**を UI 案と `docs/06-ui.md` に載せ、実装（#423）が拠り所にできる状態にする。達成した。

ずれが 1 つある。**着手時のゴールは #423（実装）だった。** UI 案に検索欄が無いことを確かめた時点で判例に従って止め、見せ方を決める単位を #498 へ割ったので、この回のゴールは docs に変わっている。

## 指摘

### 1. UI 案が描いていない対象を、確認を仰いだ記録なしで決めていた

- 分類: `ui-fidelity`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 検索欄は UI 案のどの画面にも無く、位置・案内文・絞る範囲・0 件の見せ方のすべてが UI 案の外の判断だった。それを #469 / PR #476 を前例に「Figma を既定にして自分で倒す」形で計画へ書き、「未決の判断: 無し」としていた。前例は**決めることと たたき台 がオーナーの手で Issue 本文に書かれた状態から始まっており**、自分で割った Issue はその状態になっていない。指摘を受けて実装へ入らず、選択肢と根拠を Issue へ書いて止めた
- 既存ルール: harness/case-law/ui.md「UI 案が対象を描いていない」（「対象が UI 案のどの画面にも無いと確かめたら、その時点で止めて確認する」）
- 次にどう防ぐか: 今回は記録のみ。判例には既に書かれており、`plan-reviewer` が実際に捕まえた（すり抜けていない）

### 2. 「未決の判断: 無し」と書いたが、実装が二通りに分かれる点が残っていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 1 回目の計画で「未決はありません」と書いたが、0 件のときの優先関係・`Artboards` のドラッグ・`+` の消失・共通規則の範囲・検索欄がスクロールするか、の 5 点は #423 がどちらでも実装できる状態だった。pr-476 の指摘 7（同じく「無し」と書いて 8 点残っていた）と同型
- 既存ルール: .claude/skills/implementation-flow/SKILL.md「フェーズ 3」（未決の判断）
- 次にどう防ぐか: 今回は記録のみ。再発なので `harness-growth` が数える対象

### 3. 引用した行番号が実物とずれていた

- 分類: `plan-comment-reference`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: #423 へ引き継ぐ「崩れる前提」に `assets-panel/index.tsx:24` / `assets-panel.search.test.tsx:5` と書いたが、実物は `:25` / `:11`（`:5` は `import` 行）。引き継ぎ先がこの行番号を拠り所にする
- 既存ルール: .claude/agents/plan-reviewer.md「ファイル表に挙がっているか」
- 次にどう防ぐか: 今回は記録のみ

### 4. 案内文の語が、このリポジトリで既に別の対象を指していた

- 分類: `naming-prefix-collision`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `Search layers` の `Layers` はレールの行き先・パネル見出し・節見出しの 3 箇所に出る。1 回目の計画では検索欄を節の直下に置きツリーだけを絞る形だったので、位置で対象を示しておきながら**語では対象が曖昧**になっていた。`document-tree/index.tsx:243-244` に同じ衝突が既に記録されている
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ。オーナー判断で絞る対象がパネル全体になり、`Search assets` と同じ 1:1 の関係になったため曖昧さ自体が消えた

### 5. 検算として書いた等式が、実際に走らせると必ず落ちる形だった

- 分類: `plan-scope-count-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画に `payload.count("</")` = `body.count("</")` = 1183 と書いたが、実測は後者が **0**（raw 側では `</` にエスケープされている）。スクリプト側は往復の等式で assert していたので実害は無かったが、計画の文をそのまま検算として実行すると必ず落ちる
- 既存ルール: .claude/skills/implementation-flow/SKILL.md「フェーズ 3 手順 2」
- 次にどう防ぐか: 今回は記録のみ

### 6. 絞る対象を広げたのに、`Artboards` の一覧が持つ不変条件を数えていなかった

- 分類: `plan-implicit-condition`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「絞り込み中に止めるのはツリーのドラッグだけ」は、ツリーだけを絞る案の結論をそのまま持ち越していた。`Artboards` の一覧も `useReorderDrag` で並べ替えられ、その doc は「並びが 1 つしか無いので、落ちる先が並びの外を指すことは構造上ありえない」と**一覧が全件であることを前提にした不変条件**を書いている。絞った瞬間に崩れる
- 既存ルール: .claude/agents/plan-reviewer.md「既存のコードが前提にしている不変条件を、この計画が崩さないか」
- 次にどう防ぐか: 今回は記録のみ

### 7. 0 件でパネル本体を置き換えると、artboard 追加の `+` も消える

- 分類: `plan-implicit-condition`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「`Assets` と同じく節ごと知らせに置き換える」という類推が効いていなかった。`Assets` の `Create component` は footer で本体の外にあるが、`Layers` の `+` は `ArtboardList` の見出し＝置き換え対象の中にある
- 既存ルール: 同上
- 次にどう防ぐか: 今回は記録のみ

### 8. 「`×` は持たない」の理由が、判例が名指ししている形だった

- 分類: `ui-fidelity`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「UI 案に綴りが無いので持たない」と書いていた。判例の「『ゴースト』語彙が UI 案に 0 回なので出さなかった（pr-400）→ オーナーは持っていてほしかった」とまったく同じ形。**Chromium で実測すると、検索用の入力欄は打つと `×` を描き Esc で語が消える**ので、既存の `Assets` の欄と食い違う仕様を新しく作るところだった
- 既存ルール: harness/case-law/ui.md「UI 案に語彙が無いことは『作らない』の理由にならない」 / rules/coding.md「外部の挙動は動かして確かめる」
- 次にどう防ぐか: 今回は記録のみ。指摘 1 と同じ分類で同じ回に 2 件出ている

### 9. 「`Artboards` の `+` は 1 枚目を足す唯一の導線」が偽だった

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 差分に書いた理由が事実と違っていた。`canvas-toolbar` が `artboard を追加` を常時有効で持つ。結論（`+` は置き換えの対象にしない）は変えず理由を差し替えた。**同じ誤りが `artboard-list/index.tsx:104-105` にもとからあり**、docs 側がそれを引き写していた（同じ判断根拠が 2 箇所にある形）
- 既存ルール: rules/coding.md「コメントは実装と一致させる」 / harness/case-law/ui.md「結論が変わらない指摘でも直す」
- 次にどう防ぐか: 今回は記録のみ。`claim-verification` は呼んでいたが、**既存コメントを引き写した主張**は「実物と照合する」対象として意識できていなかった

### 10. 「スクロールするのは下の一覧だけ」が UI 案の構造とずれていた

- 分類: `ui-fidelity-misread`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: UI 案ではみ出しを切り落とす器に入っているのは**ツリーだけ**で、`Artboards` の一覧も検索欄もその外にある。「下の一覧」と書くと一覧もスクロールすると読める。マークアップは見たうえで、器の範囲を取り違えた
- 既存ルール: rules/ui-verification.md「見るのはスクリーンショットではなくマークアップ」
- 次にどう防ぐか: 今回は記録のみ

### 11. 仕様書の中で、新しい節が既存の知らせと衝突していた

- 分類: `なし`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「検索語が空のときに中身が無いのは知らせない」という判定文を `Layers` に当てると、`ArtboardList` が既に出している「artboard がありません」を出すな、と読めた。あわせて **artboard 0 枚 + 語が入っているとき**にどちらが出るかも決まっていなかった。`rules-consistency` と同じ形だが、あちらの定義は**規約（`rules/`）**を指しているので当てはめていない。仕様書（`docs/`）に足す記述を同じファイルの既存の記述へ当て直す観点は、どの規約にも無い
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ。`harness-growth` が語彙の穴として見る材料

### 12. 仕様の主語を、実物より広く書いていた

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「左ペインのパネルは検索欄を持つ」と書いたが、レールの行き先は 3 つで `Tokens` には UI 案にも実装にも欄が無い。あわせて「`name` が一意なのでどの artboard にあっても一覧に現れる」も、現れる理由は一意性ではなく「配下に一致がある artboard は残す」という直後の規則だった（部分一致なので 1 つの語が複数に当たり、一意性が効く場面ではない）
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 13. フックが発火せず、禁止コマンドが素通りした

- 分類: `hook-environment-guard-miss`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されず、検証エージェントのマーカーも発火ログの見出しも無かった（README の表の 4 行目 = 本当に不発）。そのため `npx -y playwright@1.56.1 --version` が通った（`block-npx.sh` が発火していれば deny されたもの）。バージョン表示だけでリポジトリには何も入っておらず、以降はグローバルの playwright を直接呼んだ。`git add -A` の類は実行しておらず、コミットは 2 ファイルをパス指定で add した
- 既存ルール: .claude/hooks/README.md「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ。`block-npx.sh` は**セッション中の行為そのものを止める**フックなので git hooks / CI へ移せない（README「カバー範囲と残る穴」）

## 手戻り

計画を 2 回書き直している。

1. **1 回目 → 2 回目**: 判例に従って止め、オーナー判断（絞る範囲は案 A → 案 B）を受けて、置き場所・案内文の扱い・0 件の見せ方が連動して変わった。`plan-reviewer` も 2 回通した
2. **実装の直前**: 計画では `src/features/sidebar/components/left-pane/index.tsx` の 1 行もこの回で直すとしていたが、そこは doc ではなく**この JSX が何を並べないかの理由を兼ねた JSX コメント**で、欄が生えていない段階で足すと「コードが持っていないものを持っている」と読めるか、一時的な事実を理由に置くことになる。#423 へ移し、この PR は `docs/` 2 ファイルだけにした

## 発火

- 発火: 計測対象外(フック不発)

## うまくいったこと

- **判例が止め時を決めてくれた。** 「対象が UI 案のどの画面にも無いと確かめたら、その時点で止めて確認する」が無ければ、筋の通る理由を揃えたまま実装まで進んでいた。実際にオーナーの判断は私の推奨（案 A）と違った
- **外部の挙動を動かして確かめたことで、既存実装と食い違う仕様を書かずに済んだ**（`×` と Esc）。判例が名指ししている形だと指摘されたのがきっかけ
- UI 案の書き戻しは pr-476 が見つけた綴りをそのまま引き継ぎ、編集前に往復のバイト一致を assert したのでファイルを壊さずに済んだ

## 規約への反映

**無し。** この回に出た分類はいずれも既存の規約・判例・観点に書かれており、`plan-reviewer` / `implementation-reviewer` が実際に捕まえている（人・bot へのすり抜けは 0 件）。介入するかどうかは再発数を数えてから決める話なので、`harness-growth` の管轄に置く。

指摘 11 の `なし` だけは対応する規約が無い（仕様書に足す記述を同じファイルの既存の記述へ当て直す観点）。これも数える材料として残すに留める。
