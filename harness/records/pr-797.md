# PR #797 PR が閉じる Issue の判定表で、問い合わせ失敗と閉じ忘れを区別して固定する

- マージ日: 2026-09-26
- 関連 Issue: #804(元は #794 の PR として出した)
- 差分規模: 4 ファイル / +62 -45

## ゴールと結果

元のゴールは #794「opened 直後の問い合わせ直しを #787 から #789 の形に置き換える」だった。作業中に #789 自体が main へマージされて #794 が閉じたため、この PR に残ったのは #789 への上乗せだけになった。オーナー判断(B)で #804 を立て、そちらのゴールとして出し直した。
#804 のゴールは達成した。判定表の問い合わせの節は、期待を結果の種別(`pass` / `missing` / `fetch-failed`)で見るようになった。これで「問い合わせ直しの途中で 5xx が 3 回続いたら問い合わせ失敗として赤」と「`PR_ACTION` が空なら落とす」を、実装を壊せば落ちる形で固定した。
人の GitHub 上のレビュー指摘は 0 件だった(`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも確認した。コメントは bot の投稿と自分のテストケース表・停止コメントだけ)。
CI は、main を取り込んだ後の head(`8d34bdc`)で全チェックが緑、VRT も差分 0 件だった。最初の head では main と衝突していて、CI が 1 本も走らなかった。

## 指摘

### 1. 判定表の「5xx が 3 回続いたら赤」の行が #787 の実装でも通る

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: #789 の判定表は期待を終了コードで書いていた。問い合わせ失敗も閉じ忘れも exit 1 なので、5xx が空の結果として流れる #787 の実装でも通っていた。
  期待を結果の種別に変えた。#787 の実装に当てると NG になることを実測した。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 2. PR_ACTION が空の経路を判定表で踏めなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `${PR_ACTION:-synchronize}` で既定値を埋めていたので、`:?` を消しても表が落ちなかった。既定値をやめて各行で与え、「イベント種別が空なら問い合わせずに赤」の行を足した。
- 既存ルール: rules/testing.md「既定値・フォールバックがある処理では、既定値と違う答えになる入力を選ぶ」

### 3. 計画の比較コマンドが 2 点 diff で、関係の無い差分が混ざる形だった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `git diff origin/main <#789>` と書いていた。2 点で比べると #788 の `src/` 差分も混ざる。実際の作業は 3 点 diff とパス指定で行っていた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 3(検証コマンドが意図した範囲を覆っているか)

### 4. #789 と #786 の後始末が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 同じ中身を別 PR で出すのに、#789 を閉じるか・#786 をどう扱うかを計画に書いていなかった。#789 は自分の PR ではないので閉じず、オーナーに委ねると Issue に書いた。
- 既存ルール: なし

### 5. pr-783.md を据え置く理由が「方針と逆」だけだった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `対策済` の行を書くのは harness-growth だけで、窓の閉じ位置を直すかどうかも harness-growth が決める、という理由を足した。
- 既存ルール: `.claude/skills/harness-record/SKILL.md`「`対策済` の行は書かない」

### 6. スタブが result_json と同じ応答の形を組み立て直していた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: gh スタブの heredoc の jq が、同じファイルの `result_json` と同じ形を作っていた。`result_json` で先にファイルへ書き出し、スタブは `cat` するだけにした。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 7. 覆う範囲の列挙が 3 つのままで、本体と判定表の 2 か所にあった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 行を足して覆う範囲が 4 つになったのに、列挙は 3 つのままだった。同じ列挙が 2 ファイルに独立してあり、片方だけ直せる状態でもあった。判定表側だけに置き、本体からは参照するようにした。
- 既存ルール: rules/coding.md「規約の適用範囲」(doc の重複)

### 8. edited を待たない理由が、Closes を足した edited に触れていなかった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 閉じ忘れに気づいて本文へ Closes を足した edited も反映待ちで赤になりうる。コメントはこれに触れていなかった。実装は変えず、次の編集・push か再実行で揃う、という Why not を 1 行足した。
- 既存ルール: rules/coding.md「Why と Why not は別物として書く」

### 9. refetch_until_linked が「リンクされるまで続ける」と読めた

- 分類: `naming`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 実際は最大 2 回で止まり、opened 以外・記録 PR ではすぐ止まる。`awaits_closing_issue_link` に揃えて `refetch_while_awaiting_link` に改名した。
- 既存ルール: rules/naming.md「名前と実体を一致させる」

### 10. 環境変数の前置きを代入文の外に書き、スクリプトへ渡っていなかった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: 判定表で `VAR=... output="$(bash script)"` と書いた。代入だけの文になるので、前置きの変数はスクリプトへ export されず、問い合わせの節が全行 NG になった。前置きを `$(...)` の内側へ移した(コミット前)。
- 既存ルール: rules/coding.md「外部の挙動は動かして確かめる」

### 11. 同じ Issue(#794)を #789 と並行して進め、作業中に #789 がマージされた

- 分類: `parallel-issue-work`
- 出どころ: `自己修正`
- 内容: #794 は「#789 の形で #787 を置き換える」Issue で、#789 自体も同じ置き換えを含んだまま動いていた。このセッションは #789 の中身を別 PR(#797)へ移して出したが、その間にオーナーが #789 をマージし、#794 が閉じた。
  #797 は main と衝突して CI が 1 本も走らず、チェックインで気づいて判断待ちで止めた。オーナーの判断(B)で #804 に切り替えた。
- 既存ルール: なし(duplicate-issue-pr の検査は、#797 が衝突して CI が走らなかったので発火しなかった)

### 12. 取り込んだ main 自体が typecheck / test で赤かった

- 分類: `なし`
- 出どころ: `自己修正`
- 内容: #792 で消えた `TokenReferrer.toText` を、並行してマージされた #793 のテストが呼んでいた。この PR の差分とは無関係。
  修正 PR が #799 と #800 の 2 本あったので、#799 の内容を持ち込んで push した。別々の PR が互いの変更を知らずにマージされて壊れる形で、対応する規範が無い。
- 既存ルール: なし

### 13. Claude Code のフックが不発の環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: push 前に単独で打った `echo hook-canary` が、2 回とも deny されずに通った。検証エージェントを 2 回通した後も、このセッションのマーカーと発火ログが無かった(README の「本当に不発」の枝)。
  セッションは webhook(issues.assigned)で起動し、起動ディレクトリはリポジトリの親(`/home/user`)だった。不発の間に `npx` と `git add -A` は使っておらず、検証エージェントの実行中の git 操作も無い。PR 本文・Issue・Discord に記載した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- **計画段階**: plan-reviewer の指摘(1〜5)で、テストの期待の書き方と後始末の扱いを計画に足した。実装前なので作業の手戻りは無い
- **実装段階**: 環境変数の前置きの位置を直した(10)。implementation-reviewer の指摘(6〜9)を 4 コミットに分けて足した
- **PR 後**
  - #789 のマージで前提が変わり、判断待ちで 1 回止まった(11)
  - オーナー判断で #804 を立てた。main を取り込み(衝突した 3 ファイルはこちら側を採用)、main の赤の修正を #799 から持ち込んだ(12)
- どれも Issue #794 / #804 に追記済み

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。記録のみ(集計と介入の判断は `harness-growth` の管轄)。
