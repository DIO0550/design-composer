# PR #582 リサイズハンドルの左上・右上・左下の角と、左辺・上辺を掴めるようにする

- マージ日: 2026-09-18
- 関連 Issue: #396
- 差分規模: 32 ファイル / +1685 -377

## ゴールと結果

ゴールは「選択中の artboard / 絶対配置のノードで、左上・右上・左下の角と左辺中央・上辺中央の
ハンドルを掴んで大きさを変えられる。始点側の辺を引いたときは反対側の辺がその場に留まり、
大きさと位置が Undo 1 回で戻る。フロー配置のノードでは掴めずカーソルも出ない」。

達成した。Issue の「決めること」4 つのうち 1〜3 をこの回で答え、4（帯で角とみなす範囲）は
#578 へ分離した。

ゴールとのずれ: 無し。ただし計画段階で決めた規則を 1 つ、実装検証のあとで変えている
（帯が両側に重なったときの優先順位。下記「手戻り」）。

## 指摘

### 1. 仕様書がファイル表に無い

- 分類: `plan-file-omission`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `docs/06-ui.md` が「掴めるのは右辺・下辺と右下の角だけ」「例外はリサイズハンドルの
  掴める 3 箇所」と明文で書いている現状をそのまま書き換える変更なのに、計画のファイル表に
  `docs/` が 1 行も無かった。該当は 5 箇所（147 / 150 / 151 / 155 / 171 行）。`rules/testing.md`
  「テストケースの根拠は仕様・設計ドキュメントに求める」に照らすと、この回の「フローでは掴めない」
  はどこにも書かれていない仕様だった。
- 既存ルール: `plan-review.md`「変更で壊れる既存の資産を洗い出しているか」
- 次にどう防ぐか: 今回は記録のみ

### 2. コメントの中でだけ名指ししている対象が、ファイル表に挙がっていない

- 分類: `plan-comment-reference`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: コードの参照だけを grep してファイル表を作ったので、doc / Why コメントが名指ししている
  シンボルを数えていなかった。実測で 5 箇所（`artboard-canvas/index.tsx` の「この 3 箇所を例外
  として」と `NodeResize.handles`、`node-resize` の `ResizeHandleThicknessPx` の doc、
  `axis-length` の `AxisLengths` の doc、`setup.tsx`）。うち `axis-length` は**ファイル表にも
  無かった**。
- 既存ルール: `plan-review.md`「ファイル表に挙がっているか」
- 次にどう防ぐか: 今回は記録のみ

### 3. 呼び出し側の件数が実測と合っていない

- 分類: `plan-scope-count-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `NodeResize.handles` を production 1 件と書いたが実際は 2 件（`use-node-resize` を
  数え落とし）。`DesignDocument.resize` のテストを 10 件と書いたが 9 件で、10 件目は
  コメントだった。`onResize` は**テストを 1 件も数えていなかった**（`use-node-resize.normal.test.tsx`
  だけで 29 箇所・13 本）。grep はしたが、production / テスト / コメントを分けて突き合わせて
  いなかった。
- 既存ルール: `implementation-flow` フェーズ 3 手順 2
- 次にどう防ぐか: 今回は記録のみ

### 4. 署名変更で必ず落ちるファイルがファイル表から漏れている

- 分類: `plan-file-omission`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `use-node-resize.normal.test.tsx`（13 本が通知値そのものを assert）・
  `design-document.constraint.test.ts`・`editor-state.resize-continuity.test.ts`・
  `editor-state.multi-selection.test.ts`・`artboard-canvas.context-menu.test.tsx`・
  ストーリー 3 本が表に無かった。指摘 3 の数え漏らしと同根で、件数を数えてもファイル名を
  表へ写していなかった。
- 既存ルール: `plan-review.md`「変更で壊れる既存の資産を洗い出しているか」
- 次にどう防ぐか: 今回は記録のみ

### 5. 作り替えで消えるテスト観点の移送先が書かれていない

- 分類: `plan-migration-mapping`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `node-resize.handles.test.ts` については「既存 9 件を移す。観点は落とさない」と書けて
  いたが、同じ扱いが要る `node-resize.gesture.test.ts` の `lengthsAt` 9 件・`gripFor` 6 件・
  ローカルヘルパー `grabbedAt` について移送先が無かった。`gripFor` は `ResizeHandleAnchor` から
  `grip` を落とすので、引き方ごと変わる。
- 既存ルール: `plan-review.md`「リファクタ・分割で消えるテストケースの移送先が計画に書かれているか」
- 次にどう防ぐか: 今回は記録のみ

### 6. `AxisEnd` を足す理由に、既存の `Side` との関係が書かれていない

- 分類: `naming-vocabulary-alignment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `(axis, end)` の組はこのリポジトリでは既に `Side`（`unit/side`）で、`CanvasBounds.side`
  がその座標を返す。`AxisEnd` を新設するなら「`Side` を使わない理由」が要るのに計画に無かった。
  さらに `CanvasBounds` では `edge` が「軸に沿った終端」、`end` が「並ぶ向きに沿った終点」の
  意味で既に使われており、`AxisEnd` の値 `"end"` が 3 つ目の意味になる。
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ

### 7. 型名 `ResizeSubject` がリポジトリの語彙に無い

- 分類: `naming-absent-metaphor`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `subject` は `docs/06-ui.md` に 0 回、`docs/Design Composer.html` にも `grep -oi` で
  0 回。`ResizeTarget` を避けた判断（`RepositionTarget` / `DropTarget` が「落とし先」で埋まって
  いる）は妥当だが、代わりが「対象」を意味する汎用語では `rules/naming.md`「内容を表さない
  汎用語を使わない」に触れる。
- 既存ルール: rules/naming.md「内容を表さない汎用語を使わない」
- 次にどう防ぐか: 今回は記録のみ

### 8. `ResizeEdit.movedTo` の語形が既存と割れ、中身も書かれていない

- 分類: `naming-vocabulary-alignment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 既にあるのは `Placement.moveBy` / `CanvasBounds.movedBy` / `Artboard.withCanvasPosition`
  で、`movedTo` は 3 つ目の形。しかも `movedTo` は `node-resize.lengthsAt` のローカル lambda 名と
  `use-node-drag` の引数名として既に別の意味で使われている。計画には引数も戻り値も書いて
  いなかった。`CanvasBounds.placedAt` と同じ語形の `placedAt` へ直した。
- 既存ルール: rules/naming.md「メソッド名を既存のドメインに揃える」
- 次にどう防ぐか: 今回は記録のみ

### 9. 両軸まとめた `Offset` で渡すと、動いていない軸まで書き換わる

- 分類: `plan-derived-scope-omission`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `position: Option<Offset>` は x と y を必ず対で運ぶので、右上の角（幅は終点側・高さは
  始点側）を引いたときも x が書かれる。`Placement.moveBy` は `Math.round` するので、手で
  `y: 40.5` と書いたノードの**左辺**を引いただけで y が 41 になる。同じ失敗は
  `followPropEdits` で既に 1 度踏まれていて、そこに Why が残っている。計画はこの経路を
  数えていなかった。書き込む側で「今の座標と値が変わる軸だけ」を編集にする形で直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」の隣（`followPropEdits` の Why）
- 次にどう防ぐか: 今回は記録のみ

### 10. 自動配置の artboard に座標を書き込む副作用が数えられていない

- 分類: `plan-derived-scope-omission`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `Artboard.createInitial` は `canvasPosition` を書かないので、アプリで追加した artboard は
  すべて自動配置。その左辺・上辺を 1 回引くと座標が書かれ、**以後は並べ替えの対象から外れる**
  （`docs/06-ui.md`「並べ替えが動かすのは自動配置される artboard だけ」）。しかも右辺を引いた
  ときは座標を書かないので、同じリサイズなのに引く辺で挙動が割れる。計画はこの経路を
  スコープに数えていなかった。スコープに入れ、docs とテストへ足した。
- 既存ルール: `plan-review.md`（`plan-derived-scope-omission`）
- 次にどう防ぐか: 今回は記録のみ

### 11. 「未決の判断: 無し」が成立していなかった

- 分類: `plan-open-decision-declaration`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「未決の判断: 無し」で締めたが、実装中に必ず手が止まる判断が 5 つ残っていた
  （`ResizeEdit` の生成の口の中身 / `positionAt` が返すのは既定位置か最終位置か /
  ハンドル直押しの経路で掴んだ時点の位置をどこから得るか / 帯が 4 本になったときの優先順位 /
  指摘 9 の「動いていない軸を書くか」）。
- 既存ルール: `plan-review.md`「未決の判断が残っていないか」
- 次にどう防ぐか: 今回は記録のみ

### 12. 自動配置の位置を確かめるテストが、既定値と同じ答えになる入力だった

- 分類: `test-default-input`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「座標を書いていない artboard は自動配置で置かれる位置を持つ」を先頭の artboard で
  書くと期待値が `{x:0, y:0}` になり、自動配置を丸ごと壊して常に原点を返す実装でも通る。
  2 枚目を対象にし、期待値を「前の artboard の幅 + `AutoArrangeGap`(32)」＝ `{x:392, y:0}` に
  直した。
- 既存ルール: rules/testing.md「既定値・フォールバックがある処理では、既定値と違う答えに
  なる入力を選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 13. 「位置は書き換えない」を、位置を持たない対象で確かめようとしていた

- 分類: `test-assert-unfalsifiable`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「右辺を右へ引くと幅が増え、位置は書き換えない」を位置を持たない対象で書くと
  `position` が常に `none` になるだけで、位置の出し分けを丸ごと壊しても通る。位置を持つ対象の
  右辺を引いて `edit.position` が `none` であることを見る形へ直した。同じ形が
  「位置を持たないリサイズでは座標の prop を書かない」（`x`/`y` を持たないノードで見ていた）
  「絶対配置のノードは親から見た座標を持つ」（親を原点に置き `x === y` にしていた）にもあった。
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ

### 14. 対になる「掴める側」の確認が無い

- 分類: `test-coverage-branch-asymmetry`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: overlay に「位置を持たないノードでは左辺・上辺・左上にカーソルが出ない」はあったが、
  「位置を持たないノードでも右辺・下辺・右下にはカーソルが出る」が無く、位置が無いときに
  8 箇所すべてを殺す実装が通ってしまう。`design-document.constraint.test.ts` にも
  「始点側から縮めたときの追従」（この変更で初めて通る経路）が無かった。どちらも足した。
- 既存ルール: rules/testing.md「『空になる』を確かめるテストには、同じ入力の中に集まる側を
  1 件置く」
- 次にどう防ぐか: 今回は記録のみ

### 15. 帯の優先順位を決めたのに、テストが 1 件も守っていなかった

- 分類: `test-coverage-conflict`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `bandEndAt` の doc が「始点側と終点側の両方に入るのは長さが帯 2 本ぶん未満のときだけで、
  そのときは始点側を取る」と主張していたが、`Object.values(AxisEnds).find(...)` を
  `.reverse().find(...)` にしても `src/features` の 1311 件すべて通過した。テストの要素が
  200×100 で、**帯が重なる入力（辺の長さが 16px 未満）が 1 件も無かった**。規則を
  「近いほうの辺を取る」へ変えたうえで、対になる 2 件を足した（下記「手戻り」）。
- 既存ルール: `implementation-review.md`「テストが守っているかの観点」
- 次にどう防ぐか: 今回は記録のみ

### 16. `Math.round` を外しても全件通る

- 分類: `test-coverage-branch`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `Placement.positionPropEdit` の `@returns` は「座標は整数」と書いているのに、
  `Math.round` を外しても `src/domains` + `src/features` の 3005 件すべて通過した。到達経路は
  実在する（小数座標の絶対配置ノードの左辺を引くと `grabbedAt.x` が小数のまま届く）。
  `design-document.resize.test.ts` の小数テストは `y: 90.5` を**動かさない**側に置いていたので
  丸めの側が素通りしていた。「小数の座標を持つノードを動かすと、書かれる座標は整数になる」を
  足した。
- 既存ルール: `implementation-review.md`「テストが守っているかの観点」
- 次にどう防ぐか: 今回は記録のみ

### 17. `ArrangedArtboard.positionAt` の `?.` が到達しない防御

- 分類: `over-guard`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 呼び出しは `fromArtboards` の `map` の index と、範囲を確かめた後の 2 つだけ。`?.` を
  外しても 1311 件すべて通過した。さらに `@returns` の「並びの外を指したときは既定の位置の
  計算だけが効いて、間隔の累積がそのまま返る」は、**起こらない状態のためにもっともらしい嘘の
  位置を返す仕様を書き足している**形だった。`?.` と該当の 1 文を落とした。
- 既存ルール: implementation-flow フェーズ 6（過剰なフォールバック）
- 次にどう防ぐか: 今回は記録のみ

### 18. `artboardPosition` の `index < 0` も到達しない

- 分類: `over-guard`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 直前に `DesignDocument.findArtboard` で存在を確かめてから呼ぶので、`findIndex` が `-1` に
  なる経路が無い。ガードを外して `Option.some(...)` 固定にしても 1311 件すべて通過した。
- 既存ルール: implementation-flow フェーズ 6（過剰なブロック）
- 次にどう防ぐか: 今回は記録のみ

### 19. `find` と `findIndex` で同じ並びを 2 回走査している

- 分類: `duplication-traversal`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 指摘 18 と同じ箇所で、`findArtboard`（`find` 相当）と `findIndex` が同じ artboard の
  並びを別々に走査していた。`resizable()` で index を 1 回だけ引く形にしたら、指摘 17・18 の
  不在の分岐ごと消えた。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ

### 20. 「その端で掴めるか」を比率側と帯側で別々に導いている

- 分類: `duplication-derivation`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `axisGrabAt`（箇所の比率から）と `grabAt`（帯から）が、同じ「その端で掴めるなら
  `{length, end}` を組む」判断を別々に書いていた。`grabAt` 側は narrowing のために同じ条件を
  2 回書いてもいた。`grabFor(resizable, length, end)` を 1 つ切って両方から呼ぶ形へ直した。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ

### 21. (軸, 端) → 辺 の対応が `CanvasBounds.start` にだけ手書きで残っていた

- 分類: `duplication-derivation`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: この回で `EdgeSides` を新設し `edge` を `edgeAt(..., AxisEnds.End)` へ寄せ、`edgeAt` の
  doc に「対応はここ 1 箇所だけに置く」と書いた。ところが `start` は
  `direction === "row" ? Sides.Left : Sides.Top` のままで、doc の主張と実装が食い違っていた。
  `end` は既に `edge` 経由なので、対の片方だけが残っている形でもあった。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ

### 22. 掴める軸と位置を 3 箇所で導出し、凍結の判定が片方にしか無い

- 分類: `duplication-derivation`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `NodeResize.geometry(selection)` を描画用・ハンドル経路・帯経路の 3 箇所で作っており、
  **凍結（`isFrozen`）の判定が描画側にしか乗っていない**という非対称がそこにあった
  （production は `inert` が止めるので実害は無いが、判定が 1 箇所ではない）。`ArtboardCanvas` が
  1 回だけ作ってフックへ渡す形にし、凍結も `NodeResize.Unresizable` で 1 箇所で決まるようにした。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ

### 23. 同じ Why を 2 ファイルへ書いた

- 分類: `duplication-rationale`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `unit/axis` と `canvas-bounds` の両方に「対応表をどちらが持つか」の理由を書いていた。
  `canvas-bounds` 側だけに残した。丸めの理由（「手で 40.5 と書いた座標が 41 になる」）も
  `design-document` の新設箇所と既存 `followPropEdits` の 2 箇所に例ごと重複していたので、
  新設側は 1 行の参照に留めた。
- 既存ルール: rules/coding.md「規約の適用範囲」（doc の重複にも及ぶ）
- 次にどう防ぐか: 今回は記録のみ

### 24. その Why の根拠が、実物に照らして成立していなかった

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 指摘 23 の 2 つの Why はどちらも「`unit/side` は `unit/axis` を import しないので」を
  理由にしていたが、置き場所の問題は**逆向き**（`unit/axis` が `unit/side` を import できるか）。
  `src/domains/unit/offset/index.ts` は `@/domains/unit/axis` を import しており、`unit` 内の
  モジュール間 import は実際に使われている。理由を「辺の座標を返す操作が `CanvasBounds` にしか
  無いので表もそこに置く」へ書き直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 25. 絶対配置の座標を `Offset` として見る経路が 2 通りある

- 分類: `duplication-uncovered-shape`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `NodeResize` は `child.placement` から `{x, y}` を手で組み直し、`nodePositionPropEdits` は
  `AbsolutePlacement` をそのまま `Offset.along` に渡していた。`Placement.offset(placement)` を
  足して両方をそこへ揃えた。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ

### 26. 公開する必要のない型を export していた

- 分類: `module-api`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `export type AxisGrab` は外部から 1 箇所も参照されていない（overlay は `grip.width.end` と
  値で触るだけで型名を使わず、テストも import していない）。`export` を外した。
- 既存ルール: rules/architecture.md「`index.ts` から export するのは外部に公開する必要が
  あるものだけ」
- 次にどう防ぐか: 今回は記録のみ

### 27. 公開 API が、指示された位置を書けない相手に静かに成功を返す

- 分類: `comment-missing`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `DesignDocument.resize` に `ResizeEdit.placedAt(...)` とフロー配置のノード名を渡すと、
  長さだけ書かれて `Ok` になる。キャンバス経由では到達しない（`origin` が `none` なら
  `placedAt` が組まれない）が、公開 API なので指示と結果の食い違いが呼び出し側に見えない。
  `Result.err` にはせず、`@returns` に「位置が書かれるのは artboard と座標を持つノードだけ」を
  明記した。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ

### 28. 仕様書の「8 箇所 / 5 箇所」が、同じ節の前提と両立しない

- 分類: `docs-consistency`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: この回に `docs/06-ui.md` へ足した「artboard と絶対配置のノードは 8 箇所すべて / フローは
  5 箇所」は、同じ節の冒頭「`fixed` のノードにのみ表示し、`hug` / `fill` ではハンドルを
  出さない」と同時に成立しない入力がある（`placement: "absolute"` かつ `widthMode: "hug"` なら
  6 箇所）。「2 軸とも `fixed` のとき」の但し書きを足した。
- 既存ルール: AGENTS.md「規約の更新」の docs 版
- 次にどう防ぐか: 今回は記録のみ

### 29. パンの例外の書き方が、帯まで含んで読める

- 分類: `docs-consistency`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「例外はリサイズハンドルの掴める箇所」へ直したが、同じ節が「掴み口は描いてあるハンドル
  より広い」と定義しているので帯まで含んで読める。実際には帯は `canvas-surface` の上にあって
  `onPointerDownCapture` が先に止めるので、space 中は掴めない。改訂前の「掴める 3 箇所」は
  四角を指していて成立していた。「描いてあるハンドル（四角）のうち掴める箇所」へ直し、
  `artboard-canvas/index.tsx` の同じ言い換えも一緒に直した。
- 既存ルール: AGENTS.md「規約の更新」の docs 版
- 次にどう防ぐか: 今回は記録のみ

### 30. 型名 `ResizeGeometry` もリポジトリの語彙に無い

- 分類: `naming-absent-metaphor`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 指摘 7 を受けて `ResizeSubject` から改名した先の `geometry` も、`docs/06-ui.md` 0 回・
  UI 案 0 回・この差分より前の `src/` 0 回。加えてこの型が表すのは幾何情報ではなく**可否**で
  （`lengths: []` は「大きさが無い」ではなく掴めない、`origin: none` は「位置が無い」ではなく
  位置を書けない）、名前が実体より広かった。`ResizableSelection` へ改名した
  （`DocumentSelection` / `TokenSelection` に前例がある）。
- 既存ルール: rules/naming.md「比喩・抽象語は `grep -i` で 0 回なら語彙に無い」
- 次にどう防ぐか: 今回は記録のみ

### 31. `ResizeEdit.create` / `placedAt` が非対称

- 分類: `naming-vocabulary-alignment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 片方だけ `create` なので、「位置を書かない側」であることが名前に出ない。`PropEdit.set` /
  `PropEdit.clear` のように両方を役割の名前で対にする前例がある。**直さなかった**
  （`rules/naming.md`「値を組み立てるのは `create`」に従うほうを優先した）。
- 既存ルール: rules/naming.md「メソッド名を既存のドメインに揃える」
- 次にどう防ぐか: 今回は記録のみ

### 32. UI 案にも仕様書にも無い規則を新しく足した

- 分類: `ui-fidelity`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「左辺・上辺からのリサイズは座標を書くので、その artboard は以後 並べ替えでは動かなく
  なる」は、UI 案にも従来の `docs/06-ui.md` にも無い新しい規則。移動ドラッグの既存規則
  （「座標を持たない artboard は掴んだ時点で描かれている位置が起点」）へ寄せた判断で筋は通るが、
  判例 `ui-fidelity`（「根拠が筋が通っていることは、UI 案が求めていることの証明にならない」）の
  形そのもの。計画の段階で Issue に書いてはいたので、却下案の理由を 1 行添えて補強した。
  なお `nesw-resize` の追加は UI 案にカーソル指定が `default` / `pointer` しか無く `*-resize` が
  0 件なので、元から UI 案の外で決めていた領域（指摘されていない）。
- 既存ルール: rules/ui-verification.md「UIの拠り所」
- 次にどう防ぐか: 今回は記録のみ

### 33. `.claude/hooks/` が発火しない実行環境だった

- 分類: `hook-environment`
- 出どころ: `自己修正`
- 内容: push 前に `echo hook-canary` を実行したが deny されずに通った。`.claude/hooks/README.md`
  の表に従って PreToolUse の痕跡を確かめたところ、検証エージェントのマーカーも発火ログの見出しも
  このセッションのものが無く、取りこぼしではなく本当に不発と判定した。push 前検査は git hooks と
  CI が担っているので手順は変えず、PR 本文へ記録した。禁止コマンド（`git add -A` / `npx`）を
  実行していないことも見直して確認した。CI は初回で全件 green だったので、素通りした欠陥は無い。
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

**帯が両側に重なったときの規則を、計画の「先勝ち」から「近いほうの辺」へ変えた。**
計画では「始点側と終点側の両方の帯に入るときは始点側を取る」と決めていたが、指摘 15 でその規則を
テストが 1 件も守っていないことが実測で分かった。守るテストを足すにあたって、tie-break の
取り決めを doc に書いて守る形より距離で決まるほうが説明が要らないと判断し、規則そのものを
変えた（#578 で角の範囲を決めるときも同じ土俵に乗る）。

**型名と口を 2 回変えた。** `ResizeSubject`（計画）→ `ResizeGeometry`（指摘 7 を受けて）→
`ResizableSelection`（指摘 30 を受けて）。1 回目の改名が指摘 7 への対応として不十分だった。

**掴める軸と位置をフックへ渡す形へ変えた。** 計画では 3 箇所で導出する形のままだったが、
指摘 22 で凍結の判定の非対称が乗っていることが分かり、`ArtboardCanvas` が 1 回だけ作って渡す
形へ直した。

## 発火

- 発火: 計測対象外(フック不発)

## うまくいったこと

- **新しく足したテストが実際に落ちることを、実装を壊して確かめた。** 帯の規則を「先勝ち」へ
  戻すと 1 件、`Math.round` を外すと 1 件が落ちることを実測してから push した（指摘 15・16 が
  「テストが守っていない」形だったので、同じ穴を自分で作らないため）。
- **UI の表示確認で、操作の結果だけでなく途中も見た。** Storybook を chromium（CDP）で開き、
  8 箇所のカーソル（`nwse` / `ns` / `nesw` / `ew`）を実測し、左辺ドラッグの**最中**に幅と左端が
  同時に動いて右辺が留まることを確認した。
- **計画の却下案の根拠を、書いたあとに数え直して訂正した。** 「似た構造は 1 件」と書いたが実測
  2 件で、2 件目（`RepositionTarget.create`）のほうがこの計画と同じ形の先例だった。Issue に
  訂正を残した。

## 規約への反映

無し。指摘 33 件はすべて既存の分類に当たり、この回では記録のみに留めた。再発数を数えて
介入するかを決めるのは `harness-growth` の管轄。

---

**追記（`harness-growth` / ハーネス棚卸しルーティン）:** `duplication-traversal` は
`harness/records/pr-543.md`(#33、`src/utils/Json.ts` の既存の型ガード・変換を自前で
書き直した回)が人のレビューへ届いた実例で、`.claude/agents/implementation-reviewer.md`
「重複の観点」へ「形の検証・変換を自前で書く前に既存のユーティリティモジュールを検索したか」
を観点として追加した。このタグは元々個別の `対策済` を持たず `duplication`(層=hook at
pr-192)の窓に畳まれていたため、締めの行は**このタグが最後に出た本記録**に足す。

- 対策済: `duplication-traversal` 層=観点 at pr-582
