# PR #781 Json.optional が不在を Option で返すようにする

- マージ日: 2026-09-25
- 関連 Issue: #733
- 差分規模: 9 ファイル / +72 -37

## ゴールと結果

ゴールは達成した。`Json.optional` と artboard の `canvasPositionFromJson` は、省略できるフィールドが無いときに `Option` を返す。読み込み結果は変わらない。
完了条件の `grep -rn "JsonDecoded<[^>]*undefined" src/` は 0 件になった。
人のレビュー指摘は 0 件(`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも確認。コメントは bot の投稿と自分のテストケース表だけ)。
CI は最初の push の時点では main と衝突していて走らなかった。main を取り込んだ後の head で全チェックが緑になった。

## 指摘

### 1. canvasPositionFromJson の本体の書き直しが計画から抜けていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画には戻り値の型と `fromJson` 側の畳みだけがあった。x / y の `=== undefined` 判定 3 箇所を書き直す形が決まっていないのに、「未決: なし」と書いていた。
  書き直す形を計画に足した(名前を付けた `hasBothAxes` / `hasNoAxis` で分ける)。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 1

### 2. 片方の軸だけのときに報告する軸をテストが守っていなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 既存テストは `isOk` が false であることしか見ていなかった。`missing` の三項演算子を逆にしても通る状態だった。
  `a[0].y` / `a[0].x` の `missing-field` を見るテストに置き換え、軸を入れ替えるミューテーションで落ちることを確かめた。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 3. Json.optional の失敗経路のテストが計画に無かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画にあったのは none / some の 2 件だけで、`null` が書かれたときに `none` へ倒れず `invalid-type` になる経路が無かった。`Json.edge` に 1 件足した。
  some 側も `isSome` だけでなく `toEqual(Option.some(...))` で値まで見る形にした。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 4. 却下案の理由が事実と食い違っていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 却下案 `Result.map(decode, Option.fromNullable)` の理由を「null を黙って none に倒す」と書いていた。実際には既存のデコーダがすべて `null` を `invalid-type` にするので起きない。
  理由を「null を返しうるデコーダを型が受け付けてしまう」に直した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 5

### 5. 据え置いた作業の後続を計画に書いていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: ドメイン型の Option 化を「範囲超過」として却下したとき、書き出し側の `Json.definedField` / `nonEmptyField` の扱いを書いていなかった。
  起票しない理由(規約が禁じるのは戻り値の `undefined` で、これらは引数)を Issue に書いた。
- 既存ルール: CLAUDE.md「タスクの分割」

### 6. ゴールを確かめる手段が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 完了条件の grep 2 本を足した。書き直さない 3 件(component のドメイン型の読み替え)を残す理由も分けて書いた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 2

### 7. Biome の整形違反が 3 ファイルに残っていた

- 分類: `なし`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 畳みを `Option.isSome(x) ? { x: x.value } : {}` へ機械的に置換した結果、80 桁を超える行が artboard / component / typography に出た。`biome check --write` で直した。
  整形の話で、対応する規範は無い(push 前の `biome check` でも拾える位置)。
- 既存ルール: なし

### 8. 旧テストが新テストに包含される重複

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「x だけ / y だけを持つ artboard は読み込めない」の 2 件は、欠けた軸まで見る新テスト 2 件が落ちない限り落ちない。旧 2 件を消して置き換えた。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」(実装・テストとも)

### 9. Claude Code のフックが不発の環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: 単独の `echo hook-canary` が deny されずに通った。検証エージェントを 2 回通した後も、このセッションのマーカーと発火ログの見出しが無かった(README の「本当に不発」の枝)。
  セッションは webhook(issues.assigned)で起動し、起動ディレクトリはリポジトリの親(`/home/user`)だった。不発の間に `npx tsc` を 1 回実行していた(ファイル変更は無し)。PR 本文・Issue・Discord に記載した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

### 10. 最初の push の時点で main と衝突していた

- 分類: `なし`
- 出どころ: `自己修正`
- 内容: 作業中に main で `Json.ts` の `hasField` が `RecordEx.has` へ置き換わり、`Json.optional` の同じ行で衝突した。衝突している間は CI が 1 本も走らなかった。
  チェックインで気づき、main を取り込んで `RecordEx.has` と `Option` の戻り値を両方残した。
- 既存ルール: なし

## 手戻り

- 計画段階: plan-reviewer の指摘(1〜6)で計画を書き直した。実装前なので作業の手戻りは無い。完了条件の件数を誤記した計画コメントを 1 回書き直した
- 実装段階: implementation-reviewer の指摘(7・8)で整形とテストを直した。push 前なので CI の手戻りは無い
- PR 後: main との衝突を解消するマージコミットを 1 回 push した
- どれも Issue #733 に追記済み

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。記録のみ(集計と介入の判断は `harness-growth` の管轄)。
