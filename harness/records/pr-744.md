# PR #744 判例 comment-*: #743 の 5 形へ検証エージェントの指摘を反映する

- マージ日: 2026-09-24
- 関連 Issue: #747(元は #742)
- 差分規模: 1 ファイル / +28 -14

## ゴールと結果

元のゴール(#742: `comment` で再発した 5 件の形が `harness/case-law/coding.md` から NG / 判定の
分かれ目つきで引ける)は、並行して同じ Issue を実装した #743 が先にマージされて達成された。
この PR は main を取り込んで競合を解消し、#743 が入れた 5 形へ plan-reviewer / implementation-reviewer
の指摘を反映する差分(NG / OK の対・判定の分かれ目・元の記録のタグ・冗長さの分離)だけになった。
閉じる Issue は #747 に付け替えた。対策済の行は実際にマージされた `at pr-743` に揃え、この PR では足していない。
CI は全チェック通過、VRT は差分 0 件、人のレビュー指摘は 0 件だった。

## 指摘

### 1. pr-438 の元のタグ(`comment-behavior-claim`)に触れないまま `false-claim` の表へ入れる計画だった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 前回の層(skill at pr-363)との関係と、`false-claim` に置く理由が計画に無かった。
  既存の表が挙動の主張の行を既に持っていることを理由に書き、判例にも元のタグを明記した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「却下案に書いた理由が事実を主張しているなら」

### 2. 素の `comment` へ対策済を書くと、扱わない pr-718#18 の窓も閉じることが計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `count.sh` の `window_of` は分類名そのものへの対策済で全綴りの窓を閉じる。副作用として
  許容する理由を #742 に書き、#720 へ 1 行残した。分割先の番号表記も「#719 / #720」に揃えた。
- 既存ルール: `.claude/skills/harness-growth/SKILL.md`「6. PR にする」

### 3. 既存の判例見出しがあったのに再発したのではないか、への答えが計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 5 形はどれも既存の表・箇条に無い形で、同じ層の言い換えではないことを #742 に書いた。
- 既存ルール: `.claude/skills/harness-growth/SKILL.md`「介入済みの分類が再発したなら、前回の置き場所は候補から外す」

### 4. pr-718#19 のもう 1 つの形(冗長さ)が計画から落ちていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 記録には「一時的な事実」と「同じことを 2 段落で 2 回言う冗長さ」の 2 つがあったが、
  分かれ目は前者しか拾わなかった。「消すコメントの見分け」へ「同じ理由の言い直し」を足した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3

### 5. ゴールを確かめるコマンドが計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `count.sh` の起点だけでは「5 件が引ける」を確かめられない。`count.sh --list comment` が空、
  判例に 5 つの PR 番号がすべて当たる、を確認手段に足した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3

### 6. 補例の pr-210#2 の出典(分類 `なし`・自己修正)を明記する必要があった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 窓の 5 件と同列に見えないよう出典を併記する、という指摘。最終的には指摘 11 で補例から外した。
- 既存ルール: なし

### 7. 対策済の行を書く前に、pr-734 より新しい `comment` の記録が無いか確かめる段取りが無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `at pr-<番号>` は PR 作成後にしか書けないので、push 直前に記録の最新を確かめる手順を足した。
- 既存ルール: `.claude/skills/harness-growth/SKILL.md`「6. PR にする」

### 8. 「未決: なし」と書いたが、指摘 1・2・4 が未決の判断として残っていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 上の対応で決めたので無しに戻した。
- 既存ルール: `.claude/agents/plan-reviewer.md`

### 9. pr-438 の行に「閾値未満」を事実として書いたが、記録では推測だった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 記録は「閾値未満に収まったためと見ている」。確かめていない主張を戒める行そのものが同じ形に
  なっていた。「147 passed」に差し替えた。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 10. #611 の説明が記録に無い語で書かれていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「トークンパネルから編集できるようにする #611」は記録に無い説明だった。#611 のタイトル
  「トークンパネルで gradients を出す・編集する」を確かめ、そのまま残した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 11. pr-210 の補例が事実と違い、判定の分かれ目とも合わなかった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「配線を直す PR が同時に進んでおり」と書いたが、実際は既に main に入っていた #205 を古いクローンで
  書いた形で、「open な Issue / PR が入った瞬間に偽になるか」と合わない。同じ例が `process.md` にもあった。
  補例から外し、`process.md` `rules-consistency` への参照 1 行にした。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」/「同じ処理が2箇所に現れたら共通化する」

### 12. `comment-missing` に足した判定の分かれ目が、同じ節の既存の箇条で逆の答えを出した

- 分類: `harness`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「素直な置き場所・型が別にあるか」は「Issue 番号だけで指す」の箇条に当てはまらない。
  pr-359 / pr-394 の 2 つに限った分かれ目だと明記した(`process.md` 確かめ方 1 の形)。
- 既存ルール: `harness/case-law/process.md`「`rules-consistency`」

### 13. reviewer の語彙 `comment-missing-divergence` と呼び名が割れていた

- 分類: `naming`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 既存の型へ寄せなかった Why not は、reviewer が既に `comment-missing-divergence` と呼ぶ形の
  置き場所版だった。対応を 1 行書いた。
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」

### 14. 新設の見出し `comment-temporary-premise` がどの記録の分類にも現れなかった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 判例は分類で引く前提だが、記録上は `comment`(pr-718)に畳まれている。見出しの直下に記録上の
  分類を併記した。
- 既存ルール: `harness/case-law/README.md`「誰が書くか」

### 15. pr-718#19 を 2 か所に分けたのに互いを指していなかった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `comment-temporary-premise` と「同じ理由の言い直し」の行に、互いへの参照を足した。
- 既存ルール: なし

### 16. pr-718 の判例に OK の例が無かった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 現行の `GradientStop.create` の doc(仕様に拠る理由)を OK として引き、NG / OK の対にした。
- 既存ルール: `harness/case-law/README.md`「NG / OK の形と判定の分かれ目を書く」

### 17. pr-573 の行に介入の経緯が混ざっていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「手順 6(層=skill)を持つスキル自身で起きた」は記録にある経緯で、判例の中身は NG と
  確かめ方だけ。経緯を落とした。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 18. フックが発火しない実行環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が 2 回とも deny されずに通った。検証エージェントを通した後でもこのセッション
  (`99e020aa-…`)のマーカー・発火ログの見出しが無く、`.claude/hooks/README.md` の表で「本当に不発」の枝。
  起動元は issues.assigned の webhook から起動したルーティン。git hooks の pre-push は発火した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- **同じ Issue #742 を #743 と並行して実装した。** 事象そのものは `harness/records/pr-743.md` 指摘 1
  (`parallel-issue-work`・CI)に記録されているので、同じ 1 件を二重に数えないようここでは指摘に立てない。
  この PR 側では、#743 のマージ後に人の指示(「コンフリクト修正して」)で main を取り込み、
  `coding.md` はこちら、`pr-734.md` は main 側(`at pr-743`)を採って解消した。#742 は閉じたので
  開け直さず、続きの Issue #747 を立てて `Closes` を付け替えた
- 解消の結果、#743 がマージした判例の行を手直しする差分になった。`harness/case-law/README.md`
  「過去の判例を書き換えない」に当たりうることを PR 本文に書いたうえでマージされた
- 計画は plan-reviewer の指摘(1〜8)で 1 回、実装は implementation-reviewer の指摘(9〜17)で 1 回直した。
  implementation-reviewer の「行ごとに PR 番号を書くか揃っていない」(軽い指摘)は、README が PR 番号を
  認めており既存の行を書き換えないため据え置いた
- 最初の push 直後に VRT が赤くなったのは、2 回目の push で先の run が cancel されたため(差分由来ではない)

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。この回の介入(`comment` 層=case-law)は #743 側の記録で締められている(`harness/records/pr-734.md`
の `at pr-743`)。この PR はその判例の手直しで、対策済は書かない。
