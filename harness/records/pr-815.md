# PR #815 push 前の検査の結果を最後の 1 行で言い、push 前手順を pre-push 1 回にする

- マージ日: 2026-09-26
- 関連 Issue: #653
- 差分規模: 10 ファイル / +352 -127

## ゴールと結果

ゴールは途中で付け替えた。Issue の元の問いは「pre-push の出力が長い(判定表の ok 行)ので静かモードを足すか」だったが、オーナーとの対話で「長さは関係ない。直すのは通った/落ちたの読み違え」に変え、Issue のタイトルも変えた。
付け替えた後のゴールは 2 つで、どちらも達成した。
- `pre-push` の最後の行が結果を言う(すべて通過 / 飛ばした検査があるまま通過 / 失敗と件数)。落ちても止めずに最後まで走る
- push 前手順(`implementation-flow` フェーズ 7)は 20 行の一覧を写さず、`PRE_PUSH_REQUIRE_ALL=1 bash harness/githooks/pre-push` と `pnpm run test:run` の 2 行で判定する
人のレビュー指摘は GitHub 上では 0 件だった(`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも確認。コメントは bot の投稿と自分のテストケース表だけ)。セッション中の指示は指摘 1 に記録した。CI は初回で緑(VRT は差分 0)。

## 指摘

### 1. Issue の問いの立て方(出力の長さ)をそのまま受け取り、何のために短くするのかを問わなかった

- 分類: `plan`
- 出どころ: `レビュー（人）`
- 内容: 最初の停止コメントでは、静かモードを前提に対象範囲・CI の扱い・切り替えの形だけを選択肢として並べた。オーナーから「長さは関係ない。単純に短くすればいいわけではない」と返され、読み違えが起きた場所(PR #168、層 4)を記録から調べ直してゴールを付け替えた。
- 既存ルール: implementation-flow フェーズ 1「ゴールの確定」(実装手段ではなく達成状態で書く)

### 2. Issue の前提(判定表 4 本・約 60 行)が起票後に古くなっていた

- 分類: `plan`
- 出どころ: `自己修正`
- 内容: 実測すると、pre-push から呼ばれている判定表は 7 本・約 231 行あった。Issue が挙げた `check-pr-closing-issue-cases.sh` は pre-push に無く、`report()` は 4 本が `cases-report.sh` で共有済みだった。実装には入らずに止め、Issue に実測値を書いた。
- 既存ルール: implementation-flow フェーズ 1(自分で潰せないなら止める)

### 3. 「落ちた検査の名前が出る」を出力全体から探すケースは、集計を壊しても落ちない

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `run_check` の見出しにはどの検査の名前も出る。名前を探すのは内訳の行だけにし、通った検査の名前が出ないことを対照として見る形に直した。
- 既存ルール: rules/testing.md「assert は『落ちうるか』と『1 つの仕様か』で見る」

### 4. 対になる片側と境界のケースが一覧に無かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 次のケースが無かった。「後ろの検査が走る」は見出しではなく印ファイルで観測する形に直した。
  - 飛ばした検査の名前が内訳に出ること
  - 失敗と飛ばしの両方がある場合の内訳
  - 検査を 1 つも走らせなかった場合
  - 複数の名前を飛ばした場合の件数
- 既存ルール: rules/testing.md「assert は『落ちうるか』と『1 つの仕様か』で見る」

### 5. pre-push への配線と、層 4 のゴールを確かめる手段が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 判定表は lib しか見ない。次の 3 つを計画に足した。
  - `run_check` を通らない検査が残っていないかの grep
  - 本物で通す probe
  - フェーズ 7 に旧一覧のコマンドが残っていないかの grep
- 既存ルール: implementation-flow フェーズ 3 手順 3

### 6. `set -e` を外すと、準備が失敗しても先へ進む

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `set -e` が暗黙に守っていた条件が、計画に書かれないまま消えるところだった。`cd` や 2 つの `source` が失敗しても先へ進むので、別のディレクトリで検査が走る・関数が見つからず 127 で落ちる・最後の行が出ない、になる。この 3 つに `|| exit 1` を付けた。
- 既存ルール: implementation-flow フェーズ 3 手順 7

### 7. ファイル表の漏れ

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 次の 4 つがファイル表に無かった。
  - README のスクリプト表
  - pre-push 自身のコメント(「黙って通す」「1 つずつ独立に」の理由)
  - `.claude/hooks/README.md` の probe 段落
  - フェーズ 7 の箇条ごとの残す/消す
- 既存ルール: implementation-flow フェーズ 3 手順 7

### 8. 一覧の件数を「22 行」と書いていた(実際は 20 行)

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 数え直して、「19 本は pre-push と一致し、pre-push に無いのは `test:run` だけ」という対応を計画に書いた。一覧を写さなくてよい根拠そのものになった。
- 既存ルール: implementation-flow フェーズ 3 手順 2

### 9. 最後の行の文言が未決のまま、未決の判断に挙げていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 判定表の期待値が文言に依存する。3 通りの綴りと、`PRE_PUSH_REQUIRE_ALL=1` で飛ばしが原因になったときの言い方を、実装前に決めて Issue に書いた。ルーティンのプロンプトの扱い(リポジトリの外)も書いた。
- 既存ルール: implementation-flow フェーズ 3「未決の判断」

### 10. 1 PR にする理由が Issue に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 結果行とフェーズ 7 の置き換えは独立してマージできる。1 本にしたのはオーナーの判断だったが、理由を Issue に書いていなかった。
- 既存ルール: AGENTS.md「タスクの分割」

### 11. 判定表の `report()` を別に書き、共有しない理由も書いていなかった

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `count-cases.sh` の `report()` と同じものを書いていた。前例と同じ形で見送る理由を書いた。前例の「3 つのフォルダ」は 4 つになったので、`count-cases.sh` と `cases-report.sh` の 2 か所を直した。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 12. python3 の区間で、検査の名前を走らせる側と飛ばす側に 2 回書いていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `skip_checks` に 12 個の名前を並べ直していたので、片方だけに検査を足すと件数が黙ってずれる。`skip_following_checks` と `resume_checks` で区間を挟む形に作り直し、名前を 1 か所にした。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 13. 止めずに走らせる理由と `PRE_PUSH_REQUIRE_ALL` の意味を、3〜4 か所で説明していた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 説明が pre-push・集計・README・SKILL に分かれていた。`harness/githooks/README.md`「結果の読み方」を正本にし、ほかは参照にした。
- 既存ルール: rules/coding.md「規約の適用範囲」(同じ判断根拠の doc の重複)

### 14. SKILL の箇条が経緯を主にしていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「検査を一覧として写さない」の箇条が「以前は 19 本並べていたが…」という経緯を主にしていた。規範の 1 文と分類名だけに削った。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 15. 「`rules-check` にはもう 1 つ」が受けていた箇条を消していた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 受けていた「`rules-check` と同じ 13 個」の箇条を消したので、何に対するもう 1 つなのかが読めなくなった。「`pre-push` に無く、CI の `rules-check` だけが走らせる検査が 1 つある」に書き直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」(claim-verification の comment-premise-drift)

### 16. 所要時間の記述が実測と揃っていなかった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 判定表の冒頭は「40 秒近く」、README は「35.9 秒」だった。README の「ここへ載せた 2 本は合わせて 2.2 秒」も、判定表が 1 本増えたのに直っていなかった。前者の数字は消し、後者には新しい判定表の実測(0.1 秒未満)を書き足した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 17. 新しい項目だけ相対パスで書いていた

- 分類: `naming`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `harness/githooks/README.md` の表と列挙で、ほかの項目はリポジトリのルートからのパスなのに、新しい項目だけ `lib/check-tally-cases.sh` と書いていた。ルートからのパスに揃えた。
- 既存ルール: なし

### 18. 「失敗」と「飛ばしが原因の失敗」の分岐の順を入れ替えても、判定表が落ちなかった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 2 つが同時に成り立つ入力(`PRE_PUSH_REQUIRE_ALL=1` で失敗と飛ばしの両方がある)のケースが無かった。ケースを 1 件足し、分岐の順を入れ替えると NG が出ることを実測した。
- 既存ルール: rules/testing.md「assert は『落ちうるか』と『1 つの仕様か』で見る」

### 19. `PRE_PUSH_REQUIRE_ALL` の判定を `= "1"` から `-n` に変えても落ちなかった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 値が 1 以外のときに飛ばしを失敗にしないケースが無かった。`=0` のケースを足した。
- 既存ルール: rules/testing.md「assert は『落ちうるか』と『1 つの仕様か』で見る」

### 20. `finish_checks || true` にしても判定表が落ちなかった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 判定表は lib を source するだけで、pre-push の終了コードの配線は見ていなかった。pre-push の最後の文が `finish_checks` であることを見るケースを足し、その配線をコメントにも残した。
- 既存ルール: rules/testing.md「assert は『落ちうるか』と『1 つの仕様か』で見る」

### 21. `PRE_PUSH_REQUIRE_ALL=1` が判定表のケースへ引き継がれ、3 件が NG になった

- 分類: `test`
- 出どころ: `自己修正`
- 内容: push 前手順はこのモードで pre-push を走らせ、pre-push は判定表を呼ぶ。`PRE_PUSH_REQUIRE_ALL=1` を付けて判定表を走らせて確かめたところ、「飛ばしても exit 0」の 3 件が NG になった。各ケースの前で環境変数を外すようにした。
- 既存ルール: rules/testing.md「テストは1つだけ読めば前提がすべて分かる状態にする」

### 22. README の動作確認に書いた probe のパスを、名指ししたパスの検査が拾った

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `harness/githooks/README.md` のフェンスに `src/` 配下の probe を書いた。`named-path-violations.py` が probe を免除するのは `.claude/hooks/README.md` だけ(docstring「README 以外のフェンスへ広げない」)なので、違反として拾われた。同じことを確かめる `.claude/hooks/README.md` のレシピへの参照に変えた。
- 既存ルール: なし(検出器の docstring が仕様を持つ)

### 23. Issue コメントの記録番号を誤っていた(`pr-168` 指摘 17 → 14)

- 分類: `なし`
- 出どころ: `自己修正`
- 内容: Issue に書いた主張を実物と照合して見つけた。コメントを直した。あわせて `harness-process-drift` の実例の書き方も、元の記録(pr-482#40・pr-502#17)に合わせた。
- 既存ルール: なし(`claim-verification` スキルの対象)

### 24. フックが発火しない環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されなかった。plan-reviewer と implementation-reviewer を通した後に確かめたが、検証エージェントのマーカーも、このセッションの発火ログも無かった(README の表で「本当に不発」)。着手時点では `core.hooksPath` も未設定で、`pnpm install` の `prepare` で配線された。禁止コマンドは実行していない。
- 既存ルール: implementation-flow フェーズ 7(カナリア)

## 手戻り

- **ゴールの付け替え(指摘 1)。** 最初の停止コメントの選択肢(静かモードの範囲)はすべて不要になった。その後、ゴールを「読み違え」に変え、1 PR にまとめることにした(オーナーの判断)
- **計画を plan-reviewer の 8 件で第 2 版にした(指摘 3〜10)。** テストケースは 8 件から 12 件になった
- **飛ばし方の API を作り直した(指摘 12)。** `skip_checks <理由> <名前…>` を廃止して `skip_following_checks` / `resume_checks` にした。pre-push と判定表の両方を書き直した
- 計画から外れた箇所は Issue に追記した。
  - probe レシピを参照に変えた
  - 判定表で環境変数を外した
  - `.claude/agents/implementation-reviewer.md` の観点を書き換えた(ファイル表に無かった)
  - 見出しを「pre-push の結果行の判定表」にした(既存の「集計の判定表」と衝突するため)

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。記録のみ(集計と介入の判断は `harness-growth` の管轄)。

なお、ルーティンの起動プロンプト(リポジトリの外)は「pre-push 検査を 1 つずつ独立に実行する」と書いていて、新しいフェーズ 7 と食い違う。書き換え案は PR #815 の本文と Issue #653 に書いた。
