# PR #181 illegal-state (再発9・未介入) を計画レビューの観点へ反映する

- マージ日: 2026-08-10
- 関連 Issue: なし(`ハーネス棚卸しルーティン` からの直接介入。Issue を介さない
  harness-growth の定型作業のため、フェーズ 1〜3 は行っていない)
- 差分規模: 2 ファイル / +27 -1

## ゴールと結果

`harness/records/count.sh` の再発トップだった `illegal-state`(再発 9・未介入)へ
`harness-growth` スキル Step 4a で介入すること。計画レビューの観点
(`plan-review.md`)に、過去 8 件の実例から抽出した具体パターンを追記して達成した。
ずれは無い。

## 指摘

該当なし(レビューコメント 0 件、CI は全 5 チェック green、手戻り無し)。

## 手戻り

無し。

## うまくいったこと

- push 前検査(`missing-doc-comments.py --all`)が、本題と無関係な既存の
  `@returns` 欠落(`editor-top-bar/index.tsx`)で push をブロックした。
  スコープ外のコードだが、push できない限り本題も出せないため別コミットとして
  先に直し、PR 本文にも「本題とは別の理由」と明記した。ブロックされた時点で
  無視せず直したことで、後続の push もそのまま通った

## 規約への反映

`illegal-state` は `rules/coding.md`「不正な状態を型で表現できなくする」に
既存の規約があるが、計画段階で毎回別の構文の形(辞書引き・出し分けの最後の枝・
`as const` の縛り漏れ・多段参照・型エイリアス・`Option` の潰し・直交状態の同居)
で発生し、計画検証サブエージェントが後から捕まえる状態が 9 件続いていた。

- 層1(hook): 見送り。9 件の内訳が構文的に別々の形に跨り、単一の機械判定で
  共通に検出できないため
- 層2(skill): 見送り。既存の `implementation-flow` フェーズ4(計画の検証)に
  収まる観点であり、新しい段取りではないため
- 層3(観点): 採用。`plan-review.md` の既存の一般的な1行を、8 件の実例
  (PR #124 / #139 / #141 / #148 / #149 / #157 / #162 / #168)から抽出した
  具体パターンの列挙へ強化した
- 層4(rules): 変更なし。`rules/coding.md` 側は規約の記述そのものであり、層3は
  層4の代替ではなく、層4を実際に読む機会を計画検証の時点へ増やす追加のため

**この回の介入(`count.sh` が数える行):**

- 対策済: `illegal-state` 層=観点 at pr-181
