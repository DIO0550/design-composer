# PR #824 部分木の名前を出現ごとに付け替え、同じ部品を 2 つ含む部品の解除で同名が残らないようにする

- マージ日: 2026-09-26
- 関連 Issue: #818
- 差分規模: 6 ファイル / +274 -69

## ゴールと結果

ゴールは達成した。同じ名前のノードを複数含む部分木を `detach` / `insertNodeCopy` で入れても、同名のノードが残らない。
部品 `card = [b1 → badge, b2 → badge]`、`badge = [label]` を解除すると、これまでは `label-2` が 2 つ残っていた。今は `label-2` / `label-3` になる。
`Node.renameEach` を足し、`DocumentNames.renameSubtree` をその上に書き直し、`renameMap` を削除した。
GitHub 上の人のレビューは 0 件だった。`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも確認し、コメントは bot の投稿と自分のテストケース表だけだった。CI は初回から緑で、VRT は差分なし（179 件）。

## 指摘

### 1. 名前 renameInOrder が通りがけ順と読め、実装（行きがけ順）とずれていた

- 分類: `naming`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 木の用語で in-order は通りがけ順を指す。リポジトリの doc は「行きがけ順」を使っていた。`renameEach` に改め、順序は doc に書いた。
- 既存ルール: rules/naming.md「名前と実体を一致させる」

### 2. 走査順のテストが、assign の呼び出しを記録する形になりうる計画だった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: assign の中で taken を配列へ push して比べると、domains で禁止のスパイと同じ相互作用の検証になる。assign をテスト内の純粋関数にし、戻り値の名前だけを見る形にした。
- 既存ルール: rules/testing.md「モックは原則使わない」

### 3. 積むのが決めた名前か元の名前かを区別できる入力を置いていなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: assign が名前をそのまま返す入力では、どちらを積んでも結果が同じになる。`taken={item}`、`[item, item-b]` → `[item-b, item-b-b]` のケースを足した。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 4. 決めた名前の持ち越し経路（親→子 / 同じ親の兄弟 / 根をまたぐ）をケースに分けていなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 子の並びの中での持ち越しと、根の並びをまたぐ持ち越しは、実装上別の枝になりうる。detach は複数の根を渡すので後者も通る。3 経路を別のテストにした。
- 既存ルール: rules/testing.md「1テスト1振る舞い」

### 5. ゴールに挙げた insertNodeCopy のテストを足さない理由が書かれていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 貼り付ける部分木には同名が無いので、DocumentNames の単体テストで固定して足さない、と判断を Issue に書いた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3

### 6. renameSubtree の @returns の書き直しと ArrayEx の import 削除がファイル表に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `@returns` が削除する `renameMap` を名指ししていた。`ArrayEx` は `renameMap` の中でしか使っていなかった。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 7

### 7. Node.rename を部分木に使ってはいけないことを残す場所が計画に無かった

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 対応表で引く `rename` と 1 ノードずつ決める `renameEach` が並ぶ。「対応表で足りるのでは」を防ぐ 1 行を `Node.rename` の doc に足した。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 8. assign の関数型を 2 箇所に直書きしていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `renameSiblings` と `Node.renameEach` が同じ `(name, taken) => string` を書いていた。非公開の型 `NameAssigner` に切り出した。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 9. renameSiblings と Node.rename が同じ葉の判定を名前の無い組んだ条件で持っていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `Node.isRef(node) || node.children === undefined` が 2 箇所にあった。型ガード `holdsChildren` にまとめた。
  `rename` を `renameEach` の上に乗せて畳む案は採らなかった。`rename` は「名前の変わらない葉は渡したものそのもの」を約束しており、乗せるとそれが壊れるため。理由は doc に残した。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」 / rules/naming.md「条件を組んだ式に名前を付ける」

### 10. PreToolUse フックが発火しない環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が通った。検証エージェントのマーカーも、発火ログの見出しも無かった。
  セッションがリポジトリの外（`/home/user`）で起動し、`.claude/settings.json` が読まれていないと見られる。push 前の検査は `pre-push` を手動で回した。禁止コマンドは実行していない。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

計画は plan-reviewer の指摘 1〜7 を受けて、実装前に直した（名前・テストの書き方・ファイル表・doc の追記）。
実装後の手戻りは implementation-reviewer の指摘 8・9 の修正だけ。どちらもコミット前に直した。

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。集計と介入は harness-growth の管轄で、この記録では行わない。
