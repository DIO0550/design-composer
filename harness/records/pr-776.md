# PR #776 名指しパス検査の hooks/README 除外を、フェンス内の probe だけへ狭める

- マージ日: 2026-09-25
- 関連 Issue: #709
- 差分規模: 3 ファイル / +92 -21

## ゴールと結果

ゴールは達成した。`.claude/hooks/README.md` をもう一度 `named-path-violations.py` の走査対象に入れた。免除するのは、README のフェンスの中にあり、手前のフォルダが実在する probe の綴りだけ。
README の該当行を #708 以前の綴りへ一時的に戻すと `[named-path-missing] 1 件`・exit=1 になり、#708 と同じ形の再発を止められることを実物で確かめた。
人のレビュー指摘は 0 件(`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも確認。コメントは bot の投稿と自分のテストケース表だけ)。CI は初回で緑。

## 指摘

### 1. probe の綴りを丸ごと免除すると、probe より前のフォルダが移動した形が隠れる

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: フェンス内のレシピの綴り 50 件のうち 43 件が probe を含み、そのすべてで手前のフォルダが移動しても報告されない計画だった。
  免除の条件に「最初の probe の手前までのフォルダが実在する」を足した。nest-depth のレシピはトップレベルの `src/features/probe` へ移した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 3

### 2. 免除を全 `.md` に広げる理由が書かれていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 却下案「README だけに掛ける」の理由は表し方の説明で、範囲を広げる理由になっていなかった。README 以外で該当する綴りは 0 件。
  免除は README に限り、「全 `.md` に掛ける」を却下案へ移した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 5

### 3. #708 の形を捕まえることを実物で確かめる手順が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 判定表の行しか置いていなかった。README の該当行を #708 以前の綴りへ一時的に戻して検出器を走らせる手順を完了条件に足した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 3

### 4. 語の境界のテストが片側しか無かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: deny の `probes` 1 行だけで、セグメントの先頭しか見ない実装でも判定表が通った。`internal-probe` の miss 行と `reprobe` の deny 行を足した。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 5. 判定表を通り抜ける変異が 4 件あった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: ミューテーション 19 件のうち、次の 4 件で判定表が全件 ok のままだった。
  - 右の `-` の境界を外す
  - 最初ではなく最後の probe の手前を見る
  - エイリアスのときにルートからも探す
  - `lstrip` を外す
  行を 4 つ足し、4 件とも NG になることを確かめた。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 6. README が probe の条件を実際より広く書き写していた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: README は「名前に `probe` を含め」と書いていたが、`probes` や `reprobe` も名前に `probe` を含むのに免除されない。条件の書き写しをやめ、docstring を参照する形にした。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 7. 経緯の文が README と docstring の 2 箇所にあった

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「丸ごと外していた間に #708 の 1 件が隠れていた」の文が 2 箇所に独立して書かれていた。docstring の 1 箇所に寄せた。
- 既存ルール: rules/coding.md「規約の適用範囲」(doc の重複にも適用する)

### 8. Claude Code のフックが不発の環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: 単独の `echo hook-canary` が deny されずに通った。検証エージェントを 2 回通した後も、マーカーと発火ログの見出しが無かった(README の「本当に不発」の枝)。
  セッションは webhook(issues.assigned)で起動し、起動ディレクトリはリポジトリの親(`/home/user`)だった。禁止コマンドを使っていないことは見直した。PR 本文・Issue・Discord に記載した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- 計画段階: plan-reviewer の指摘(1・2)で、免除の条件と範囲を変えた。nest-depth のレシピの置き換え先も、`canvas/features/probe` からトップレベルの `src/features/probe` に変えた。実装前なので作業の手戻りは無い
- 実装段階: implementation-reviewer の指摘(5〜7)で、判定表に 4 行を足し、README の 2 箇所を書き直した。push 前なので CI の手戻りは無い
- どちらも Issue #709 に追記済み

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。記録のみ(集計と介入の判断は `harness-growth` の管轄)。
