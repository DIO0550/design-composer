# PR #400 開始画面と「開く / 新規作成」の導線を作り直す

- マージ日: 2026-09-04
- 関連 Issue: #374（分離先: #375 / #376）
- 差分規模: 42 ファイル / +2048 -447（9 コミット）

## ゴールと結果

#374: 開始画面が灰色の地に案内 1 行と左上の 2 ボタンしか無い（症状 1）／`DocumentToolbar` と
`EditorTopBar` で帯が 2 本になっている（症状 2）を直す。

達成した。ただし**着手前に判断待ちで停止し、ボタンの見た目はマージ直前に 3 回作り替えている**。

- **着手前の停止**: UI 案（`docs/Design Composer.html`）は 5 画面とも開く前の画面を描いておらず
  （`open` / `recent` / `welcome` がいずれも 0 件）、見せ方を自分で決めるのは
  `rules/ui-verification.md`「プロトタイプに無い操作を思いつきでボタンとして足さない」に反する。
  導線の選択肢 A（Tauri のネイティブメニュー）/ B（帯のパンくずをメニュー化）/ C（常設ツールバーの
  まま）をコストとともに出し、セッション URL を添えて Issue で停止した。回答は **A**、
  最近使ったファイルとドラッグ & ドロップは**両方今回含める**
- **ボタンの見た目の 3 回**: 黒塗り `#1e1e1e` → 青 `#0d99ff` → 一段薄い青 `#4db2ff` +
  新規作成をゴースト。いずれもオーナーの指示が起点（下記の指摘 18〜20）

## 指摘

`plan-reviewer` の 17 件は Issue #374 の
[comment 5525979759](https://github.com/DIO0550/design-composer/issues/374#issuecomment-5525979759)
に処理表が残っている。`implementation-reviewer` の 7 件は**セッションのトランスクリプトにしか
残っていない**（Issue にも PR にも投稿していない）ので、そこから復元した。

### 1. `Menu::default` の `File` サブメニューがプラットフォームで違った

- 分類: `plan-scope-premise-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 既定メニューへ項目を insert する計画だったが、`File` サブメニューは macOS / Windows に
  しか無く Linux には無い。insert 位置がプラットフォームで変わる。`Menu::with_items` で
  メニュー全体を自前構築する形に変えた（macOS はアプリ / ファイル / 編集 / ウィンドウ、
  非 macOS はファイル / 編集 / ウィンドウ）
- 既存ルール: `implementation-flow` フェーズ 3 手順 3
- 次にどう防ぐか: 今回は記録のみ

### 2. ネイティブメニューには `disabled` が無く、読み込み中から戻れなくなる

- 分類: `plan-implicit-condition`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 開始画面のボタンは `disabled` で二重起動を止めていたが、**ネイティブメニューとドロップには
  押せなくする手段が無い**。`Opening` 中にもう一度開くと、その時点の状態（`Opening`）が
  「選ばずに閉じた」ときの戻り先になり、読み込み中から戻れなくなる。`Opening` 中の要求を捨てる
  ガードを足した
- 既存ルール: `plan-review.md`「既存のコードが前提にしている不変条件を、この計画が崩さないか」
- 次にどう防ぐか: 今回は記録のみ

### 3. 購読の listener が読む `session` の最新性が計画に無かった

- 分類: `plan-implicit-condition`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 購読ハンドラを Effect の依存に入れると、レンダーのたびに購読が張り直され、その隙間に
  届いた指示を落とす。`use-document-reload` と同じ `useEffectEvent` に寄せ、deps を
  `[ports.menu, ports.drop]` だけにした
- 既存ルール: `plan-review.md`「既存のコードが前提にしている不変条件を、この計画が崩さないか」
- 次にどう防ぐか: 今回は記録のみ

### 4. ゴールに書いたドロップの受け口が、計画から落ちていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「ドラッグ & ドロップを含める」と決めたのに、計画には購読の話しか無く**画面側の受け口**が
  無かった。開始画面に静的な案内を置く形にした（`drag-enter` / `over` / `leave` は購読しない —
  強調は見た目だけの話なので、今回のスコープでは要らない）
- 既存ルール: `plan-review.md`
- 次にどう防ぐか: 今回は記録のみ

### 5. `DocumentCommand` / `drop` が既にこのリポジトリで別の意味を持っていた

- 分類: `naming-prefix-collision`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `DocumentCommand` は既に Tauri のコマンド名、`drop` は既にキャンバスのドロップ先を指す。
  `libs/document-menu` → `libs/app-menu`（`AppMenu` / `AppMenuCommands`）、
  `libs/document-drop` → `libs/file-drop`（`FileDrop.subscribeDropped`）へ改名した
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ

### 6. 却下理由に書いた「拡張子の知識が 2 箇所になる」が実物と違った

- 分類: `plan-rejection-reasoning`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: ドロップされたファイルを拡張子で絞らない理由を「絞ると `.dcmp` の知識が 2 箇所になる」と
  書いたが、数え直すと**既に 3 箇所**あり、理由が成立していない。却下理由を「絞ると開けない
  ファイルを黙って捨てることになり、`rules/coding.md`『失敗を握りつぶさない』に反するため」へ
  書き直した
- 既存ルール: `implementation-flow` フェーズ 3 手順 5
- 次にどう防ぐか: 今回は記録のみ

### 7. docs の更新が計画のファイル表に無かった

- 分類: `plan-file-omission`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `docs/05-architecture.md`「Tauri IPC」の表（`document-menu` / `tauri://drag-drop` の 2 行）と
  `docs/06-ui.md`（「開始画面と導線は規定しない」が偽になる）が表に無かった
- 既存ルール: `plan-review.md`「変更で壊れる既存の資産を洗い出しているか」
- 次にどう防ぐか: 今回は記録のみ

### 8. 直すコメントの行が特定されていなかった

- 分類: `plan-comment-reference`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「コメントも直す」とだけ書いて対象を挙げていなかった。5 箇所を明記した
  （`document-start/index.ts` / `App.tsx` / `editor-screen/index.stories.tsx` /
  `opened-document` の Windows ドライブ直下の規則 / `screen-height-shell.tsx`）。
  pr-394 の指摘 12・pr-391 の指摘 2・17 と同じ形の再発。**介入の起点は pr-317（層=観点）**で、
  それ以降も繰り返している（再発数を数えるのは `count.sh` / `harness-growth` の管轄）
- 既存ルール: `plan-review.md`「ファイル表に挙がっているか」
- 次にどう防ぐか: 今回は記録のみ

### 9. `EditorScreen` の props が 5 つになる計画だった

- 分類: `signature`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `ipc` / `dialog` / `menu` / `drop` / `clock` を並べる計画だった。常に対で渡る 4 つを
  `DocumentSessionPorts` にまとめ、`{ clock, ports }` の 2 つにした
- 既存ルール: rules/coding.md「関数のシグネチャ」
- 次にどう防ぐか: 今回は記録のみ

### 10. フックの戻り値の語形が 3 通りに割れていた

- 分類: `naming-vocabulary-alignment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画のフックが `open` / `handleCreate` / `onOpenPath` のように語形の違う名前を返していた。
  `{ session, actions }` を返し `actions` は `openDocument` / `createDocument` / `openDocumentAt`
  に揃えた。あわせて `recents` → `recentPaths`
- 既存ルール: rules/naming.md「メソッド名を既存のドメインに揃える」
- 次にどう防ぐか: 今回は記録のみ

### 11. 最近使ったファイルの同名の区別・並び順・空のときが未決だった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 一覧を出すとだけ書いて、同名のファイルをどう見分けるか・どの順に並べるか・0 件のときに
  何を出すかが決まっていなかった。フォルダ名 + ファイル名の 2 段 + `title` にフルパス、
  並びは受け取ったまま（並べ替え・重複除去は供給側 = #376）、0 件なら枠ごと出さない、と決めた
- 既存ルール: `plan-review.md`
- 次にどう防ぐか: 今回は記録のみ

### 12. Rust 側のテストの置き場所と表示確認の手順が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: メニューを Rust で組む計画なのに、テストをどこにどう置くか・ネイティブメニューを
  どう確かめるかが無かった。`src-tauri/src/menu/menu_normal_test.rs`（リポジトリの
  `{対象}_{カテゴリ}_test.rs` に従う）へ id → 指示の対応表テストを置き、ネイティブメニューは
  Storybook にも Vite にも出せないので `cargo check` + 対応表テストで代える、と決めた
- 既存ルール: `plan-review.md`
- 次にどう防ぐか: 今回は記録のみ

### 13. 新しく足すストーリーは VRT のベースラインを持たない

- 分類: `vrt-blind-spot`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `DocumentStart` の 5 ストーリーはすべて新規なので `status:"new"` 扱いになり、
  **この PR の成果物である開始画面の絵は視覚差分では守られていない**（実際のレポートも
  1 changed / 5 new / 141 passed）。初回は playwright-cli の撮影を目視して代えた。
  pr-394 の指摘 37 と同じ形の再発。**介入の起点は pr-301（層=観点）**
- 既存ルール: rules/ui-verification.md「Storybook の視覚差分（VRT）」
- 次にどう防ぐか: 今回は記録のみ

### 14. `ScreenHeightShell` が feature の中にあり、他 feature から使えなかった

- 分類: `なし`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: ストーリー用の器が `features/editor/__stories__/` にあり、新しく足す
  `features/document-start` のストーリーから使えなかった。2 feature が使うので
  `src/components/__stories__/` へ移した。**`rules/architecture.md`「配置の判断基準」に
  ストーリー専用の共有物の昇格規則はあるが、分類の語彙は domains の昇格
  （`domain-scope-promotion`）しか無い**
- 既存ルール: rules/architecture.md「配置の判断基準」（ストーリー専用の共有物）
- 次にどう防ぐか: 今回は記録のみ

### 15. 畳むモジュールのテストの観点が、移送先を決めないまま消えかけた

- 分類: `plan-migration-mapping`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `DocumentToolbar` の撤去と `opened-document.path.test.ts` の `FilePath` への移設で、
  計画のテスト一覧が**新しく思いついたケースだけ**になっていた。対応表を作って移した
  （`document-toolbar` 4 ケース → 開始画面へ 4 ケース、`opened-document.path` 8 ケース →
  `FilePath` へ 8 ケース、`OpenedDocument` 側には委譲確認の 2 ケースを残す）
- 既存ルール: rules/testing.md「API を作り替えるときは、既存テストの観点を先に数える」
- 次にどう防ぐか: 今回は記録のみ

### 16. 計画に載せたケースに、落ちうるか怪しいものが混ざっていた

- 分類: `test-assert-unfalsifiable`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「開いた後は『開く』のボタンが無い」は、導線をメニューへ移した時点で入力から自明に
  なるので、実装をどう壊しても通る。守りたいのは帯の二重化の解消なので
  「上端の帯は 1 つだけ」（`getAllByRole("banner").length === 1`）へ寄せた
- 既存ルール: `plan-review.md`「テストケースの観点」
- 次にどう防ぐか: 今回は記録のみ

### 17. `App` の配線が切れていても全部緑になる状態だった

- 分類: `test-coverage-wiring`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `App.tsx` で `AppMenu` / `FileDrop` を `TauriIpc` へ繋ぐ配線を切っても既存テストが
  すべて通る。実際に走らせると Tauri 不在で `listen` が reject するので、購読の失敗が
  `Result` として握られて開始画面が描けるケースを 1 件足した
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ

### 18. 開く / 新規作成のボタンの見た目が、UI 案のボタンに寄っていなかった

- 分類: `ui-fidelity`
- 出どころ: `レビュー（人）`
- 内容: 実装後にオーナーから「開くと新規作成のボタンがダサくない？」。UI 案のボタン 2 種
  （主要 = 塗り・副 = 白 + 灰枠、高さ 30px / 角丸 5px / font-size 11px / 横 10px）を測って
  そこへ寄せた。**開始画面は UI 案に無いが、ボタンは UI 案にある**ので、寄せる先はあった
- 既存ルール: rules/ui-verification.md「UIの拠り所は `docs/Design Composer.html`」
- 次にどう防ぐか: 今回は記録のみ

### 19. UI 案に忠実な黒塗りが、オーナーの望むものではなかった

- 分類: `ui-fidelity`
- 出どころ: `レビュー（人）`
- 内容: 指摘 18 の対応で主要ボタンを `#1e1e1e` の黒塗りにした。`#0d99ff` を採らなかったのは
  UI 案で**この色が選択・ハンドル・挿入位置の指示にしか使われておらず、ボタンには一度も
  現れない**ため（`background:#0d99ff` は 20 回あるが、`cursor:pointer` を持つものは 0 件）。
  その直後にオーナーから
  「ボタンの色は青でいいぞ」。**UI 案に合わせたことが正解ではなかった**形で、pr-394 の
  指摘 2 と同じ性質
- 既存ルール: rules/ui-verification.md「プロトタイプと違う形にする必要が出たら、実装前に
  理由を添えて確認する」
- 次にどう防ぐか: 今回は記録のみ

### 20. UI 案に無い「ゴースト」を、聞かれるまで選択肢に出していなかった

- 分類: `ui-fidelity`
- 出どころ: `レビュー（人）`
- 内容: 青にした直後にオーナーから「ボタンでもゴーストとかそんな感じのなかったっけ？」。
  数えると `ghost` / `ゴースト` / `subtle` / `tertiary` は UI 案・`docs/06-ui.md` とも **0 回**、
  `src/` の `ghost` 7 件は全部テストの用例データ（ノード名・プロパティパネル見本の enum）で、
  UI 案が持つボタン系 4 種にも**地も枠も持たない押せる形は無い**。意図した乖離として
  新規作成をゴーストにし、開く を `#4db2ff`（UI 案の `a:hover` が `#0d99ff` と対にしている色）
  へ薄めた
- 既存ルール: rules/ui-verification.md「プロトタイプと違う形にする必要が出たら、実装前に
  理由を添えて確認する」
- 次にどう防ぐか: 今回は記録のみ

### 21. `Opening` 中のガードを守るテストが無かった

- 分類: `test-coverage-branch`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 指摘 2 で足したガードを外しても全件通った。`DocumentIpcFake.holdWrites` で新規作成の
  書き込みを止めて `Opening` に留めたまま別のファイルを落とす形のテストを足した。
  書き込みを解く前に見るのは、解いたあとは新規作成の完了で収束して差が消えるため
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ

### 22. ドロップ側の購読失敗を守るテストが無かった

- 分類: `test-coverage-branch`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 経路ごとに失敗を持つ設計（`CommandSources.Menu` / `Drop`）にしたのに、ドロップ側の
  `hold` を壊してもメニュー側のテストだけで全件通った。`denyDrop` のケースを足して対にした
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ

### 23. `commandFailure` を `EditorScreen` へ通す配線を守るテストが無かった

- 分類: `test-coverage-wiring`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: フック単体と開始画面単体のテストは揃っていたが、その間の配線を切っても全件通った。
  `EditorScreen` で `denyMenu` を立てて「メニューからの操作を受け取れません」が画面に出る
  ケースを足した
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ

### 24. 最近使ったファイルの `title` を守るテストが無かった

- 分類: `test-coverage-wiring`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 同名のファイルを別フォルダで開いたときの区別はフォルダ名と `title` のフルパスで付ける
  設計だが、`title={path}` を落としても全件通った。`getByTitle(path)` で引くケースを足した
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ

### 25. クリーンアップの `unsubscribe` を守るテストが無かった

- 分類: `test-coverage-wiring`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 購読の解除を落としても全件通った。`libs` 側（`app-menu` / `file-drop`）に
  「購読を解除すると、その後に起きても届かない」を各 1 件足した
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ

### 26. `begin` を Effect Event にしたままクリックハンドラから呼んでいた

- 分類: `なし`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 購読ハンドラを `useEffectEvent` に出した流れで、開く / 作るを始める `begin` まで
  `useEffectEvent` にしていた。**Effect Event をイベントハンドラから呼ぶのは React 公式が
  明示的に禁じている**（Effect の中からしか呼べない）。ただの関数へ戻した。
  **`rules/hooks.md` に `useEffectEvent` の記述は無く、このリポジトリの lint も CI も
  止めない**
- 既存ルール: なし（rules/hooks.md は `useEffect` / `useState` / `useRef` までしか扱っていない）
- 次にどう防ぐか: 今回は記録のみ

### 27. 2 つの `fake` が同じ「単一イベントを配る」実装を持っていた

- 分類: `duplication-uncovered-shape`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `app-menu/fake` と `file-drop/fake` が、購読 1 本を保持して任意の payload を流す同じ
  実装を別々に持っていた。`libs/tauri-ipc/fake` の `SingleEventIpcFake.create(event)` へ
  寄せ、両者はその薄い包みにした
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ

### 28. 検証エージェントが git の index をリセットした

- 分類: `subagent-control`
- 出どころ: `自己修正`
- 内容: `implementation-reviewer` の実行中に、staged だった変更がすべて unstaged へ戻った
  （ファイルの中身は無事）。`git add -A` で積み直した。pr-394 の指摘 14・15 は
  **変異をコミットへ巻き込む**形だったが、今回は**index の状態だけが動いた**形で、
  #395 のフック（`block-git-during-verification-agent.sh`）が覆う範囲とも違う。
  なおこの環境では PreToolUse フックが発火しない（指摘 31）ので、どちらにせよ効かなかった
- 既存ルール: `implementation-flow`「サブエージェントの使い方」
- 次にどう防ぐか: 今回は記録のみ

### 29. 既にある `ArrayEx.first` を使わず、同じものを書こうとした

- 分類: `duplication-traversal`
- 出どころ: `自己修正`
- 内容: ドロップされた並びの先頭を取るのに `ArrayFirst(paths)` という関数を書いた。
  `src/utils/ArrayEx.ts` に `first` が既にある。書いた直後に気づいて差し替えた
- 既存ルール: rules/architecture.md「組み込み型に対する汎用操作は `<型名>Ex` に集約する」
- 次にどう防ぐか: 今回は記録のみ

### 30. Rust のテストファイルを解決されないパスに置いた

- 分類: `test-placement`
- 出どころ: `自己修正`
- 内容: `src-tauri/src/menu_normal_test.rs` に置いたが、`src/menu.rs` の中で宣言した `mod` は
  `src/menu/` を見る。`src-tauri/src/menu/menu_normal_test.rs` へ移した
- 既存ルール: rules/testing.md「配置と命名」
- 次にどう防ぐか: 今回は記録のみ

### 31. `echo hook-canary` が deny されず、`.claude/hooks/` が発火しない環境だった

- 分類: `hook-environment`
- 出どころ: `自己修正`
- 内容: この PR の push はすべてカナリアが通過した（3 回の push すべてで確認）。検査は手で
  1 つずつ独立に実行して代替し、git hooks（`harness/githooks/`）は push のたびに発火して
  いる（biome / doc コメント / import 規約 / テスト規約が毎回出力を返した）。PR 本文と
  Discord 通知に明記した。pr-394 の指摘 39 と同じ形の再発
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ

### 32. テストで `as never` を使って型の境界をすり抜けた

- 分類: `illegal-state`
- 出どころ: `自己修正`
- 内容: 開始画面の edge テストで、あり得ない `session` を作るのに `as never` を書いた。
  `rules/coding.md` は境界以外での `as` を禁じている。素のリテラルで `UnopenedSession` を
  書く形に直し、そのケース自体も `document-start.failure.test.tsx` と重複していたので落とした
- 既存ルール: rules/coding.md「境界以外での `as` によるすり抜けは禁止」
- 次にどう防ぐか: 今回は記録のみ

### 33. 「`#0d99ff` は UI 案が持つ唯一の青」がどちらも事実と違った

- 分類: `comment-structure-claim`
- 出どころ: `自己修正`
- 内容: 青にしたときのコメントに「`#0d99ff` は UI 案が持つ唯一の青」「`#4db2ff` はこの青から
  派生している唯一の色」と書いた。`claim-verification` の手順で色相を数え直すと、青系
  （hue 190-260 / 彩度 > 0.2）は **7 色**あり（`#0d99ff`48 / `#e5f4ff`8 / `#111827`7 /
  `#5b8ac0`3 / `#3b82f6`3 / `#4db2ff`1 / `#cfe8fb`1）、**どちらの主張も偽**だった
  （`#3b82f6` / `#111827` は Tokens 画面のトークン見本で、アプリ自身の色ですらない）。
  「UI 案のアクセントの青」「UI 案が持つ唯一の `:hover` 宣言がこの色へ振っている」という
  確かめられる書き方へ直した。**コミット前に捕まえた**
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

5 件。

1. **着手前に停止した。** UI 案が開始画面を持たないため導線を自分で決められず、選択肢 A/B/C を
   コスト付きで出して Issue で回答を待った。回答（A + 一覧 + D&D）を得てから計画へ進んでいる。
   **推測で埋めていない**のでやり直しは発生していないが、実装開始までに 1 往復かかった
2. **ボタンの見た目を 3 回作り替えた**（黒塗り → 青 → 一段薄い青 + ゴースト）。3 回ともオーナーの
   指示が起点で、そのたびにコメントの Why / Why not・Issue の判断記録・スクリーンショットの
   撮り直しをやっている（指摘 18・19・20）
3. **`cargo check` が依存不足で落ちた。** `gdk-3.0` が見つからず、`libgtk-3-dev` /
   `libwebkit2gtk-4.1-dev` / `libjavascriptcoregtk-4.1-dev` / `libsoup-3.0-dev` / `pkg-config` を
   入れてから通した（その後 `cargo test` 40 件通過）。**CI は cargo をまったく走らせない**
   （`release-desktop` がタグでのみ動く）ので、Rust 側は手元で確かめるしかない
4. **機械的な差し戻しが 3 件。** `DocumentAccessFailure` の type-import と value-import が
   2 行に割れていた（tsc）、Biome の整形が 13 件（`biome check --write` で 11 ファイル）、
   コミット本文に綴りの誤り（`#f0f0f e0`）。いずれも push 前に検査・目視で捕まえた
5. **`w-80` で案内の 1 行が「〜してくださ / い。」と折り返した。** Storybook の撮影を目視して
   気づき、`w-96` + `text-center` へ。**テストでは捕まらない**（happy-dom は Tailwind を
   解決しない）ので、規約どおり絵を見たことが効いた

## 発火

- 発火: 計測対象外(フック不発)

`echo hook-canary` が 3 回とも deny されていない（指摘 31）。発火ログ
（`design-composer-firings-*.log`）にこのセッションの見出しが無い（残っているのは別セッションの
ものだけ）ため、装置の起動回数は数えられない。**「未使用」と結論してはいけない** — 実際には
`implementation-flow` / `plan-reviewer` / `implementation-reviewer` / `claim-verification` /
`harness-record` が起動している（ログが無いだけ）。

## うまくいったこと

- **UI 案に無いものを、推測で埋めずに止めた。** 開始画面は UI 案の 5 画面のどれにも無いことを
  `open` / `recent` / `welcome` の 0 件で確かめたうえで、A/B/C をコストとともに出して判断を
  仰いだ。着手時点でセッション URL を Issue に残しているので、止まった経緯も追える
- **検証エージェントの指摘を鵜呑みにせず、前提を実測して却下した**（`plan-reviewer` の 12 番）。
  「Windows の HTML5 DnD と両立しない」は、このアプリがアセットのドラッグに HTML5 DnD を
  使っていることを前提にしていたが、`grep -rn "onDragStart\|onDrop\|draggable\|dataTransfer"`
  （`__tests__` 除く）が **0 件**。UI 案が絵で描いているだけで実装が無い。却下の根拠を Issue の
  処理表に残し、将来効く話なので `file-drop` の doc に Why not として残した
- **コメントに書いた事実の主張を、コミット前に数え直して否定できた**（指摘 33）。
  `claim-verification` の手順どおり色相を数えた結果、書いた 2 つの主張がどちらも偽と分かった。
  **この分類（`comment-structure-claim`）が `自己修正` で挙がったのは、スキル化の効果が
  出ている側の材料**
- **比喩がリポジトリの語彙にあるかを、答える前に数えた**（指摘 20）。「ゴースト」を
  UI 案・docs・`src/` で数え、`src/` の 7 件が全部テストの用例データだと確かめてから
  「この語彙は無い」と答えている。`rules/naming.md`「比喩・抽象語は `grep -i` で数える」の
  手順が、命名だけでなく**問いへの回答**にも効いた
- **UI 案から意図的に離れる判断を、離れる前に確認して記録した**（指摘 20）。ゴーストは UI 案に
  無い形だと数えて示したうえでオーナーの判断を取り、コードの Why not と Issue の両方に残した
- **色の選択をすべて UI 案の実測値に接地させた。** 薄い青 `#4db2ff` は思いつきではなく、
  UI 案が持つ唯一の `:hover` 宣言（`a:hover`）が `#0d99ff` から振っている先。ゴーストの
  hover `#f0f0f0` も UI 案に 31 回ある色で、同じ画面の最近使ったファイルの行と揃えた

## 規約への反映

無し。`harness-record` の管轄は記録を 1 ファイル足すところまでで、集計・介入の判断・
`rules/` やフック・スキルの変更は `harness-growth` が行う。

この回の材料は次の 6 点。

- **すり抜けが 3 件（指摘 18・19・20）で、3 件とも同じ「ボタンの見た目」。** `plan-reviewer` も
  `implementation-reviewer` も通過し、**オーナーが絵を見るまで誰も気づかなかった**。
  pr-394 が「pr-384・pr-391 に続き 3 回連続」と記録した、「オーナーが実際に見る/操作するまで
  気づかない」性質のすり抜けの、次の再発にあたる。ただし今回の 3 件は性質が少し違い、
  **19・20 は「UI 案に忠実であること」自体が正解ではなかった**形（19 は UI 案どおりの黒塗りを
  覆され、20 は UI 案に無い形を求められた）。pr-394 の指摘 2 と同じ形で、
  `rules/ui-verification.md` が「UI 案が拠り所」と「違う形にするなら確認する」の両方を
  持っているのに、**「UI 案が描いていない画面では、どちらを既定にするか」が書かれていない**
- **テストの穴が 5 件（指摘 21〜25）。** 内訳は分岐 2・配線 3 で、**すべて「全件緑なのに実装を
  どう壊しても落ちない」形**。`test-coverage-wiring` は pr-391 の指摘 6・pr-394 の指摘 16・18 でも
  出ている（**介入の起点は pr-244（層=観点）**）。今回はさらに `plan-reviewer` 段階でも
  同じ形（指摘 17）が挙がっている
- **`plan-comment-reference` が再発した**（指摘 8。pr-391 の指摘 2・17、pr-394 の指摘 12）。
  「コメント・doc の中でだけ対象を名指ししているファイルが、import を数えたファイル表に
  挙がっていない」形が、**pr-317 の介入（層=観点）以降も**繰り返している
- **`vrt-blind-spot` が再発した**（指摘 13。pr-394 の指摘 37。**介入の起点は pr-301（層=観点）**）。
  どちらも「この PR の成果物そのものが視覚差分では守られていない」形。今回は新規ストーリー
  5 本すべてがベースラインを持たない
- **`分類: なし` が 2 件（指摘 14・26）。** 内訳は
  (a) ストーリー専用の共有物の昇格に分類の語彙が無い（規約側には規則がある）、
  (b) `rules/hooks.md` に `useEffectEvent` の記述が無く、Effect Event をイベントハンドラから
  呼ぶ誤りを止める層が無い（lint も CI も止めない）。
  **(b) は `rules/hooks.md` が「記録 32 本で 1 件も引かれていない」として縮められた側の規約**で、
  縮めた結果として React 19 の新しい API が扱われていない形になっている
- **`hook-environment` が再発した**（指摘 31。pr-394 の指摘 39。**介入の起点は pr-272（層=観点）**）。
  `.claude/hooks/` が発火しない環境が連続しており、**`claim-verification` を含む層=skill の
  装置が本当に効いているかを発火ログで裏取りできない**状態が続いている（指摘 33 は効いた
  実例だが、ログではなくセッションの記憶からしか確かめられない）
