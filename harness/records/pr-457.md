# PR #457 harness棚卸し: test-coverage-branch を語彙分割し観点を追加

- マージ日: 2026-09-08
- 関連 Issue: 無し(スケジュール実行のハーネス棚卸しルーティンが起動)
- 差分規模: 4 ファイル / +50 -2

## ゴールと結果

`harness-growth` の Step 1(`count.sh`)を数え、Step 2 の分岐に該当する分類があれば
介入した PR を 1 本出すことがゴール。`test-coverage-branch`(観点層まで介入済み・pr-259)が
再発 35 件で 2c(飽和 → 語彙を割る)に該当し、通算 40 件のうち 17 件が「対になる入出力・状態の
片方しか確かめていない」という共通の形だったため `test-coverage-branch-asymmetry` を新設、
`implementation-reviewer.md` / `plan-reviewer.md` へ観点を追加した。締めの `対策済` 行は
PR 番号確定後にこの PR 自身で `harness/records/pr-442.md` へ追記し、積み残しなく完了した。
ゴールどおり達成。

なお `count.sh` の出力順(再発数降順)では `hook-environment`(再発 38)が最上位だったが、
中身を確認すると全件が同一の環境事実(`.claude/hooks/` 不発)の反復で 2c が前提とする
「別種の問題の混在」に当たらないため見送り、次点の `test-coverage-branch` を対象にした。

## 指摘

無し。人のレビュー(`get_review_comments` / `get_reviews` / `get_comments` の 3 つとも 0 件、
bot コメント 3 件は Storybook プレビュー・VRT レポート・カバレッジレポートのみ)、
CI 失敗も無し(全チェック success、`visual-regression` は対象ファイルなしで skipped)。
サブエージェント(`plan-reviewer` / `implementation-reviewer`)はこの回では呼んでいない
(規約ファイル自体の編集であり、実装検証の対象コードが無いため)。

## 手戻り

無し。締めの `対策済` 行は当初から「PR 作成後に実際の番号を確認してから追記する」という
二段階の計画どおりに進めた(pr-301 / pr-352 のように積み残しにする案もあったが、今回は
番号を確認できたためその場で解消した)。

## 発火

- 発火: 計測対象外(フック不発)(このセッションの ID に一致する発火ログ
  (`design-composer-firings-<session_id>.log`)が無く、見つかった唯一のログは無関係な
  別セッション(2026-09-04 開始)の見出しのみだった)

## うまくいったこと

- `count.sh` が返した数字(通算・再発・すり抜け)を鵜呑みにせず、`count_tag` と同じ
  awk ロジックを手元で再実行して独立に検算した(結果は一致)
- 過去の 2c 分割(`なし`→5 分割・`plan-scope-verification`→2 分割)の「書き方の例」を読み、
  同じ形式(内訳の件数・PR#・層の判断・フック化/skill化を採らなかった理由)で書いた
- 分割対象の 17 件を実際に全レコード本文を読んで拾い、推測でカテゴライズしなかった
  (`pr-442` 自身の申し送りが同じ偏りを指摘していたことも裏取りに使えた)

## 規約への反映

無し。このPR自体が `harness-growth` の Step 2(2c)の介入そのものであり、内容は本記録の
対象PR(#457)の差分にすでに反映済み。`harness-record` は集計・介入の判断を行わない
(`harness-growth` の管轄)。
