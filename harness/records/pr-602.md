# PR #602 Box に子の折り返し（wrap）を指定できるようにする

- マージ日: 2026-09-19
- 関連 Issue: #595
- 差分規模: 12 ファイル / +117 -13

## ゴールと結果

ゴールは「`layout: row` / `column` の Box に折り返しを指定でき、指定したときだけコンパイル
結果に `flex-wrap` が出る。パネルからも編集できる」。達成した。

**ゴールとのずれ: 「UI 案に無い行をパネルへ足してよいか」を確認待ちにしたままマージされた。**
Issue と PR 本文の両方に「マージ前に確認したい」と書いたが、回答は来ずにマージされたので、
行が増える側で確定した（#595 に追記）。

## 指摘

### 1. 折り返しが新しく開く経路（複数行になった子への挿入）をスコープが数えていなかった

- 分類: `plan-derived-scope-omission`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `DropZone.targetAt`（`features/canvas/domains/node-drop/drop-zone`）は挿入 index を
  「子が並ぶ向きの軸上でポインタが中点を越えた子の数」で決め、挿入線も同じ軸 1 本で引く。
  `docs/06-ui.md` も「向きの軸上でしか決まらない」と書いている。`row` の親が折り返せるように
  なるとこの前提が崩れる（2 行目の子が 1 行目と x 範囲で重なる）のに、計画の
  「変更しないもの」にも挙がっていなかった。#597 を起票してスコープ外の理由を書いた。
- 既存ルール: `implementation-flow` フェーズ 3〜4
- 次にどう防ぐか: 今回は記録のみ

### 2. UI 案に無い行を足すのに、確認を仰いだ記録が計画に無かった

- 分類: `ui-fidelity`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: UI 案（`docs/Design Composer.html`）の Box のプロパティパネルに出ている行は
  `direction` / `gap` / `padding` / `align` / `width` / `height` / `background` / `radius` /
  `shadow` の 9 本で、`wrap` の行はどの画面にも無い。計画はこの grep を却下案 4
  （`wrap-reverse` を足さない理由）の根拠にしか使っておらず、「実画面に UI 案に無い行が
  1 本増える」という事実そのものを書いていなかった。指摘を受けて、UI 案に無いこと・
  同じ `layout` 節の `justify` / `rotation` / `placement` も UI 案に無く実装では行が出て
  いることを計画へ書き、Issue と PR で確認を仰いだ。
- 既存ルール: rules/ui-verification.md「プロトタイプと違う形にする必要が出たら、実装前に
  理由を添えて確認する」/ `harness/case-law/ui.md`「UI 案が対象を描いていない」
- 次にどう防ぐか: 今回は記録のみ。**ただし判例の当たり判定が今回の形と噛み合っていない。**
  判例は「止めて確認する」としているが、パネルがスキーマ走査で自動生成される以上、
  止める対象は prop の追加そのものになり Issue のゴールと衝突する。判例の見直しは
  `harness-growth` の管轄

### 3. 「未決の判断: 無し」で締めたのに、3 つ残っていた

- 分類: `plan-open-decision-declaration`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: (a) セグメントに出る綴り（`nowrap` / `wrap` をそのまま画面に出すか）、
  (b) パネルの行の位置（`layout` の直後か）、(c) コンパイル出力順のどこへ挿すか、の 3 つが
  決まっていなかった。指摘を受けて 3 つとも理由付きで決め、Issue に残した。
- 既存ルール: `plan-review.md`「未決の判断が残っていないか」
- 次にどう防ぐか: 今回は記録のみ

### 4. 表示確認と VRT の扱いが計画に書かれていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: パネルに行が 1 本増える UI 変更なのに、Storybook / playwright-cli での表示確認手順も、
  VRT が赤くなる前提も、`visual-approved` を付ける判断も計画に無かった。実装後に
  Storybook + Chromium headless で確認し、VRT の差分画像も確認してラベルを付けた。
- 既存ルール: rules/ui-verification.md「表示確認」
- 次にどう防ぐか: 今回は記録のみ

### 5. 新しく付ける識別子のリポジトリ内 grep を計画に残していなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: UI 案の `grep -i wrap` は計画に書いたが、**リポジトリ内**の grep 結果を書いていな
  かった。実際に走らせると `reposition-preview-style` の `wrappingNames`（包んでいる祖先の
  ノード名）とテストの固定値 `"wrapper"` が同じ語幹を別の意味で使っている（衝突では
  ないが、確認した記録が要る）。
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ

### 6. 直す doc / コメントの行が計画で特定されていなかった

- 分類: `plan-comment-reference`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「間隔・揃え」を列挙している production のコメント 3 箇所は計画に挙げていたが、
  `compiled-element` だけ「doc とコメントの列挙を更新」としか書かず、
  `index.ts:243` のインラインコメントと `BoxElement.declarations` の doc のどちらを直すのかが
  特定されていなかった。
- 既存ルール: `plan-review.md`「ファイル表に挙がっているか」
- 次にどう防ぐか: 今回は記録のみ

### 7. 検算に使った実例が、影響を受ける側ではなかった

- 分類: `plan-scope-count-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「変更しないもの」の根拠として `node-html.box.test.ts` の
  「style は style 属性へ載せられる宣言の並びに直列化できる」を挙げたが、既定の出力を
  丸ごと固定しているのは同じファイルの
  「Box は flex コンテナになり、未指定の prop はスキーマのデフォルトで出力される」の
  `toEqual` のほう。結論（既定 `nowrap` は宣言を出さないので影響なし）は合っていたが、
  検算した対象が違っていた。
- 既存ルール: `implementation-flow` フェーズ 3 手順 2
- 次にどう防ぐか: 今回は記録のみ

### 8. 据え置いた `align-content` について、起票する / しないの判断を書いていなかった

- 分類: `plan-split-followup-omission`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 却下案に「Issue のスコープ外のまま」とだけ書いており、`align-content`（折り返しを
  入れて初めて意味を持つ prop）を後続 Issue にするのか、しないのかが記録されていなかった。
  指摘を受けて「立てない」と理由（Issue 本文が既に理由付きで据え置いている。起票すると
  やると決まっているものに見える）を Issue へ書いた。
- 既存ルール: AGENTS.md「タスクの分割」
- 次にどう防ぐか: 今回は記録のみ

### 9. 計画のテストケースが、既存テストの対の形に揃っていなかった

- 分類: `test-coverage-branch-asymmetry`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `schema.box.test.ts` の既存 3 本（`gap` / `align` / `justify`）は
  `free` → false と `row` → true を同じテストで対にしているのに、計画のケース名は
  `free` 側しか書いていなかった。実装では対にした。
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ

### 10. 計画のコンパイルのテストが、既定値と同じ入力で重複していた

- 分類: `test-default-input`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「折り返しを書いていない Box は宣言を出さない」は、既定解決で `nowrap` になるため
  「明示的に `nowrap` と書いた Box」と入力が同じになる。3 本の計画を、既存ファイルの書き方
  （`free` と `column` を 1 テストに並べる）に揃えて 2 本へ整理した。
- 既存ルール: rules/testing.md「既定値・フォールバックがある処理では、既定値と違う答えに
  なる入力を選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 11. 版を上げない根拠が、サブエージェントへ渡した要約から落ちていた

- 分類: `なし`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「1.1 の行へ追記・`Current` の minor は据え置き」の根拠（docs/01「その版でまだ
  動作確認をしていない間は上げ直さない」）は Issue の計画には書いてあったが、
  `plan-reviewer` へ渡した要約から落ちており、根拠が無いという指摘になった。
  **計画本体ではなく、検証エージェントへ渡す要約の欠落。** 計画を丸ごと渡すのか要約を
  渡すのかの基準は、どの規約にも書かれていない。
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### 12. 検証エージェントの実行中にコミットし、ミューテーションが混入した

- 分類: `hook-environment-guard-miss`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `implementation-reviewer` がミューテーション実測のために一時的に入れていた
  「`...wrap` を宣言配列の末尾へ動かす」書き換えを、検証の実行中にコミットしてしまい
  `a0e3d5a` がその状態で入った。これを機械的に止める
  `block-git-during-verification-agent.sh` は CI・git hooks に代替が無く、このセッションは
  フック不発（カナリアが deny されず、検証エージェントのマーカーも発火ログの見出しも無い）
  だったため誰も止めなかった。`git commit --amend` は権限で拒否されたので、打ち消しの
  コミット `834d4c0` を積んだ。**混入は既存のテストでは 1 件も落ちなかった**（下の 15 番）。
  もう 1 つの代替不能なフック `block-npx.sh` については `npx` 未実行を確認した。
- 既存ルール: `implementation-flow`「サブエージェントの使い方」/
  `.claude/hooks/README.md`「代替不能が実際に不発だったとき、手動で肩代わりする」
- 次にどう防ぐか: 今回は記録のみ

### 13. `wrapDeclarations` と `overflowDeclarations` が同じ形・同じ理由なのに、畳まない理由が無かった

- 分類: `comment-missing-precedent-echo`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 同じファイルの `rotationDeclarations` は「`opacityDeclarations` と形は似ているが、
  既定を出さない理由が別なので畳まない」と Why not を書いている。`overflow` と `wrap` は
  理由まで同じ（どちらも既定が CSS の初期値）なので、その先例に照らすと畳む側に倒れるのに、
  畳まない理由がどこにも書かれていなかった。畳まない理由（3 引数の関数になり呼び出しから
  どの prop の規則か読めなくなる）を doc に書いた。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ

### 14. 同じ判断根拠（`free` では効かない）を 4 箇所へ人手で書き足していた

- 分類: `duplication-rationale`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: prop を 1 つ足すのに、docs/03 の箇条書き・`FlexOnly` の doc・`Layout.direction` の
  doc・`compiled-element` のインラインコメントの 4 箇所で同じ列挙を伸ばしていた。
  後ろ 2 つから列挙を落とし（インラインは `enabledWhen`（`FlexOnly`）への参照だけにした）、
  2 箇所へ減らした。
- 既存ルール: rules/coding.md「規約の適用範囲」（同じ判断根拠を説明する doc の重複）
- 次にどう防ぐか: 今回は記録のみ

### 15. `BoxElement.declarations` の doc の列挙が、実物と合っていないまま 1 項目だけ足されていた

- 分類: `comment-enumeration-drift`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「このファイルのローカル関数（回転・折り返し・`overflow`・不透明度）」と書き足したが、
  実際に `declarations` が呼ぶローカル側は 6 系統あり、`placementDeclarations` /
  `tokenDeclarations` / インラインで組む `alignment` がもともと入っていなかった。
  実物に合わせて伸ばす形は drift が残るので、列挙ごと落とした。
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」/「1 つ挙げれば足りる例示」
- 次にどう防ぐか: 今回は記録のみ

### 16. 直近で「落とす」と決めた形の doc の列挙を、伸ばす側で直していた

- 分類: `duplication-rationale`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `Layout.direction` の doc の「コンパイル（`fill` と間隔・揃えの出し分け）」へ
  「折り返し」を足したが、前日にマージした #585 / #586（pr-593）で
  「消費側・用途を人手で列挙する doc は drift するので落とす」を決めていた。同じ形なので
  伸ばすのではなく落とすのが直近の判断と揃う。落とした。
- 既存ルール: rules/coding.md「1 つ挙げれば足りる例示」/ pr-593 の判断
- 次にどう防ぐか: 今回は記録のみ

### 17. スキーマの Why が主張している利得を、実装が受け取っていなかった

- 分類: `comment-premise-drift`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「値は CSS の `flex-wrap` の綴りをそのまま採る。別の語彙にすると CSS への対応表が
  要る」と書いたのに、`wrapDeclarations` は `CssDeclaration.create("flex-wrap", "wrap")` と
  CSS 値のリテラルを書き直しており、綴りを揃えた利得を受け取っていなかった
  （同じファイルの `alignment` は `String(props.align)` と値を通している）。
  `String(wrap)` で通す形へ直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 18. `wrapDeclarations` の doc が、宣言を出さなくてよい根拠を取り違えていた

- 分類: `comment-behavior-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「折り返さない**既定**は宣言を出力しない」と書いたが、落として安全な根拠は
  「スキーマの既定だから」ではなく「`nowrap` が `flex-wrap` の CSS 初期値だから」。
  隣の `overflowDeclarations` は「初期値と同じ `visible` は…」と書いており、
  `rotationDeclarations` はその 2 つを別の理由として区別している。言い回しを揃えた。
  括弧内の「docs/03 の表は `wrap` のみを規定」も、prop 名と値のどちらを指すか読めないので
  「`wrap: wrap` の行だけを規定」に直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 19. 同じ 4 つを指す列挙の並びが、`docs/03-schema.md` の中で 3 通りに割れていた

- 分類: `docs-consistency`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `layout: free` の箇条書きは `gap` / `align` / `justify` / `wrap`（wrap が最後）、
  Ellipse の「持たない prop」は `layout` / `wrap` / `gap` /…（2 番目）、コンパイル規則の表は
  `wrap` / `gap` / `align` / `justify`（先頭）と割れていた。スキーマの宣言順へ揃えた。
- 既存ルール: AGENTS.md「規約の更新」の docs 版
- 次にどう防ぐか: 今回は記録のみ

### 20. コミット本文の「同じ節の」が事実と違っていた

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「同じ節の `justify`・`overflow`・`opacity`・`visibility`・`rotation`・`placement` も
  UI 案に描かれていない」と書いたが、`overflow` / `opacity` / `visibility` は
  `group: "appearance"` で別の節。`layout` 節で UI 案に無いのは `justify` / `rotation` /
  `placement` の 3 つ。PR 本文へ転記する前に直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」/ `claim-verification`
- 次にどう防ぐか: 今回は記録のみ

### 21. 宣言の出力順が、どのテストにも守られていなかった

- 分類: `test-default-input`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `...wrap` を宣言配列の末尾へ動かしても 3632 件すべて通った（対照として `...gap` を
  同じく動かすと `node-html.box.test.ts` の直列化のテストが落ちる）。順序を固定している
  唯一のテストの fixture が `layout: row` / `gap: md` だけで、既定 `nowrap` では
  `flex-wrap` が出力に現れないのが原因。fixture へ `wrap: "wrap"` を足して固定した。
  **12 番の混入がテストで落ちなかったのはこの穴のため。**
- 既存ルール: rules/testing.md「既定値・フォールバックがある処理では、既定値と違う答えに
  なる入力を選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 22. 条件の形（`wrap === "wrap"`）が、どのテストにも守られていなかった

- 分類: `test-coverage-branch`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `wrap === "wrap"` を `wrap !== "nowrap"` に反転しても 240 件すべて通った。
  両者は語彙に無い綴り（`wrap: "wrap-reverse"`。docs/03「ドメイン違反」として別途エラーに
  出る値）で挙動が分かれる。その 1 件を足した。
- 既存ルール: rules/testing.md「古典学派のテスト」/ `implementation-reviewer`
  「テストが守っているかの観点」
- 次にどう防ぐか: 今回は記録のみ

### 23. ゴールの後半（パネルから編集できる）を名前で言うテストが無かった

- 分類: `なし`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `wrap` をスキーマから外すと `prop-control.shorthand.test.ts` の 2 本が落ちるので
  守られてはいたが、どちらも**名前は shorthand の話**で、落ちたテスト名から原因が読めない。
  `prop-control.enabled-when.test.ts` へ「子を並べる Box では折り返しのコントロールが出る」
  「子を並べない Box では折り返しのコントロールは出ない」の 2 本を足した。
  **カバレッジの穴ではなく、テスト名とゴールの対応の話**で、対応する規約が無い。
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

- **計画からの外れは無し。** 実装は計画（`plan-reviewer` の指摘を反映した後のもの）の順に進めた
- **打ち消しのコミットが 1 本（`834d4c0`）。** 12 番の混入を戻すためのもので、
  `git commit --amend` が権限で拒否されたため通常のコミットとして積んだ
- 実装検証の後に、doc の整理（14 / 15 / 16 / 17 / 18）と docs の並び（19）とテスト 3 本
  （21 / 22 / 23）を足すコミットを 1 本（`ed59921`）

## 発火

- 発火: 計測対象外(フック不発)

## うまくいったこと

- **ミューテーションを自分でも回した。** `wrapDeclarations` を常に宣言を返す形へ壊す /
  `arrangesChildren` の枝を外す、の 2 つを実装直後に実測して、テストが落ちることを確かめた
  （`implementation-reviewer` へは「実測済みなので重複しなくてよい」と渡した）
- **表示確認を操作まで通した。** Storybook を Chromium headless の CDP で開き、
  artboard を選ぶ → `Layout = row` → `Wrap = wrap` と押して、描かれた要素の style が
  `display:flex;flex-direction:row;flex-wrap:wrap;…` になることと、`Layout = free` で
  `Wrap` 行が `Gap` / `Align` / `Justify` と同じく消えることを確かめた
- **VRT の差分画像を 8 件とも読んだ。** パネル 8 ストーリーは `Wrap` 行から下が 1 行ぶん
  ずれているだけ（`Placement` / `Rotation` / `Layout` は無変化）、
  `createcomponent` の 2 件は名前欄のキャレットの点滅位置だけ、と確かめてからラベルを付けた

## 規約への反映

無し。`harness-record` の管轄は記録を 1 ファイル足すところまでで、集計と介入の判断は
`harness-growth` が行う。

**この回で気づいた、数える材料になりそうなもの（介入はしない）:**

- 2 番の `ui-fidelity`: 判例「UI 案が対象を描いていない」の「止めて確認する」が、
  パネルがスキーマ走査で自動生成される形と噛み合っていない
- 11 番・23 番は `分類: なし`（既存の規約に対応が無い）
