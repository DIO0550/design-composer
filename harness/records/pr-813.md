# PR #813 数字だけの名前を識別子の規則で禁止する

- マージ日: 2026-09-26
- 関連 Issue: #772
- 差分規模: 14 ファイル / +199 -20

## ゴールと結果

ゴールは達成した。数字だけの名前（`3` / `20` / `007`）は、部品名・artboard 名・ノード名・トークン名のどれでも、読み込みの検証・追加・改名のすべてで弾かれる。これで `Record` のキー順が書いた順と一致し、定義順と「位置を保つ」が成り立つ。
Issue はゴールを 2 択（順序を保つ / 仕様で禁止する）のまま残していた。docs はどちらとも決めていなかったので、実装前に止めてユーザーに確認した。回答は B（禁止）・識別子すべて・既存ファイルは読み込みでエラー。`publicProps` / `overrides` のキーは #812 に分けた。
GitHub 上の人のレビューは 0 件（`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも確認。コメントは bot の投稿と自分のテストケース表だけ）。CI は初回から緑で、VRT は差分なし（179 件）。

## 指摘

### 1. 2 つの述語の同じ条件について、逆向きの委譲を検討していなかった

- 分類: `duplication`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `token` → `document-names` は循環するとして却下したが、逆向き（`document-names` → `token`）は依存方向として通ることを見ていなかった。
  逆向きは仕様の参照の向き（04 → 01）と逆になるので採らず、重複を残して一致をテストで固定した。理由は Issue とコミット本文に書いた。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 2. 書き直す doc の箇所を特定していなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画は「doc 更新」とだけ書いていた。`isValidIdentifier` の「規則は kebab-case そのもの」「CaseStyle で共有」と、`isValidName` の `@returns` が変更後に事実と食い違う。行を特定してファイル表に足した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 7

### 3. docs に書く予定の理由が事実と違った

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「JSON オブジェクトで整数形のキーは並び替わる」と書く計画だった。実際には JS オブジェクトのプロパティ順の規則で、動くのは配列の添字として読めるキーだけ（`007` は動かない）。
  node で実測して理由を直し、`007` まで禁じる理由を 1 行添えた。
- 既存ルール: rules/coding.md「外部の挙動は動かして確かめる」

### 4. 検証を通らないドキュメントでは順序が崩れたまま残る前提と、publicProps のキーをスコープに書いていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 識別子規則違反のドキュメントも開いて編集できる（03-schema）ので、定義順は検証を通ったドキュメントに限って成り立つ。`names` の doc に明記した。
  `publicProps` / `overrides` のキーも同じ問題を持つが識別子の規則の外なので、#812 に分けた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 7

### 5. design-document 側で「追加」経路のテストが無かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: ゴールは読み込み・追加・改名の 3 経路だが、design-document 側のテスト計画には改名しか無かった。`isUsableName`（部品化・包む操作と同じヘルパーを通る）のケースを足した。
- 既存ルール: rules/testing.md「1. 振る舞い(仕様)をテストする」

### 6. 探した場所と grep の結果を計画に書いていなかった

- 分類: `naming`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `isAllDigits` の命名に問題は無かったが、`StringEx` / `NumberEx` / `CaseStyle` を探した記録が計画に無かった。Issue に追記した。
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」

### 7. 識別子の規則を持つ第三のモジュールへ集約する案が未検討

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 2 つの述語が共に import するモジュール（`dcmp/identifier/` 等）に置く案。domains に置くには型が要り、`Brand` が要るほどの誤用がまだ無いので採らなかった。却下理由を Issue とコミット本文に残した。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 8. 委譲しない理由を 2 つの doc に別々に書き、テストが守る内容まで写していた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 両方の doc に別の理由と「片方だけ直さない」を書いていた。理由は `TokenSet.isValidName` 側だけに残し、もう片方はそこを参照する形にした。一致テストが守る内容は消した。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 9. docs/04 が 01 の規則を写していた / docs/01 の対象にトークン名が無かった / docs/03 の列挙が追随していなかった

- 分類: `harness`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 04 は 01 への参照だけに縮めた。01 の「対象」にトークン名を足し、03 のバリデーション一覧に「数字だけの名前」を足した。
- 既存ルール: なし

### 10. isAllDigits のテストに先頭 0 の綴りが無かった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 判定を「配列の添字として正規な綴り」へ狭めるミューテーションが、utils のテストでは落ちなかった（document-names の 1 件だけが拾った）。`"007"` のケースを足し、1 テスト 1 assert に分けた。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 11. @returns の折り返しの行幅が揃っていなかった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `TokenSet.add` の `@returns` の 2 行目だけが突出して長かった。折り返しを揃えた。
- 既存ルール: なし

### 12. implementation-reviewer がミューテーションの後始末で未コミットの差分を戻した

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: エージェントが `git checkout <file>` で後始末し、未コミットの 3 ファイルが HEAD に戻った。エージェント自身が元の diff を `git apply` で戻し、blob hash の一致を報告した。
  返答を受けてから `git diff` で差分が揃っていることを確かめた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md`「サブエージェントの使い方」

### 13. テストを既存テストの内側へ挿入した

- 分類: `なし`
- 出どころ: `自己修正`
- 内容: `rename-name` へテストを足すスクリプトの挿入位置を誤り、既存の `test()` の中へ入った（vitest の「test の中の test」で検出）。ファイルを戻して挿入し直した。
- 既存ルール: なし

### 14. ファイル冒頭のコメントが番号で名指ししているテストの位置を動かしかけた

- 分類: `comment`
- 出どころ: `自己修正`
- 内容: `usable-name` のテストを途中へ挿入したが、冒頭コメントが「5 番目のテスト」を名指ししていた。末尾へ移して位置を保った。
- 既存ルール: なし

### 15. Claude Code のフックが不発の疑いがある環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されずに通った。検証エージェントを 2 回通した後もマーカーが無く、`CLAUDE_CODE_SESSION_ID` の発火ログも無かった。
  別 ID のログに SessionStart の見出しがあるだけで、Skill / Agent の追記は無い。禁止コマンドを使っていないことは見直した。git の pre-push フックは発火した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- 着手前: Issue のゴールが 2 択のままで、docs も決めていなかったため止めてユーザーに確認した（A / B / C と、禁止範囲・既存ファイルの扱い）
- 計画段階: plan-reviewer の指摘（1〜6）で計画を直した。実装前なので作業の手戻りは無い
- 実装段階: テストの挿入位置の誤り（13・14）。implementation-reviewer の指摘（7〜11）で doc・docs・テストを直した。エージェントの後始末で差分が戻りかけた（12）
- PR 後: 無し

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。記録だけを残す（集計と介入の判断は harness-growth の管轄）。
