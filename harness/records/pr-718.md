# PR #718 GradientToken ドメインと TokenSet の gradients 種別を実装する

- マージ日: 2026-09-23
- 関連 Issue: #607
- 差分規模: 34 ファイル / +852 -44

## ゴールと結果

ゴール（`.dcmp` の `tokens.gradients` を読み込んで値を保ったまま書き戻せる／値域の検証が
`GradientToken` に閉じている）は達成した。CSS 出力（#609）・トークンパネル（#610 / #611）・
名前衝突の検証（#608）は Issue のスコープ外の列挙どおり入れていない。

**ゴールの文言が 1 つ間違っていた。** 計画では「既存の挙動は 1 つも変わらない」と書いたが、
デフォルトテーマにグラデーションを同梱した結果、新規ドキュメントが必ず `tokens.gradients` を
書き出すようになり、`docs/01-file-format.md` の版の表も直す回になった。正しくは「画面の
見え方は変わらない」で、`implementation-reviewer` の指摘で気づいて直した。

## 指摘

### 1. `docs/01-file-format.md` の版の表への追加が計画に無い

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `docs/04-tokens.md`「gradients」が「アプリが読み書きできるようになった時点で表へ
  足す（まだ足していない）」と名指ししているのに、変更ファイル表に `docs/` が 1 件も無かった。
  このとき「#606 のマージ後コメントが #608 へ引き継いでいる」を根拠に受け入れなかった。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「変更するファイル」

### 2. 同じ指摘を `implementation-reviewer` が出し直して、今度は受け入れた

- 分類: `plan`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 1 の却下は**自分がその後に下した決定で前提が崩れていた**。デフォルトテーマに
  グラデーションを入れた時点で「アプリが読み書きできるようになった時点」はこの回になる。
  計画の判断を変えたとき、その判断に依存していた却下理由を見直していなかった。
- 既存ルール: `.claude/skills/claim-verification/SKILL.md`（`comment-premise-drift`）

### 3. 実行時にだけ落ちるテストを数え漏らした

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `tsc` で波及を実測して 17 箇所を数えたが、`toEqual` で種別の件数を数えている
  テストは型エラーにならない。2 件と書いたが実際は 4 件で、`document-json.parse.test.ts` と
  `template.theme.test.ts` が表に無かった。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 2

### 4. 却下案の代償を数えていなかった（初期テーマ）

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「初期テーマにグラデーションを入れない」を却下案に置いたが、既存の
  `template.theme.test.ts`「デフォルトテーマはトークン種別を1つも空にしない」が
  `docs/04-tokens.md` の「空はあり得ない」を実装側で固定していた。却下は無償ではなかった。
- 既存ルール: rules/testing.md「テストケースの根拠は仕様・設計ドキュメントに求める」

### 5. 絞り込みの二次波（型の波及）を測っていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 波及の実測は「`TokenSet` にキーを足した一次波」だけで、パネルを 5 種別へ絞る変更が
  生む二次波を測っていなかった。`token-editor` の見出しが絞られていない `Token` を受け取る
  経路が 1 件残っていた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 1

### 6. 「未決の判断: 無し」が誤りだった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 版の扱い・初期テーマ・`SingleVariableTokenKind` を絞るか・`InitialValues` に入れる
  具体値の 4 つが、実装中に手が止まる判断として残っていた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「未決の判断」

### 7. ゴールの確認手段が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「値域の検証が `GradientToken` に閉じている」「画面が動かない」をどう確かめるかが
  書かれていなかった。grep と VRT を完了条件に足した。
- 既存ルール: `.claude/agents/plan-reviewer.md`（`plan-goal-verification`）

### 8. 変更で嘘になる doc / Why を行単位で数えていなかった

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 変更ファイル表が「ファイル名 + 直すメンバー」止まりで、同じファイルの中の
  直すべき doc（`Token.normalized` の列挙・`InitialValues` の Why・`KindLabels` の件数）が
  落ちていた。移動でも改名でもないぶん見落としやすい形。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 9. 兄弟モジュールと非対称なテスト構成だった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `color` / `shadow` / `typography` はいずれも `*.json.test.ts` と `*.type.test.ts` を
  持つのに、gradient は JSON の観点を `normal` へ混ぜ、型テストを持っていなかった。
  `Json.knownFields` が 2 段（トークンと stop）要ることも計画に書いていなかった。
- 既存ルール: rules/testing.md「配置と命名」

### 10. CSS 変数を出さないことを 1 件も守っていなかった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `token-css` の `case "gradients": return []` を「出す」へ壊してもテストが 0 件しか
  落ちなかった。原因は、`token-css.default-theme.test.ts` の fixture から `gradients` を
  外したことで、「初期トークンセット**全体**が変換される」という既存の観点から 1 種別が
  欠けていたため。実物どおりに戻すと完全一致の `toEqual` がそのまま固定する。
- 既存ルール: rules/testing.md「assert は『落ちうるか』と『1つの仕様か』で見る」

### 11. 新しく足した配線をテストが守っていなかった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 見出しを `TokenControl.forSelection` 経由へ変えた判断が、壊しても 1 件も落ちない。
  `token-editor.title.test.tsx` の setup にグラデーションが 1 件も無く、分岐へ到達しないため。
- 既存ルール: rules/testing.md「その assert は落ちうるか」

### 12. 到達しない綴りを 1 つ新設していた

- 分類: `over-guard`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `KindLabels` に `gradients: "Gradient"` を足したが、見出しを描く経路が同じ差分で
  絞られたため**どの状態でも描画されない**。しかも UI 案に無い綴りを 1 つ決めており、
  同じ差分の「無い絵を思いつきで足さない」と逆を向いていた。
- 既存ルール: rules/ui-verification.md「プロトタイプに無い操作を…足さない」

### 13. 実行時に 1 件も落とさない絞りが残った

- 分類: `over-guard`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `TokenSection.forDocument` の token 側 `filter` は、種別を先に絞っているので到達 0 回
  （throw を仕込んで実測）。`TokenSet.tokensOf` の戻りが種別で狭まらない型の都合で残る。
  ジェネリクス化すると `as` が 1 つ増えるので、理由を 1 行書いて残した。
- 既存ルール: rules/coding.md「狭い型への `as` は…戻り値1箇所のみ」

### 14. モジュールの公開範囲が広すぎた

- 分類: `dependency`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `token/index.ts` から `GradientShapes` / `GradientShape` / `GradientStop` を
  re-export していたが、モジュールの外に消費側が 0 件。`fields()` も同ファイル内でしか
  呼ばれないのに公開メソッドだった。
- 既存ルール: rules/architecture.md「`index.ts` から export するのは外部に公開する必要が
  あるものだけに絞る」

### 15. 兄弟モジュールの doc を逐語コピーしていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `GradientStopFields` の doc が `ShadowFields` の doc の逐語コピーで、同じ判断根拠が
  2 ファイルに独立して存在し片方だけ直せる状態だった。`Capitalize<keyof Required<T>>` の
  `Required<>` も、省略可能フィールドを持たない型へそのまま持ってきて無効だった。
- 既存ルール: rules/coding.md「規約の適用範囲」（doc の重複）

### 16. doc が仕様の非自明な部分を落としていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `@param angle` が `docs/04-tokens.md` の「`0` が下から上。`rotation` と違い `0` は
  無回転ではない」を落としていた。このリポジトリには `rotation` が別の `0` の意味で実在する。
  ほかに、出典に無い主張（「角度に値域は無い（docs…）」）と、同じ差分で前提が崩れた Why
  （`InitialValues` の「追加直後の見え方はこちらの関心事」）が各 1 件。
- 既存ルール: rules/coding.md「型の再掲は書かない…取りうる値の意味」

### 17. `Panel` がこのリポジトリで別の意味を持っていた

- 分類: `naming`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `PanelTokenKind` と名付けたが、`Panel` は `PropertyPanel` / `AssetsPanel` /
  `LayersPanel` と常に具体名で使われており、しかも**プロパティパネル側は同じ差分で
  グラデーションを受け入れている**。`EditableTokenKind` へ改名した。
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」

### 18. コンパニオンオブジェクトのメソッドに doc が無かった

- 分類: `comment`
- 出どころ: `レビュー（人）`
- 内容: 新設した `GradientStop` / `GradientToken` の `fromJson` / `toJson` 4 件が doc 無しで
  push まで通った。`missing-doc-comments.py` が**ファイル直下の宣言しか見ておらず**、
  ロジックが集まるメソッドに届いていない。検出器を広げると既存 303 件が出るため、#719 /
  #720 へ分けた。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 19. doc の説明が冗長だった

- 分類: `comment`
- 出どころ: `レビュー（人）`
- 内容: `GradientStop.create` が「型では閉じられず入口の `Option` だけが境界」を 2 段落で
  2 回言っており、後段は「その入口はまだ無い」という #611 で嘘になる一時的な事実に
  寄りかかっていた。述語 2 つも 1 行で済むのに 6 行の doc を付けていた。
- 既存ルール: rules/coding.md「理由の根拠に、いま直されつつある一時的な事実を置かない」

### 20. 同じ Issue を 2 つの PR が並行して閉じようとした

- 分類: `parallel-issue-work`
- 出どころ: `CI`
- 内容: 別セッションの #717 が 30 秒差で同じ Issue #607 を実装しており、
  `duplicate-issue-pr` が検出した。どちらも CI 緑で、争点は「CSS 出力とトークンパネルまで
  入れるか」だけだった。比較を Issue へ残して判断を仰ぎ、#718 がマージされた。
  **#717 は open のまま残っている。**
- 既存ルール: AGENTS.md「着手した Issue は、その回で閉じる」

### 21. 値の使用を消したのに import を `type` へ変えていなかった

- 分類: `type`
- 出どころ: `自己修正`
- 内容: `TokenEditorTitle` が `TokenSelection.token` を呼ばなくなったのに
  `import { TokenSelection }` のままで、`biome check` が 1 件警告した。push 前検査で気づいた。
- 既存ルール: なし（lint が守っている）

## 手戻り

- **却下した指摘を後で自分で取り消した**（1 → 2）。`plan-reviewer` の「版の表」を
  「#608 の担当」として受け入れず、その後に下した「初期テーマへ同梱する」という決定で
  前提が崩れ、`implementation-reviewer` の同じ指摘で取り消した。フェーズ 4 で
  「`v0.1.0` タグがあるので #608 が判断し直す材料が増えた」とも書いたが、タグの日付
  （2026-08-30）が 1.1 の中身が入った 9/18 より前だと確かめておらず、これも取り消した
- **パネル側の絞り方を 2 回変えた**。`PanelTokenKind` で実装 → `implementation-reviewer` の
  指摘で `KindLabels` の扱いと名前を変更 → `EditableTokenKind` へ改名
- **doc を 3 回書き直した**。実装時 → `implementation-reviewer` の指摘（6 件）→ 人の指摘で
  冗長を削り、足りない `@param` / `@returns` を埋める
- CI は 1 度も赤くなっていない

## 発火

- 発火: 計測対象外(フック不発)

`echo hook-canary` が deny されず、`plan-reviewer` を通した後にもかかわらず検証エージェントの
マーカーもこのセッションの発火ログも無かった（`.claude/hooks/README.md`「発火しているかを
確かめる(カナリア)」の表の 4 行目 = 本当に不発）。代替不能な 2 つは手動で肩代わりした
（`npx` は 0 回、検証エージェント実行中のコミットも 0 件）。git hooks（層 2）は push 時に
実際に発火している。

## 規約への反映

無し。このスキルの管轄は記録までで、数える・介入するのは `harness-growth`。

ただし `18`（コンパニオンのメソッドに doc の検査が届いていない）については、**この回で
検出器の拡張を書いて偽陽性 0 件まで確かめ、既存 303 件の測定と合わせて #719 / #720 へ
退避してある**（差分は #719 のコメントに全文があり `git apply` で戻せる）。介入の可否は
`harness-growth` の棚卸しで判断する。
