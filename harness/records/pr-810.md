# PR #810 import 規約の検出器から単独では発火しない feature-cycle を外す

- マージ日: 2026-09-26
- 関連 Issue: #670
- 差分規模: 2 ファイル / +22 -18

## ゴールと結果

ゴールは達成した。`import-rule-violations.py` から `feature-cycle` を外し、feature 単位の閉路を見ない理由(違反なしで通る辺は入れ子の段数を必ず 1 増やす)と戻す条件を docstring に書いた。
Issue は閉じた PR #668 の構成(`feature-direction`)を前提にしていたが、main の構成(`feature-sibling` / `feature-ancestor`)でも同じ主張が成り立つことを論証と実測で確かめてから進めた。
GitHub 上の人のレビュー指摘は 0 件(`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも確認。コメントは bot の投稿と自分のテストケース表だけ)。CI は初回で緑。

## 指摘

### 1. 戻す条件を「兄弟参照を許すなら」とだけ書く計画だった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 論証が頼っているのは「許される辺は段数を必ず 1 増やす」という不変条件で、子 → 親やトップレベル同士を許しても崩れる。不変条件そのものを書き、戻す条件をそこから導く形に直した。
- 既存ルール: なし

### 2. 同じ docstring 内のコメントが存在しない doc を根拠にしていた

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 型だけの import を拾う理由が `canvas/index.ts` の「canvas -> editor の辺を作ると循環する」を引いていたが、その記述は既に無かった。計画のファイル表に入っておらず、例を外して理由だけ残した。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 3. 兄弟だけで閉じる閉路を README の probe に残さない計画だった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 閉路が向きの違反だけで閉じる 2 形のうち、兄弟同士の形を既存の片方向 probe が再現していなかった。逆向きの 1 行を足した。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 4. 「probe なしで 0 件」を今回の変更を守るテストケースに数えていた

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: main は変更前から 0 件で、閉路の検出を残しても消しても同じ答えになる。テストケースから外し、退行確認として別に扱った。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 5. トップレベルの feature 同士を通す改変が、どの probe でも落ちなかった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: ミューテーションで実測。`src/features/` のトップレベルが `editor` 1 つだけで、probe がすべて `editor` 配下にあった。docstring の戻す条件 3 種のうち 1 種が守られていなかったので、`probe-a` / `probe-b` の probe を足した。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 6. 足した docstring の段落が 6 行で理由が長かった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 理由 4 行 + 戻す条件 2 行を 3 行に畳んだ。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 7. README の検出器の表が報告する種別を列挙しきれていなかった

- 分類: `harness`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 差分以前から `feature-sibling` / `feature-ancestor` / `feature-nest-depth` が抜けていた。同じ行を書き換えるので揃えた。
- 既存ルール: なし

### 8. Issue の前提が、マージされずに閉じた PR の構成だった

- 分類: `parallel-issue-work`
- 出どころ: `自己修正`
- 内容: #670 は #667 の並行 PR のうち閉じた #668 から切り出されたため、`feature-direction` / `import-rule-cases.sh` という main に無いものを前提にしていた。採られたのは #679 側。主張が main でも成り立つかを論証と実測で確かめ直し、Issue に書いてから計画した。
- 既存ルール: なし

### 9. Claude Code のフックが不発だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が通り、検証エージェントのマーカーもこのセッションの発火ログの見出しも無い(本当に不発の枝)。`pnpm install` で `core.hooksPath` を向けてから push し、`harness/githooks/pre-push` で全検査を通した。
- 既存ルール: なし

### 10. feature の中の循環は検出されているのかを問われた

- 分類: `harness`
- 出どころ: `レビュー（人）`
- 内容: マージ前にセッション中で受けた質問。ファイル単位で閉じる循環は `import-cycle` が拾う。一方、モジュール同士では閉じてもファイル同士では閉じない循環(`a/index.ts` → `b`、`b/inner.ts` → `a`)は、変更前後どちらの検出器でも 0 件だった。この PR の退行ではなく、以前からの検出器の穴。
- 既存ルール: rules/architecture.md「依存方向のルール」(循環依存は全面禁止)

## 手戻り

計画を plan-reviewer の指摘 4 件で直してから実装に入った(指摘 1〜4)。実装後、implementation-reviewer の指摘 3 件を push 前に反映した(指摘 5〜7)。PR を出した後の手戻りは無し。

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。記録のみ(集計と介入の判断は `harness-growth` の管轄)。
