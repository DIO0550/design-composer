# PR #593 消費側を列挙する残り 2 件の doc から列挙を落とす

- マージ日: 2026-09-18
- 関連 Issue: #585
- 差分規模: 2 ファイル / +3 -3

## ゴールと結果

ゴールは「doc が消費側を列挙し、消費側が増えるたびに人手で伸ばす形」になっている 2 件
（`CanvasPointer.ts:6` / `canvas-measure.ts:11-12`）を、実物とずれた記述が残らない形に
決めて直すこと。達成した。

**ゴールとのずれ: Issue が立っていた前提（「`src/` に残るのは 2 件」）が間違っていた。**
`plan-reviewer` の実測で同じ形が 9 件あり、うち `canvas-gesture.ts:4-6` は既に drift して
いた。スコープは Issue 本文どおり 2 件に留め、慣習ごと決める #591 と `drawnAt` の重複 #592
へ分離した。その結果、`src/features/canvas/__tests__/` の中で `canvas-measure` だけが列挙を
持たない**一時的な割れ**が残る。これは PR 本文と #591 に明記した。

## 指摘

### 1. 同じ形の doc を探す grep が、書いたとおりの範囲を覆っていなかった

- 分類: `plan-scope-premise-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 却下案に「消費側を列挙している doc は本件の 2 件だけ」と書いたが、grep が
  **モジュール名（`` `use-xxx` ``）での列挙しか見ておらず、役割での列挙を拾えていなかった**。
  doc 行の中黒列挙（`grep -rn '^\s*\*.*・.*・' src/`）で数え直すと同じ形が 9 件あり、
  `canvas-gesture.ts:4-6` は消費側 12 ファイルのうち `use-node-resize` と
  `CanvasPointer.normal.test.tsx` が列挙のどれにも入らず**既に drift していた**。
- 既存ルール: `implementation-flow` フェーズ 3 手順 3 / `claim-verification` 手順 3。
  **計画を書く時点で `claim-verification` を呼んでおらず、却下案の「他に無い」を
  確かめないまま `plan-reviewer` へ渡した。**
- 次にどう防ぐか: 今回は記録のみ

### 2. Issue のコメントにあった消費側「14 件」が、別ヘルパーを取り違えた数だった

- 分類: `plan-scope-count-verification`
- 出どころ: `自己修正`
- 内容: #585 のコメント（前のセッションが残したもの）が
  `grep -rln "stubBounds\|drawnAt\|clearDrawn" src/` で `canvas-measure` の消費側を 14 件と
  数えていたが、**`drawnAt` は `canvas-measure` の export ではない**。
  `artboard-canvas/__tests__/setup.tsx:228` のローカルヘルパーで `canvas-measure` を
  import していない（`stubBounds` の再実装）。実際の消費側は 7 ファイル。
  この数は「どの案を採るか」の材料として使われていたので、計画の前に数え直して訂正した。
- 既存ルール: `implementation-flow` フェーズ 3 手順 2
- 次にどう防ぐか: 今回は記録のみ

### 3. `CanvasPointer` について「文ごと落とす」案を却下案に挙げていなかった

- 分類: `plan-rejection-coverage`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `canvas-measure` では「Why ごと落とす」を候補に挙げて理由付きで却下していたのに、
  `CanvasPointer` では検討そのものが無く、「`DrawnBounds.ts:6` の参照先が無くなる」とだけ
  書いて打ち切っていた。`DrawnBounds.ts:6` は「1 箇所に集める」を自分でも書いているので、
  括弧の参照を落とすという近い代替が実在した。
- 既存ルール: `implementation-flow` フェーズ 3 手順 6
- 次にどう防ぐか: 今回は記録のみ

### 4. `canvas-measure` の doc を名指しする 5 行が、計画のファイル表に無かった

- 分類: `plan-comment-reference`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `grep -rn "canvas-measure" src/` で、import 以外に doc を名指しするコメントが 5 行
  ある（`opened-document-editor.zoom-fit:15` / `use-canvas-view.fit:16` /
  `use-drawn-bounds.normal:12` / `DrawnBounds.normal:9` / `DrawnBounds.edge:9`）。
  確認の結果どれも書き換え不要だったが、計画は「呼び出し側の変更は 0 件」としか書いておらず、
  **この 5 行を見たうえで 0 件なのか、見ずに 0 件なのかが区別できなかった**。
- 既存ルール: `plan-review.md`「ファイル表に挙がっているか」
- 次にどう防ぐか: 今回は記録のみ

### 5. ゴールの達成を確かめるコマンドが計画に無かった

- 分類: `plan-goal-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: typecheck も lint も test も doc の drift を検出しないので、確認コマンドが無いと
  「直した」と宣言する以外に確かめる手段が無い。指摘を受けて、2 つの doc から消費側の
  名指し・役割の列挙が消えたことを見る grep（0 件で完了）と、`DrawnBounds.ts:6` の参照先が
  残っていることを見る grep（1 件で完了）を完了条件に足した。
- 既存ルール: `plan-review.md`「ゴールの達成を確かめる手段」
- 次にどう防ぐか: 今回は記録のみ

### 6. 「未決の判断: 無し」で締めたが、実装中に手が止まる判断が 3 件残っていた

- 分類: `plan-open-decision-declaration`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 兄弟 8 件をどうするか / 同じフォルダ内の書式の割れを揃えるか / drift を無くすのか
  伸びる形を無くすだけか、の 3 件が未決のままだった。指摘を受けて 3 件とも決め、
  表にして Issue へ書いた。
- 既存ルール: `plan-review.md`「未決の判断が残っていないか」
- 次にどう防ぐか: 今回は記録のみ

### 7. 後続 Issue が「マージ後に立てる」としか書かれていなかった

- 分類: `plan-split-followup-omission`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `row-names.ts` の別 Issue も `drawnAt` 重複の別 Issue も、起票のタイミングだけで、
  分けた側に何をスコープ外と書くか・元の Issue にどうリンクするかが計画に無かった。
  指摘を受けて**マージ後ではなく分割が起きた時点で**起票した（#591 / #592）。
- 既存ルール: AGENTS.md「タスクの分割」
- 次にどう防ぐか: 今回は記録のみ

### 8. 新しい doc が「各フック」という、書いた時点で嘘になる主張を持っていた

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画の書き換え案「各フックが同じ読み方をするため 1 箇所に集める」は、消費側が
  全部フックであることを主張していた。`utils/__tests__/CanvasPointer.normal.test.tsx:14-22`
  の `PointerHarness` は**フックではなくコンポーネント**から `offsetOf` を呼んでいるので、
  書いた時点で既にずれていた。「呼び出し側」に直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 9. 書き換え案が、同じフォルダのバレルの doc と同じ根拠を二重に持っていた

- 分類: `duplication-rationale`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 案「消費側がキャンバスの複数モジュールと `features/editor` にまたがるので」は、
  `canvas/__tests__/index.ts:1-4` が既に書いている「外の feature（`features/editor` の
  `opened-document-editor`）が…使う」と重なっていた。固有名を外す方針にした。
- 既存ルール: rules/coding.md「規約の適用範囲」（同じ判断根拠を説明する doc の重複）
- 次にどう防ぐか: 今回は記録のみ

### 10. 固有名を外した結果が「二重は残ったまま情報だけ減る」中間形になっていた

- 分類: `duplication-rationale`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 指摘 9 への対応として固有名だけを外したが、「外の feature が使う」という事実自体は
  バレルの doc と重なったままで、具体性だけが落ちていた。加えて「またがる」は
  editor 側 3 ファイルが `stubBounds` を使わなくなれば黙って嘘になる**数え直せる事実**で、
  この Issue のゴール（実物とずれた記述を残さない）と食い違っていた。
  置き場所を決めているのは**またがる範囲ではなく持ち主**だと書き直した
  （`canvas-elements.ts:11-14` と同じ判断）。
- 既存ルール: rules/coding.md「規約の適用範囲」
- 次にどう防ぐか: 今回は記録のみ

### 11. 残した 1 文が、規約をこのファイルに当てはめただけの一般論だった

- 分類: `comment-excess`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「呼び出し側が同じ読み方をするため 1 箇所に集める」は、モジュールが存在すること
  自体から読める内容だった。**残す価値があったのは、この Issue で `plan-reviewer` への
  反論として自分で書いた事実のほう**（「`offsetOf` を通さず直読みしても型・テスト・lint の
  どれも止めない」）で、理由として書いておきながら doc には一般論を書こうとしていた。
  「1 箇所に集めるのは、`event.clientX` を直に読んでも型・テスト・lint のどれも止めない
  ため」に書き直し、`use-range-select:83` を直読みへ差し替えて
  typecheck / oxlint / biome / range-select 22 件がすべて通ることを実測してから確定した。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」（消すのは…一般論）
- 次にどう防ぐか: 今回は記録のみ

### 12. `.claude/hooks/` が発火しない実行環境だった

- 分類: `hook-environment`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されずに通った。`.claude/hooks/README.md`
  「発火しているかを確かめる」の表で切り分けたところ、検証エージェントのマーカーも
  発火ログの見出しも**どちらも無い**ので「本当に不発」の枝。`plan-reviewer` /
  `implementation-reviewer` をどちらも通した後の判定なので、着手直後の取りこぼしではない。
  セッション中に禁止コマンド（`npx` / `git add -A`）を実行していないことを確認済みで、
  素通りしたものは無い。**git hooks（層 2）は生きており**、push 時に
  `check-added-test-helper-duplication.sh` の出力が出た。
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

**doc の文面を計画から 2 回書き換えた**（指摘 10・11）。どちらも `implementation-reviewer`
の指摘で、実装済みの差分を出してから直している。

**計画そのものを 1 回書き直した**（指摘 1・3〜7）。`plan-reviewer` が 10 件返し、うち 8 件を
受け入れて計画を改訂、2 件は理由を書いて受け入れなかった（`CanvasPointer` の文ごと落とす案、
`canvas-measure` の Why ごと落とす案）。

**Issue のコメントにあった「14 件」を計画前に訂正した**（指摘 2）。

依存関係が未インストールの状態からセッションが始まったので `pnpm install` を実行した。
baseline の typecheck / test:run はいずれも green。

## 発火

- 発火: 計測対象外(フック不発)

## うまくいったこと

**残す doc の主張を、書いたあとに実際に壊して確かめた。** 「型・テスト・lint のどれも
止めない」という開示は読んでも確かめられないので、`use-range-select:83` を直読みへ
差し替えて 4 つの検査が通ることを見てから確定した（`rules/coding.md`「外部の挙動は
動かして確かめる」と同じ向き）。指摘 11 の書き直しがこれで裏付きになった。

**スコープを広げずに、広げたくなる材料を全部 Issue へ落とした。** 同じ形が 9 件あると
分かった時点で 2 件に留め、#591（慣習 8 件）と #592（`drawnAt` の重複）へ実測ごと渡した。
一時的な割れが残ることも PR 本文に書いた。

## 規約への反映

**無し。** この回に出た 12 件はいずれも既存の規約・観点に対応があり、新しい判断基準は
示されていない。

ただし**指摘 1（`plan-scope-premise-verification`）は、`claim-verification` スキルを
計画の時点で呼んでいれば防げた形**で、スキル自体は pr-394 で既に層を上げてある。
呼ばなかったことが原因なので、規約・スキルへの追記ではなく運用の問題として記録に留める。
再発数を数えて層を上げるかどうかの判断は `harness-growth` の管轄。
