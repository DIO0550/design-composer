# PR #799 main の typecheck を直す（参照元のテストが TokenReferrer.toText を呼んでいた）

- マージ日: 2026-09-26
- 関連 Issue: #798
- 差分規模: 1 ファイル / +11 -8

## ゴールと結果

ゴールは達成した。main（d0479f6）で `tsc -b` が TS2339 で落ちていたのが直った。
原因は、#792(#771)が `TokenReferrer.toText` を移した後に #793 がマージされ、#793 の新規テストがその `toText` を呼んでいたこと。
テストを参照元の構造(`name` / `prop`)で比べる形へ書き換えた。#792 が既存テストに対して行った書き換えに揃えている。
人のレビュー指摘は 0 件(`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも確認。コメントは bot の投稿と自分のテストケース表だけ)。CI は初回で緑。

## 指摘

指摘は 0 件。原因そのもの(2 つの PR の組み合わせで main が落ちた件)は `harness/records/pr-793.md` の指摘 22 に記録した。

## 手戻り

無し。計画・検証のサブエージェントは通していない。1 ファイルのテストの書き換えで、#792 が既存テストに施した形をそのまま当てはめたため。

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。記録のみ(集計と介入の判断は `harness-growth` の管轄)。
