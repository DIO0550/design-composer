# PR #258 ♻️ [Refactoring]: #246 可否判定の空撃ち 2 箇所を純粋な述語に置き換える

- マージ日: 2026-08-19
- 関連 Issue: #246
- 差分規模: 11 ファイル / +319 -75

## ゴールと結果

ゴールは「部品化ボタンと解除ボタンの可否が、`EditorState`（書き込み API）を空撃ちせずに、ドキュメントと名前だけで決まる状態にする」。達成した。`EditorState` の書き系メソッドを呼ぶのは reducer だけになった。

**Issue 本文のスコープと実コードが食い違っていた。** 本文は潰す対象として 5 箇所（`applyPropEdit` ×2 / `resize` / `reveal` / 空撃ち 2 件）を挙げていたが、計画前に実測したところ**書き込み 3 箇所は既に `useNodeActions` の `dispatch` へ寄っており**、本文が指していたのは**コメント内の言及**だった。#174 の訂正コメントが tokens について「使用箇所として数えていたが実体はコメントの中だった」と書いた数え違いが、canvas と `document-error-list` でも起きていた形。スコープを実体の 2 箇所に確定し、実測結果を Issue に残して進めた。

human レビューは 0 件（レビュー・レビューコメントとも 0）。CI は 1 度目で 6 チェックすべて green（視覚差分も 0 / 105 passed）。**指摘はすべてサブエージェント（`plan-reviewer` 11 件 / `implementation-reviewer` 9 件）から出た。**

## 指摘

### 1. 変更で嘘になる既存コメントを持つファイルが、計画のファイル表から漏れていた

- 分類: `plan-scope-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `use-editor-state/index.ts` の `create_component` の Why が「部品化のボタンは `EditorState.createComponent(state, 下書き).some` が偽の間は押せない」と書いており、可否の出どころを変えるこの差分で前提ごと嘘になるのに、計画の変更ファイル表に無かった。呼び出し側の grep は `EditorState\.createComponent(` のように**呼び出しの形**で数えており、同じ API を根拠として挙げているコメントは初めから対象外になっていた
- 既存ルール: `implementation-flow` フェーズ 3「消す/変える公開 API があれば、呼び出し側を grep で数える」
- 次にどう防ぐか: 今回は記録のみ（**可否の出どころを変える差分では、その API を Why の根拠にしているコメントも数える対象になる**、という形。pr-244#1 の「数え方が狭かった」と同じ形で、あちらは型のフィールドを直書きしている箇所、こちらはコメント）

### 2. 述語へ移すと「凍結中は押せない」が黙って外れる

- 分類: `plan-integration-coverage`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 空撃ちは `EditorState` の `withEdit` を通るため、「ドキュメント上できるか」に加えて**「ファイルが不正でないこと」（#155 の凍結）も同時に答えていた**。ドキュメントだけの述語に替えるとこの条件が消えるのに、計画にその検討が 1 行も無かった。しかも退行を検知するテストが 1 件も無い（`prop-control/__tests__/` と `create-component/` に凍結の観点が 0 件、`opened-document-editor.frozen.test.tsx` の 15 件も部品化・解除を見ていない）ので、落としても誰も気づかない状態だった
- 既存ルール: `plan-review.md`「未決の判断が残っていないか」/ `rules/testing.md`「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ（**空撃ちを述語へ置き換える形の変更では、空撃ちしていた関数が「ついでに」答えていた条件を数える**、という形）

### 3. 新テストの入力が計画に書かれておらず、既定値と一致するか判定できなかった

- 分類: `test-default-input`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画のテスト一覧はケース名だけで入力が無く、「重複を見るケースの名前が kebab-case でないと識別子規則だけで false になり、重複の検査を消しても通る」「規則違反を見るケースの名前が名前空間にあると逆が起きる」を判定できなかった。指摘を受けて入力（`Card` = 名前空間に無い綴り / `card` = kebab-case）を計画に明記した
- 既存ルール: `rules/testing.md`「既定値・フォールバックがある処理では、既定値と違う答えになる入力を選ぶ」
- 次にどう防ぐか: 今回は記録のみ（**2 つの検査を順に通す述語では、片方だけを消しても落ちる入力を検査ごとに選ぶ**、という形）

### 4. 循環ドキュメントを構成できるかを、確かめずに条件付きで計画へ書いた

- 分類: `plan-scope-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画に「循環したドキュメントが `DesignDocument.create` で構成できなければテストを落とし、理由を Issue に残す」と書いた。**確かめれば済む前提を散文のまま残した形。** 実際には構成できる（`create` に検証は無く、実例が `design-document.validate-ref.test.ts:66-81` にある）
- 既存ルール: `implementation-flow` フェーズ 3「テストケースが前提とするヘルパー・フィクスチャ・API が実在し、想定通り動くかを確認する」
- 次にどう防ぐか: 今回は記録のみ

### 5. 「失敗しうる部分だけを切り出す」の根拠がどこにも書かれていなかった

- 分類: `comment-missing`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `isDetachable` を私有ヘルパー（探索 → ref 判定 → 展開）の成否で実装したが、`detach` の後半にある `DesignDocument.replaceNode` も `Result` を返すため、述語は**書き込みの失敗経路を 1 つ置き去りにする形**になっていた。結論としては正しい（`findNode` と `replaceNode` はどちらも `Node.children` 経由の同じ走査なので、探索できたノードの置き換えは必ず成功する）が、その根拠が計画にも doc にも無く、次に `replaceNode` へ条件が増えたときに黙ってずれる
- 既存ルール: `rules/coding.md`「消してはいけない Why: 型やテストで表現できず、外すと壊れるのに壊れたことに気づけないもの」
- 次にどう防ぐか: 今回は記録のみ

### 6. `isAvailableName` の `available` が、既に別の意味で使われていた

- 分類: `naming-prefix-collision`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 述語名を `isAvailableName` にしたが、`NameSpace.nextAvailableName` が "available" を**「空いている（識別子の規則は見ない）」**の意味で先に使っている。新しい述語は「規則 + 空き」の合成なので、同じ語が 1 リポジトリ内で 2 つの意味を持つ。消費側の `create-component` にも `UnusableNameReason`（名前が使えない理由）が既にあった。`isUsableName` へ変えた
- 既存ルール: `rules/naming.md`「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ（**接頭辞だけでなく、語幹が既存の関数名の中で持っている意味の広さも数える**、という形）

### 7. 同じ問いに答える公開述語が 2 つ並ぶ計画になっていた

- 分類: `module-api`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `DesignDocument.isValidIdentifier` は**本番の呼び出しが 0 件**（テストのみ）で、そこへ `isUsableName` を足すと名前の可否を答える公開述語が 2 つ、片方は消費者ゼロで並ぶ。計画の「消すもの」の欄が空でこの観点が拾えていなかった。新しい私有ヘルパーから `isValidIdentifier` を呼ぶ形にして消費者ゼロを解消した（削除はしない。識別子の規則はドキュメントの公開規則で、`design-document.identifier-rule.test.ts` の観点を落とさないため）
- 既存ルール: `rules/architecture.md`「`index.ts` から export するのは外部に公開する必要があるものだけに絞る」
- 次にどう防ぐか: 今回は記録のみ

### 8. `services/` に判定を置く根拠が 1 行では規約を通らなかった

- 分類: `service-placement`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `InstanceComposition.isDetachable` は `services/` に置く**判定**なので、`rules/architecture.md`「services に『判定』が現れたら帰属先のドメインが無いサイン」に正面から当たる。計画の却下理由は「展開と循環の検出は `InstanceComposition` の知識」だけで、同節の判定文を通らなかった。「`detach` 自身がこの service にあり、述語はその私有ヘルパーの成否で、展開規則の再実装ではない」まで降ろして PR 本文に書いた
- 既存ルール: `rules/architecture.md`「services はドメインを探してから使う」（「なぜどのドメインにも帰属しないのかを PR に一言書く」）
- 次にどう防ぐか: 今回は記録のみ

### 9. 述語と書き込みの一致が、ノード側だけ散文に依っていた

- 分類: `duplication-derivation`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 名前側は私有ヘルパーを共有するので構造で保証されるが、ノード側の 2 条件（ノードが在る / 参照ノードでない）は `Componentization.forSelection` の `ready` と `DesignDocument.createComponent` の検査が**同じ事実を独立に導いており**、一致は「`Component.fromNode` が `isPrimitive` を見ていて `ready` は `!isRef` で決まる」という散文の議論に依っていた。ずれても typecheck もテストも落ちない。一致を留めるテストを足す案を採り、失敗集合の補集合そのものを述語にする案（`isComponentizable(document, sourceName, componentName)`）は却下理由とともに Issue に残した
- 既存ルール: `rules/coding.md`「同じ処理が2箇所に現れたら共通化する」/「前提をコメントで書くのは、型で閉じたことにならない」
- 次にどう防ぐか: 今回は記録のみ

### 10. 新テストの入力が既存テストと丸ごと同じになっていた

- 分類: `duplication-test`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画の `design-document.usable-name.test.ts` 5 件のうち 4 件が `design-document.componentize-edge.test.ts` と入力ごと同じ、`instance-composition.detachable.test.ts` 3 件のうち 2 件が `instance-composition.detach-edge.test.ts` と同じだった。同じ私有ヘルパーを通るので、あちらが落ちればこちらも落ちる。重複ぶんを落とし、新観点（部品内部のノード名 / 循環参照）と一致テストに絞った
- 既存ルール: `rules/coding.md`「同じ処理が2箇所に現れたら共通化する（実装・テストとも）」
- 次にどう防ぐか: 今回は記録のみ

### 11. 書き換え対象の doc を 1 つ挙げ漏らした

- 分類: `comment-stale-edit`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `nodeControls` の `@param state 解除できるかの判定に使うエディタの状態` は引数を差し替えると嘘になるのに、計画の「書き換えるコメント」に挙げていたのは二重管理を戒める本文コメントだけだった
- 既存ルール: `rules/coding.md`「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ

### 12. 凍結の条件を重ねた片方が、どこからも観測できなかった

- 分類: `over-guard`
- 出どころ: レビュー（`implementation-reviewer` のミューテーション実測）
- 内容: 指摘 2 を受けて凍結の条件を**両方の呼び出し側に重ねた**が、解除ボタン側（`prop-control`）は `!isFrozen &&` を外しても **2312 件すべて通過**した。`isDetachEnabled` の消費者は `property-panel` 1 箇所だけで、そこへ至る `InspectorBody` が凍結中は本文ごと出さないため、値を誰も読んでいない。観測できない条件を運ぶために `nodeControls` へ 3 つ目の引数を足し、`SelectionControls` の doc に主張を 1 行足していた。剥がして `nodeControls(document, node)` に戻し、doc は「凍結をここで見ない理由」に書き直した。部品化ボタン側（左ペインは `inert` のまま描かれ続ける）は外すと追加したテストが落ちるので残した
- 既存ルール: `implementation-flow` フェーズ 6（過剰なブロック）
- 次にどう防ぐか: 今回は記録のみ（**「条件が落ちる」と気づいた時点で、落ちた先が観測できるかまで確かめる**、という形。指摘 2 の対応が片側で行き過ぎた）

### 13. 一致を留めるはずのテストが、正の方向だったため何も留めていなかった

- 分類: `test-coverage-branch`
- 出どころ: レビュー（`implementation-reviewer` のミューテーション実測）
- 内容: 指摘 9 に対して足した一致テストは「使えると判定した名前で部品化すると成功する」（正の方向・1 入力）で、**`createComponent` から私有ヘルパーの呼び出しを丸ごと消しても 5 件とも通った**。同じ形が `detachable` 側の「解除できると判定したインスタンスは実際に解除できる」にもあった。両方とも負の方向（述語が false を返したもので書き込みが失敗する）へ差し替え、差し替え後に同じミューテーションで落ちることを実測した
- 既存ルール: `implementation-review.md`「テストが守っているかの観点」/ `rules/testing.md`「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ（**「A と B が一致する」を留めるテストは、成立する側ではなく成立しない側で書く**、という形）

### 14. 一致を留めていると書いたテストヘッダが、最初から事実と食い違っていた

- 分類: `comment-false-claim`
- 出どころ: レビュー（`implementation-reviewer` のミューテーション実測）
- 内容: 新規テスト 2 ファイルの先頭に「両者が同じ答えを返すことを N 番目のテストで留めている」と書いたが、指摘 13 のとおり留めていなかった。**主張を実測に照らさずに書いた形。** 差し替え後の事実（構造で共有しているのでずれない / ずれうるのは参照しなくなったときで、それを N 番目が留める）に書き直した
- 既存ルール: `rules/coding.md`「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 15. 凍結中にも名前の理由が出るようになったのに doc が名前の話のままだった

- 分類: `comment-stale-edit`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `UnusableNameReason` の doc は「**打った名前では作れないとき**に、ボタンの `title` へ出す理由」だが、`isCreatable = !isFrozen && ...` にしたことで**使える名前を打っていても凍結中なら**同じ理由が出るようになった。差分前も挙動は同じだったが、当時は `EditorState.createComponent` の `Option` に埋まっていて区別する術が無かった。2 つの条件を並べた以上ここは解けるので、`reason` を名前が理由のときだけ添える形（`isUsableName ? undefined : UnusableNameReason`）に直し、doc の主張を成立させた
- 既存ルール: `rules/coding.md`「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ

### 16. 書き直した Why が `none` の経路を 1 つ数え落とした

- 分類: `comment-stale-edit`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 指摘 1 を受けて `use-editor-state` の Why を書き直したが、新しい文は「`Componentization.forSelection` が `ready` でない」「`isUsableName` が偽」の 2 つだけを挙げており、**凍結中も `withEdit` で `none` になる**ことが落ちた。元の文（`createComponent(...).some` が偽の間）はそれを含んでいたので、書き直しで網羅が減った形。3 つ目を書き足した
- 既存ルール: `rules/coding.md`「挙動を曖昧にまとめず対象を列挙する」
- 次にどう防ぐか: 今回は記録のみ

### 17. 同一ファイル内で `unavailable` / `usable` に語彙が割れていた

- 分類: `naming-prefix-collision`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 指摘 6 で公開述語を `isUsableName` へ変えたが、私有ヘルパーは `unavailableNameError` のままにしたため、同じ概念を逆語幹で呼ぶ形が 1 ファイル内に残った。`DesignDocument.usedNames` が隣にあることも読み違えを誘う。`unusableNameError` に寄せた
- 既存ルール: `rules/naming.md`「1つのモジュール内で語彙を混在させない」
- 次にどう防ぐか: 今回は記録のみ（**公開名を直したら、同じ概念を指す私有名も同時に直す**、という形）

### 18. 私有ヘルパーの語形が、同じモジュールの既存ヘルパーから浮いていた

- 分類: `naming-prefix-collision`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 切り出した私有ヘルパーを `expandedInstance`（名詞句）と名付けたが、同じモジュールの隣は `expandNode` / `expandNodes` の動詞形。リポジトリ全体では名詞句の私有ヘルパー（`nameSpaceOf` / `sectionsOf`）もあるので規約違反ではないが、このモジュール内では語形が 2 通りに割れる。`expandInstance` に改名した
- 既存ルール: `rules/naming.md`「1つのモジュール内で語彙を混在させない」
- 次にどう防ぐか: 今回は記録のみ

### 19. `.tsx` の prop 名 `document` が DOM グローバルを隠していた

- 分類: `naming-prefix-collision`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `ReadyBody` の props を `document: DesignDocument` にしたが、これは DOM グローバルの `document` を関数スコープで隠す。`document: DesignDocument` の既存例は hooks / domains（JSX 無し）だけで、**コンポーネントでは初めて**だった。中で DOM を触っていないので実害は無いが、`artboard-canvas` が既に使っている `designDocument` に揃えた
- 既存ルール: `rules/naming.md`「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ

### 20. 私有ヘルパーの中で、同じドキュメントを引く経路が 2 通りに割れていた

- 分類: `なし`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `unusableNameError` の中で識別子の規則は `DesignDocument.isValidIdentifier(name)`（1 行の委譲メソッド）、名前空間の重複は `NameSpace.has(nameSpaceOf(document), name)`（`NameSpace` 直接）と、同じドキュメントの性質を引くのに経路が 2 通りになっていた。元のコードは 2 つとも `NameSpace.` 直接。両方ともコンパニオン経由（`isValidIdentifier` / `usedNames`）に揃えた（指摘 7 の解消も兼ねる）
- 既存ルール: なし（`rules/naming.md`「1つのモジュール内で語彙を混在させない」は識別子の**名前**の話で、**同じ事実を引く呼び出し経路**の統一までは規定していない）
- 次にどう防ぐか: 今回は記録のみ

### 21. テストヘルパーの `@param` に doc ではなく手順が入っていた

- 分類: `なし`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 共通化した `frozen()` の `@param state` に「凍結で可否が変わることを見るテストは、凍結する前に対象を用意する」というテストの書き方を書いていた。`rules/coding.md` の doc の表は `@param` に「その引数が何を表すか」を書くと規定している。内容自体は残す価値があるので `@param` から外して Why として本文へ移した
- 既存ルール: なし（`rules/coding.md`「doc に書く項目」に対応する分類の語彙が無い。`comment-false-claim` / `comment-stale-edit` / `comment-missing` はいずれも内容の誤り・欠如で、**書式の逸脱**を指す語彙が無い）
- 次にどう防ぐか: 今回は記録のみ

### 22. 検証エージェントが `git checkout` で未コミットの実装を消した

- 分類: `subagent-control`
- 出どころ: 自己修正（`implementation-reviewer` の自己申告）
- 内容: `implementation-reviewer` がミューテーション検証の復元に `git checkout` を使い、**未コミットだった差分ごと一度 HEAD へ戻した**。本人が全ハンクを再適用し、`git diff` の blob ハッシュが検証前と一致することを確認したうえで報告してきたので、こちらでも実装 5 ファイルの差分を全文読み直して検算した（一致）。`git status` の内容も受け取り時と同一
- 既存ルール: `implementation-flow`「サブエージェントの使い方」（「返ってきたら、指摘を読む前に `git status` を見る」）
- 次にどう防ぐか: 今回は記録のみ（**pr-244#32 と同一の形が同じエージェントで再発した。** 既存ルールは「書き換えを戻す」ことは求めているが、戻す手段までは規定していない。回数を数えて層を上げるかどうかの判断はこのスキルの管轄外 → `harness-growth`）

### 23. `.claude/hooks/` が発火しない実行環境だった

- 分類: `hook-environment`
- 出どころ: フック（不発）
- 内容: 着手時と push 前の 2 回 `echo hook-canary` を実行したが、どちらも deny されずそのまま通った。PR 本文・Issue #246・Discord 通知のいずれにもその旨を記載した。push 前検査の enforcement は git hooks（`harness/githooks/pre-push`）が担っており、`git push` 時に doc コメント / テスト規約が実行されて通過した出力を確認している（同じ検査を push 前に typecheck / oxlint / biome / vitest と併せて 1 つずつ独立にも実行した）
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ（pr-233・pr-236・pr-244 でも同じ現象が記録されている。回数を数えて層を上げるかどうかの判断はこのスキルの管轄外 → `harness-growth`）

## 手戻り

計画から外れたのは 2 つ。どちらも `implementation-reviewer` の指摘後で、Issue へ理由とともに追記した。

1. **凍結の条件を解除ボタン側から剥がした**（指摘 12）。計画検証（指摘 2）で「両方の呼び出し側に重ねる」と決め、そのとおり実装したが、実装検証のミューテーションで**解除ボタン側は誰も値を読んでいない**ことが分かった。`nodeControls` に足した 3 つ目の引数（`isFrozen`）と、`SelectionControls` の doc に足した 1 行を外し、doc は「ここで見ない理由」に書き直した。**計画 → 実装 → 剥がし、と 1 往復した**
2. **一致テストを正の方向から負の方向へ差し替えた**（指摘 13）。計画では「使えると判定した名前で部品化が成功する」と書いており、`plan-reviewer` もそれを承認していた。実装検証のミューテーションで無効と分かって書き直した

1 の原因は、計画の時点で「条件が落ちる」ことだけを見て、**落ちた先が観測できるか（外して実際にテストが落ちるか）を確かめていなかった**こと。2 も同じで、「一致を留める」と書いた時点でどちら向きなら落ちるかを確かめていなかった。どちらも実装検証のミューテーションで初めて分かった。

## フックの発火

**`.claude/hooks/` のフックは 1 つも発火しなかった**（指摘 23）。git hooks（`harness/githooks/pre-push`）は `git push` 時に動作し、`pre-push: doc コメント`（doc の無い宣言 0 件 / 項目が欠けた doc 0 件）と `pre-push: テスト規約` が出力されて通過した。

## うまくいったこと

- **Issue 本文の前提を、計画を書く前に実測した。** 本文が挙げた 5 箇所を `grep` でコメント行と実体に分けたところ 3 箇所は既に済んでおり、スコープが 2 箇所に縮んだ。実測しないまま計画に入っていたら、既に `dispatch` へ寄っているものを「寄せる」計画に時間を使っていた
- **ミューテーションを 7 種掛けて、追加・変更したテストが落ちることを実測した**（凍結条件を外す / 重複の検査を消す / 識別子規則の検査を消す / `isDetachable` を退化させる / `isDetachEnabled` を固定する / `createComponent` から述語の呼び出しを消す / `detach` を私有ヘルパー非経由にする）。うち 2 つは**テストが無効だったことを暴いた**（指摘 13）ので、実測しなければ空撃ちのテストが 2 本入っていた
- **VRT の結果を事前に見込んで、そのとおりになった。** マークアップとクラス名の差分が 0 行であることを `git diff` で数えたうえで「差分は出ない見込み」と PR 本文に書き、実際に 0 / 105 passed だった
- **push 前の検査を `&&` で連ねず 1 つずつ独立に実行した**（typecheck / oxlint / biome / vitest）。biome だけが 4 件の書式差分を出したので、その場で `--write` を通してから push できた
- **計画検証・実装検証の指摘 20 件をすべて Issue に表で残した。** 受け入れた理由・却下した案（`isComponentizable` / `available` 側へ寄せる案）も同じ表に並べたので、同じ案が再浮上したときに前回の理由が読める

## 規約への反映

**無し。** このスキルの管轄は記録を 1 ファイル残すところまでで、集計・介入の判断・`rules/` やフック・スキルの変更は行わない（→ `harness-growth`）。

同じ回に複数回出た分類（`plan-scope-verification` 2 件、`naming-prefix-collision` 4 件、`comment-stale-edit` 3 件、`なし` 2 件）と、他の記録にも出ている分類（`subagent-control` は pr-244 と同一の形、`hook-environment` は pr-233・pr-236・pr-244 に続く 4 回目）があるが、**回数を数えて層を上げるかどうかの判断はこのスキルの管轄外**。

**後続の介入（`harness-growth`、棚卸しルーティン / pr-266）:** `naming-prefix-collision` が
未介入のまま 7 回再発した（2a）。指摘の出どころは毎回 `plan-reviewer` / `implementation-reviewer`
のレビューで、既存ルール（`rules/naming.md`）も `implementation-reviewer.md`「命名の観点」も
既にあったが、実装**後**にしか効いていなかった。層 1（hook）は意味の一致判断が要るため不成立、
層 2（skill）は既存フェーズの観点追加で足りるため不要と判断し、層 3（観点）で
`.claude/agents/plan-reviewer.md` へ実装前のチェックポイントとして追加した。

- 対策済: `naming-prefix-collision` 層=観点 at pr-266
