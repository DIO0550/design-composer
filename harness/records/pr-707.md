# PR #707 RightPaneSource に重なった doc ブロックを消す

- マージ日: 2026-09-22
- 関連 Issue: #700
- 差分規模: 1 ファイル / +0 -5

## ゴールと結果

ゴール（`RightPaneSource` に `@returns` 付きの doc が残らず、`/** 右ペインの中身を決めるのに
要るもの。 */` だけが付く）は達成した。計画から外れた実装は無い。

doc の削除は typecheck / lint / vitest / VRT のどれも動かさないため、**ゴールを確かめる手段が
計画に無いまま `plan-reviewer` へ渡していた**（指摘 1）。完了条件を走査 2 本に置き直して実測した。

`rightPaneParts` 側に残る doc の食い違い 2 件は #706 へ分けた。

## 指摘

### 1. ゴールを確かめる手段が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: doc の削除は typecheck も lint も vitest も VRT も動かさないので、消し忘れ・消しすぎが
  起きても全部緑で通る。完了条件へ走査を 2 本（「`*/` の次行が `/**`」が 4 件 → 3 件 /
  ファイル内の `@returns` が 2 件）置いて実測した。
- 既存ルール: `implementation-flow` フェーズ 3（`harness/case-law/planning.md`「`plan-goal-verification`」）

### 2. テストを足さない根拠が事実と食い違っていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画は「退行の検知は既存の `editor-right-pane` 周りのテストと typecheck が担う」と
  書いたが、`editor-right-pane/` に `__tests__/` は無く（中身は `index.tsx` 1 ファイル）、
  typecheck も vitest も doc コメントを見ない。結論（テストを足さない）は変えず根拠を
  「自動検知する手段は現状無い」へ差し替えた。
- 既存ルール: `implementation-flow`「計画を渡す前に踏む 8 つの手順」3

### 3. 却下案の後続をどうするかが計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「`missing-doc-comments.py` に doc の重なり検出を足す」を却下したまま、後続 Issue を
  立てるのか立てないのかが書かれていなかった。介入の条件（最後の介入より後に 2 回以上）を
  数えるのが `harness-growth` の管轄なので、**立てずに記録へ回す**と明記した。
- 既存ルール: `AGENTS.md`「タスクの分割」

### 4. 「完全版が正しく付いている」という断定が実体と食い違っていた

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 34-39 行を「`@param source` を足した完全版が**正しく**付いている」と書いたが、
  `rightPaneParts` の引数は分割代入で `source` という引数は存在しない。削除する側と同じ class の
  不一致が 6 行下にあった。断定を取り下げ、#706 として分離した。
- 既存ルール: `.claude/skills/claim-verification`（Issue/PR 本文の事実の主張）

### 5. 「未決の判断: 無し」で締めるのが早かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 削除後に残る 25 行の型 doc と 37 行の `@param` が同じ対象を 2 箇所で説明している。
  残すという判断自体は変わらないが、それを 1 行書かないと次に読む人が同じ疑問で止まる。
- 既存ルール: `implementation-flow` フェーズ 3「未決の判断」

### 6. 背景に書いた「古い複製」が履歴と食い違っていた

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `git blame -L 14,40` の結果、消した 20-24 行も 34-39 行も同じコミット `3db1b64`
  「Editor をペインごとのモジュールへ分ける」で同時に入っていた。「古い doc の残骸」ではなく
  「分割時に貼った片方の消し忘れ」。修正内容は変わらないが再発の読み筋が変わる。
- 既存ルール: `.claude/skills/claim-verification`（Issue/PR 本文の事実の主張）

### 7. 類似箇所の走査が「形」だけで、「症状」で数え直していなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 走査条件は「`*/` の次行が `/**`」という並びの形だけで、ゴールの文言（型宣言に付いた
  `@returns`）という症状では数えていなかった。症状側で `src/` を走査すると
  `RenderDocumentErrors` が 1 件出たが、関数型エイリアスなので正当。**この走査では対象の
  `RightPaneSource` はヒットしない**（宣言の直前は正しい 1 行 doc）ので、両方合わせて
  「他に該当なし」が言える。
- 既存ルール: `implementation-flow`「計画を渡す前に踏む 8 つの手順」2

### 8. 残った `@returns` が経路を 1 つ落としていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `rightPaneParts` の `@returns` は「Tokens ならトークンの編集欄、Layers / Assets なら
  プロパティパネル」だが、実体は行き先より先に凍結を見て `isFrozen` なら行き先に関係なく
  プロパティパネルを返す。この差分で壊れたわけではないが、削除後はこれが唯一の綴りになる。
  #706 へ 2 つ目の項目として足した。
- 既存ルール: `rules/coding.md`「`@returns` は…どんなときにそうなるか」

### 9. `@param source` を消すと落ちる説明が後続に書かれていなかった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `@param source 出し分けに要る行き先とエディタの状態、操作の受け口` は、
  `RightPaneSource` が `node` / `token` / `onGoToSource` も運ぶことを書いている唯一の箇所。
  残る型 doc は「決めるのに要るもの」しか言っていない。丸ごと削ると説明が消えるので、
  #706 に「直すときに落としてはいけない情報」として明記した。
- 既存ルール: `.claude/skills/claim-verification`（`comment-premise-drift`）

### 10. 計画に書いた呼び出し側の件数が実体と 1 件ずれていた

- 分類: `plan`
- 出どころ: `自己修正`
- 内容: `grep -rn "RightPaneSource"` を「同ファイル内の 2 箇所」と書いたが実際は 3 箇所
  （`EditorRightPane` の props 型を数え落とし）。`claim-verification` で照合して訂正した。
  結論（フォルダ外からの参照が無い）は変わらない。
- 既存ルール: `implementation-flow`「計画を渡す前に踏む 8 つの手順」1

## 手戻り

実装のやり直しは 0 回（差分は計画どおり 20-24 行の削除 1 回で確定）。手戻りはすべて
**計画・本文の記述の書き直し**で、内訳は上の指摘 1〜7・10。

`implementation-reviewer` に渡した時点で `node_modules` が無く、検証側は typecheck / test /
biome を実行できなかった（差分がコメント 5 行の削除のみであることから影響なしと判断している）。
push 前検査は `pnpm install --frozen-lockfile` を実行してから 16 項目を 1 つずつ走らせ、
すべて green。CI の失敗・人 / bot のレビュー指摘は 0 件。

## 発火

- 発火: 計測対象外(フック不発)

カナリア（素の `echo hook-canary`・連結なし）が deny されず、`plan-reviewer` と
`implementation-reviewer` を両方通した後でも検証エージェントのマーカーが無く、このセッションの
発火ログの見出しも無い枝（`/tmp` に残っていた発火ログは 2026-09-18 の別セッションのもの）。
代替不能なフック（`block-npx.sh` / `git add -A` の抑止）は、禁止コマンドを実行していないことを
Bash の履歴で見直して肩代わりした。

## 規約への反映

無し。このスキルの管轄は記録までで、集計と介入は `harness-growth` が行う。
