# PR #741 src/domains/ のコンパニオンオブジェクトのメソッドの doc の抜けを埋める（#720 層 2）

- マージ日: 2026-09-24
- 関連 Issue: #736
- 差分規模: 35 ファイル / +1268 -86

## ゴールと結果

ゴール（`missing-doc-comments.py --all src/domains --include-methods` が 0 件）は達成した。着手時点は
doc 無し 46 件 / 項目の欠け 155 件 / 35 ファイル（Issue 本文の概算 176 件は `--all` の 10 件打ち切りで少なかった）。
引数ごとの `@param <名前>` も使い捨てのスクリプトで 0 件にした。doc だけの差分で挙動は変えていない。
doc を書く中で見つけた規約違反・疑わしい挙動は #740 に切り出した。CI は 1 回目で全チェック通過、VRT の差分
0 件、人のレビュー指摘 0 件でマージされた。

## 指摘

### 1. 実装と食い違う既存の概要文を、直す対象として列挙していなかった

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `NodeTree.moveWithin` / `DesignDocument.reorderArtboard` の「入れ替える」（実際は抜いて差し込む移動）と
  `moveNode` の「別の親の下へ」（同じ親も拒まない）。層 1 の指摘 7 と同じ形の再発。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 2. 据え置きを決めることになる規約違反が既知の 2 件より多かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: プロトタイプ上の名前（`constructor`）を辞書で引く箇所が 5 つ以上、`ResolvedProps.forNode` の境界外 `as`、
  `DesignDocument` の `NameSpace` 委譲メソッドの置き場所など。実測してから #740 を先に立てた。層 1 の指摘 8 と同じ形。
- 既存ルール: `AGENTS.md`「着手した Issue は、その回で閉じる」

### 3. 薄いメソッドに書く中身が 1 件ずつ計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「下見した範囲では書ける」だけで #719 へ持ち帰らないと判断していた。18 件の中身を表にした。層 1 の指摘 5 と同じ形。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 5

### 4. 引数ごとの `@param` 突き合わせスクリプトが分割代入を誤検出し、0 件に届かなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 検出器の `parameter_names` は先頭以外の分割代入（`{ from, to }`）を名前として返す。`{` で始まる名前を飛ばすよう直した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 3

### 5. 使い捨て vitest の置き場所と手順を確かめていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `node_modules` が無く、scratchpad は vite の root の外。`pnpm install` のあと `src/__claimcheck__/` に置いて走ることを確かめ、計画に書いた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 3

### 6. 未決の判断を「無し」としていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 名前が重複した不正なドキュメントでどれを返すかの書き方、委譲先の doc を書く順、`DesignDocument.create(params)` の書き方の 3 つ。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3

### 7. 分けない理由が「独立してマージできるか」に答えていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「#720 が層で分けた単位」は Epic の区切り方で判断軸への答えではない。分けない理由を書き直し、コミットをまとまりごとに分けた。
- 既存ルール: `AGENTS.md`「タスクの分割」

### 8. 下書きの「整数に読める名前」の言い方が層 1 の指摘と同じ形で再発していた

- 分類: `comment`
- 出どころ: `自己修正`
- 内容: サブエージェントの下書き 4 ファイルで、`Object.keys` の並びを「整数に読める名前が先頭」と書いていた（`01` は先頭へ来ない）。
  全件を読んだときに直した。値の再掲（`TokenSet.kinds` などの列挙）と裏付けの無い主張（`isPrimitiveType` の「部品名もここに来る」）も同じ回で直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 9. `Px.create` の指数表記の条件が誤っていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 私が下書きを直したときに「`1e-7` 未満」と書いたが下限は `1e-6`（node で 1 行確かめただけで境界を見ていなかった）。
  数値の文字列化の一般論なので記述ごと消した。`AxisLength.create` の「`Infinity` はそのまま」（`-Infinity` は 0）も同じく消した。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 10. `insertNode` の `children-not-allowed` の条件の列挙が欠けていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「Text・参照ノード」だけでなく、スキーマに無い `type` のプリミティブも該当する。「参照ノードか、スキーマが子を認めていないプリミティブ」に直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 11. `ResolvedProps.forNode` の例外を `@param` に書いていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 投げる経路は `@throws` の項目。移した。型の穴は #740 の 3。
- 既存ルール: rules/coding.md「doc に書く項目」

### 12. `Component.publicPropNames` の「数字だけの名前」が不正確だった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `"007"` は数字だけだが先頭へ来ない。指摘 13 の対応で規則ごと消した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 13. `Object.keys` の並び規則を 3 箇所に独立して書いていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `ComponentSet.names` / `TokenSet.names` / `Props.toAssignments`。JS の一般論なので規則を消し、各所は「`Object.keys` の列挙順」とだけ書いた。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 14. `ComponentAsset.isUnused` の `@returns` が `ComponentSet.assets` の数え方を写していた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「参照回数が 0 なら `true`（数え方は `ComponentSet.assets`）」に留めた。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 15. `Rgb.create` の doc が定数 `AnyCaseRgbPattern` の doc を言い換えていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `@returns` を「`AnyCaseRgbPattern` に合わなければ `none`」にした。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 16. `PropDefinition.collectErrors` の概要と `@returns` が同じ規則を 2 回書いていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 概要の括弧（domain ごとに何を見るか）を消し、`@returns` の kind ごとの条件に寄せた。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 17. `Selection.fromArtboard` の概要と `@returns` が同じことを言っていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `@returns` を「artboard の名前を持つ選択」にした。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 18. `@returns` が三項演算子の値を写していた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `Offset.along` / `Size.modeProp` / `CssDirection.mainAxis` / `fillDeclaration`。`fillDeclaration` は綴り（`flex-grow: 1`）も実物（`flex-grow:1`）と違っていた。意味で書き直した。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 19. `@returns` が `ok` / `some` の包みを書いていない（不採用）

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 30 件以上。層 1 の指摘 9 と同じ形として挙がったが、層 1 は `build` の戻り値という包む前後で同じに読める曖昧さへの指摘で、
  「〜したドキュメント」は読み違えようがなく既存の domains の doc も包みを書かないので不採用にした。理由は #736 に書いた。
- 既存ルール: rules/coding.md「doc に書く項目」

### 20. フックが発火しない環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されず、検証エージェントのマーカーもこのセッションの発火ログの見出しも無かった（README の表で「本当に不発」）。
  起動時の作業ディレクトリがリポジトリの親（`/home/user`）で、層 1 と同じ構成。`npx` / `git add -A` の実行と、検証エージェント実行中の git 操作が無いことを履歴で確認した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- **計画を plan-reviewer の指摘 7 件で直した**（指摘 1〜7）。うち 4 件（1・2・3 と、未決の判断の扱い）は層 1 の plan-reviewer 指摘と同じ形の再発
- **下書きを 6 つのサブエージェントに並行で書かせ、全件を読んで直した**（指摘 8）。下書き中に見つかった規約違反は #740 へ 2 回に分けて追記した
- **実装を implementation-reviewer の指摘 11 件で直し、1 件は不採用にした**（指摘 9〜19）。指摘 9 は自分が直したときに入れた誤り
- push 前検査 21 項目（typecheck / lint / biome / test / ハーネスの検査 17 本）と Storybook の撮影範囲、CI は 1 回目で通過した

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。この回は Issue #736 の実装で、記録を数えた結果の介入ではない。`対策済` を書けるのは
`harness-growth` の棚卸しだけ。
