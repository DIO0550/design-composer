# PR #723 gradients トークンを --gradients-<名前> の linear-gradient として書き出す

- マージ日: 2026-09-23
- 関連 Issue: #722
- 差分規模: 11 ファイル / +287 -24

## ゴールと結果

ゴール（`tokens.gradients` の各トークンが `--gradients-<名前>: linear-gradient(<angle>deg, <color> <ratio×100>%, …)`
として書き出される）は達成した。`ratio×100` の丸めのために `NumberEx.round` を足し、
`ColorToken.alphaPercentOf` の手書きの丸めを寄せた。綴りと丸め桁は `docs/04-tokens.md` に書き足した。
CI は 1 回目で全チェック通過、VRT の差分 0 件、人のレビュー指摘 0 件でマージされた。

## 指摘

### 1. `round(value, decimals)` の位置引数は取り違えが型で落ちない

- 分類: `type`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 値も桁数も `number` で、入れ替えても tsc が通る。同じファイルの `clamp` は同じ理由で
  オブジェクト引数にしていた。`round(value, { fractionDigits })` に直した。
- 既存ルール: rules/coding.md「関数のシグネチャ」

### 2. `NumberEx` 冒頭の doc が実体とずれていた

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「数値の性質を答える汎用操作」は `clamp` が入った時点で既に実体とずれており、
  `round` を足すとさらにずれる。同じ差分で直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 3. 綴りの仕様が docs に無く、docs の更新が計画のファイル表から漏れていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `%` の綴り・`deg`・4 桁の丸めがどこにも書かれておらず、テストが仕様に無い振る舞いを
  固定する形だった。`docs/04-tokens.md`「gradients」へ 1 行足してテストの根拠にした。
- 既存ルール: rules/testing.md「テストケースの根拠は仕様・設計ドキュメントに求める」

### 4. 4 桁にした理由が計画に無かった

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 丸めが要る理由はあったが、2 桁でも 6 桁でもなく 4 桁にした根拠が無かった。
  定数の doc に「残るずれがグラデーション線 1px あたり 1e-6px に満たない」を書いた。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 5. テストの入力が壊し方で落ちなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `0.123456 * 100` は誤差なしで `12.3456` になり、丸めを外しても通る。昇順の stops では
  並べ替える実装でも通る。`toFixed` の却下理由を守る `0` / `1` のケースも無かった。
  `0.333333333`・降順の stops・`0%` / `100%` を足した。
- 既存ルール: rules/testing.md「assert は『落ちうるか』と『1つの仕様か』で見る」

### 6. 範囲外の比率のテストが片側だけだった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `1.5 → 150%` だけで、負の側が無かった。`-0.1 → -10%` を足した。
- 既存ルール: rules/testing.md「assert は『落ちうるか』と『1つの仕様か』で見る」

### 7. 「未決: 無し」の下に stops 0 件の出力が決まっていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: そのまま綴ると `linear-gradient(90deg, )` になり、1 件の場合と性質が違う。
  Issue の実測（宣言ごと捨てられる）と docs の「解釈はブラウザに委ねる」から、そのまま出すと決めて期待値に書いた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「未決の判断」

### 8. shape の switch を「網羅検査の相手が無い」として却下していた

- 分類: `type`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 検査の相手は今ある 1 件ではなく将来増える値。switch が無いと radial を足しても黙って
  linear として出る。`default` 無しの switch を採用し、`Radial` を一時的に足して TS2366 を確かめた。
- 既存ルール: rules/coding.md「列挙した状態の網羅を型で強制する」

### 9. `AlphaPercentDigits` を 1→2 にしても落ちなかった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 既存の「小数第1位まで読める」の入力 `#0000001a` は 1 桁でも 2 桁でも `10.2`。
  変更前の `PercentStepsPerUnit` から残っていた穴。`#00000001 → 0.4` を足した。
- 既存ルール: rules/testing.md「既定値・フォールバックがある処理では、既定値と違う答えになる入力を選ぶ」

### 10. 角度を名指しするテストの入力が既定テーマと同じ 90 だった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 角度を 90 に固定するミューテーションは落ちたが、落としたのは別の仕様名のテストだった。
  入力を 135 に変えた。
- 既存ルール: rules/testing.md「既定値・フォールバックがある処理では、既定値と違う答えになる入力を選ぶ」

### 11. doc の中で「丸め」が 2 つの意味で使われていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「範囲外でも丸めず」（範囲へ収める）と「小数 4 桁で丸めた」（四捨五入）が同じ doc に並び、
  矛盾して読めた。前者を「範囲へ収めず」に直した。
- 既存ルール: rules/naming.md「名前と実体を一致させる」

### 12. token-css の 4 枝が同じ形だった

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: colors / spacing・radius / shadows / gradients が同じ `Object.entries(...).map(...)` の形で、
  違うのは値の綴りだけ。`singleVariableEntries(kind, tokensOfKind, toValue)` に畳んだ。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 13. 丸めの理由を docs とコードに二重に書いていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 誤差の例（`0.007` → `0.7%`）を両方に書いていた。加えて根拠の「要素の幅 1px あたり」は
  斜めのグラデーションでは不正確。理由は docs に残し、コードは参照と根拠だけにした。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 14. モジュール外で使わない値の型を export していた

- 分類: `dependency`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `GradientStopCssValue` / `LinearGradientValue` は gradient モジュールの外で使われていなかった。
  export を外し、CSS の構文名に揃えて `ColorStopValue` に改名した。
- 既存ルール: rules/architecture.md「モジュールの公開API」

### 15. `NumberEx.round` の doc が「ちょうど半分は正の無限大側へ寄る」と書いていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `1.005` のように十進で半分に見える値は `value * scale` の誤差で下へ落ちる（`1`）。
  成り立たない主張ごと消した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 16. フックが発火しない環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されず、検証エージェントのマーカーも発火ログの見出しもこのセッションの
  ものは無し（`/tmp` にあったのは別セッションのログ）。README の表では「本当に不発」の枝。起動時の作業
  ディレクトリがリポジトリの親（`/home/user`）だった。`npx` / `git add -A` の実行と、検証エージェント
  実行中のコミットが無いことを手で確認した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- **計画を plan-reviewer の指摘 8 件で直した**（指摘 1〜8）。docs の更新がファイル表から漏れていた
  こと、テスト入力が落ちうるかを確かめていなかったこと、shape の switch の却下理由が誤っていたこと
- **実装を implementation-reviewer の指摘 7 件で直した**（指摘 9〜15）。ミューテーション 11 件中 10 件は
  初回から落ちた
- **コミットの分割**: token-css の `index.ts` にリファクタ（`singleVariableEntries`）と機能追加が混ざって
  いたので、コミット時に 2 つへ分けた
- push 前検査 19 項目と CI は 1 回目で通過した

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。この回は Issue #722 の実装で、記録を数えた結果の介入ではない。`対策済` を書けるのは
`harness-growth` の棚卸しだけ。
