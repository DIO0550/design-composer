# PR #310 [test] 同じ `__tests__` 内で本体が同じテストヘルパー 13 組を共通化する

- マージ日: 2026-08-24
- 関連 Issue: #153
- 差分規模: 39 ファイル / +424 -553

## ゴールと結果

ゴールは「`duplicate-test-helpers.py --all src` が 0 組を報告する状態にする（既存テストの観点・件数は 1 つも減らさない）」。達成した。

観点が減っていないことは、変更前後で `vitest --reporter=json` の `assertionResults[].fullName` 一覧を diff して**差分 0**（2373 → 2373）で確認した。CI 側のカバレッジレポートもテスト 2373 件 / ファイル 353 件で一致している。

**Issue 本文の一覧は起票時（PR #152 時点）のもので、着手時に数え直したら組数は 13 のままだが顔ぶれが半分入れ替わっていた**（`injectedStyles` / `renderTree` / `renderPanel` / `controlOf` / `use-editor-reducer:childNames` / `artboard-canvas:setupState` は解消済み、代わりに `setupSelection` / `toolbar` / `goTo` / `nodeNamed` / `openedState` が出ていた）。数え直しから始めたので実装前に拾えている。

human レビューは 0 件。CI は 1 回の push で全 6 チェック success（視覚差分も Changed 0 / Passed 136）。**指摘はすべてサブエージェント（`plan-reviewer` 9 分類 / `implementation-reviewer` 12 件）から出た。**

**この回の中心は 2 つ。** 1 つは、**同じ判断基準を自分の差分の中で一貫して適用できていなかった**こと（指摘 2・3・4・7・8）。「同フォルダに別意味の名前があるから共有 export に使えない」という基準を `setupSelection` には当てたのに `openedState` には当てず、「生の `querySelector` ではなく共有ヘルパーへ寄せる」を artboard-canvas では決めたのに asset-drag では残し、「1 行ラッパーは畳む」を componentize ではやって asset-drag ではやっていない。もう 1 つは、**寄せ先の doc に書いた主張を、壊せるテストが無いまま断定していた**こと（指摘 10・11）。`stateWithComponentDefinitions` の「この状態を使う理由」は、その形を丸ごと落としても 178 件が全通する。

## 指摘

### 1. この計画が消す前提そのものを名指ししている 2 ファイルがファイル表に無かった

- 分類: `plan-comment-reference`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `.claude/hooks/check-test-helper-duplication.sh:13` と `.claude/hooks/README.md:145` が「既存の重複はリポジトリに 13 組ある」と書いており、README は解消先として #153 を明示していた。13 組が 0 になった瞬間に両方の記述が嘘になるのに、コードの参照だけを拾ったファイル表に挙がっていなかった
- 既存ルール: `.claude/agents/plan-reviewer.md`「ファイル表に挙がっているか」/ rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ（両方をファイル表に足し、0 組になった事実へ更新した）

### 2. 同じ処理の 3 本目が 3 箇所あり、計画がどれも数えていなかった

- 分類: `plan-file-omission`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 検出器の守備範囲（本体完全一致・同フォルダ）より `rules/coding.md`「同じ処理が2箇所に現れたら共通化する（実装・テストとも）」のほうが広い。`opened-document-editor.componentize` の `goToAssets` は #5 の `goTo` のインライン展開、`editor-state.reorder` の `childNames(state)` は #9 の共有 `childNames` と同フォルダ・同名・別アリティ、`use-editor-state.detach` の `childNamesOf` は #11 の共有名と語幹が衝突する。どれも計画の変更表に無かった
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ（3 件とも対応した。`childNamesOf` は統合できないことを実測して確かめ、共有側を `homeChildNames` へ改名した）

### 3. `setupSelection` の件数を数え違えており、同型の `.zoom-pan` を見落としていた

- 分類: `plan-scope-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画は「同フォルダに別本体の `setupSelection` が 3 つある」と書いたが、実際は 6 ファイル（`.normal` / `.selection` / `.drag-move` / `.resize` / `.text-edit` / `.zoom-pan`）。さらに `.zoom-pan` の版は計画が `.drag-move` を書き換える根拠に挙げた「`DocumentTemplate.Default` の tokens/components + artboards という形が一致」にそのまま当てはまるのに、片方だけ書き換える理由が計画に無かった
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md`「計画」フェーズ 3 手順 2
- 次にどう防ぐか: 今回は記録のみ（件数を訂正し、`.drag-move` と `.zoom-pan` の両方を共有ファクトリ経由へ書き換えた）

### 4. `openedState` の据え置きが、同じ計画の中の改名判断と矛盾していた

- 分類: `plan`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画は `setupSelection` について「同フォルダに本体の違う名前があるから共有 export には使えない」と判断したのに、同じ条件を 3 ファイル分満たしている `openedState`（`.document-errors` / `.frozen` / `.reload` に別本体がある）は据え置きにしていた。据え置きの理由として書いた「doc コメントが充実しているから」は、名前が衝突するかどうかと無関係
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ（`stateWithComponentDefinitions` へ改名した）

### 5. `setupState` の改名理由が実物と合っていなかった

- 分類: `plan`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 改名は妥当だったが、書いた理由「同じ setup.ts に `openedState` も並ぶため区別が付かない」は弱く、実際の理由は「同フォルダに別本体の `setupState` が 16 個残るから」だった。理由が正しくないと、次に似た値が出たときに同じ判断ができない
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ（理由を訂正した）

### 6. 「0 組を保つ手段」が未決なのに「未決の判断: 無し」と書いていた

- 分類: `plan-goal-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 0 組にしてもフックは非ブロック・編集ファイル絡みのみで、CI にも `--all` の実行が無いので、0 を保つゲートが存在しない。`.claude/hooks/README.md` には同型の前例（#159 で doc の抜け 149 件を 0 にしたので絞る理由が無くなった）があり、Issue 本文自身も「解消後にブロックへ格上げするかは改めて判断する」と書いているのに、計画は未決を 1 つも挙げていなかった
- 既存ルール: `.claude/agents/plan-reviewer.md`「ゴールの達成を確かめる手段」
- 次にどう防ぐか: 今回は記録のみ（格上げ判断を #309 として分離し、この PR ではコメント 2 箇所の更新に留めた）

### 7. 寄せ先の `setup.ts(x)` 自身が検出器の比較対象であることを検算していなかった

- 分類: `plan-goal-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `duplicates_in()` は `folder.glob("*.ts*")` なので、新設・追記する `setup.ts` / `setup.tsx` / `Json.setup.ts` もそのフォルダの比較対象に入る。「移動によって新しい組が生まれない」ことを確かめる手順が計画に無かった
- 既存ルール: `.claude/agents/plan-reviewer.md`「ゴールの達成を確かめる手段」
- 次にどう防ぐか: 今回は記録のみ（実装後に `--all` を再実行して 0 組を確認する手順を計画へ足した）

### 8. `of*` を避ける規約を新しい識別子に適用していなかった

- 分類: `naming-prefix-collision`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 共有名を `selectionOfArtboards` と決めたが、rules/naming.md は「`of*` は使わない。`create` / `from*` のどちらかに寄せる」と明示している。テストヘルパーはコンパニオンメソッドではないので厳密な適用対象外だが、リポジトリが明示的に捨てた語形を新規に持ち込むことになる。加えて新識別子の grep 結果が計画に書かれていなかった
- 既存ルール: rules/naming.md「メソッド名を既存のドメインに揃える」
- 次にどう防ぐか: 今回は記録のみ（`selectionFromArtboards` へ変え、4 つの新識別子の grep 結果（いずれも 0 件）を計画に記載した）

### 9. 観点の担保方法が「passed 件数の一致」で、入れ替わりを検出しなかった

- 分類: `plan-goal-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画は「変更前後で passed 件数が一致すること」で観点の維持を担保するとしていたが、1 件消えて 1 件増えれば同数になるため入れ替わりを検出しない。CI が既に `--reporter=json` を使っているので、`assertionResults[].fullName` の一覧を diff するほうが「観点を 1 つも減らさない」に直接対応する
- 既存ルール: rules/testing.md「API を作り替えるときは、既存テストの観点を先に数える」
- 次にどう防ぐか: 今回は記録のみ（`fullName` 一覧の diff へ差し替えた。結果は差分 0）

### 10. `drawnAt` の統合後の doc が、片方の呼び出し側にしか当てはまらない主張になっていた

- 分類: `comment-structure-claim`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 冒頭で「どこが掴める帯か（リサイズ）」「入力欄を重ねる位置（インライン編集）」の 2 つを併記した直後に、「掴めるかどうかと長さの決まり方は実物の `node-resize` が答える」と片方しか挙げていなかった。`getBoundingClientRect` を読むのは `CanvasBounds.ofElement` で、その呼び出し元は `use-node-resize` / `use-node-drag` / `use-text-edit` の 3 つ。text-edit 側は `domains/text-edit` が答えるので `node-resize` は嘘
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ（両方を挙げ、`CanvasBounds.ofElement` 経由であることを明記した）

### 11. 寄せ先の doc に書いた「使う理由」を、壊せるテストが 1 件も無いまま断定していた

- 分類: `comment-behavior-claim`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `stateWithComponentDefinitions` の doc に「artboard 配下のノードと部品定義の中のノードを両方持つことが、この状態を使う理由」と書いたが、factory から `tokens` / `components` を丸ごと落としても `editor-state` の 178 テストが全件通る。`editor-state.reveal` の「部品定義の中のノードは飛び先にならない」の assert が直前のテストと一字一句同じ `expect(revealed.some).toBe(false)` で、部品定義が無ければ「ただ存在しないノード」になり 2 テストが同じものになるため。加えてこの doc は revert の 2 テストにも掛かるが、revert は部品定義を一切見ない
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ（doc から断定を外し、いま守られていない事実そのものを書いた。テスト側の補強は観点が変わるためこの PR では行わず、Issue #153 へ記録した）

### 12. `stateWithNestedBox` の doc が挙げた 2 用途のうち片方が成立していなかった

- 分類: `comment-behavior-claim`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 「入れ子を 1 段持たせているのは、サブツリーごと動く操作（削除・履歴の巻き戻し）を平らな並びでは確かめられないため」と書いたが、`body` の入れ子を平らにして落ちたのは `editor-state.remove` の 1 件だけ。`editor-state.history` は 18 件すべて通る（history が動かすのは `title` と `body-text` という葉で、サブツリーを動かしていない）
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ（doc を「削除」に絞り、history が入れ子に依らないことを明記した）

### 13. `ToolbarProps` が本体の props 型の逐語コピーになっていた

- 分類: `duplication-derivation`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 新設した `setup.tsx` に `ToolbarProps` を手書きしたが、`node-insert-toolbar/index.tsx` の inline props 型のコピーで、`dragged` に付いていた doc も落ちていた。本体の props が増減しても気づけない
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ（`Partial<ComponentProps<typeof NodeInsertToolbar>>` で受ける形にした）

### 14. `drawn` を共有ヘルパーへ寄せる判断が、1 フォルダ隣で反転したまま残っていた

- 分類: `duplication-traversal`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: artboard-canvas では「生の `document.querySelector('[data-name=...]')` 版ではなく `renderedElement(canvasContent(), name)` 版へ寄せる」と決めたのに、`opened-document-editor.asset-drag` の `drawn` は生版のまま残った。同フォルダの `componentize` / `prop-edit` / `selection` は既に `renderedElement(canvasPane(), name)` を使っており、asset-drag 自身も `@/features/canvas/__tests__` を import していた
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ（共有版へ寄せた）

### 15. `goTo` の 1 行ラッパーを畳む判断が、同フォルダ内で片方だけ残っていた

- 分類: `duplication-derivation`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `componentize` の `goToAssets` は共有 `goTo` の直呼びへ畳んだのに、同フォルダの `asset-drag` に残っていた `goToAssets` / `goToLayers`（どちらも `goTo` を 1 行包んだだけ）はそのままだった
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ（両方畳んだ）

### 16. file-local だった名前を export に格上げしたら、既存の同名・別物と衝突した

- 分類: `naming-prefix-collision`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `setupRecord` を `Json.setup.ts` から export したが、`src/domains/design-document/v1/__tests__/design-document-v1.json.test.ts` に `setupRecord(formatVersion: string)` が既にあり、そちらは「フォーマット版を差し替えた生の JSON レコード」を返す。file-local のうちは衝突しなかったが、export した時点でリポジトリに同名・別物が 2 つ並ぶ。加えて返すのは `JsonRecordCursor` なのに名前が `setup*` で、戻り値が名前に出ていなかった
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」「名前と実体を一致させる」
- 次にどう防ぐか: 今回は記録のみ（`recordCursor` へ改名した）

### 17. ローカル fixture を共有ファクトリ経由へ変えたとき、doc が前提にしていた値が呼び出しから消えた

- 分類: `comment-premise-drift`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `artboard-canvas.drag-move` の `setupSelection` の doc は「**未選択**の対」と書いているが、共有ファクトリ経由へ書き換えた際に第 2 引数の `[]` が消えて既定に依存する形になった。共有側の既定を `["home"]` に壊しても drag-move は 1 件も落ちない
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ（`selectionFromArtboards([...], [])` と明示した）

### 18. 同じ判断が shell ヘッダと README の 2 箇所に別文面で書かれていた

- 分類: `comment-premise-drift`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 「なぜブロックしないか」が `check-test-helper-duplication.sh` のヘッダと `.claude/hooks/README.md` の両方に別の文面で書かれており、片方だけ直ると食い違う。**まさにこの PR の指摘 1（「13 組」の記述が 2 箇所にあった）で起きたことの再来**。加えて shell 側に書いた「導入時の理由は #153 の解消で消えたが…」は経緯であり、`rules/coding.md`「Why not の根拠に、いま直されつつある一時的な事実を置かない」に照らすと短命
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」「Why not の根拠に一時的な事実を置かない」
- 次にどう防ぐか: 今回は記録のみ（README へ一本化し、shell は参照のみにした）

### 19. CI の biome が落ちる状態だった（2 回）

- 分類: なし
- 出どころ: 自己修正 / レビュー（`implementation-reviewer`）
- 内容: 実装直後に format 差分 3 件、`implementation-reviewer` の復元後にさらに 6 件（format 3 + `useImportType` 3）。`useImportType` は、`DocumentSelection` の値としての使用（`DocumentSelection.fromNames`）が共有ファクトリへ移った結果、呼び出し側で型専用 import になったもの。`typecheck` / `vitest` / `oxlint` はいずれも通るので、biome を独立に実行しない限り気づけない
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md`「フェーズ 7」（`&&` で連ねない / oxlint と biome は別物）
- 次にどう防ぐか: 今回は記録のみ（既存ルールどおり 1 つずつ独立に実行して 2 回とも push 前に拾えている。CI 失敗は 0 回）

### 20. `implementation-reviewer` が作業ツリーを書き換え、復元が自分の変更を巻き戻した

- 分類: `subagent-control`
- 出どころ: フック（`Edit` の外部変更検知）
- 内容: ミューテーション実測のため `node-insert-toolbar/__tests__/setup.tsx` の `isInsertEnabled` を `false` に変更した。エージェントは復元したが、**復元先が「差分受領時点」だったため、検証中に自分が入れた `biome check --write` の結果 6 ファイル分が巻き戻った**。`git status` の件数（39）は変わらないため、件数だけを見ると気づけない
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md`「サブエージェントの使い方」（返ってきたら指摘を読む前に `git status` を見る）
- 次にどう防ぐか: 今回は記録のみ（既存ルールは `git status` の確認を求めており実行したが、**件数が同じで内容だけが巻き戻る形は `git status` では見えない**。今回は指摘 19 として biome を再実行したことで拾えた。検証中は作業ツリーを触らないのが確実）

### 21. フックが発火しない実行環境だった

- 分類: `hook-environment`
- 出どころ: フック（カナリア）
- 内容: `echo hook-canary` が deny されず通った。`.claude/hooks/` の PreToolUse が発火していない
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ（push 前検査の enforcement は git hooks と CI が担うので手順は変えていない。実際 push 時の git pre-push フックは正常に発火し全検査を通過した）

## 手戻り

3 件。

1. **`.drag-move` / `.zoom-pan` の書き換えを 2 度やり直した。** 最初にローカル関数を `DragMoveSelection` / `ZoomPanSelection` という仮名へ機械置換してしまい、camelCase でもなく意図（ローカル名は据え置き、本体だけ共有ファクトリ経由にする）とも違ったので `git checkout` して書き直した
2. **作業ディレクトリの取り違えで同じスクリプトを 2 回走らせた。** `cd <サブフォルダ>` した直後の Bash 呼び出しで相対パスを使い、`FileNotFoundError` で失敗。以降は絶対パスに統一した
3. **`biome check --write` を 2 回実行した**（指摘 19・20）

## うまくいったこと

- **着手時に Issue 本文の一覧を鵜呑みにせず数え直した。** 顔ぶれが半分入れ替わっていたので、そのまま進めていたら存在しないヘルパーを探すことになっていた
- **`plan-reviewer` の指摘 2-1（`childNamesOf` を統合せよ）を鵜呑みにせず実物で確かめた。** `DesignDocument.findChildren` は `NodeTree.allowsChildren`（= プリミティブのみ）が false だと `none` を返すので、解除前の参照ノードを読む `childNamesOf` は統合すると落ちる。統合していれば detach テストの観点そのものが壊れていた
- **`fullName` 一覧の diff で観点の維持を機械的に示せた。** 「テストを 1 つも変えていない」を主張だけで終わらせず、2373 → 2373 かつ差分 0 で裏付けられた
- **`token-used-by` の `renderFor` を共通化しない判断ができた。** 本体は一字一句同じだが参照する `ReferringDocument` がファイルごとに別物で、検出器は自由変数の束縛先を見ていない。共有すると既存の `renderUsedBy` そのものになる
- **押し戻すべき指摘は根拠付きで押し戻し、Issue に残した**（`implementation-reviewer` の 12 件中 11 件受け入れ・1 件却下）

## 規約への反映

**無し。** この回の指摘 21 件はいずれも既存ルールが既に書いている内容で、`分類: なし` は指摘 19（biome の型専用 import）の 1 件のみ。1 件しか出ていない分類は何もしない（`harness-growth`「1 件しか出ていない分類は何もしない」）。

集計と介入の判断はこのスキルの管轄外なので行わない（→ `harness-growth`）。以下は次に数える人への申し送り。

- **指摘 18 は、同じ PR の指摘 1 と同じ形の再発**（同じ事実が 2 箇所に別文面で書かれ、片方だけ古くなる）。`comment-premise-drift` は pr-305 時点で観点層まで介入済み
- **検出器（`duplicate-test-helpers.py`）に盲点が 2 つ確認された。** (1) フォルダをまたぐ重複を見ない（既知 / #179）、(2) `f(props: T = {})` 形の関数は既定引数の `{}` を本体と読むため `MIN_BODY_CHARS`(20) 未満で捨てられる（この PR で足した `renderToolbar` が該当）。どちらも `.claude/hooks/README.md` に明記し、格上げ判断（#309）の材料にした
- **この差分の外側にあるテストの弱さを 6 件見つけたが直していない**（観点が変わりこの PR の担保が崩れるため）。内訳は Issue #153 のコメントにある。うち `editor-state.reveal` の 1 件は `分類: test-coverage-branch` に相当する
