# PR #757 doc 検査でメソッドを常に見て、追加行だけの検査を外す（#720 仕上げ）

- マージ日: 2026-09-24
- 関連 Issue: #739
- 差分規模: 12 ファイル / +83 -263

## ゴールと結果

ゴールは達成した。コンパニオンオブジェクトのメソッドの doc の抜けを、ファイル直下の宣言と同じく `src` 全体で検査するようにした
（`--all`・CI の `rules-check`・git hooks・Claude Code のフック）。そのうえで、追加行だけを見る `check-added-doc-comments.sh` と
graduation の記述を外した。着手条件（`--all src --include-methods` が 0 件）は着手時に確認した。
`--all` が `// @doc-comments-ok` を見ない件は、以前からあった食い違いとして #756 に切り出した。
人のレビュー指摘は 0 件（`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも確認。コメントは bot の投稿と自分の報告だけ）。

## 指摘

### 1. 後続 Issue を番号無しの「別 Issue」とだけ書いていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `--all` がハッチを見ない件をスコープ外にしたが、起票もリンクもしていなかった。#756 を立てて #739 からリンクした。
- 既存ルール: `AGENTS.md`「タスクの分割」

### 2. `--all` の経路でメソッドを見ていることを確かめる手段が計画に無かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 判定表は単一ファイルの形しか呼んでいなかった。`check_all` 側の配線だけが切れても `src` が 0 件なら緑のままになる。
  呼び方の列（`file` / `all`）を足し、`all` の行を 3 つ置いた。ミューテーションで該当する行が NG になることを確かめた。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 3. 直す箇所を行単位で挙げていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 判定表の `--lines` への言及・表の形式・`verdict` の分岐、`frontend.yml` のステップ前のコメント、README の 5 か所が、
  計画にまとめて書かれていただけだった。行番号で挙げ直した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 1

### 4. 「3 つ→2」を一括で置き換えると、別の意味の「3 つ」まで変わる計画だった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `harness/githooks/README.md` の「飛ばす単位は 3 つある」は別の数。直すのは 36・38 行だけと明記した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 2

### 5. 既存フックの冒頭コメントをどう直すかが未決のまま「無し」とされていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `check-doc-comments.sh` の「導入時点で 73 件…だからブロックしない」を残すかどうかを書いていなかった。
  ブロックしない理由の履歴なので残す、と書いた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「未決の判断」

### 6. README の消える節のうち、どれを残すかの線引きが無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 検出器の性質（メソッドと読む条件）は残し、追加行の判定の性質（触った行・改行したシグネチャ・ファイル分割）は消す、と分けた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 7

### 7. 検出対象を広げたことで、別ファイルの「検知しない」の前提が崩れた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `implementation-reviewer.md:295` の「入れ子の宣言を対象外にしているため検知しない」が崩れた。doc の無いメソッドは
  拾うようになったので、いまも拾わない「ブロックの重なり」の理由に直した。判例 `harness/case-law/coding.md:195` は、
  `harness-growth` しか書けないのと当時の事実であることから据え置いた。
- 既存ルール: `.claude/skills/claim-verification/`（comment-premise-drift）

### 8. ハッチが層ごとに効き方が違うことが README に書かれていなかった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「ファイル単位で無効化」の行が、`--all`（CI・git hooks）でも効くと読めた。「`--all` はこのハッチを見ない(#756)」を添えた。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 9. 「見る範囲」の説明をもう 1 か所に足していた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `pre-push-doc-comments.sh` の冒頭に、検出器の docstring と README にある説明を写していた。取り下げた。
  判定表の冒頭で同じ理由を 2 段落で書いていた箇所も 1 つにまとめた。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 10. `if __name__` の前の空行が 1 行に減っていた

- 分類: `なし`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `main()` を書き換えたときに、関数間の空行 2 行が 1 行に減っていた（PEP 8）。戻した。
- 既存ルール: なし

### 11. 判定表の case 文字列の閉じ `"` を、消した行と一緒に落とした

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `check-added-cases.sh` の doc の行を消したとき、表を閉じる `"` が最後の doc の行に付いていた。
  push 前に判定表を走らせて構文エラーで気づき、直した。
- 既存ルール: なし

### 12. 「PR が閉じる Issue のチェック」が PR を作った直後に赤になった

- 分類: `harness`
- 出どころ: `CI`
- 内容: 本文には最初から `Closes #739` があった。`opened` の数秒後に `closingIssuesReferences` を問い合わせ、その時点では
  リンクがまだ無かったと見ている。1 回だけ再実行して通った。差分は検査スクリプトに触れていない。
- 既存ルール: なし

### 13. フックが発火しない環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されなかった。このセッションには検証エージェントのマーカーも発火ログの見出しも無かった
  （`/tmp` の発火ログは別のセッション ID のもの）ので、README の表では「本当に不発」に当たる。
  `npx` / `git add -A` の実行と、検証エージェント実行中の git 操作が無いことを履歴で確認した。git hooks の pre-push は発火した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- **計画を plan-reviewer の指摘 6 件で直した**（指摘 1〜6）
- **実装を implementation-reviewer の指摘 4 件で直した**（指摘 7〜10）。`pre-push-doc-comments.sh` に 1 行足すという計画は、指摘 9 で取り下げた
- 判定表の構文エラー 1 回（指摘 11）。push 前に手元で気づいた
- push 前検査 20 項目は 1 回目で通過した。CI は「PR が閉じる Issue のチェック」が 1 回赤になり（指摘 12）、再実行で通った。ほかは 1 回目で通過した

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。この回は Issue #739 の実装で、記録を数えた結果の介入ではない。`対策済` を書けるのは
`harness-growth` の棚卸しだけ。
