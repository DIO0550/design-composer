# PR #280 [architecture] prop-control を src/domains/ へ昇格する

- マージ日: 2026-08-22
- 関連 Issue: #254
- 差分規模: 17 ファイル / +786 -617

## ゴールと結果

ゴールは「`features/editor/domains/prop-control`（787 行）が `src/domains/prop-control` に在り、`features/` と `services/` のどちらにも依存しない状態」。達成した（`grep -rn "@/features\|@/services" --include="*.ts" --include="*.tsx" src/domains/` はコメント 1 行のみで import 0 件）。

Issue の未決だった「どこまで昇格させるか」は **案 A（丸ごと昇格）** を採った。案 B（スキーマから編集可能な形を導くところまでをドメイン、行 / 節の並びは inspector に残す）は、`group` ごとのセクション・宣言順・`shorthand` の束ねという `docs/03-schema.md` 由来の規則を features 層へ残すことになり、`rules/coding.md`「features 層にドメイン知識を書かない」を逆向きに破るため却下した。

human レビューは 0 件（レビュー・レビューコメントとも 0）。CI は 6 チェックすべて 1 回目で success（視覚差分も 0 changed / passed 105）。**指摘はすべてサブエージェント（`plan-reviewer` 17 件 / `implementation-reviewer` 8 件）から出た。**

テストは 349 ファイル / 2361 件。移動前（349 / 2357）との差は追加した 4 本で、既存 64 本は観点ごと移した。

**Issue 本文の依存の数え上げが 1 つ足りなかったことが、この回のいちばん大きい発見。** 本文は `EditorState` 5 箇所を挙げていたが、うち 1 つは #246 で解消済みで、代わりに数えられていなかった `services/instance-composition` への依存が残っていた。`src/domains/` は `src/services/` を import できないので、`(document, selection)` を受け取る形にするだけでは依存が消えない。

## 指摘

### 1. 昇格すると `editFrom` の「空欄 = 未設定へ戻す」がドメインに入る

- 分類: `domain-input-convention`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `PropControl.editFrom` / `PropPairControl.editFrom` は `raw === ""` を「未設定へ戻す」と読み、その doc 自身が「`PropEdit` ではなく**コントロールを知っているここで**解釈する」を根拠にしていた。つまり **feature 側に在ることが根拠**で、そのまま昇格すると `rules/architecture.md`「入力欄の約束事をドメインへ持ち込まない」に正面から当たる。しかも `harness/records/pr-141.md` は `TypographyToken.withField` の空文字解釈を feature へ移した回で、**その判断の根拠に `PropControl.editFrom` を挙げている**。昇格ついでに持ち込むと、その回の判断を黙って覆すことになる。計画は「人が読む綴り（`unsetLabel` / `Mixed` / 単位）は移らない」しか見ておらず、**入口側の解釈**を数えていなかった
- 既存ルール: rules/architecture.md「逆向きも見る: 入力欄の約束事をドメインへ持ち込まない」
- 次にどう防ぐか: 今回は記録のみ（第 2 引数を `Option<string>` にして `""` の解釈をパネルの `valueFrom` へ戻した。数値への変換はスキーマの `literalType` の性質なのでドメインに残した）

### 2. 昇格で嘘になるコメントが、コード参照だけを拾ったファイル表に挙がっていなかった

- 分類: `plan-comment-reference`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `features/tokens/domains/token-control/index.ts:22-30` が「`src/domains/` ではなくこの feature に置く（`prop-control` と同じ理由）」と書いている。昇格するとこの Why が嘘になるが、`token-control` は `prop-control` を import しておらず**コメント内でだけ名指し**しているため、`grep` でコード参照を数えたファイル表に入っていなかった。**pr-261#3 / pr-269#3 と同じ形**
- 既存ルール: `plan-review.md`「ファイル表に挙がっているか」
- 次にどう防ぐか: 今回は記録のみ（両方のコメントを直し、分かれ目 —`token-control` は `valueText` / `TokenPreview.widthPx` のように綴りと見せ方そのものを持つので feature に残る— を 1 行で書いた。`text-edit` / `create-component` / `instance-composition.detachable.test.ts` の名指しも同時に追随させた）

### 3. 昇格の根拠が「消費する feature の数」で書かれていなかった

- 分類: `domain-scope-promotion`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `rules/architecture.md` は「2 つ以上の feature が必要としたら昇格」と書いているが、消費側は `property-panel` の 1 箇所だけで、`features/inspector` を切り出した後も 1 つのまま。計画は帰属（スキーマの性質）だけを根拠にしており、規約の引き金との関係を書いていなかった
- 既存ルール: rules/architecture.md「配置の判断基準」
- 次にどう防ぐか: 今回は記録のみ（規約の 2 feature 条項は**重複実装を防ぐために昇格を強制する引き金**であって昇格してよい唯一の条件ではない、と整理し、帰属を根拠に進めた。`prop-control/index.ts` の冒頭 doc にも「消費 feature は 1 つだが帰属を根拠に置く / 判断は #174」を書いた）

### 4. precedent の引き方が理由になっていなかった

- 分類: `ownership-reasoning`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `services` 依存を引数で外す根拠に `Padding.cssValue(padding, resolveToken)` を引いたが、`rules/architecture.md` の該当節は「ドメインが**出力形式**を必要とする場合」で、`resolveToken` は表現の変換手段。`isDetachable` は変換ではなくドメイン判定なので、構造が同じでも理由は同じではない。このまま書くと「述語なら何でも引数で渡してよい」と読める
- 既存ルール: rules/architecture.md「ドメインが出力形式を必要とする場合」
- 次にどう防ぐか: 今回は記録のみ（Why を「`isDetachable` の中身は `expandInstance` の再帰で、規約の『services はドメインを探してから使う』3（走査・再帰）に当たり、いずれドメインへ移りうる暫定の境界」へ書き直し、follow-up を #279 に分けた）

### 5. 却下案に、実在するいちばん近い候補が入っていなかった

- 分類: `plan`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `EditorState` は既に `InstanceComposition` を import しているので、`EditorState.selectionControls(state)` に配線する案が実在した。計画が却下していたのは「パネル側で計算する」だけで、この案が漏れていた
- 既存ルール: rules/coding.md「Why not を書く相手は、実際に検討して捨てた案だけ」
- 次にどう防ぐか: 今回は記録のみ（#174 / #246 の方向はペインが `EditorState` を手放すことなので採らない、と理由を書いて Issue の却下案に足した）

### 6. 型で閉じられないことを承知で選んだ、という Why が無かった

- 分類: `comment-missing`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 「渡された述語が本当に解除可否か」は型では閉じられず規律のままだが、その承知が doc に無かった。`rules/coding.md`「前提をコメントで書くのは型で閉じたことにならない」を踏まえた 1 行が要る
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ（`forSelection` の doc に 1 行足した）

### 7. 受け取る型を変えると、既存 doc の `@returns` が嘘になる条件が増えていた

- 分類: `plan-implicit-condition`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `EditorState.select` は `selectableName` で存在しない名前を選択に入れないが、`DocumentSelection` は「名前がドキュメントに在ることは**検証しない**」と doc に明記されている。受け取る型を変えると「単一選択だが名前がドキュメントに無い → `none`」が新たに起きうるようになり、`forSelection` の `@returns`（「何も選んでいないときは `none`」）が不足になる。計画に書かれていなかった
- 既存ルール: `plan-review.md`「既存のコードが前提にしている不変条件を、この計画が崩さないか」
- 次にどう防ぐか: 今回は記録のみ（`@returns` を `DocumentSelection.singleSelection` と同じ書き方へ直した）

### 8. 新しく観測できるようになった状態のテストが計画に無かった

- 分類: `plan-implicit-condition`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 指摘 7 の裏返し。旧 API では作れなかった「選んでいる名前がドキュメントに無い」が新 API では作れるので、**新しく観測できるようになった仕様**。1 本足さないと `findArtboard` / `findNode` の合流を壊しても誰も落ちない
- 既存ルール: `plan-review.md`「既存のコードが前提にしている不変条件を、この計画が崩さないか」
- 次にどう防ぐか: 今回は記録のみ（1 本足し、`findArtboard` の分岐を常に真にするミューテーションで 22 件落ちることを実測した）

### 9. 件数の取り出し方が計画に書かれていなかった

- 分類: `plan`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `multiple` の件数を `EditorState.selectedNames(state).length` から取っていたが、`DocumentSelection` は `count` を公開していない（`SelectionState.count` は在る）。対の内部を開いて `SelectionState` を直接読むか、`DocumentSelection.count` を足すかの分岐が計画に無かった
- 既存ルール: rules/architecture.md「ロジックの帰属先」
- 次にどう防ぐか: 今回は記録のみ（`DocumentSelection.count` を足した。`isSelected` / `singleName` が「答えは選択だけで決まるが、対しか持っていない消費側のために置く」として既に同じ形になっているので、それに揃えた）

### 10. 計画のテスト件数表が実測とずれていた

- 分類: `plan-scope-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画は normal 26 / shorthand 16 と書いたが、実測は normal 25 / shorthand 17（合計 64 は一致していたので、合計だけ見ると気づけない）
- 既存ルール: implementation-flow フェーズ 3「立てた収集条件を実際にコードへ当てて件数を検算する」
- 次にどう防ぐか: 今回は記録のみ（`grep -c '^test(' *.test.ts` をファイル別に取り直した。合計が合っていても内訳を突き合わせる）

### 11. 移送先が決まっていないテストが 62 本あった

- 分類: `plan-migration-mapping`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `setup.ts` の `controlsOf` が `forSelection` を呼ぶため、**64 本すべてに述語が要る**。計画は解除可否の 2 本にしか触れていなかった。さらに「`setup.ts` の既定に実物を置く」案を採ると、`src/domains/**` のテストが `@/services` を import する初めての例になる（現状 0 件、lint / CI での強制は無い）
- 既存ルール: `plan-review.md`「リファクタ・分割で消えるテストケースの移送先が計画に書かれているか」
- 次にどう防ぐか: 今回は記録のみ（一度は実物を渡す形で実装したが、実装レビュー（指摘 20）を受けてテスト側の述語へ変えた）

### 12. 追加する `multiple` テストが既定と区別できなかった

- 分類: `test-default-input`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `multiple` へ入る条件は `length > 1` なので、`count: 2` は境界かつ最小。`count` を定数 `2` にする壊し方でも通ってしまう
- 既存ルール: rules/testing.md「既定値・フォールバックがある処理では、既定値と違う答えになる入力を選ぶ」
- 次にどう防ぐか: 今回は記録のみ（3 件選ぶ形に変え、`count` を定数 2 に固定するミューテーションで落ちることを実測した）

### 13. 配線を守る既存テストが、計画のテスト一覧に挙がっていなかった

- 分類: `plan-path-coverage`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: パネルが `InstanceComposition.isDetachable` を渡す配線が切れたときに赤くなるのは `property-panel.instance.test.tsx:143,151` の 2 本だけ。計画のテスト一覧はドメイン単体しか挙げておらず、「通しは既存の 2 本が担当・変更不要」が書かれていなかった（挙げ忘れなのか要らないと確定したのかが区別できない）
- 既存ルール: `plan-review.md`「単体しか無いなら、通しが要らないと確定したのか挙げ忘れなのかを 1 行で書かせる」
- 次にどう防ぐか: 今回は記録のみ（明記し、実装後にパネルが「常に解除できる」判定を渡すミューテーションで 1 件落ちることを実測した）

### 14. `Selection*` 接頭辞を移り先で数えていなかった

- 分類: `naming-prefix-collision`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `src/domains/` には既に `Selection` / `SelectionKind`（`selection`）・`SelectionState`（`selection-state`）・`DocumentSelection`（`document-selection`）があり、`SelectionControls` が加わると `Selection*` が 4 つ並ぶ。名前据え置きの理由（`token-control` と語彙を揃える・波及 30 箇所）はモジュール名と型名の話で、**移り先の既存接頭辞との衝突は数えていなかった**
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ（`grep -n "^export \(type\|const\) Selection" src/domains/*/index.ts` で数え、あちらは「選択そのもの」で `SelectionControls` は「選択に対して出す編集欄」なので指すものは重ならないと確認して据え置いた）

### 15. `isDetachEnabled` が、ドメインへ移すと表示側の語彙になっていた

- 分類: `なし`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `Enabled`（ボタンが押せるか）は UI の状態を指す語で、features にある間は自然だが `src/domains/` に置くと「ドメインが持つのは値の種別まで」という昇格の根拠と食い違って見える。中身は `InstanceComposition.isDetachable` の答えそのものなので `isDetachable` へ揃えた（実装 1・パネル 2・テスト 2・コメント 2 の計 7 箇所）。**`rules/naming.md`「名前と実体を一致させる」に対応する分類タグが語彙に無い**（`naming-*` の 4 つはいずれも「その名前が既に別の意味を持っていないか確認する」に紐づいている）
- 既存ルール: rules/naming.md「名前と実体を一致させる」（対応する分類タグは無し）
- 次にどう防ぐか: 今回は記録のみ

### 16. `valueFrom` に渡している値が doc の説明と食い違っていた

- 分類: `comment-false-claim`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 空欄の解釈をパネルへ戻すときに置いた `enteredValue(raw)` を、セグメントの**表示値**（`field.value`）にも使った。doc は `@param raw 入力欄が持っている生の文字列` と書いており、渡しているものと食い違う。さらにレビュアーが実測したところ、この読み替えを `Option.some` に固定しても**全 2360 件が通る**（enum の選択肢に `""` は無いので `some("")` と `none` が同じ描画になる）ので、「空文字を未選択として読み替える」というコメントの主張も実装で確かめられていなかった
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ（`valueFrom` へ改名し、入れる向き（`FieldBinding.value` が未設定・不揃いを空文字で表す）と受け取る向きの両方に効く約束事として doc を書き直した。overclaim していたコメントは消した）

### 17. 述語の型が 2 箇所に直書きされていた

- 分類: `なし`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `(document: DesignDocument, name: string) => boolean` が `nodeControls` と `forSelection` の両方に直書きされており、片方だけ変えても型エラーにならない（`forSelection` が受けた関数をそのまま渡すので、引数の意味が入れ替わっても通る）。**関数型の注釈の重複に対応する分類タグが語彙に無い**（`duplication-*` の 4 つはテストヘルパー・導出・分岐の形・走査に紐づいている）
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」（対応する分類タグは無し）
- 次にどう防ぐか: 今回は記録のみ（`DetachableCheck` 型を切って両方から参照した）

### 18. テストの前提の組み立てが 5 ファイルに散っていた

- 分類: `duplication-test`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `home` 360x240 + Box `"box"` を選ぶ前提が 5 ファイルにあり、`enabled-when` と `shorthand` は `tokens:` の 1 行しか違わなかった。`check-test-helper-duplication.sh` は**本体が一字一句同じ**ものしか見ないのでこの 1 行差で素通りする。重複自体は移動前からあるが、この差分で 5 本とも本体を書き換えている。さらに `setupSelection` が同じフォルダの 2 ファイルで別のシグネチャを持っていた（こちらはこの差分で作った）
- 既存ルール: rules/testing.md「テスト用ヘルパーの置き場所」
- 次にどう防ぐか: 今回は記録のみ（`setup.ts` の `boxSelection` に集約し、同名で別シグネチャの状態も解消した）

### 19. `DocumentSelection.count` の doc が名指しした消費側が実体と違った

- 分類: `comment-false-claim`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 「対しか持っていない消費側（**右ペインの帯と本文**）のために置く」と書いたが、`count` の呼び出しは `SelectionControls.forSelection` の 1 箇所だけで、帯と本文が読んでいるのは `controls.value.count`。隣の `isSelected` は実際の呼び出し元（ツリーの行）を名指ししているので、書き方は揃っていたのに中身が事実と違った
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ（実体に直した。**先例の書き方を真似るときは、真似た枠に入れる中身も同じ手順で確かめる**）

### 20. `src/domains/**` のテストが初めて `@/services` を import していた

- 分類: `layer-dependency`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 解除可否のテストが偽物を検証しないよう、`setup.ts` から実物の `InstanceComposition.isDetachable` を渡した。しかし**述語は公開 API の引数**であって差し替えられた依存ではないので、テストが自前の述語を渡してもモックにはならない。むしろそのほうが「渡された答えが `isDetachable` に載る」というこの差分固有の配線を直接固定でき、`src/domains/` から `@/services` が消える。解除可否そのものの規則は `instance-composition.detachable.test.ts`（および `detach-edge.test.ts`）と `property-panel.instance.test.tsx:143,151` が既に持っている
- 既存ルール: rules/architecture.md「依存方向のルール」
- 次にどう防ぐか: 今回は記録のみ（テスト側の述語に変え、2 本を「渡された答えがそのまま載るか」の観点へ書き直したうえで、名前が判定へ渡ることを見る 1 本を足した。`grep -rn "@/features\|@/services" --include="*.ts" --include="*.tsx" src/domains/` が 0 件になった）

### 21. 判断を持たなくなった側に Why が残り、持つ側に無かった

- 分類: `comment-missing`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 「解除できるかは、解除と同じ判定に答えさせる。書き写すと `InstanceComposition.detach` と二重管理になる」という Why を `nodeControls` に残したままにしたが、**このモジュールはもうその判断をしていない**。決めているのはパネルの呼び出しで、そちらには「なぜ `InstanceComposition.isDetachable` でなければならないか」が 1 行も無かった（「常に解除できる」判定を渡す差分がレビューを通ってしまう）
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ（Why をパネルの呼び出し側へ移した。**判断を引数へ出したら、その Why も渡す側へ移す**）

### 22. ゴールが作った不変条件を、何も強制していない

- 分類: `layer-dependency`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: レビュアーが実際に壊して確認した。`prop-control/index.ts` に `@/services` を import し直して `InstanceComposition.isDetachable` を直接呼んでも、テスト 349 ファイル全通過・`tsc` 通過・`oxlint` は未使用引数の警告 1 件で**終了コード 0**（`--deny-warnings` なし）。つまりこの Issue の中心の性質が、次に誰かがここを触ったとき黙って壊れる。**pr-267#14 と同じ形の再発**（あちらは `document-sync` → `editor`）
- 既存ルール: rules/architecture.md「依存方向のルール」
- 次にどう防ぐか: 今回は記録のみ（層を上げるのは `harness-growth` の管轄なのでこの PR では触らず、拾うべき形 — `oxlint` の `no-restricted-imports` を `src/domains/**` に効かせる / `.claude/hooks/lib/` に `check-*.sh` を 1 本足して pre-push と CI に載せる — を Issue #254 へ書き出した。**#257「依存方向を CI / lint で強制する」がまさにこのスコープ**）

### 23. 自己チェックの grep が、深さ 1 の `index.ts` しか見ていなかった

- 分類: `plan-scope-verification`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 依存方向の確認に `grep -rn "@/features\|@/services" src/domains/*/index.ts` を使ったが、これは深さ 1 の `index.ts` しか見ない（`src/domains/` には深さ 2 の `index.ts` が 11 個あり、`__tests__/` も見ていない）。0 件という結果は正しかったが、**確認手段が確認したい範囲を覆っていなかった**
- 既存ルール: implementation-flow フェーズ 3「立てた収集条件を実際にコードへ当てて件数を検算する」
- 次にどう防ぐか: 今回は記録のみ（`--include="*.ts" --include="*.tsx"` を付けてディレクトリ全体を見る形に直した）

### 24. `.claude/hooks/` が発火しない実行環境だった

- 分類: `hook-environment`
- 出どころ: フック
- 内容: push 前の手順どおり `echo hook-canary` を実行したが **deny されずに通った**。`.claude/settings.json` の配線が読まれないリモート実行環境だった
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ（push 前検査 6 つを 1 つずつ独立に実行して結果行を確認し、CI も 6 チェック全 success。ゲートは効いているので手順は変えず、PR 本文と Discord 通知に記録した）

## 手戻り

**未使用 import で typecheck が 1 度落ちた。** 指摘 18 の対応で `shorthand.test.ts` のローカルファクトリを `setup.ts` へ移した際、そのファクトリだけが使っていた `DesignDocument` / `DocumentTemplate` / `Props` の import を消し忘れた。`pnpm run typecheck` で TS6192 / TS6133 が出て、その場で直した。

**biome の整形を 2 回当てた**（実装後と、レビュー対応後）。いずれも `--write` で解消。

計画から外れた判断が 2 つあり、どちらも Issue へ追記した。

- **`editFrom` の入口を `Option<string>` にした**（指摘 1）。当初の計画は「787 行をそのまま昇格」だったが、そのままだと規約違反を持ち込むため。関連して、既存 2 本の観点（「空欄にすると未設定へ戻す」）をパネル側のテストへ 1 本足して補った
- **テストの述語を実物からテスト側の述語へ変えた**（指摘 20）。実装の途中で一度実物を渡す形にしたので、書き直しになった

## うまくいったこと

- **追加・書き直した assert を、すべてミューテーションで実測した。** `count` の定数化 / `findArtboard` の分岐を常に真に / `valueFrom` を常に `some` / `isDetachable` を `true` 固定 / パネルが「常に解除できる」判定を渡す、の 5 通りを試し、狙った件数が落ちることを確認した
- **Issue 本文の数え上げを鵜呑みにせず、実際に import を読み直した。** その結果 `services/instance-composition` への依存（本文が数えていなかった 1 件）が見つかり、これが A / B の判断より先に効く制約だと分かった
- **A / B の判断を「片方の規約を守るともう片方を破るか」で決めた。** 案 B が `rules/coding.md`「features 層にドメイン知識を書かない」に当たることを示せたので、好みではなく規約の突き合わせで結論を出せた
- **レビューの指摘のうち採らないものに理由を書いた**（`plan-reviewer` 3 件 / `implementation-reviewer` 1 件）。Issue に残したので、同じ案が再浮上したときに前回の理由が辿れる

## 規約への反映

**無し。** 今回の 24 件は、次の理由でいずれもこの回では介入しない。

- **`layer-dependency`（指摘 20・22）** は指摘 22 が pr-267#14 と同じ形の再発で、介入先（`oxlint` の `no-restricted-imports` / `.claude/hooks/lib/` のスクリプト）も見えている。ただし **#257「依存方向を CI / lint で強制する」が既に立っている**ので、この PR では強制を足さず、拾うべき形を Issue へ書き出すに留めた。層を上げるかどうかの判断は `harness-growth` の管轄
- **`plan-scope-verification`（指摘 10・23）** は pr-227 で観点層へ介入済み。そこからの再発数は集計しないと分からないので `harness-growth` へ回す
- **`plan-implicit-condition`（指摘 7・8）** は 2 件とも「`DocumentSelection` が名前の実在を検証しない」という 1 つの前提から連鎖したもの
- **`comment-false-claim`（指摘 16・19）** は 2 件とも「先例・doc の枠を真似たが中身を実測に照らしていない」形で揃っている
- **`comment-missing`（指摘 6・21）** は 2 件とも「判断を引数へ出したのに Why が付いてこない」形
- **`分類: なし`（指摘 15・17）** は 2 件。語彙への昇格は 10 件溜まってから（`harness-growth` の逃し弁）。それぞれ `rules/naming.md`「名前と実体を一致させる」と `rules/coding.md`「同じ処理が2箇所に現れたら共通化する」に対応する既存ルールはあるが、分類タグの語彙に紐づく先が無い

集計と介入の判断は `harness-growth` の管轄なので、ここでは記録を残すまでとする。
