# PR #634 Box のサイズに最小 / 最大を足す（minWidth / maxWidth / minHeight / maxHeight）

- マージ日: 2026-09-20
- 関連 Issue: #596
- 差分規模: 14 ファイル / +551 -42

## ゴールと結果

ゴール（`hug` が下限より縮まず、`fill` が上限より伸びない）は達成した。Issue が判断を求めて
いた「`fixed` との関係」は**型で作れなくする**側を採り、計画から変えずに実装した。

**この回は先行セッションからの引き継ぎだった。** `issues.assigned` で起動した時点で別セッションが
44 秒前に #596 を claim していたため一度降板し、63 分後に先行セッションが claim コメント直後に
停止・切断していること（`updated_at` 据え置き・ブランチ head 不変・PR 無し）を確認して着手した。

**`visual-regression` は赤いままマージされた。** 提示した 3 つの選択肢はどれも採られていないので、
原因は main に残っている（#645）。

## 指摘

### 1. UI 案にプロパティパネルの最小 / 最大の行が無い

- 分類: `ui`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: UI 案の Size 節は width/height のモードと数値欄だけを描き、min/max の行は 0 件。
  `grep -i` のヒットはすべてモックアップ自身の CSS。計画に「UI 案に無い」という事実も
  確認の記録も無かった。
- 既存ルール: `rules/ui-verification.md`「プロトタイプと違う形にする必要が出たら…確認する」

### 2. ゴールを確かめる手段が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画のテストは宣言の並びまでしか見ておらず、happy-dom はレイアウトしないので
  「実際に縮まない / 伸びない」は 1 件も落ちない。表示確認の手順も計画に入っていなかった。
- 既存ルール: `rules/ui-verification.md`「表示確認」

### 3. 却下理由が引いた前例が逆を指していた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「`minWidth > maxWidth` を検証しない」根拠に opacity の「丸めずそのまま出す」を引いたが、
  それはコンパイル時の話で、docs/03:111 は opacity の範囲外を**バリデーションエラーにする**と
  書いていた。正しい根拠（`range` は 1 prop の値域しか表せない）へ差し替えた。
- 既存ルール: `implementation-flow`「計画を渡す前に踏む 8 つの手順」5

### 4. `fixed` に許す案の、いちばん近い代替が検討されていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「`fixed` に min/max を許し `fixedLength` がクランプ後を返す」案は理由 1 を丸ごと消すので、
  検討して却下した記録が要る。却下理由（`followPropEdits` が書き戻して元の値が失われる）を足した。
- 既存ルール: `implementation-flow`「計画を渡す前に踏む 8 つの手順」6

### 5. 帰属の理由が `Size` 自身の性質になっていなかった

- 分類: `ownership`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「`node-resize` が壊れるから」は消費側の事情で、同じ理由が保存値を読む他のどの prop にも
  成り立ってしまう。「`fixed` の意味は長さがその値で決まっていること」と書き直した。
- 既存ルール: `rules/architecture.md`「ロジックの帰属先」

### 6. `SizeLimits` の公開範囲が未決のままだった

- 分類: `dependency`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画に export するかどうかが書かれていなかった。実装後に `grep` すると
  `size/index.ts` の外から 0 件だったため、型だけ export しコンパニオンは非公開にした。
- 既存ルール: `rules/architecture.md`「`index.ts` から export するのは外部に公開する必要があるものだけ」

### 7. ファイル表が「何行目のどの記述を直すか」まで書いていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: docs/03:187 の artboard の行・`artboard/index.ts` の doc・`size/index.ts` の型 doc と
  `fixedLengthFromProps` の doc が、ファイル名止まりで漏れていた。
- 既存ルール: `implementation-flow`「計画を渡す前に踏む 8 つの手順」7

### 8. 据え置いた判断の後続 Issue が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `minWidth > maxWidth` の検証を据え置くなら後続 Issue の起票とリンクが要る。#632 を起票した。
- 既存ルール: `AGENTS.md`「タスクの分割」

### 9. 「未決の判断」に 3 件が挙がっていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: パネルでの 4 行の見せ方 / `fromProps` の戻り値を `Option` にするか `| undefined` のままにするか /
  `SizeLimits` の公開範囲。いずれも公開 API を作り替えるこの回が決める場所だった。
- 既存ルール: `implementation-flow` フェーズ 3「未決の判断」

### 10. 既存テストの移送で、期待値が変わる件が計画に書かれていなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `hug` / `fill` 枝に `limits` が載ると `toEqual({mode:"hug"})` の期待値が変わる。
  観点そのものは 14 件とも移送できていた。
- 既存ルール: `rules/testing.md`「API を作り替えるときは、既存テストの観点を先に数える」

### 11. テストケースの対が片方しか無かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `fixed` の読み捨てが最小側だけ、軸の対も幅に偏っていた。最大側と高さ側を足した。
- 既存ルール: `rules/testing.md`「assert は「落ちうるか」と「1つの仕様か」で見る」

### 12. 「出さない」を確かめる assert が入力から自明だった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「最大の宣言を出力しない」は入力に `maxWidth` が無いので自明。期待値を並び全体の
  `toEqual` にして、出しすぎでも出さなすぎでも落ちる形にした。
- 既存ルール: `rules/testing.md`「「空になる」を確かめるテストには、同じ入力の中に集まる側を1件置く」

### 13. formatVersion を上げない判断の前提を確かめていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 1.1 の行へ書き足す側を選んだが、`FormatVersion.Current` が `{1,1}` であることと
  1.1 が未配布であることを確かめた記録が無かった。直前の `wrap`（#595）も同じ扱いだった。
- 既存ルール: `implementation-flow`「計画を渡す前に踏む 8 つの手順」3

### 14. `minHeight` の `enabledWhen` を反転してもテストが 1 件も落ちなかった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `notEquals` → `equals` にしても**フルスイート 3654 件で 0 件失敗**。schema 側は
  `minWidth` と `maxHeight` の斜め 2 件、パネル側は幅軸だけだった。縦軸と最大側を足して
  3 件落ちることを再実測した。
- 既存ルール: `rules/testing.md`「その assert は落ちうるか」

### 15. `maxWidth` の `literalType` を変えてもテストが 1 件も落ちなかった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `number` → `string` にしても 0 件失敗。タイトルが「最小 / 最大は」なのに `minWidth`
  1 件しか見ていなかった。4 prop を回す形にして 1 件落ちることを再実測した。
- 既存ルール: `rules/testing.md`「その assert は落ちうるか」

### 16. docs に明記した「エラーにしない」を固定するテストが 0 件だった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 差分の中心の判断の片側（`fixed` に書いた最小 / 最大を読み捨てる）が、CSS 出力側だけ
  守られていて検証側は無防備だった。対照に `unknown-prop` を 1 件置いた並びで固定した。
- 既存ルール: `rules/testing.md`「テストケースの根拠は仕様・設計ドキュメントに求める」

### 17. 同じ判断根拠が 3 箇所に独立して書かれていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「`fixed` はクランプで意味が崩れる」が docs/03・`size/index.ts`・`schema/index.ts` の
  3 箇所にあった。schema 側は docs/03 を引用した直後に同じ内容を書き写していた。
- 既存ルール: `rules/coding.md`「規約の適用範囲」（同じ判断根拠を説明する doc の重複）

### 18. コメントの理由が 4 行あった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `size/index.ts` の型 doc に書いた理由が 4 行。docs/03 への参照 1 行へ畳んだ。
- 既存ルール: `rules/coding.md`「理由は1つにつき1〜2行」

### 19. コメントブロックが、説明していない prop の直前にあった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `widthMode` の直前に置いたが中身は `minWidth` / `maxWidth` の説明。このファイルの流儀は
  「説明する prop の直前」（`layout` / `wrap` がそう）なので、`minWidth` の直前へ移した。
- 既存ルール: `rules/coding.md`「コメントは実装と一致させる」

### 20. `@returns` の「だけ」が成り立っていなかった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「`fill` がフローの外にあり下限・上限も無いときだけ空」と書いたが、`size === undefined`
  でも空になる。元の doc も同じ形だったが、この差分がこの行を書き換えていた。
- 既存ルール: `rules/coding.md`「`@returns` は…どんなときにそうなるか」

### 21. 理由の違う prop を 1 つのリストへ混ぜ、引いた先例も誤っていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `artboard/index.ts` は「1 つの理由 = 1 つのリスト」で構成されているのに最小 / 最大を
  `ArtboardFixedSizeProps` へ混ぜた。さらに先例に挙げた `rotation` は「書けても効かない」ではなく
  「効いてしまうとずれる」で理由が違った。専用リストへ分け、先例を `visibility` だけに直した。
- 既存ルール: `rules/coding.md`「コメントは実装と一致させる」

### 22. UI 案に無い行を足したのに、その注記が差分に無かった

- 分類: `ui`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 同じファイルの `wrap` には「UI 案はこの行を描いていない」という注記の先例があるのに、
  4 prop には無かった。`schema/index.ts` へ同じ形で残した。
- 既存ルール: `rules/ui-verification.md`「UIの拠り所は `docs/Design Composer.html`」

### 23. docs に無い値域を、テストが仕様として固定していた

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「取りうる範囲を宣言しない」をテストだけが持っていた。`rotation` が docs/03:124 に
  同じ行を置く先例があるので docs/03 側に 1 行足した。負の値を許すこともここで決まった。
- 既存ルール: `rules/testing.md`「仕様にない振る舞いをテストで固定しない」

### 24. `Option` を畳む同型のコードが同一ファイルに 2 箇所あった

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `Option.isSome(x) ? [f(x.value)] : []` が `size/index.ts` に 2 箇所。同じ形は
  `prop-control` と `document-selection` にもあり、この差分だけで直す話ではないので**受け入れなかった**。
- 既存ルール: なし

### 25. 撮影範囲の検査で CI が落ちた（表示確認のビューポートが CI と違った）

- 分類: `ui`
- 出どころ: `CI`
- 内容: 表示確認を 1280x900 で行い「撮影範囲では落ちない」と PR 本文に書いたが、CI の撮影は
  1280x720。PropertyPanel の 6 本が 786〜840 と判定され `visual-regression` が赤くなった。
  **push 前検査の一覧に `pnpm visual:capture` が無い**のが根本で、手元で走らせていれば push 前に分かった。
- 既存ルール: `rules/ui-verification.md`「ビューポートはデスクトップ(1280x720 以上)を基準とする」

### 26. 撮影範囲の検査が、クリップ済みの中身を数えている

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: プローブの doc は「クリップされた中身は数に入らない」と書くが、
  `documentElement.scrollHeight` ではその前提が崩れている（`body.scrollHeight` は 3 本とも 544px で同じ）。
  器を与えても外れず、`overflow-hidden` を足してビルドし直しても 1px も変わらなかった。#645 を起票。
- 既存ルール: `rules/ui-verification.md`「直すと、そのストーリーはこの検査の対象から外れる」

### 27. PR 本文に事実と違う主張を 2 つ書いた

- 分類: `comment`
- 出どころ: `自己修正`
- 内容: 「撮影範囲では落ちない」「視覚差分は意図した変更なので `visual-approved` を付けます」。
  どちらも CI が赤くなって誤りと分かり、本文を訂正した。書いた時点で確かめていなかった。
- 既存ルール: `.claude/skills/claim-verification`（Issue/PR 本文の事実の主張）

### 28. 並行する別セッションが同じ Issue を claim していた

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: 起動時点で別セッションが 44 秒前に #596 を claim。#620 → #622 / #623、#624 → #625 に続き
  **3 回目**。ただし前 2 回と違い、降板を先に判断したので**実装の重複は発生していない**
  （差分を 1 行も作る前に降りた）。63 分後に先行セッションの停止を確認して引き継いだ。
- 既存ルール: なし

## 手戻り

- **降板 → 引き継ぎ**: 並行着手を検出して一度降り、先行セッションの停止（`updated_at` 据え置き・
  `connection_status` が `disconnected`・ブランチ head 不変・PR 無し）を確認してから着手した。
  この間に作った差分は無い。
- **計画から外れた 2 点**: `SizeLimits` を型だけ export に変更（計画では両方 export）／
  artboard の 4 prop を `ArtboardFixedSizeLimitProps` へ分離（計画では既存リストへ追加）。
  どちらも `implementation-reviewer` の指摘を受けての変更で、Issue #596 に記録済み。
- **PR 本文の訂正**: 撮影範囲の記述と `visual-approved` の記述を、CI が赤くなった後に書き直した。

## 発火

- 発火: 計測対象外(フック不発)

カナリア（`echo hook-canary`）が deny されず、`plan-reviewer` を通した後も検証エージェントの
マーカーが無く、このセッションの発火ログの見出しも無い枝。代替不能な 2 フック
（`block-npx.sh` / `block-git-during-verification-agent.sh`）は手で肩代わり確認した。

## 規約への反映

無し。このスキルの管轄は記録までで、集計と介入は `harness-growth` が行う。
