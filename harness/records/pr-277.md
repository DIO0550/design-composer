# PR #277 [architecture] features/sidebar を切り出す（左ペイン）

- マージ日: 2026-08-22
- 関連 Issue: #253
- 差分規模: 56 ファイル / +924 -441

## ゴールと結果

ゴールは「左ペイン（レール + パネル + Layers / Assets / Tokens の 3 つの行き先）が `features/sidebar/` として独立し、`EditorState` を受け取らずに描ける」。達成した。#174 の feature 分割の 3 つ目（#250 tokens・#251 assets に続く束ね役）。

移したのは 5 モジュール（`left-pane` / `left-pane-rail` / `left-pane-panel` / `artboard-list` / `document-tree`）、`src/domains/` へ昇格したのは 2 つ（`selection-state` / `selection`）、新設したドメインが 1 つ（`document-selection`。ドキュメントと選択中のものの対）。テスト用の行の読み方（`row-names`）は `src/components/__tests__/` へ移した。`sidebar → editor` の import は 0 件。

human レビューは 0 件（reviews / review threads とも 0）。CI は 6 チェックすべて 1 回目で success、視覚差分も 0 changed（移した story は New 17 / Deleted 17 として扱われ、比較はされていない）。**指摘はすべてサブエージェント（`plan-reviewer` 11 件 / `implementation-reviewer` 11 件）から出た。**

テストは 349 ファイル / 2357 テスト。

**この回の中心は、名前が 2 人のレビュアーで割れたこと（指摘 1・22）。** 対の型に `NodeSelection` と付けたところ `plan-reviewer` が「選択には artboard も入るので実体と合わない」と指摘し、改名した `DocumentSelection` を今度は `implementation-reviewer` が「`TokenSelection` と接頭辞の規則が逆（あちらは選ばれるもの、こちらは入れ物）」と指摘した。**選ばれる対象を 1 語で言えない**（artboard + ノード）ことが根にあり、どちらの名前も一長一短のまま `DocumentSelection` で確定させ、両方の却下理由を Issue に残した。

## 指摘

### 1. 対の型名が、選択に入るものの実体とずれていた

- 分類: `naming-prefix-collision`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画は `NodeSelection`（`DesignDocument` と `SelectionState` の対）と名付けていたが、`EditorState` の `selectableName` は「artboard か配下のノード」を選択可能としており、`currentArtboard` / `singleName` は artboard を選んでいる状態でも意味を持つ。`Node` という接頭辞が指す対象と、型が実際に持つ対象が食い違っていた
- 既存ルール: rules/naming.md「名前と実体を一致させる」
- 次にどう防ぐか: 今回は記録のみ（`DocumentSelection` へ改名し、doc に「選ばれうるのは artboard とその配下のノード」を明記した）

### 2. feature 名がリポジトリの語彙に無く、別の意味で既に使われていた

- 分類: `naming-absent-metaphor`
- 出どころ: レビュー（`plan-reviewer` / `implementation-reviewer` の両方）
- 内容: `sidebar` は `docs/Design Composer.html` にも `docs/06-ui.md` にも `grep -i` で 0 件。UI 案・仕様書・実装・読み上げ名はすべて「左ペイン」「`LeftPane*`」で揃っている。さらに `services/document-html` のテストでは**ユーザーのドキュメント側のレイアウト領域**の名前として `sidebar` が使われていた。計画にはこの数え上げが書かれていなかった
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ（フォルダ名は Issue #253 と #174 の決定なので据え置き、シンボルは `LeftPane*` のままにして「フォルダ名 = Issue が決めた器の名前 / シンボル = UI 案の語彙」という線引きを Issue に残した。**新しく作る feature 名も、識別子と同じく数えてから計画に書く**）

### 3. 移設するテストの書き換え量を「props の書き換え」と見積もっていた

- 分類: `plan-scope-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画は移設する 8 テストファイルを「観点はそのまま、props の組み立てだけ書き換え」と書いていたが、実際は 6 ファイルが `EditorState.create` / `EditorState.select` でセットアップしており、`DocumentSelection` を直接組み立てる形への**セットアップの作り直し**が要った。1 ファイルは doc コメントで `EditorState.currentArtboard` にも言及していた
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「一括置換・複数箇所の書き換えを伴う計画は、立てた収集条件を実際にコードへ当てて件数を検算する」
- 次にどう防ぐか: 今回は記録のみ（計画を「セットアップから作り直す」に書き換えた）

### 4. ゴール（依存の辺が 0 であること）を確かめる手段が計画に無かった

- 分類: `plan-scope-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `sidebar → editor` の辺を作らないことがゴールなのに、`.oxlintrc.json` は `curly` と `max-depth` の 2 つだけ、`biome.json` に import 制限は無く、tsconfig にも path 制限が無い。**辺ができても typecheck も lint もテストも全部緑になる**ため、ゴールの達成を確かめる手段が存在しなかった
- 既存ルール: なし（依存方向の規約はあるが、それを検算する手順は無い）
- 次にどう防ぐか: 今回は記録のみ（完了条件として `rg -n "features/editor" src/features/sidebar` が 0 件であることを計画へ足し、実装後に実行した。指摘 13 のとおり、この検算をしていなければ deep import が残っていた）

### 5. 新規テストの観点が既存テストと重複していた

- 分類: `plan`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 新設ドメインのテストとして挙げた 6 観点のうち 4 つ（選択に含まれる / 含まれない / 単一なら読める / 複数なら読めない）は `selection-state.normal.test.ts` / `.edge.test.ts` が既に固定しており、委譲していることを確かめるだけの再掲になっていた。残り 2 つ（出どころの部品）は `editor-state` 側にある観点と重なっていた
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する（実装・テストとも）」
- 次にどう防ぐか: 今回は記録のみ（再掲 4 件を落とし、`sourceName` は純粋な導出の観点だけを `editor-state` 側から**移送**する形へ変えた）

### 6. 新しく作る対から誰でも不正な状態を組み立てられる点が、計画で未決だった

- 分類: `illegal-state`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `EditorState` は「選択に入っている名前は必ずドキュメントに在る」を `select` / `reveal` / 履歴の取り込みで守っているが、`DocumentSelection` を `src/domains/` に置いて誰でも組み立てられるようにすると、ドキュメントに無い名前を含む対が作れる。テストとストーリーがまさに直接組み立てる側になる
- 既存ルール: rules/coding.md「生成時に検証し、不正な値を存在させない」
- 次にどう防ぐか: 今回は記録のみ（**検証しない**と決め、理由（弾くと編集画面の選択と対の答えが食い違い、不変条件が二重管理になる）を型の doc に書き、「在らない名前は引き直しで無視される」ことをテスト 2 件で固定した）

### 7. 対へ何を移すかの基準が計画に書かれていなかった

- 分類: `ownership-reasoning`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画が挙げた理由は「props が 11 個から 8 個になる」で、これは呼び出し側の都合であって帰属の理由ではなかった。実際、同じ「ドキュメント + 選択」からの導出である `singleSelection` は `EditorState` に残す計画になっており、基準では説明が付かなかった
- 既存ルール: rules/architecture.md「ロジックの帰属先」
- 次にどう防ぐか: 今回は記録のみ（「2 つが揃って初めて答えが決まるものは対、選択だけで決まるものは `SelectionState`、状態が遷移するものは `EditorState`」という基準を書き、それに従って `singleSelection` も対へ移した）

### 8. ストーリー 4 ファイルが変更するファイルの表に挙がっていなかった

- 分類: `plan-scope-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 移す 5 モジュールのうち 4 つが `index.stories.tsx` を持ち、`title` と import 元、`NodeActions` / `TokenActions` のサンプル組み立ての書き換えが要ったが、計画のファイル表に 1 つも挙がっていなかった（テストは数えていた）
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「公開 API を変える場合は、それを呼んでいる `*.stories.tsx` と `__tests__/` も変更するファイルとして数える」
- 次にどう防ぐか: 今回は記録のみ（表に足した。**規約に明記があるのに数え漏らした**形）

### 9. doc コメントの中でだけ名指しされている参照が、ファイル表に無かった

- 分類: `plan-comment-reference`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `src/domains/token-selection/index.ts` の「`features/editor` の `Selection` … あちらはドキュメントを持たない」、`src/features/assets/index.ts` の「features/editor 側の consumer が居ない」、`row-names.ts` 自身の doc がいずれも移動で嘘になるが、コードの参照だけを拾ったファイル表には挙がっていなかった
- 既存ルール: `.claude/agents/plan-reviewer.md`「ファイル表に挙がっているか」
- 次にどう防ぐか: 今回は記録のみ（3 つとも表に足して直した。pr-261#3・pr-269#3 と同じ形の再発）

### 10. 計画に書いた件数の検算が 2 箇所ずれていた

- 分類: `plan-scope-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `row-names` の import 元を「13 テストファイル + コメント 1 = 15 件」と書いたが 13 + 1 = 14 で、実際は 14 + 1 = 15。`LeftPaneViews` / `LeftPaneViewLabels` を import している `opened-document-editor` のテストを「5 ファイル」と書いたが実際は 3 ファイルで、通しテストの一覧（5 本）と取り違えていた
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「出てきた件数と、計画が前提にしていた件数が一致するかを確認する」
- 次にどう防ぐか: 今回は記録のみ（**grep は実行していたのに、書き写すときに足し算と対象を間違えた**。数えた出力をそのまま貼る形にすれば防げる）

### 11. props が 8 個になる設計で、Composition を採らない理由が書かれていなかった

- 分類: `composition`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画は「個別 props なら 11 個、対にまとめれば 8 個」という比較で止まっており、規約の目安（概ね 5 個）を超えたまま Composition を検討した記録が無かった
- 既存ルール: rules/components.md「Composition パターン」
- 次にどう防ぐか: 今回は記録のみ（`LeftPaneContent` の `switch` が行き先の網羅を戻り値型で強制しており、children 化すると強制の持ち主が呼び出し側へ移って消える、という理由を書いて据え置いた）

### 12. VRT が移動後のストーリーを比較しないことに、計画時点で気づいていなかった

- 分類: `plan`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `.github/scripts/storybook-visual-regression.mjs` は新規・削除を退行に数えないため、story の `title` を `features/sidebar/*` へ変えると移した 4 つは baseline と比較されない（赤くならないが、混入した見た目の変化も出ない）。計画には表示確認の手順そのものが無かった
- 既存ルール: rules/ui-verification.md「表示確認」「Storybook の視覚差分(VRT)」
- 次にどう防ぐか: 今回は記録のみ（Storybook のスクリーンショットで初期表示 6 枚と操作 2 通りを確認し、取得コマンドと結果を PR 本文へ残す形に計画を直した。実測でも Changed 0 / New 17 / Deleted 17 となり、比較されないことが確認できた）

### 13. モジュールの移動をパス置換で行い、feature 外部からの deep import が入り込んだ

- 分類: `module-api`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `opened-document-editor` のテスト 3 本が `@/features/editor/components/left-pane-rail` を import していたのを機械置換した結果、`@/features/sidebar/components/left-pane-rail`（**feature 外部から内部ファイルへの deep import**）になった。移動前は feature 内なので正当だった。公開 API から同じものが出ているので `@/features/sidebar` にするだけで直る
- 既存ルール: rules/architecture.md「モジュールの公開API」（フォルダ外部からの import は必ず `index.ts` 経由）
- 次にどう防ぐか: 今回は記録のみ（**typecheck も lint も落ちない**形。指摘 4 で足した `rg` の検算は「`features/editor` を import していないか」だけを見ており、逆向き（editor から sidebar の内部へ）は見ていなかった）

### 14. テスト用の対の組み立てが、同じ本体で 6 箇所に並んでいた

- 分類: `duplication-test`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `DocumentSelection.create(document, SelectionState.create(names))` を包む `setupSelecting` を、ドメインのテスト 3 ファイルと sidebar のテスト 2 ファイル、ストーリー用サンプル 1 ファイルの計 6 箇所に同じ本体で書いていた。`.claude/hooks/lib/duplicate-test-helpers.py` は同じフォルダ内の 3 件を検出する（フォルダを跨いだ 3 件は検出しない）
- 既存ルール: rules/testing.md「テスト用ヘルパーの置き場所」（汎用の操作なら実装側に足りていない機能）
- 次にどう防ぐか: 今回は記録のみ（`DocumentSelection.fromNames(document, names)` を対のコンパニオンへ置いて 6 箇所とも解消した。**フックが発火していれば編集時に出たはずの指摘**で、指摘 23 の環境と重なって出なかった）

### 15. 新設した導出の分岐を守るテストが 1 件も無かった

- 分類: `test-coverage-branch`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `DocumentSelection.sourceName` の `refs.length === names.length`（インスタンスでないものが混ざれば `none`）を消しても、新しい `document-selection.source-name.test.ts` の 5 件はすべて通った。入力に「インスタンス + 非インスタンスの混在」が無く、「別々の ref 2 件」は `distinct` 側で弾かれてこの枝に届かないため。doc には明記していた規則だった
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ（「インスタンスでないものが混ざっているときは元の部品が無い」を追加し、ミューテーションでこの 1 件だけが落ちることを実測した）

### 16. 値を 3 つに割った副作用で、関数の引数が 4 つになった

- 分類: `signature`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `leftPaneFooter` は移動前が `(view, state, node)` の 3 引数で、`state` を `selection` / `isFrozen` に割った結果 4 引数になった。規約の上限を超えている
- 既存ルール: rules/coding.md「関数のシグネチャ」（引数は最大 3 つまで）
- 次にどう防ぐか: 今回は記録のみ（オブジェクト引数にした。**引数を減らす変更ではなく増える変更だと気づきにくい**形で、`state` を割る計画の時点では数えていなかった）

### 17. 実装を対へ移したのに、Why / Why not の doc を元の場所にも丸ごと残した

- 分類: `comment-stale-edit`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `sourceName` / `currentArtboard` / `singleSelection` の 3 つで、規則の理由・Why not・「状態として持たない理由」がほぼ逐語で `EditorState` と `DocumentSelection` の両方に残っていた。規則の持ち主は対にしたので、片方だけ直したときに気づけない
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ（`EditorState` 側を 1 行の概要 + 「規則は `DocumentSelection` が持つ」に落とした）

### 18. 新しく書いた doc の列挙が、実装と一致していなかった

- 分類: `comment-stale-edit`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `EditorState.documentSelection` の doc に「選択から決まる読み（今見ている artboard・選んでいる 1 つの正体・出どころの部品）は**すべて**この対が持ち」と書いたが、`isSelected` も委譲しているのに列挙に無く、逆に `singleName` は対を通さず `SelectionState` を直に読んでいた
- 既存ルール: rules/coding.md「挙動を曖昧にまとめず対象を列挙する」
- 次にどう防ぐか: 今回は記録のみ（`singleName` も対を通す形に揃えて「選択から決まる読みはすべて対が持つ」を成立させ、列挙を実装に合わせた）

### 19. 移動で嘘になったコメント内のパス参照を取りこぼした

- 分類: `comment-stale-edit`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `artboard-frame-list` の「名前が `ArtboardList` でないのは、左ペインの一覧（`components/artboard-list`）と綴りがぶつかるため」と、`row-names.ts` 自身の doc（消費側のモジュール名の列挙）が、移動後の場所を指していなかった。指摘 9 で 3 件は直したが、この 2 件は表に入っていなかった
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ（**消したパス・移したパスをコメント本文にも grep する**。指摘 9 と同じ根で、拾い方が「移動するモジュール名」だけだった）

### 20. 委譲だけになったメソッドが、本番の呼び出し側を失ったまま残っていた

- 分類: `module-api`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `EditorState.currentArtboard` は唯一の消費側（`DocumentTree`）が対を直接呼ぶようになったため、`src` 全体で呼ぶのはテスト 2 箇所だけになっていた。長い Why doc も、呼び出し側の無いメソッドに載っていた
- 既存ルール: rules/architecture.md「モジュールの公開API」（export するのは外部に公開する必要があるものだけ）
- 次にどう防ぐか: 今回は記録のみ（落として、テストは対を直接呼ぶ形にした）

### 21. 消費側が feature の内部だけの型を、公開 API から export していた

- 分類: `module-api`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `features/sidebar/index.ts` が `LeftPaneNodeActions` / `LeftPaneTokenActions` を export していたが、使っているのは feature の内部（`left-pane` とその stories）だけ。呼び出し側は構造的部分型で渡していて型名を書かない
- 既存ルール: rules/architecture.md「モジュールの公開API」
- 次にどう防ぐか: 今回は記録のみ（export を落とした）

### 22. 改名した対の型名が、隣に並ぶ同種の型と接頭辞の規則が逆だった

- 分類: `naming-prefix-collision`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `src/domains/` に並ぶ 2 つの対のうち、`TokenSelection` は接頭辞が**選ばれているもの**（トークン）を指すのに対し、`DocumentSelection` は**選択を引く入れ物**を指しており、規則が逆になっていた。隣で読むと「ドキュメントが選ばれている」と読める
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ（**指摘 1 と真逆の方向の指摘**。選ばれる対象を 1 語で言えない（artboard + ノード）ことが根で、`NodeSelection` は指摘 1 で、`CanvasSelection` は既存の `editor-state.canvas-selection.test.ts` が別の意味で使っていることで却下済み。`DocumentSelection` で確定させ、両方の却下理由を Issue に残した）

### 23. `.claude/hooks/` が発火しない環境で作業した

- 分類: `hook-environment`
- 出どころ: フック（カナリア）
- 内容: push 前のカナリア `echo hook-canary` が deny されずに通った。PreToolUse / PostToolUse はこのセッションで 1 度も発火しておらず、`post-edit-lint` / `block-lint-suppress` / `check-test-helper-duplication` も動いていない
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ（CI と同じ検査 6 つを手で 1 つずつ実行した。**実害として指摘 14（テストヘルパーの重複）が編集時に出ず、レビューまで残った** — `check-test-helper-duplication` は同じフォルダ内の 3 件を検出できる指摘だった）

## 手戻り

- テストの一括置換（`EditorState` → `DocumentSelection`）を python で行った際、import 行の形が想定と違って 2 回停止し、1 ファイルは置換を取りこぼして typecheck で気づいた
- 未使用になった import（`ArrayEx` / `Artboard`）で typecheck が 2 回落ちた（いずれもその場で解消）
- `LeftPane` の「ノードを選択中」ストーリーが artboard（`home`）を選んでいて行の選択が出ていないことに、Storybook のスクリーンショット確認で気づいた（移動前からの状態）。ストーリーの doc が言っていることと表示が合うよう、ノード（`home-title`）を選ぶ形に直した

## うまくいったこと

- **#250（tokens）の 2 コミット構成をそのまま踏襲した**（対を置く → 切り出す）。前例のコミットメッセージと PR 本文を読んでから計画を書いたので、却下案の粒度と PR の書き方が揃った
- `plan-reviewer` の指摘 4 で足した「`rg -n "features/editor" src/features/sidebar` が 0 件」の検算が、ゴールの達成を確かめる唯一の手段になった（lint も typecheck も見ない）
- `implementation-reviewer` がミューテーションで実測したことで、「移設したテストで落ちなくなった assert は 0 件」を確認でき、かつ守られていない枝（指摘 15）が 1 件見つかった

## 規約への反映

**無し。** このスキルの管轄は記録を 1 ファイル残すところまでで、集計と介入の判断は `harness-growth` が行う（記録の時点で `対策済` を書くと、`count.sh` の再発数がここで 0 に戻ってしまう）。

この回に出た分類のうち `plan-scope-verification`（3 件）・`comment-stale-edit`（3 件）・`module-api`（3 件）・`naming-prefix-collision`（2 件）は同じ回で複数出ているので、次の棚卸しで「最後の介入以降の再発数」を数える材料になる。
