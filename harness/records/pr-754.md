# PR #754 services・features・hooks のコンパニオンオブジェクトのメソッドの doc の抜けを埋める

- マージ日: 2026-09-24
- 関連 Issue: #738(親 #720 の層 4)
- 差分規模: 17 ファイル / +487 -73

## ゴールと結果

`missing-doc-comments.py --all <範囲> --include-methods` が `src/services` / `src/features` / `src/hooks` で 0 件になる、は達成
(着手時 87 件 / 17 ファイル。Issue 本文の概算 77 件は `--all` の 10 件打ち切りを含まない数だった)。
引数ごとの `@param <名前>` の突き合わせも 0 件。実装と食い違っていた既存の概要文 5 件を直し、後続 Issue #752 / #753 を立てた。
CI は lint / typecheck / test・規約の検査・Issue の検査が通過。`visual-regression` はマージの時点でまだ実行中だった。
人のレビュー指摘は 0 件(`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも確認。コメントは自分のテスト報告と bot だけ)。

## 指摘

### 1. `setTokenValue` の `none` の条件が抜け、既存の概要文の誤りも見落としていた

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 値の種別で差し替える相手が決まるので、種別が違う値では `replaceToken` が失敗するか別種別が書き換わる。
  「選択していないトークンを編集する状態を作れない」は誤り。doc を直し、型の穴は #753 へ切り出した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 2. `KeyShortcut.matches` の「修飾キーの有無がすべて一致」が誤り(Alt は見ない)

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 見るのはコマンドキー(⌘/Ctrl)と Shift だけ。既存の概要文も同じ誤りで、両方直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 3. `NodeDrag.consumesClick` の `@returns` が不正確

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `true` なのは既存ノードを運んで離した直後だけ(雛形・動かさずに離したときは `false`)。型の doc を参照する形にした。
- 既存ルール: rules/coding.md「doc に書く項目」

### 4. `DocumentSession.isClosed` の既存の概要文の誤りを引き継いでいた

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「開く操作も一度も始まっていない」は誤り(全部閉じれば `true` に戻る)。計画の `@returns` も同じ誤りだった。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 5. `select` / `insertPosition` の条件の書き方が割れ、概要文の抜け(複数選択)を見落としていた

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画の中で `select` の条件が 2 通り。`insertPosition` の概要文は複数選択の `none` が抜けていた。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 6. 届かない失敗の扱いが揃っていなかった(`pasteNode` / `insertNode` / `removeSelected`)

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `DesignDocument` 側の失敗を参照する方針なのに、この 3 つは黙って落としていた。「〜の失敗は起こらない」に揃えた。
- 既存ルール: なし

### 7. 概要文と `@returns` の二重化を 2 件しか片付けていない

- 分類: `duplication`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 20 件近いメソッドで概要文が既に `none` の条件を言っていた。`reorderNode` などの概要文は `DesignDocument` 側の
  err 条件を展開したままで、却下案(展開しない)と食い違っていた。条件は `@returns` にだけ書く方針にした。pr-741 の 16・17 と同じ形の再発。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」(doc の重複)

### 8. 型 doc・定数 doc・委譲先の `@returns` の言い直し

- 分類: `duplication`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `TokenCss.variables`(型 `CssVariables` の doc と同じ)/ `TokenTemplate.baseName`(3 箇所同じ)/ `toToken` と
  `DropZone.create` の概要文と `@param` / `DocumentSession.failure` の「同上」/ `compile` が非公開関数の `@returns` を写す。
  pr-730 の 11・pr-741 の 14・15 と同じ形の再発。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 9. 却下案の理由が事実と違った(検出器は `@doc-comments-ok` を見ない)

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「マーカーでゴールを満たすことになる」と書いたが、マーカーを見るのは push 側の検査だけで、検出器は見ない。
  #720 の目的の言い方も README の別の節と混ざっていた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「却下案に書いた理由が事実を主張しているなら」

### 10. 後続 Issue に入れる規約違反が既知の 1 件だけだった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `CanvasBounds.ofElement` は `of*` の名前だけでなく domains の中で DOM を実測している(`domains/` での I/O)。
  `setTokenValue` の型の穴も漏れていた。#752 / #753 を立てた。pr-741 の 2 と同じ形の再発。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3

### 11. 引数ごとの突き合わせスクリプトが誤検出する

- 分類: `harness`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 検出器の `parameter_names` を呼ぶ使い捨てスクリプトで、`editor-context-menu` の `operationHandlers` が名前 `)` として出る
  (戻り値の型の括弧を拾う)。`closing_paren` で切ってから渡して避けた。検出器本体の直しは #720 の範囲。
- 既存ルール: rules/coding.md「外部の挙動は動かして確かめる」

### 12. 「未決の判断: 無し」に判断が隠れ、doc 無しの 12 件の中身が決まっていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 概要と `@returns` のどちらに条件を置くか・届かない失敗・誤検出の扱いが未決のまま。doc 無しの分は概要しか決めていなかった。
  pr-741 の 3・7 と同じ形の再発。
- 既存ルール: `.claude/agents/plan-reviewer.md`

### 13. 分けない理由が判断軸と逆向き

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 独立してマージできる単位を認めたうえで分けていない。「判断が残らないは起きない」も言い過ぎ。
  規約の文面から外れることを明示し、前例(#736)を理由に書き直した。
- 既存ルール: AGENTS.md「タスクの分割」

### 14. 直した `isClosed` の doc がまだ実装と合わない

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 概要「開く操作も動いていない」は `failed` でも成り立つように読め、「すべて閉じた後も `true`」は直近が失敗なら成り立たない。
  概要を直し括弧書きを消した。テスト名が旧い概要文の語のままなのは doc だけの Issue の外として残した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 15. 届かない失敗を条件として書いた(`addToken`)/ 書き漏れ(`removeToken`)

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 計画の方針(「〜の失敗は起こらない」)を 2 箇所で守っていなかった。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 16. `clearSelection` の doc の中で概要と `@returns` が矛盾

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 概要は「トークンの選択は残す」、`@returns` は「何も選んでいない状態」だった。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 17. 境目・フィールドの一部・数の意味の不正確さ(`moveTo` / `grab` / `selectAllInstances` / `matches`)

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `moveTo` は `<` なので閾値ちょうどで動き出す。`grab` の `@param` がフィールドを一部だけ並べ `grabbedAt` が抜け。
  `count` は `collectInstanceNames` の件数。`matches` の「並びにある」は大小を無視した比較。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 18. 型 doc・既存 doc の写しと二重化 7 件

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `TokenCss` の「`--` で始まる」(型の再掲)/「書体は項目ごとに変数」(型 doc と同じ)、`baseName` の概要、
  `DropZone.create`、`EditorState.create`、`TextEdit.toPropEdit` の括弧、`DocumentSession.close`。指摘 8 と同じ形が計画の検証の後にも残った。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 19. フックが発火しない環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されず、検証エージェントのマーカーもこのセッションの発火ログの見出しも無かった(README の表で「本当に不発」)。
  起動時の作業ディレクトリがリポジトリの親(`/home/user`)で、層 1〜3 と同じ構成。`npx` / `git add -A` の実行と、検証エージェント実行中の git 操作が無いことを履歴で確認した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- **計画を plan-reviewer の指摘 8 件(細目 20 余り)で直した**(指摘 1〜13)。うち `@returns` の二重化・型 doc の言い直し・
  後続 Issue の漏れ・未決の判断の隠れは pr-730 / pr-741 と同じ形の再発
- **実装を implementation-reviewer の指摘で直した**(指摘 14〜18)。二重化は計画の検証で方針を決めた後にも 7 件残った
- Issue 本文の概算(77 件)が `--all` の打ち切りを含まない数で、着手時に 87 件へ数え直した
- 計画のコメントを投稿する際、本文の代わりに `PLACEHOLDER` を一度投稿し、直後に更新した(Issue の履歴に編集が残る)
- 3 つ目のコミットの本文の件数(36 → 35)を push 前に amend で直した
- push 前の検査(typecheck / lint / biome / test 3739 件 / ハーネスの検査 17 本 / Storybook の撮影範囲 179 枚)は 1 回目で通った。CI の失敗は無し

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。この回は Issue #738 の実装で、記録を数えた結果の介入ではない。`対策済` を書けるのは
`harness-growth` の棚卸しだけ。
