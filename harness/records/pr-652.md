# PR #652 既存の判定表 3 本をゲートへ配線する

- マージ日: 2026-09-20
- 関連 Issue: #643
- 差分規模: 7 ファイル / +98 -26

## ゴールと結果

判定表 3 本それぞれについてゲートへ配線するかを決め、配線すると決めたものが実際に
ゲートで走る状態にする。達成した(`canary-cases.sh` / `result-option-read-cases.sh` は
層 1・層 2 の両方へ、20.3 秒の `check-pr-closing-issue-cases.sh` は層 1 だけへ)。
判定表のケースは 1 行も変えていない。

## 指摘

### 1. 「判定表が落ちたら pre-push も落ちる」ケースが、足したステップを何も確かめていない

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画のミューテーション(免除の判定を壊す)は `src` の走査が先に 19 件を報告して
  既存ステップで `set -e` が効くため、判定表のステップへ到達しない。配線を足さなくても
  同じ結果になる。`src` を緑に保ったまま判定表だけが落ちる形へ差し替えた。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 2. カナリアの判定表に落ちる向きのケースが無い

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: pre-push へ足すのは 2 本なのに、失敗方向を確かめるケースが判別子側にしか無かった。
  `hook-canary.sh` を壊して判定表が NG → pre-push が exit 1 になることを 1 件足した。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 3. ファイル表から `harness/githooks/README.md` の「飛ばす」の段落が漏れていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「型 / lint / format だけを飛ばす」と書いてある段落は、jq の枝を足した時点で
  事実でなくなる。同じ節の乖離は pr-627 でも指摘されている。
- 既存ルール: なし

### 4. ファイル表から `canary-cases.sh` の doc が漏れていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: doc が「手で走らせるこの表は落ちれば分かる」と書いており、ゲートに載せた時点で
  前提が変わる。ケース(表の中身)には触れないので、スコープ外にもならない。
- 既存ルール: なし

### 5. pre-push へ足す 2 行の個別コメントが計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 既存の pre-push は `.claude/hooks/` 側に対応物を持たない検査すべてに個別コメントを
  付けている。pr-627 の指摘も「足した検査にその開示コメントが無かった」という同じ形。
- 既存ルール: なし

### 6. `.claude/hooks/README.md` のどの段落を触るかが行レベルで書かれていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 117 行は「CI へ上げた」もの、126-128 行は「新設時から層 1 と層 2 に置いた」もので
  主語が違う。混ぜると pr-627 の指摘(主語が割れる)の再演になる。
- 既存ルール: なし

### 7. 同じ判断根拠が 4 箇所に散る計画になっていた

- 分類: `duplication`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 配線の理由を `frontend.yml` / 両 README / pre-push へそれぞれ書くと、片方だけを
  直せる状態ができる。正典を `.claude/hooks/README.md`「カバー範囲と残る穴」に決めた。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 8. 時間の閾値が却下側にしか当てられていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「1 秒未満の側だけ」という線の外側に、採用する 2 本(1.08 / 1.11 秒)自身がある。
  引用した 0.3 秒も repo 内で裏が取れなかった。pre-push 全体の実測を基準へ置き換えた。
- 既存ルール: なし

### 9. 「未決の判断: 無し」の裏に 4 件残っていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: CI 側の jq を無条件に扱う非対称 / pre-push の出力量 / 2 行を入れる位置 /
  `rules-check` を選ぶ理由。いずれも決めずに実装へ入るところだった。
- 既存ルール: なし

### 10. 据え置いた案の後続が記録されていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 却下案「再試行の待ち時間を縮める」を「スコープ外」で終えていた。後続 Issue を
  立てるか立てない理由を書くかのどちらかが要る(立てない理由を Issue へ書いた)。
- 既存ルール: なし

### 11. 書き換える件数が数字として計画に書かれていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: コマンド一覧 12 → 14 行、「下の 8 つ」→ 10、`rules-check` は 9 ステップだが
  git hooks と両方にあるのは 8。この 9 と 8 のずれが既存の文の構造を壊すことも未検算だった。
- 既存ルール: なし

### 12. 計画に書いた pre-push の実測が、node_modules の無い環境の数字だった

- 分類: `plan`
- 出どころ: `自己修正`
- 内容: 20.3 秒として書いたが、その測定では型 / lint / format が丸ごと飛ばされていた。
  `pnpm install` 後に測り直すと 35.9 秒で、「ほぼ倍」は「1.5 倍超」が正しい。
- 既存ルール: rules/coding.md「外部の挙動は動かして確かめる」

### 13. コメントに書いた理由が実測と食い違っていた(免除を壊すと src 側が先に落ちる)

- 分類: `comment`
- 出どころ: `自己修正`
- 内容: 「免除が変わっても src が緑なら気づけない」と 3 箇所へ書いたが、免除を壊すと
  `src` の走査が 19 件を報告して直前のステップが先に赤くなる。実際に判定表でしか
  捕まらないのは取りこぼす向きの退行だけ。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 14. jq が欠けたとき NG になるケースの列挙が不足していた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「引用符を含む deny のケースが pass になる」と書いたが、NG 6 件のうち 1 件は
  引用符を含まない(改行で連ねたケース)。同じ事実を `canary-cases.sh` は 2 つ列挙している。
- 既存ルール: rules/coding.md「挙動を曖昧にまとめず対象を列挙する」

### 15. ガードは python3 かつ jq なのに、説明が jq しか書いていなかった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: pre-push のコメントと `harness/githooks/README.md` のどちらも、python3 が欠けた
  場合(deny 側が全件 NG)に触れていなかった。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 16. 「ここでしか捕まらない」が、同じ差分で嘘になっていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 同じ判定表を CI にも載せたので「ここ(pre-push のこのステップ)でしか」は成立しない。
  正典側は主語が「判定表」で正しかった。参照 1 行へ畳んで解消した。
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」

### 17. `lib/` の表を導入する 1 文が、更新した表と矛盾したまま残っていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 表の「使う側」をゲートに書き換えたのに、導入文は「フックの判定を手で確かめる
  ための表の置き場」のままだった。
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」

### 18. 「飛ばした検査は出力に残る」が、検査を足した枝に当てはまらなかった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: pre-push の python3 の枝には `else` が無く、doc コメント / import 規約 /
  判別子の直読みは無言で飛んでいた。そこへ判定表を 1 本足していた。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 19. 「ランナーは選べるので」が根拠に辿り着けない書き方だった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 主張の実体は「ubuntu-24.04 の image に jq がある」。`ci-trace.yml` が素の jq を
  呼ぶ先例があるので、そちらを書けば次に読む人が確かめられる。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 20. 「正典 + 参照 1 行」にしたつもりが、同じ根拠がほぼ逐語で 3 箇所にあった

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 正典 / `frontend.yml` / `pre-push` の 3 箇所に同じ文があり、実際に 3 つ目だけが
  既に食い違っていた(指摘 16)。理由文を落として参照 1 行だけにした。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 21. `README:117` の「CI へ上げた」の列挙が、足した 3 ステップに追随していなかった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: この文は `harness/case-law/coding.md` の `comment-enumeration-drift` に載っている
  実例そのもの。列挙をやめて新設した表へリンクする形に作り替えた。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 22. 新設した配線表の層 2 列が非対称だった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: カナリアにだけ「python3 と jq が揃う環境だけ」と付け、同じく条件付きの
  `result-option-read-cases.sh` には付けていなかったので、読み手は無条件と読む。
- 既存ルール: なし

### 23. SKILL.md の「git hooks と CI が走らせる」が条件付きを無視していた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 8 つのうち `canary-cases.sh` だけは git hooks では道具が揃う環境に限られる。
- 既存ルール: なし

### 24. 検査対象と同じワークフローに置かなかった理由が書かれていなかった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: コメントは「pre-push に載せない理由」しか説明していなかった。`pr-closing-issue.yml`
  は自分の配置理由を冒頭に書いている(このリポジトリは配置理由を書く流儀)。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 25. `check-pr-closing-issue-cases.sh` の doc だけ配線に追随していなかった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 兄弟 2 本は doc に配線を書いているのに、CI へ繋いだこの 1 本だけが手で走らせる
  前提のままで、差分のファイル一覧にも入っていなかった。
- 既存ルール: なし

### 26. pre-push の出力が 44 行から 122 行になった

- 分類: `harness`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 増えた約 60 行はすべて判定表の `ok`。pre-push 自身が「途中の出力が流れて末尾しか
  見えない」を理由に `&&` を禁じているので、同じ失敗モードへ寄る。判定表の中身を
  変えることになるためこの回では受け入れ、#653 へ分けた。
- 既存ルール: なし

### 27. visual-regression が赤のまま(原因は main 側)

- 分類: `harness`
- 出どころ: `CI`
- 内容: 撮影範囲の検査が `documentElement.scrollHeight` を読むためクリップ済みの中身を
  数えており、差分なしの `origin/main` でも 6 ストーリーが落ちる。原因は #634 が赤のまま
  マージしたことで main に残っており、#645 が 3 案を未決のまま open。同一 head で 1 回
  再実行しても同じ失敗で、修正 PR は 0 件だったので PR にスタンドダウンを 1 件コメントした。
- 既存ルール: rules/ui-verification.md「差分ではなく撮影で落ちることがある」

## 手戻り

- 計画の基準値を測り直した。`pnpm install` をせずに `pre-push` を測っており、型 / lint /
  format が丸ごと飛ばされた 20.3 秒を「全体」として計画へ書いていた(指摘 12)
- コメントを書いた直後に `claim-verification` で 3 箇所を書き直した(指摘 13)。書いた時点で
  確かめる層に置いてあるので検証エージェントまでは持ち越していない
- 実装検証の後、pre-push と `frontend.yml` の理由コメントを参照 1 行へ畳み直した(指摘 20)。
  計画の時点で「正典 + 参照」と決めていたが、書くときに理由文をそのまま複製していた

## 発火

- 発火: 計測対象外(フック不発)

`echo hook-canary` が deny されず、このセッション ID の発火ログも検証エージェントの
マーカーも存在しなかった(`.claude/hooks/README.md`「発火しているかを確かめる」の 4 行目)。
層 3 でしか止まらない操作(`npx` / `git add -A`)は実行していないことを実行履歴で確認した。

## 規約への反映

無し。この回は介入していない(集計と介入の判断は `harness-growth` の管轄)。
続きは #653(pre-push の出力量)へ分けた。
