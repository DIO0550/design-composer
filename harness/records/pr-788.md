# PR #788 DesignDocument / Component の置き場所違いのメソッドを直し、NameSpace を DocumentNames へ改名する

- マージ日: 2026-09-26
- 関連 Issue: #770
- 差分規模: 22 ファイル / +409 -434

## ゴールと結果

ゴールは達成した。`DesignDocument` と `Component` にあった、第 1 引数の型を受け取らないメソッドを直した。
- `isValidIdentifier` と `renameBindings` は消した
- `renameSubtree` は `DocumentNames` へ移した
- `uniqueName` はドキュメントを受け取る形に変えた
- トークン名の採番は `TokenSet.uniqueName` を新しく作って移した

オーナーの判断で、`NameSpace` から `DocumentNames` への改名も同じ回に含めた。振る舞いは変わっていない。
人の GitHub 上のレビュー指摘は 0 件だった(`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも確認した。コメントは bot の投稿と自分のテストケース表だけ)。人の指摘はセッションの中で受けたもの(1〜4)だけ。
CI は最初の head で全チェックが緑、VRT も差分 0 件だった。

## 指摘

### 1. 連番の規則を utils へ出す案を推奨した

- 分類: `ownership`
- 出どころ: `レビュー（人）`
- 内容: `uniqueName` の置き場所として、連番の規則を `src/utils/` へ出す案を推奨した。`CaseStyle` を先例として挙げていた。
  オーナーから「連番はドメインの知識では？ utils にあるのは変」と指摘を受け、案を取り下げた。
- 既存ルール: rules/architecture.md「utils の形式」(ドメイン概念になった時点で utils から domains へ)

### 2. コードが同じことを理由に、知識の違う 2 つを 1 つのドメインで共有しようとした

- 分類: `ownership`
- 出どころ: `レビュー（人）`
- 内容: 次に「使用済みの名前の集合」を表す共通のドメインを作り、ドキュメントの名前とトークン名の連番を共有する案を出した。
  オーナーから「コードが同じだからまとめようとしてないか？」と指摘を受けた。衝突してはいけない相手も採番の仕様の出どころも別(docs/01・06 と docs/04)なので、取り下げた。
- 既存ルール: rules/architecture.md「ロジックの帰属先」

### 3. 選択肢の説明が抽象的で、何をするのかが伝わらなかった

- 分類: `plan`
- 出どころ: `レビュー（人）`
- 内容: 案を A〜D の比較で説明し続けたため、オーナーから「結局何やるのかがわからない」と言われた。メソッドごとの具体的な作業の一覧で説明し直した。
- 既存ルール: なし

### 4. NameSpace という名前が実体を表していなかった

- 分類: `naming`
- 出どころ: `レビュー（人）`
- 内容: 仕様書の「名前空間」を英語にした名前だった。ところが実体は「ドキュメントの中で一意でなければならない名前の一覧」で、名前から想像しにくいとオーナーが指摘した。
  改名を別 Issue に切り出すことを提案したが、オーナーの判断で #770 に含めた。改名先は `DocumentNames`。
- 既存ルール: rules/naming.md「名前と実体を一致させる」

### 5. 連番を共有しない理由が「知識が別」だけで具体的でなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `-2` から振るという綴りは docs に無いこと、今ある共有を 2 つに増やすことを指摘された。
  オーナーの判断は変えず、分けないと壊れる場面(ドキュメントの名前の採番を変えると、トークン名の採番も黙って変わる)を理由に足した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 8

### 6. toToken が種別を渡しているかを守るテストが無かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 既存の衝突テストは colors しか使っていなかった。そのため、種別を `"colors"` に固定する壊し方でも全件緑になる状態だった。
  spacing で衝突させるケースを足し、implementation-reviewer のミューテーション(M6)で落ちることを確かめた。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 7. 別種別の同名のテストの組が決まっていなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: colors / gradients の組を選ぶと、docs/04-tokens.md が不正と決めているふるまいをテストで固定してしまう。組を colors / spacing と計画に明記した。
  あわせて、#608 で `TokenSet.uniqueName` の範囲が変わることを #608 にコメントした。
- 既存ルール: rules/testing.md「振る舞い(仕様)をテストする」

### 8. 未決の判断を「なし」と書いていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 呼び出しが減る `DesignDocument.usedNames` / `DocumentNames.has` / `renameMap` を消すか残すかが決まっていなかった。
  置き場所の観点の外なので残す、と理由付きで Issue に書いた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「未決の判断」

### 9. 直すコメントを行まで挙げていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `DesignDocument.uniqueName` / `renameSubtree` / `NameSpace` を名指ししている doc を、ファイル表に行まで挙げていなかった。design-document / token-template / node-template / artboard の該当行を計画に足した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 7

### 10. ゴールを確かめるコマンドが計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 旧シンボルが 0 件になることを確かめる `rg` を 2 本、計画に足した。移すテストの行き先(`document-names.rename.test.ts`)も決めた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 2

### 11. DocumentNames だとトークン名も含むように読める

- 分類: `naming`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 改名はそのままにした。型の doc の 1 文目に、仕様書の「単一名前空間」に当たることとトークン名を含まないことを書いた。
- 既存ルール: rules/naming.md「名前と実体を一致させる」

### 12. Biome の整形違反と import の並び順が残っていた

- 分類: `なし`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 改名で import の並べるべき位置が変わった 2 件と、1 行が長すぎる 3 件。`biome check --write` で直した。整形の話で、対応する規範は無い。
- 既存ルール: なし

### 13. 改名後、トークン名の検証が DocumentNames を通っていた

- 分類: `naming`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 検証の `collectTokenNameErrors` が、トークン名を `DocumentNames.isValidIdentifier` で判定する形になった。「トークン名は含まない」と書いた doc と名前が食い違う。編集の経路は `CaseStyle` を直接呼んでいて、判定の経路も 2 通りに割れていた。
  `TokenSet.isValidName` を足して両方を揃えた。
- 既存ルール: rules/naming.md「名前と実体を一致させる」

### 14. 同じファイルの中で使用済みの名前を引く経路が 2 つあった

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `unusableNameError` だけが `usedNames` 経由で、他は `documentNamesOf` 経由だった。`DocumentNames.has(documentNamesOf(document), name)` に揃えた。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 15. テストの重複

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 次の 2 件は、別のファイルに同じ仕様のテストがあったので消した。
  - `design-document.rename.test.ts` の「さらに次の連番」(`document-names.normal.test.ts` と同じ仕様)
  - token-template の「別の種別の同名では連番が付かない」(`token.naming.test.ts` と同じ仕様)
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」(実装・テストとも)

### 16. Claude Code のフックが不発の環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: push 前に単独で打った `echo hook-canary` が deny されずに通った。検証エージェントを 2 回通した後も、このセッションのマーカーと発火ログの見出しが無かった(README の「本当に不発」の枝)。
  セッションは webhook(issues.assigned)で起動し、起動ディレクトリはリポジトリの親(`/home/user`)だった。不発の間に `npx` と `git add -A` は使っておらず、検証エージェントの実行中の git 操作も無い。PR 本文・Issue・Discord に記載した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- **計画段階**
  - 人の指摘(1〜3)で置き場所の案を 2 回取り下げた。実装前なので作業の手戻りは無い
  - 最初の判断待ちのコメントに書いた A〜C は、その後の計画コメントで取り下げを明記した
- **実装段階**
  - implementation-reviewer の指摘(13)で `TokenSet.isValidName` を足した。計画から外れた変更として Issue に追記した
  - 実装を 1 つの作業ツリーで終えてから、コミットを 1 変更ずつ積み直した。最終状態を退避して HEAD から順に再構成し、最終のツリーと 1 ファイルずつ一致することを `cmp` で確かめた
- **PR 後**: 無し
- どれも Issue #770 に追記済み

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。記録のみ(集計と介入の判断は `harness-growth` の管轄)。
