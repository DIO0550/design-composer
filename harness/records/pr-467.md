# PR #467 ✨ [New Feature]: #411 空き領域のドラッグを範囲選択にする

- マージ日: 2026-09-09
- 関連 Issue: #411
- 差分規模: 36 ファイル / +1616 -32

## ゴールと結果

ゴールは「キャンバスの空き領域からドラッグすると青い枠が出て、範囲に重なった artboard 直下の子がまとめて選ばれる。パンは space + ドラッグ / 中ボタンへ移す」。**達成した。**

Issue に**疑問点が 1 つあり、実装に入らずに止めて回答を待った**（範囲選択が「範囲内をすべて選ぶ複数選択の入口」なのか、別の意味なのか）。A / B / C を実測付きで出して「A でいいよ」の回答を得てから着手している。

レビューで**「リアルタイムにドラッグ中に選択されるようにしてほしい」を受け、ゴールが 1 段広がった**（離したときに選ぶ → 引いている間ずっと選び直す）。ずれではなく追加で、`89e2528` で入れた。

スコープ外にしたものは Issue へ分けた。複数選択中の編集操作（#464）/ artboard の背景から引けるようにする（#465）/ 土台の下端を `CanvasDockStack` が覆っている既存不具合（#466。UI 確認中に見つけた）。

## 指摘

### 1. 押して離すだけで選択が消えることが計画に書かれていない

- 分類: `plan-implicit-condition`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画に閾値の話が無く、そのままだと空き領域のクリックが 0 幅の範囲 → 一致 0 件 → 選択解除になる。`docs/06-ui.md`「選択」の「外へ戻るには…Esc で選択を『なし』へ戻す」と食い違う。**閾値は `node-drag` が `DragThresholdPx` として export 済みで、`ArtboardDrag` が 2 つ目の消費側**（E-1「既存の公開 API を探していない」も同じ件）。計画のテスト一覧にも「動かしていない（0 幅 0 高）」が無かった（F-5）。3 つ目の消費側として使い、docs は「選択」節も更新対象に足した
- 既存ルール: `plan-review.md`「既存のコードが前提にしている不変条件を、この計画が崩さないか」
- 次にどう防ぐか: 今回は記録のみ

### 2. space が artboard の枠の活性化キーとして既に使われている

- 分類: `plan-implicit-condition`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `artboard-frame/index.tsx` の `ActivationKeys = ["Enter", " "]` が space で `onSelect` する。枠は `role="button" tabIndex={0}` でクリック直後は必ずフォーカスが載っているので、その状態の space が「パンの構え」と「その artboard を選択」を同時に起こす。**既存テストは Enter を使っているので 1 件も落ちない**。space を `ActivationKeys` から外し、外した理由を Why not に書いた
- 既存ルール: `plan-review.md`「既存のコードが前提にしている不変条件を、この計画が崩さないか」
- 次にどう防ぐか: 今回は記録のみ

### 3. artboard の上から始めた space / 中ボタンのパンが始まらない

- 分類: `plan-implicit-condition`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `artboard-frame` / `artboard-label` の `stopPropagation` は**入力の種類を見ていない**。「左ドラッグ = パン」だった間はこの 2 行で足りていたが、space 中は artboard の上でもパンしたい。土台側の `onPointerDownCapture` でパンを開始してから `stopPropagation` する形（capture 相で先に決める）に変えた。`artboard-label/index.tsx` は**計画のファイル表に無かった**
- 既存ルール: `plan-review.md`「既存のコードが前提にしている不変条件を、この計画が崩さないか」
- 次にどう防ぐか: 今回は記録のみ

### 4. 凍結中（`isFrozen`）に範囲選択だけが通る

- 分類: `plan-implicit-condition`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `inert` が付いているのは `canvas-content` で、ポインタを受ける `canvas-surface` は inert ではない。凍結中のクリック選択は止まるが範囲選択は通る。`artboard-canvas.frozen.test.tsx` が「活性化しても選択が起きない」を固定しているので、範囲選択も凍結中は始めないことにしてガードとテストを足した
- 既存ルール: `plan-review.md`「既存のコードが前提にしている不変条件を、この計画が崩さないか」
- 次にどう防ぐか: 今回は記録のみ

### 5. 常時「開いた手」のカーソルが嘘になる

- 分類: `plan-implicit-condition`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 左ドラッグがパンでなくなるのに `cursor-grab` を出し続けると誤誘導になる。**カーソルはスクリーンショットにも VRT にも happy-dom にも出ない**ので、計画で決めないと誰も気づけない。space を押している間だけ `cursor-grab`、通常は既定、パン中は `cursor-grabbing` にした
- 既存ルール: `plan-review.md`「既存のコードが前提にしている不変条件を、この計画が崩さないか」
- 次にどう防ぐか: 今回は記録のみ

### 6. 実測 0 サイズの要素が範囲に入るかを確かめていない

- 分類: `plan-scope-premise-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画は `DrawnBounds` が「描かれていない名前は飛ばす」ことを前提にしていたが、**要素が存在して 0 サイズ（未レイアウト）の場合は `some` で (0,0,0,0) が返る**。実ブラウザでは左上へ引くと隠れたノードが入り、テストでは `drawnAt` で差し替えなかった全要素が原点の 0×0 になるので、原点付近へ範囲を引くと逆に落ちなくなる（F-7）。`CanvasBounds.hasArea` で外し、テストは原点から離した座標で引くことにした
- 既存ルール: `plan-review.md`（フェーズ 3 手順 3）
- 次にどう防ぐか: 今回は記録のみ

### 7. 辺だけが接するときに重なりとみなすかが決まっていない

- 分類: `plan-implicit-condition`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画のテスト一覧は観点名（「辺だけが接する」）だけで期待値が書かれていなかった。同じ型の `CanvasBounds.contains` は境界を含む（`>=` / `<=`）実装なので、`overlaps` で縁の扱いを割ると 1 つの型が 2 通りの縁を持つ。**含む側に揃え**、揃えた理由をコミットへ書いた
- 既存ルール: `plan-review.md`「既存のコードが前提にしている不変条件を、この計画が崩さないか」
- 次にどう防ぐか: 今回は記録のみ

### 8. 範囲の枠が左右ペインの上まで出る

- 分類: `plan-implicit-condition`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `position: fixed` は器の `overflow-hidden` を抜けるので、ドラッグを画面外へ引くと矩形が左右ペインの上に乗る。**8 件の未決のうち、これだけは指摘の方向（器で切る）を採らなかった**。器からの相対に直すにはレンダー中に器を実測する必要があり、実測を持つ `useDrawnBounds` は名前で引く形なので噛み合わない。引いている間だけの見え方なので現状のままにし、理由をコミットの「影響とトレードオフ」に残した
- 既存ルール: `plan-review.md`「既存のコードが前提にしている不変条件を、この計画が崩さないか」
- 次にどう防ぐか: 今回は記録のみ

### 9. 計画のファイル表に消費側が 4 つ足りない

- 分類: `plan-file-omission`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `artboard-label/index.tsx`（`stopPropagation` の対）/ `use-canvas-view/index.ts`（`PanHandlers` を spread から条件付きの呼び出しに変えるので「変えない」が成り立たない）/ `src/components/__tests__/pointer-gesture.ts`（`pressPointer` が `button` を渡せず、中ボタンのテストの入口が無い）/ `docs/06-ui.md`「選択」節 が挙がっていなかった
- 既存ルール: `plan-review.md`「変更で壊れる既存の資産を洗い出しているか」
- 次にどう防ぐか: 今回は記録のみ

### 10. 計画が前提を崩す doc / Why コメントがファイル表に無い

- 分類: `plan-comment-reference`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `artboard-canvas/index.tsx:48-52`（「props が 9 つ…前半 3 つ / 後半 6 つ」が prop を 1 つ足すと嘘になる）/ 同 `:90-91`（ポインタを捕捉する主体がパンから範囲選択へ変わる）/ 同 `:142-143` / `artboard-frame/index.tsx:170`（「パンにしない」→ 止めているのは範囲選択）/ `features/canvas/index.ts:4-6`（feature の doc の操作の列挙に範囲選択が無い）。**コードの参照だけを grep したファイル表では拾えない形**
- 既存ルール: `plan-review.md`「ファイル表に挙がっているか」
- 次にどう防ぐか: 今回は記録のみ

### 11. 「範囲選択」という語が既に別の意味で 3 箇所にある

- 分類: `naming-prefix-collision`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `asset-row/index.tsx` / `artboard-label/index.tsx` / `artboard-frame/index.tsx` の 3 箇所の「範囲選択」は**文字の選択**（ドラッグが文字の範囲選択に化ける、の意味）。同じ語がキャンバスの矩形選択にも使われると、コメントを読む側が取り違える。`asset-row` の 1 件を「文字の範囲選択」へ直した
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ

### 12. 「パンの入力に依存しているテスト」の数え方が誤っていた

- 分類: `plan-scope-count-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画は「`canvas-surface` を触るテストは 5 ファイル」で数えた（ファイル数自体は実測と一致）が、**`canvas-surface` を触らずにパンの不変条件を固定しているテスト**（`artboard-canvas.artboard-drag.test.tsx`「見出しを掴んでもキャンバスは動かない」）を落としていた。`canvasContent().style.transform` を読むだけなので surface の grep に掛からない。**数えるべきは「パンが起きるかを見ているか」だった**
- 既存ルール: `implementation-flow` フェーズ 3 手順 2
- 次にどう防ぐか: 今回は記録のみ

### 13. 入力を変えると既存の assert が「どう壊しても通る」側へ倒れる

- 分類: `test-assert-unfalsifiable`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `artboard-canvas.zoom-pan.test.tsx:46`「artboard の上で始めたドラッグではキャンバスが動かない」は、変更後は**どこを左ドラッグしてもパンしない**ので実装をどう壊しても通る。同 `:66`「ズームやパンをしても選択は変わらない」は入力がパンではなく範囲選択に化けてテスト名と食い違う。space / 中ボタンで押し直して観点を保った
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ

### 14. `SelectionRange` が既に「選択されているものの範囲」の意味を持っている

- 分類: `naming-prefix-collision`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `docs/06-ui.md:89`「掴めない 5 個のハンドルは、**選択の範囲**を示す目印」/ `canvas-bounds` の `enclosing` の doc「選択したものすべてを 1 つの範囲として扱いたいとき」。同じ綴りが「これから選ぶために引いている矩形」を指すと逆に読める。`RangeSelect` へ改名した（`marquee` は UI 案・docs に `grep -i` で 0 件なので不採用）
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ

### 15. 計画自身が語形を割っていた（`selection-range` と `range-select`）

- 分類: `naming-vocabulary-alignment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: ドメインは `selection-range` / `SelectionRange`、テストファイルは `artboard-canvas.range-select.test.tsx` と、同じ計画の中で 2 通りになっていた。**兄弟のドメインはすべて操作名**（`artboard-drag` / `node-drag` / `node-resize` / `text-edit` / `side-snap`）なので、操作名側（`range-select`）へ揃えた
- 既存ルール: rules/naming.md「メソッド名を既存のドメインに揃える」
- 次にどう防ぐか: 今回は記録のみ

### 16. フックが `range` という名前で `RangeSelect` ではない型を返していた

- 分類: `naming-vocabulary-alignment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画のフックは `{ range: Option<CanvasBounds>, … }` を返す形で、名前が指す型と返す型が違った。`bounds` へ改名した
- 既存ルール: rules/naming.md「名前と実体を一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 17. `useKeyShortcuts` を使わない理由が却下案に無い

- 分類: `plan-rejection-coverage`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「ページ全体のキー購読」と「文字を打ち込める場所では通さない」規則は `src/hooks/use-key-shortcut/` に既に閉じている。`useSpaceHeld` はこれをそのままは使えない（keyup が要る / 押している間の状態が要る / `preventDefault` したくない）が、**その選択肢自体が却下案の一覧に出てこなかった**。却下理由を書き、規則の共有だけは `KeyShortcut.isConsumedBy` 経由に寄せた（後述 26）
- 既存ルール: `implementation-flow` フェーズ 3 手順 6
- 次にどう防ぐか: 今回は記録のみ

### 18. 2 点から最小の矩形を作る処理を 2 本目として書こうとしていた

- 分類: `duplication-traversal`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `CanvasBounds.enclosing` が既に「複数の矩形を含む最小の矩形」を持っているのに、`RangeSelect.bounds` に min / max を直接書く計画だった。`CanvasBounds.spanning`（2 点を対角にした矩形）を同じ型へ足し、`RangeSelect.bounds` はそれを呼ぶだけにした
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ

### 19. `selectAllInstances` と同じ形を通すかどうかが計画に無い

- 分類: `duplication-derivation`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `EditorState.selectAllInstances` が `SelectionState.create(<集めた名前>)` という `selectNodes` と同じ形を既に持つ。通す / 通さないの判断が計画に無かった。**通さない**（対象の決め方が違う。一方は状態から決まり、他方は引数で受ける。共通なのは 1 行だけ）と決めて理由をコミットに書いた
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ

### 20. 「範囲に何も入らなければ選択は『なし』」が既定値と同じ答えになる

- 分類: `test-default-input`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 範囲選択が丸ごと動かない実装（`onSelectInRange` を呼ばない）でも「選択は変わらない = なし」に見える。**呼ばれない ≠ 空で呼ばれる**（前者は選択維持、後者は解除）ので、assert を「`[]` で呼ばれること」まで書いた
- 既存ルール: rules/testing.md「既定値・フォールバックがある処理では、既定値と違う答えになる入力を選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 21. パン側の「起きない」の対が範囲選択側に無い

- 分類: `test-coverage-branch-asymmetry`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: パン側には「修飾なしの左ドラッグでは中身が動かない」があるのに、範囲選択側の対「space / 中ボタンのドラッグでは枠が出ず選択も変わらない」が計画のテスト一覧に無かった
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ

### 22. 他所で始まったドラッグの `pointerup` が土台に届く経路を見落としていた

- 分類: `plan-path-coverage`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `opened-document-editor.asset-drag.test.tsx` はパレット行で press し、**土台で move / release** する。範囲選択が「開始していない up」で空の並びを渡すと、**パレットからの配置のたびに選択が消える**。今のテスト一式では 1 件も落ちない。開始していない `pointerup` では何もしない実装にし、ケースを 1 件足した
- 既存ルール: `plan-review.md`「ゴールが『画面から届く経路を通すこと』なら」
- 次にどう防ぐか: 今回は記録のみ

### 23. 計画のテスト一覧に観点が 3 つ足りない

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 複数 artboard にまたがる範囲 / artboard 自身は選ばれない（枠を丸ごと囲む） / space を押したままウィンドウのフォーカスが外れて keyup が来ない（パンの構えが固着する）。3 つとも足した（`blur` は `useSpaceHeld` に解除を実装した）
- 既存ルール: `implementation-flow` フェーズ 3
- 次にどう防ぐか: 今回は記録のみ

### 24. happy-dom で `button` がイベントに載るかを確かめていない

- 分類: `plan-scope-premise-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 中ボタンのテストは `fireEvent.pointerDown(el, { button: 1 })` が `event.button === 1` になることを前提にしていた。**同じリポジトリに前例がある**（`canvas-gesture.ts`「happy-dom の `WheelEvent` は `UIEvent` 派生で修飾キーを持たない」）。使い捨てのテストを 1 本走らせて `button` が載ることを確かめてから書いた（capture 相の `stopPropagation` が親の bubble も止めることも同じ形で確かめた）
- 既存ルール: rules/coding.md「外部の挙動は動かして確かめる」
- 次にどう防ぐか: 今回は記録のみ

### 25. 計画に表示確認の手順が無い

- 分類: `drag-feedback-incomplete`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 今回は**ドラッグの最中の見え方そのものが成果物**なのに、「操作の途中でスクリーンショットを撮り、そこに操作対象が映っているか」を確認手段として計画に書いていなかった（規約は Storybook の `OpenedDocumentEditor` で操作せよと名指ししている）。手順を計画へ足し、離していない状態を 4 段階で撮った
- 既存ルール: rules/ui-verification.md「表示確認」
- 次にどう防ぐか: 今回は記録のみ

### 26. 先行の overlay から外れる判断の理由が計画に無い

- 分類: `comment-missing-divergence`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 既存の overlay 2 つ（`drop-marker` / `snap-guide-overlay`）は `__tests__/` を持つのに `RangeSelectOverlay` には置かない判断、選択の枠と同じ `SelectionColor` を借りる判断（**同じ青が同時に画面へ出る**）のどちらも理由が書かれていなかった。`SnapGuideOverlay` の Why not（色の使い分けを明記している）と同じ粒度で書いた
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ

### 27. 「パン中」と「範囲選択中」が同時に成立する状態を型で作れる

- 分類: `illegal-state`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `CanvasView.dragFrom` と範囲の `Option` が別々のフックにあるので、両方が立った状態が型では作れる。`pointerdown` が 1 回なので実際には起きないが、**なぜ排他が保てるのか**がコードのどこにも無かった。capture 相で入力を見て一方だけを開始する形にし、その 1 箇所で決まることを Why に書いた
- 既存ルール: rules/coding.md「不正な状態を型で表現できなくする」
- 次にどう防ぐか: 今回は記録のみ

### 28. 「中ボタン or space 押下ならパン」の条件式に名前が無い

- 分類: `なし`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: rules/naming.md「条件を組んだ式に名前を付ける」に該当する指摘だが、**分類の語彙にこの規約に対応する行が無い**。`pansCanvas(event)` という名前の関数に入れてから使った
- 既存ルール: rules/naming.md「条件を組んだ式に名前を付ける」
- 次にどう防ぐか: 今回は記録のみ（語彙の追加は `harness-growth` の管轄）

### 29. 呼び出し先の無い `isPrimaryButton` を先回りで足そうとしていた

- 分類: `utils-form`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画は `CanvasPointer.isPrimaryButton` を足す形だったが、**呼び出し先が計画に書かれていなかった**。呼ばれないなら「揃っているから」の先回りになる。実際には呼び出し先があった（範囲選択の開始判定）ので、置き場所のほうを直した（`CanvasPointer` は `features/canvas/` にあり、横断のテストヘルパー `src/components/__tests__/` から import できないため `src/utils/PointerButton.ts` へ）
- 既存ルール: rules/architecture.md「utils の形式」（呼び出しの無い操作を先回りで足さない）
- 次にどう防ぐか: 今回は記録のみ

### 30. フックの引数が計画に書かれておらず、帰属を判定できない

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `useRangeSelect` が document をどこから受けるか / `onSelect` の型が計画に無く、**フックがドメイン知識を持たないかを計画の段階では判定できない**状態だった
- 既存ルール: `implementation-flow` フェーズ 3
- 次にどう防ぐか: 今回は記録のみ

### 31. `select_nodes` の配線が 1 件も守られていない

- 分類: `test-coverage-wiring`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: reducer の `case "select_nodes"` を `return state` にして全実行 → **3049 件すべて通過**。「範囲選択が実際に選択を変える」を確かめるテストが 1 本も無かった。`EditorState.selectNodes` の単体テストと `ArtboardCanvas` の `vi.fn()` は個別に緑にできるので、その間（`useNodeActions` → reducer → 3 ペイン）が切れていても気づけない。**PR 本文が実ブラウザで確かめたと書いている経路が、そのままテストに無い形**。3 ペインを実物のまま組み立てるテストを 2 件足し（ツリーの選択行 / 右ペインの `2 selected`）、範囲の外に 3 つ目のノードを置いて選びすぎでも落ちるようにした
- 既存ルール: rules/testing.md「古典学派のテスト」/ `implementation-reviewer`「テストが守っているかの観点」
- 次にどう防ぐか: 今回は記録のみ

### 32. 主ボタンの判定が守られておらず、しかもテストが型として書けなかった

- 分類: `test-coverage-branch`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `if (!PointerButton.isPrimary(event)) return;` を丸ごと消して全実行 → 3049 件すべて通過。中ボタンは capture 側の `stopPropagation` が先に止めるので、このガードが実際に効くのは**副ボタン（右）だけ**。ところが `PointerButtons` が「操作へ割り当てているものだけ」（主 / 中）を並べていたため、**右ボタンを押すテストが型として書けなかった**。DOM の語彙（主 / 中 / 副）で閉じ、割り当ての事情は呼び出し側へ置いた
- 既存ルール: rules/testing.md「古典学派のテスト」/ `implementation-reviewer`「テストが守っているかの観点」
- 次にどう防ぐか: 今回は記録のみ

### 33. 「枠をキーボードから選ぶのは Enter だけ」が守られていない

- 分類: `test-coverage-branch`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `ActivationKeys` を `["Enter", " "]`（＝指摘 2 の判断を戻す）にして全実行 → 3049 件すべて通過。`docs/06-ui.md` に明文で書いた仕様（`role="button"` の既定から意図的に外れた判断）なのに、それを固定するテストが無かった
- 既存ルール: rules/testing.md「古典学派のテスト」
- 次にどう防ぐか: 今回は記録のみ

### 34. keyup 側だけ対の片方が無い

- 分類: `test-coverage-branch-asymmetry`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `use-space-held` の release 側の `if (event.code === SpaceKeyCode)` を外して常に解くようにしても 3049 件すべて通過。keydown 側には「space 以外のキーでは構えない」があるのに、keyup 側の対（「space 以外のキーの keyup では構えが解けない」）が無かった。**指摘 21 で計画段階に対の非対称を直したのに、別の対で同じ形が残っていた**
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ

### 35. 枠の位置・大きさの配線が守られていない

- 分類: `test-coverage-wiring`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `RangeSelectOverlay` の `bounds` を固定値へ差し替えても canvas 配下 468 件すべて通過。テストが `toHaveLength` しか見ておらず、**出る / 出ないは守られているが、引いた 2 点が矩形になって出ることは守られていない**。`RangeSelect.bounds` の単体テストがあることは、それが画面の `style` まで届く配線とは別の主張
- 既存ルール: `implementation-reviewer`「テストが守っているかの観点」
- 次にどう防ぐか: 今回は記録のみ

### 36. `canvasCursor` の真偽値の位置引数が 2 つ並んでいた

- 分類: `signature`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `canvasCursor(isDragging, isSpaceHeld)` は**取り違えてもコンパイルが通る**。しかもこの関数自身の doc が「カーソルは happy-dom にも視覚差分にも出ないので気づく手段が無い」と書いている（指摘 5）。オブジェクト引数にした
- 既存ルール: rules/coding.md「関数のシグネチャ」
- 次にどう防ぐか: 今回は記録のみ

### 37. 「この 1 箇所だけで決まる」というコメントが事実ではなかった

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `artboard-canvas/index.tsx` の「パンと範囲選択のどちらが始まるかはこの 1 箇所だけで決まる」は、**右ボタンについては `use-range-select` 側が決めている**ので成立していなかった。コメントを実態へ直した
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 38. 選べるものへの絞り込みが 2 箇所に丸ごと現れた

- 分類: `duplication-traversal`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `editor-state/index.ts` の `selectAt` と `selectNodes` に `names.filter((name) => selectableNodeName(document, name).some)` が丸ごと 2 つ。**`selectNodes` の doc 自身が「絞り方を `selectAt` と揃えるのは、キャンバスから選べるものの定義を 1 つに保つため」と書いているのに、揃えているのは綴りであって実体は 2 箇所**だった。`selectableNodeNames(document, names)` を切り出して両方から呼ぶ形にした
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ

### 39. 「素のキーは入力欄に食われる」を 2 経路で独立に導いていた

- 分類: `duplication-derivation`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `KeyShortcut.isConsumedBy` と `use-space-held` が同じ規則を別々に導出していた。`use-space-held` の doc が「同じ線引きへ揃える」と書いている＝**2 経路で同じ規則を導いている自覚がある**状態。`isConsumedBy` は公開されており features から呼べるので、そちら経由へ寄せた（`KeyShortcut.matches` ごと共有しなかったのは、修飾キーの一致まで要求されて Shift 併用の space で構えられなくなるため）
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ

### 40. 同じ Why が 3 箇所にあり、引いている docs の節名まで割れていた

- 分類: `duplication-uncovered-shape`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「手ぶれでは選択に手を付けない / クリックで選択が外れるのは docs と食い違う」が `range-select/index.ts`・`use-range-select/index.ts`・`artboard-canvas.range-select.test.tsx` の 3 箇所にあり、**引いている docs の節名が 2 通り**（「キャンバスのクリックが選ぶ階層」と「選択」）だった。判定を持つ側に 1 つ置き、他は参照に留めた
- 既存ルール: rules/coding.md「規約の適用範囲」（同じ判断根拠を説明する doc の重複）
- 次にどう防ぐか: 今回は記録のみ。**同じ形を pr-462#8 は `comment-premise-drift` として記録している**（doc の重複を重複側で数えるかコメント側で数えるかが割れている）。数える人はこの 2 タグを並べて見てほしい

### 41. 「名前 → 実測 → 不在を落とす」走査が 2 本になった

- 分類: `duplication-traversal`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `DrawnBounds.enclosing` と `collectOverlappingNames` が同じ走査を各々持つ。戻り値が違う（矩形 / 名前）ので今回は畳まなかったが、**3 つ目が来たら畳む合図**
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ（畳まない判断をした側）

### 42. `Range` の語幹が既に「数値の区間」を指している

- 分類: `naming-prefix-collision`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `src/utils/Range.ts` の `Range` は `{min,max}` で、`Token`（`FontWeightRange` / `AlphaPercentRange`）が使っている。新しい `RangeSelect` とローカル変数 `range` は**画面上の 2 次元の矩形**を指す。ローカル名を `drawing` へ改め、型名は docs の語彙「範囲選択」に沿うので残した
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ

### 43. `PointerButtons` が DOM の `PointerEvent.buttons` と綴りで衝突する

- 分類: `naming-borrowed-convention`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: DOM の `buttons`（複数形）はビットマスクで**中ボタンは 4**。この定数は `Middle: 1` なので、複数形の綴りだけを見た読み手が逆の意味に取る。DOM から借りた綴りが、このリポジトリで何を指すかが読めない形。doc に「`PointerEvent.button` の値であり `buttons` とは別物」と明記した
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ

### 44. `onSelect` と `onSelectNodes` の使い分けが名前から読めない

- 分類: `naming-vocabulary-alignment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: どちらも「ノードを選ぶ」で、範囲由来であることが名に出ていない。prop を `onSelectInRange` へ改名した（`EditorState.selectNodes` / `NodeActions.selectNodes` は名前を指してまとめて選ぶ汎用の操作なので据え置き）
- 既存ルール: rules/naming.md「名前と実体を一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 45. フックの引数名 `document` がグローバルを隠していた

- 分類: `naming-vocabulary-alignment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 同じファイルは DOM のポインタイベントも扱っており、呼び出し側は同じ値を `designDocument` と呼んでいる。呼び出し側の語彙へ揃えた
- 既存ルール: rules/naming.md「名前と実体を一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 46. `globalThis` の受け方が兄弟モジュールと割れていた

- 分類: `なし`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `use-space-held` の `const document = globalThis.document;` は、兄弟の `use-drawn-bounds`（`globalThis.window.addEventListener` を直書き）と流儀が割れ、シャドウイングも生む。直書き側へ揃えた。**「兄弟モジュールと書き方の流儀を揃える」に対応する分類が語彙に無い**（`duplication-*` は同じ処理の重複、`comment-missing-precedent-echo` は注記の転記で、どちらも「同じ処理を別の綴りで書く」形を指していない）
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ（語彙の追加は `harness-growth` の管轄）

### 47. `pointer-events-none` の理由が事実として誤っていた

- 分類: `comment-behavior-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「`pointer-events-none` を外すと、矩形の下を通った `pointermove` の `target` が矩形になる」は**成立しない**。矩形が出ている間は必ず土台が pointer capture を持っており、Pointer Events 3 の仕様どおり `target` は `pointer-events` に関係なく土台に固定される。加えて範囲選択は `event.target` を一切読まない。**この一文は `snap-guide-overlay` からの借用で、あちらは pointer capture を取らないので真、こちらでは偽**という形。実際の理由（矩形の下の要素が hover やカーソルを失わないため）へ書き直し、外してもテストにも視覚差分にも出ないことを併記した
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 48. `SelectionColor` の消費側の列挙が据え置きだった

- 分類: `comment-enumeration-drift`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `artboard-frame-list/index.tsx` の「export しているのは、枠とリサイズハンドルが同じ色でなければならないため」に対し、この差分で 3 つ目の消費側（`range-select-overlay`）が増えたのに列挙が 2 つのままだった
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ

### 49. ハンドルを器の外へ置く Why not が、同じ差分の docs の決定で崩れた

- 分類: `comment-premise-drift`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「ハンドルを重ねる器は土台の中には置けない（ハンドルを押しても space 押下中ならパンが始まる）」は、**同じ差分の `docs/06-ui.md` でその挙動を仕様として決めた**ので、もう「置けない理由」ではない。むしろ現状の配置ではハンドルの上だけ space + ドラッグでパンできず docs と食い違う。**実装は変えず、docs 側にハンドルを例外として明記**し、コメントの理由も実態へ直した（直すなら別の Issue）
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ

### 50. 「同じ理由」と書いた参照先の理由が別物だった

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `use-space-held` の「空白の `event.key` が `" "` という 1 文字で見分けにくいため（`use-key-shortcut` の `PhysicalKey` と同じ理由）」に対し、**参照先が挙げている理由は「Shift を押している間の数字段が打つのは数字ではなく記号だから」**で、空白の綴りの話ではなかった。参照先を読まずに「同じ理由」と書いた形
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 51. 検知手段が無いことの注記が、先行 3 箇所にあるのに書かれていない

- 分類: `comment-missing-detection-limit`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `RangeSelectOverlay` の `fixed` と `pointer-events-none` は外してもテストも視覚差分も落ちない。同じ立場の `snap-guide-overlay` / `artboard-frame` / `editor-top-bar` は**その一文を持っている**のに、新しく足した側へ転記されていなかった
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ

### 52. 引いている docs の規則の係り先が、この用途を覆っていない

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `use-space-held` が引いている `docs/06-ui.md` の規則は「**キーボードだけで行う操作**（削除・…・収めるズーム）」に係っており、ポインタの修飾である space 押下はその列挙に入っていない。docs の列挙側を直した
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 53. 引いている間ずっと選び直してほしい（離したときだけでは足りない）

- 分類: `なし`
- 出どころ: `レビュー（人）`
- 内容: PR レビュー本文「リアルタイムにドラッグ中に選択されるようにしてほしい / 最悪選択状態の見た目だけでもいい」。実装は `docs/06-ui.md` に書いた「離すと選ばれる」に沿っており**規約にも仕様にも違反していない**が、Figma の挙動として期待されていたのはドラッグ中の追随だった。**見た目だけでなく選択そのものを動かす形**を採った（見た目だけにすると 3 つの表示先へ配る経路をもう 1 本作ることになる）。`plan-reviewer` / `implementation-reviewer` のどちらも「仕様に無い期待」は見に行かないので、すり抜けとして残る形
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### 54. `PointerButton` の置き場所が `utils/` でよいか（未解決）

- 分類: `utils-form`
- 出どころ: `レビュー（人）`
- 内容: `src/utils/PointerButton.ts` への行コメント「なんかUtilとも言い難いなぁ。。。/ UIとの境界だから？/ I/O関連とかは別のフォルダの方がいいかもねぇ。」。AGENTS.md「設計判断の確認」に従い**勝手に動かさず**、実測を添えて選択肢を返した（同じ形が `utils/` に既に 2 つある: `CommandKey` 13 消費側 / `ElementEx` 3 消費側 → 新しいものだけ移すと語彙が層をまたいで割れる。現在の `libs/` はすべて**差し替えられる境界**で `PointerButton` は何も実行しない。`src/components/` と `src/hooks/` からの `@/libs/` import は 0 件）。**この回答は返答が無いままマージされ、スレッドは未解決のまま**。A（`utils/` に残す）で入っている
- 既存ルール: rules/architecture.md「utils の形式」
- 次にどう防ぐか: 今回は記録のみ。**未解決のまま閉じた指摘**なので、数える人はここを見てほしい

### 55. 「引かれたか」を 2 点から毎回導くと、縮めたときに選び直しが止まる

- 分類: `illegal-state`
- 出どころ: `自己修正`
- 内容: 指摘 53 に応えて「引いている間ずっと選び直す」に変えたところ、**広げたあとに掴んだ点まで縮める**と、距離が閾値を割った時点で「引かれていない」に戻って選び直しが止まり、広げたときの選択が残った。「一度でも閾値を超えたか」は 2 点からは導けない履歴なので、`RangeSelect` の持ち回るフィールドにした（超えたあとは縮めても真のまま）。**導出で表せない状態を導出で持っていた形**
- 既存ルール: rules/coding.md「不正な状態を型で表現できなくする」
- 次にどう防ぐか: 今回は記録のみ

### 56. 表示確認で「出ない」を見て、実装を疑って時間を使った

- 分類: `なし`
- 出どころ: `自己修正`
- 内容: 実ブラウザで範囲選択が出ず、最初に実装を疑った。`console.log` の探りで `pointerdown` だけがフックに届いていないと分かり、`document.elementFromPoint(324, 760)` を打つと**土台ではなく `CanvasDockStack`**（下端 70px を横いっぱいに覆う透明な帯）が返った。**座標の選び方が誤っていただけで、コードは正しかった**。既存不具合として #466 を起票し、起点を変えて確認し直した。「表示確認が空振りしたら、実装を疑う前に操作点に何が載っているかを `elementFromPoint` で確かめる」に対応する規範が無い
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### 57. `echo hook-canary` が deny されずに通った（2 回とも）

- 分類: `hook-environment`
- 出どころ: `自己修正`
- 内容: push 前の確認として 2 回実行し、**2 回とも通った**。`.claude/hooks/` は発火していない。PR 本文と PR コメント、Discord 通知に「フック不発環境」であることを記載した。**git hooks（`harness/githooks/`）は両方の push で正常に走り**、「テスト規約」「import 規約の違反 0 件」まで実行されたことをログで確認している
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

**実装コードの手戻りは 3 回。**

1. **計画を書き直した**（`plan-reviewer` の 8 件の未決を潰した）。「未決の判断: 無し」と書いていたのが誤りで、閾値・space の衝突・凍結・カーソル・0 サイズ・辺の扱い・枠のはみ出し・artboard 上のパンを 1 つずつ決め直した。**8 件のうち 7 件は指摘の方向を採り、枠のはみ出し（指摘 8）だけは採らずに理由を残した**
2. **`implementation-reviewer` の 22 件に対応して差分を組み直した**（`3ebf8c7`）。うちミューテーションで「壊しても 1 件も落ちない」が実測された 5 件がテストの追加、4 件がコメントの書き直し
3. **レビュー後に「引いている間ずっと選び直す」へ作り替えた**（`89e2528`）。この過程で指摘 55（縮めたときの取りこぼし）を自分で見つけて `isDrawn` を導出からフィールドへ変えた

計画から外れたのは 3 箇所。

- **`PointerButton` の置き場所**。計画は `CanvasPointer`（`features/canvas/`）だったが、横断のテストヘルパー `src/components/__tests__/pointer-gesture.ts` から呼べない（`components/` は features を import できない）。`src/utils/` へ置いた。**この判断が指摘 54 として人からの疑義になった**
- **`RangeSelect.bounds` の中身**。計画は min / max を直接書く形だったが、`CanvasBounds.spanning` を足してそちらへ委ねた（指摘 18）
- **枠を出す条件**。計画は「引いている間ずっと出す」だったが、閾値を超えるまでは出さない形にした（手ぶれで一瞬枠が出るのを避けるため）

## 発火

- 発火: 計測対象外(フック不発)

`${TMPDIR:-/tmp}` にあった発火ログは別セッション（`205cdeb0-…` / 2026-09-04）のもので、このセッションの `session` 見出しは書かれていない。したがって**このセッションではスキル・サブエージェントの発火を計測できていない**（`implementation-flow` / `harness-record` / `plan-reviewer` / `implementation-reviewer` / `commit` / `pull-request` は実際に起動しているが、ログには残らない）。

不発への対処として次を手順の側で守った。

- push 前検査を `&&` で連ねず 1 つずつ独立に実行し、それぞれの結果行を確認（typecheck / oxlint / biome / doc コメント / テスト規約 / テスト 3063 件）
- 検証エージェントの実行中に `git add` / `commit` / `push` を挟まない、を手動で守った。**ただし 1 度崩れている** — `implementation-reviewer` の起動直後に未コミット分を `c7a4b7a` としてコミットしており、エージェント側が「私の最初のミューテーションは 14:56 以降なので混入していない」と時刻で切り分けて報告している。`block-git-during-verification-agent.sh` が効いていれば止まっていた操作
- `git add -A` は使わず、ファイルを個別に add した

## うまくいったこと

- **Issue の疑問点で止まり、推測で埋めなかった。** 「範囲内をすべて選ぶ」以外の読み方が 2 つ成立したので、A / B / C を実測（`docs/06-ui.md`「複数選択」の現状 / `SelectionState` が既に複数を持つこと / 掘る階層の決着 #412）付きで出して回答を待った。**着手時と停止時の 2 回、Issue にセッション URL を残している**
- **外部の挙動を使い捨てのテストで確かめてから書いた**（指摘 24）。`fireEvent.pointerDown(el, { button: 1 })` が `event.button === 1` になること、React の capture 相 `stopPropagation` が子と親の bubble を両方止めることの 2 つ。確かめたテストは消した
- **ミューテーションを自分でも回した。** 実装本体で 12 件、レビュー対応で 4 件、リアルタイム化で 2 件。特にリアルタイム化では「引いている間の選び直しをやめる → 5 件落ちる」「一度引いたことを覚えず毎回距離から出す → 2 件落ちる」を実測してから PR コメントに書いた
- **表示確認を「離していない状態」で撮った。** 閾値の内側 / 1 件覆う / 2 件覆う / 掴んだ点へ戻す の 4 段階で、キャンバスの枠・ツリーの行・右ペインの 3 つが同じ選択を映していることを見た（規約が名指しで要求している「操作の途中の見え方」）
- **設計判断の疑義（指摘 54）を勝手に動かさず、実測を添えて選択肢で返した。** 消費側の件数・`libs/` の現メンバーの性質・`components/` からの `libs/` import 0 件を数えてから A / B / C を出している

## 規約への反映

無し。`harness-record` の管轄は記録を 1 本足すところまでで、集計と介入は `harness-growth` が別の機会に行う。

**数える人へ 5 つ申し送る。**

1. **指摘 1〜5・7・8 の 7 件は `plan-implicit-condition` だが、原因は 1 つ**（計画が「未決の判断: 無し」と書いた）。タグごとに数えると 7 件に見えるので、束ねて 1 件と読むべきかは数える人の判断に委ねる。ただし**中身は 7 つとも別の不変条件**（閾値 / space の活性化 / `stopPropagation` / `inert` / カーソル / 縁の扱い / `overflow-hidden`）で、1 つ決めても他は決まらない
2. **指摘 21（計画段階で対の非対称を直した）と指摘 34（実装で別の対の非対称が残っていた）が同じ回に出ている。** 計画で 1 つ直したことが、他の対を見に行く契機にならなかった形。`test-coverage-branch-asymmetry` の再発を数えるときはこの 2 件の関係を見てほしい
3. **指摘 40 は pr-462#8 と同じ形なのに別のタグを付けている**（`duplication-uncovered-shape` / `comment-premise-drift`）。「同じ判断根拠を説明する doc の重複」を重複側で数えるかコメント側で数えるかが割れているので、語彙の側の整理が要るかもしれない
4. **`分類: なし` が 3 件出た**（28: 条件式に名前を付ける / 46: 兄弟モジュールと流儀を揃える / 56: 表示確認の空振りをどう切り分けるか）。28 は rules に規範があるのに分類の語彙に行が無い形、46 と 56 は規範自体が無い形で、性質が違う
5. **`hook-environment`（指摘 57）は今回も出ており、しかも「手順で代替すれば同じ」が崩れた実例が 1 つある** — `implementation-reviewer` の実行中にコミットを挟んでしまい、混入していないことをエージェント側の時刻で切り分けて確認する羽目になった（`block-git-during-verification-agent.sh` が止めるはずの操作）。pr-462 の `git add -A` と同じく、**止めたのは規律であってゲートではない**形が 2 回連続で出ている
