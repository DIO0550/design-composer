# PR #605 pr-601 の max-lines 導入で残った 3 点（抑制の迂回路・rules への宣言・Step 3 の見送り行）

- マージ日: 2026-09-19
- 関連 Issue: #616（着手時は #600。下記「ゴールと結果」）
- 差分規模: 7 ファイル / +37 -3

## ゴールと結果

**着手時のゴールは Issue #600「`module-api` を 2b で層=hook へ上げる」だったが、達成したのは
別のゴールになった。** 同じ Issue を別セッションが並行して実装し、**PR #601 が先にマージされた**
ため、#601 と重なる部分をすべて main 側へ寄せ、重なっていなかった 3 点だけを残す形へ絞り直した
（#616 を新しく立てて `Closes` を付け替えた）。

最終的に入ったのは次の 3 点。

1. `lint-suppressions.py` の検出へ `oxlint-disable` の綴りを追加（#601 が入れた層 1 が
   1 行で迂回できる状態だったのを塞いだ）
2. `rules/architecture.md`「モジュールの公開API」へ `.tsx` の 600 行制限を 1 行宣言
   （`max-depth` の先例と対称）。あわせて `.claude/hooks/README.md` へ対象範囲・数え方・
   残る迂回路・エスケープハッチを書いた
3. Step 3（棚卸し）で見送った 6 分類の `見送り` 行を 4 記録へ追加（`見送り` 行はこれが初出）

**`.oxlintrc.json` / `harness/case-law/architecture.md` / `harness/records/pr-588.md` は
main（#601）のまま 1 行も変えていない。** 特に `対策済: module-api 層=hook at pr-601` は、
`at` を書き換えると `count.sh` の数え直しの起点が動くため触っていない。

## 指摘

### 1. 同じ Issue を 2 つのセッションが並行して実装し、一方が丸ごと捨てになった

- 分類: `なし`
- 出どころ: `自己修正`
- 内容: Issue #600 に紐づくセッションが 2 つ起動しており、**こちらが PR #605 を出した時点で
  もう一方の PR #601 が既に存在していた**（気づいていない）。#601 は 02:20 にマージされ、
  Issue #600 は閉じられた。こちらが気づいたのは **1 時間後の定期チェックインで main を
  取り込んだとき**で、`mergeable_state` が `clean` から `dirty` に変わったのが最初の兆候。
  `harness/records/pr-533.md` が #532 / #533 で同じ形を記録しており、**そのときの対処
  （重複部分は main 側を採る・`対策済` の `at` 番号は動かさない）をそのまま適用できた**。
  着手時に PR 一覧を引いていれば重複に気づけた（pr-573 の「うまくいったこと」が
  まさにこれを挙げている）。
- 既存ルール: なし（`AGENTS.md` にも `implementation-flow` にも「着手前に同じ Issue の
  既存 PR を確認する」手順は無い）
- 次にどう防ぐか: 今回は記録のみ（`pr-533` / `pr-573` に続いて 3 回目の形。数えるのは
  `harness-growth` の管轄）

### 2. 入れたばかりの層 1 が、1 行のコメントで迂回できる状態だった

- 分類: `comment-behavior-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `.claude/hooks/README.md` へ「エスケープハッチは `// @lint-suppress-ok` を書くと
  外れる」と書いたが、**綴りを実際に試さず先例からの類推で書いていた**。検証エージェントが
  実測したところ、`lib/lint-suppressions.py` の `SUPPRESSION` は `biome-ignore` /
  `eslint-disable` の 2 綴りしか見ておらず、oxlint が同じく読む `oxlint-disable` は
  素通りしていた。つまり `// oxlint-disable eslint/max-lines` の 1 行で、
  `@lint-suppress-ok` 無しに、`block-lint-suppress.sh`（層 3）にも
  `check-added-lint-suppressions.sh`（層 1・CI・pre-push）にも引っかからずに
  **#601 が入れた層 1 を無効化できた**。`max-lines` に限らず oxlint の全ルールに効く穴。
  同じ差分で `SUPPRESSION` を `(es|ox)lint-disable(-next-line|-line)?` へ広げて塞いだ
  （既存の抑制は `biome-ignore` 3 件だけで新しい違反は 0 件）。
- 既存ルール: rules/coding.md「外部の挙動は動かして確かめる」/ `claim-verification`
  「動かして確認する主張（comment-behavior-claim）」
- 次にどう防ぐか: 今回は記録のみ

### 3. 判例の「`implementation-reviewer` の観点が毎回捕まえている」が事実と違った

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 判例へ「未使用 export の形は `implementation-reviewer` の観点が毎回捕まえている」と
  書いたが、記録を引き直すと **`pr-258#7` / `pr-261#8` / `pr-286#10` は出どころが
  `plan-reviewer`**（いずれも計画の「消すもの」の欄が空だったことが起点）。片方だけを
  名指しすると、次にこの形が出たときに `plan-reviewer` 側の観点が見落とされる。主語を
  「計画検証・実装検証の観点」へ広げた（最終的にこの節は main の判例を採ったので差分には
  残っていない）。
- 既存ルール: `claim-verification`「読んで確認する主張（comment-structure-claim）」
- 次にどう防ぐか: 今回は記録のみ

### 4. 見送りの理由を 4 つの記録へ一字一句同じ形でコピーしていた

- 分類: `duplication-rationale`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `pr-192.md` / `pr-211.md` / `pr-219.md` / `pr-233.md` へ、見送りの理由 5 行を
  逐語コピーしていた。`harness-growth` SKILL.md が求めているのは「**理由を PR に書き**、
  その分類が最後に出た記録へ ``- 見送り: `<分類>` at pr-<番号>`` を足す」で、記録側に
  本文は要らない。参照 1 行へ置き換えた（-55 行）。
- 既存ルール: rules/coding.md「規約の適用範囲」（同じ判断根拠を説明する doc・README の重複）
- 次にどう防ぐか: 今回は記録のみ

### 5. 判例が `.claude/hooks/README.md` の理由を書き写していた

- 分類: `duplication-rationale`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「閾値の根拠は README に集め、判例は所在だけ」という意図で書いたつもりだったが、
  実際には `__tests__` を外した根拠と `.ts` の迂回路の説明が README と判例にほぼ同文で
  入っており、さらに `pr-588.md` が 3 つ目の写しを持っていた。判例からは落として
  README への参照 1 行にした。
- 既存ルール: rules/coding.md「規約の適用範囲」
- 次にどう防ぐか: 今回は記録のみ

### 6. `rules/` に閾値の宣言が 1 行も無かった

- 分類: `harness-process-drift`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `max-lines` は `rules/` 全体を grep しても 0 件だった。判例と
  `.claude/hooks/README.md` は常時ロードされないので、次に `.tsx` を書く人は CI か push が
  落ちて初めて閾値の存在を知る。**同じ形の先例が `rules/coding.md:149`
  （「ブロックのネストは3段まで(lint の `max-depth` で強制)」）にあり、非対称だった。**
  対称な 1 行を `rules/architecture.md`「モジュールの公開API」へ足した（常時ロード 845/900 行）。
- 既存ルール: `implementation-flow`「push 前に確認するコマンド」/ rules/coding.md「規約の適用範囲」
- 次にどう防ぐか: 今回は記録のみ

### 7. `count.sh --shrink`（Step 1 の必須 2 本目）を走らせていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `harness-growth` SKILL.md は「**2 つとも必ず走らせる**」と明記しているが、
  計画は `count.sh` だけを走らせていた。指摘を受けて実行したところ **Step 3 の節 1 に
  6 件該当**しており、`見送り` 行 6 本という差分がまるごと計画から漏れていた。
- 既存ルール: `.claude/skills/harness-growth/SKILL.md`「Step 1」
- 次にどう防ぐか: 今回は記録のみ

### 8. 2b が明文で要求する「`.claude/agents/` へもう 1 項目足す案の却下」を書いていなかった

- 分類: `plan-rejection-coverage`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 2b は「すり抜けが 2 以上なら…`.claude/agents/` へもう 1 項目足す案は、**採らなかった
  理由を書いて捨てる**」と規定している。計画にあったのは「既存の観点を参照 1 行へ縮める案」の
  却下で、これとは別物だった。
- 既存ルール: `.claude/skills/harness-growth/SKILL.md`「Step 2b」
- 次にどう防ぐか: 今回は記録のみ

### 9. 2c（語彙を割る）を検討した記録が無かった

- 分類: `plan-rejection-coverage`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `module-api` は再発 28 で 2c の閾値（介入後 5 回以上）も満たしており、計画自身が
  3 つの亜種を名指ししていた（＝2c が言う「別種の問題を 1 つのタグに畳んでいるサイン」
  そのもの）。2b 優先で進めること自体は手順どおりだが、**2c を採らなかった理由が無い**のは
  却下案の網羅漏れ。指摘を受けて、帰結（`対策済: module-api 層=hook` でタグ全体の最終介入が
  hook になり、次に未使用 export が 2 件出たとき「hook の上」が無い）まで書き足した。
- 既存ルール: `implementation-flow` フェーズ 3 手順 6
- 次にどう防ぐか: 今回は記録のみ

### 10. 却下案の判定文が、採用した案にもそのまま当たっていた

- 分類: `plan-rejection-reasoning`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 閾値 650 を退ける理由を「620 行のファイルが 1 行増えた瞬間に落ちる位置に閾値が来て、
  閾値が現状最大に引きずられる」と書いたが、**採用した 600 も非テスト最大 575 の 25 行上**で
  同じ性質だった。理由を別の軸（捕まえたい pr-240 の 716 行と現行最大 575 の間に置く／
  余裕が小さいのは意図）へ書き直した。
- 既存ルール: `implementation-flow` フェーズ 3 手順 5
- 次にどう防ぐか: 今回は記録のみ

### 11. `.stories.tsx` の扱いが決まっていないのに「未決の判断: 無し」で締めていた

- 分類: `plan-open-decision-declaration`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `src/**/*.tsx` は `.stories.tsx` も対象にするので、「`__tests__` は外す /
  `.stories.tsx` は残す」という非対称が理由なしで計画に入っていた。検証エージェントが
  probe で「650 行の `index.stories.tsx` は error になる」ことを確かめて指摘した。
- 既存ルール: `plan-review.md`「未決の判断が残っていないか」
- 次にどう防ぐか: 今回は記録のみ

### 12. ゴールの検証手段が完了条件に書かれていなかった

- 分類: `plan-goal-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: ゴールは「`.tsx` の肥大化を oxlint で機械的に止められる状態」だが、
  **`pnpm exec oxlint` が 0 件であることは証明にならない**（ルールが効いていなくても 0 件）。
  閾値超えの `.tsx` を置いて error になることまでが完了条件で、それが計画の「実測済みの事実」
  には書かれていたが完了条件としては書かれていなかった。
- 既存ルール: `plan-review.md`「ゴールの達成を確かめる手段」
- 次にどう防ぐか: 今回は記録のみ

### 13. `.claude/hooks/README.md` が計画の変更ファイル表に無かった

- 分類: `plan-file-omission`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `harness-growth` SKILL.md「弱めるなら、その理由といつ強められるかを
  `.claude/hooks/README.md` へ書く」に該当する差分（`__tests__` の除外）なのに、README が
  ファイル表に無かった。README は既に各検査ごとに「導入時点の違反件数・絞った理由・
  絞りを外せる条件・エスケープハッチの有無」を揃った体裁で持っており、新しい override だけが
  そこに無い状態になっていた。
- 既存ルール: `plan-review.md`「変更で壊れる既存の資産を洗い出しているか」
- 次にどう防ぐか: 今回は記録のみ

### 14. 記録の件数を `tail -5` の出力で数え、25 本を 5 本と読んでいた

- 分類: `plan-scope-count-verification`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 計画に「`module-api` が出た記録は 482 / 497 / 544 / 582 / 588」と書いたが、
  これは `grep -ln ... | tail -5` の出力をそのまま総数として読んだもの。実際は
  **pr-152 〜 pr-588 の 25 本**。結論（pr-588 が最新）は変わらないが、列挙が誤っていた。
  **`harness/records/pr-573.md` の「うまくいったこと」が、まったく同じ形
  （`tail` の出力を総数と読んだ／`wc -l` を取っていれば初回で分かった）を記録している。**
- 既存ルール: `implementation-flow` フェーズ 3 手順 2（項目を 1 つずつ突き合わせる）
- 次にどう防ぐか: 今回は記録のみ

### 15. 引用した記録の行番号が 2 行ずれていた

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: PR 本文が `harness/records/pr-219.md:357-361` を「`module-api` の申し送り」として
  引いていたが、実際は **359-362**。しかも引用の要になる「次回層 1 へ上げる候補」の一文が
  361 の外（362 行目）に落ちていた。引用の中身自体は正しいので結論は変わらない。
- 既存ルール: `claim-verification`「読んで確認する主張（comment-structure-claim）」
- 次にどう防ぐか: 今回は記録のみ

### 16. 「非テスト 8 件」の例として挙げた 1 件がテストファイルの中だった

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 判例と PR 本文に「indexed access 型は `src/` に 21 件・うち非テスト 8 件。
  `CSSProperties["cursor"]` / `DocumentSaveState["kind"]` / `ArtboardBoxProps["rotation"]` など」と
  書いたが、**`ArtboardBoxProps["rotation"]` は `artboard.type.test.ts:23` にしか無い**。
  「非テスト 8 件」の直後に並ぶので 8 件の例示と読まれる。非テスト側の実例へ差し替えた。
- 既存ルール: `claim-verification`「読んで確認する主張（comment-structure-claim）」
- 次にどう防ぐか: 今回は記録のみ

### 17. 件数を行単位で数え、1 行に 2 つある箇所を取りこぼしていた

- 分類: `plan-scope-count-verification`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: indexed access 型の件数を `grep -rn ... | wc -l`（行数）で 19 件と数えて Issue へ
  書いたが、正しくは `grep -rEo ... | wc -l`（出現数）で **21 件**。1 行に 2 つある箇所を
  取りこぼしていた。却下の結論（汎用化すると誤検知する）は変わらず、むしろ根拠は強くなる向き。
- 既存ルール: `implementation-flow` フェーズ 3 手順 2
- 次にどう防ぐか: 今回は記録のみ

### 18. `.oxlintrc.json` の変更が `plan-reviewer` を通す前に入っていた

- 分類: `なし`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `implementation-flow` は「計画 → 計画の検証 → 実装」の順だが、計画を渡した時点で
  既に `.oxlintrc.json` が変更済みだった（検証エージェントが `git diff --stat` を見て
  報告している）。フェーズ 3 手順 3「想定した前提が実例で成立するかを**実際に動かして**
  確かめる」を踏むと設定ファイルを触ることになる、という形で、手順そのものが
  「検証前に実装へ触れる」ことを要求している面がある。
- 既存ルール: `implementation-flow` フェーズ 4（実装に入る前に `plan-reviewer` へ渡す）
- 次にどう防ぐか: 今回は記録のみ

### 19. oxlint の override の glob を、リポジトリ外へ置いた設定で検証しかけた

- 分類: `tool-behavior-unverified`
- 出どころ: `自己修正`
- 内容: `max-lines` の override が効くかを確かめるとき、検証用の設定ファイルを
  スクラッチパッド（`/tmp` 配下）へ置いて `oxlint -c` で走らせた。`src/**/*.tsx` が 1 件も
  一致せず、**「既存の `no-restricted-imports` の overrides も含めて glob が効いていない」
  という誤った結論を出しかけた**。`overrides.files` の glob は**設定ファイルのある
  ディレクトリ基準**で解決されるため、リポジトリ直下へ置き直すと一致する。
  6 通りの glob を試してから気づいた。
- 既存ルール: rules/coding.md「外部の挙動は動かして確かめる」
- 次にどう防ぐか: 今回は記録のみ（確かめること自体はしていたが、**確かめる道具の置き場所**が
  結果を変える形だった）

### 20. PR コメントに貼ったテストケース表が、スコープ変更後に古いまま残った

- 分類: `comment-premise-drift`
- 出どころ: `自己修正`
- 内容: PR コメントとして 2 回テストケース表を投稿したが、どちらも #601 マージ前の設定
  （素の行数 + `__tests__` 除外 / `対策済 at pr-605`）を前提に書かれている。PR 本文は
  スコープ変更時に書き直したが、**コメントは編集していない**ので、マージ後の PR を読むと
  本文とコメントで違う設定を説明している状態になった。`claim-verification` は
  「Issue/PR 本文の事実の主張」を対象に含むが、**一度投稿した PR コメントが後の変更で
  古くなる形**は手順に入っていない。
- 既存ルール: `claim-verification`（Issue / PR 本文の事実の主張）
- 次にどう防ぐか: 今回は記録のみ（マージ後の PR へ訂正コメントを 1 本足した）

### 21. `echo hook-canary` が deny されず、`.claude/hooks/` が発火しない環境だった

- 分類: `hook-environment`
- 出どころ: `自己修正`
- 内容: push 前にカナリアを実行したところ deny されずに通った。
  `.claude/hooks/README.md`「発火しているかを確かめる」の表で切り分けたところ、
  **このセッション ID の検証エージェントのマーカーも発火ログの見出しもどちらも無い**ので、
  取りこぼしではなく本当の不発（表の 4 行目）。`plan-reviewer` を 1 度通した後に確認して
  いるので「着手直後だからマーカーが無い」枝ではない。`issues.assigned` webhook 起動で
  不発になった PR #192 と同じ形。push 前検査 11 本は 1 つずつ独立に実行して全て緑で、
  `npx` / `git add -A` などの禁止コマンドは実行していないので、素通りした操作・欠陥は無い。
  git hooks は push のたびに実際に走っている（出力で確認）。
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ（ゲートは git hooks と CI にあるので実害無し）

## 手戻り

- **差分の 3/5 を捨てた。** #601 が先にマージされたため、`.oxlintrc.json`（12 行）・
  `harness/case-law/architecture.md`（45 行）・`harness/records/pr-588.md`（19 行）を
  main 側へ寄せて破棄した。コミット 4 本ぶんの内容のうち、最終的に残ったのは
  `lint-suppressions.py` の 3 行と `rules/` の 1 行、README、`見送り` 行だけ
- **`rules/architecture.md` と `.claude/hooks/README.md` を書き直した。** どちらも
  こちらのセッションが入れようとしていた設定（素の行数 + `__tests__` 除外）を説明しており、
  main に載っている設定（空行・コメントを除いた行数・除外なし）と食い違っていた。
  `__tests__` が除外されているから 620 行のテストが通る、ではなく、空行 96 行を引くから
  通る、という違いを probe で測り直して書き直した
- **`見送り` 行 6 本の `at` を pr-605 → pr-601 へ、理由の参照先を PR #605 → Issue #600 へ
  付け替えた**（見送りの判断をしたのは pr-601 のサイクルなので）
- **計画の却下理由 1 件を書き直した**（指摘 10）
- **変更ファイルを 5 本足した**（指摘 7・13。README と `見送り` 行 4 記録）
- **未決の判断 1 件を確定させた**（指摘 11。`.stories.tsx` を対象に含める）
- **列挙 1 件・件数 1 件・行番号 1 件を訂正した**（指摘 14・17・15）
- **`Closes` の付け替えと Issue の新規作成。** #600 が #601 で閉じられたため、
  残った 3 点で #616 を立て直した

## 発火

- 発火: 計測対象外(フック不発)

発火ログ（`${TMPDIR}/design-composer-firings-<session_id>.log`）がこのセッション ID では
存在せず（残っていたのは別セッション `50f8c937-...` の分）、検証エージェントのマーカーも
無い。`.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」の表の 4 行目に当たる
（`分類: hook-environment` / 指摘 21）。**実際には `implementation-flow` /
`harness-record` の 2 スキルと `plan-reviewer` / `implementation-reviewer` の
2 エージェントを起動しているが、計測できていないので「無し」とは書かない。**

## うまくいったこと

- **定期チェックインが並行実装の重複を拾った。** イベント（webhook）は「main が動いた」
  ことを教えてくれないので、1 時間ごとに `mergeable_state` を見る check-in が無ければ、
  重複に気づかないまま「レビュー待ち」で止まり続けていた
- **前例（`pr-533.md`）がそのまま使えた。** 「重複部分は main 側を採る」「`対策済` の
  `at` 番号は動かさない（`count.sh` の起点が動く）」という 2 つの判断を、その場で考えずに
  記録から引けた。記録を残す仕組みが、まさに同じ事故の 2 回目で効いた
- **検証エージェントの指摘を鵜呑みにせず自分で測り直した。** `oxlint-disable` の迂回路は
  3 綴りを自分で走らせて再現してから直している。行番号のずれ（指摘 15）と記録の件数
  （指摘 14）も、指摘された数字をそのまま採らず `grep` し直して確かめた
- **捨てる差分と残す差分を機械的な基準で分けられた。** 「#601 と重なるか」だけで判定し、
  設計の優劣を議論しなかったので、判断に時間を使わずに済んだ

## 規約への反映

**無し。** このスキルの管轄は記録を 1 ファイル足すところまでで、集計と介入は
`harness-growth` が行う。

今回の 21 件のうち、`分類: なし` が 2 件（指摘 1 の並行実装・指摘 18 の手順の順序）。
**指摘 1 は `pr-533.md`（#532 / #533）と同じ形で、少なくとも 2 回目。** `pr-573.md` も
「重複 PR を出す前に既存 PR を検出できた」を「うまくいったこと」に挙げており、
裏を返せばそこが落ちやすい箇所だと分かっている。数えるのは `harness-growth` の管轄。

指摘 14（`tail` の出力を総数と読む）も `pr-573.md` が同じ形を記録している。
`対策済` / `見送り` の行は書いていないので、`count.sh` が数える再発数の起点は動かない。

`tool-behavior-unverified`（指摘 19）は `層=rules at pr-444` で一度介入済みだが、その後
pr-573 / pr-604 で 2 件再発したため、`harness-growth`（#696）が判例（層=case-law）へ引き上げた
（`harness/case-law/process.md`「`tool-behavior-unverified` — `対策済` 行の PR 番号を書き間違える」）。

- 対策済: `tool-behavior-unverified` 層=case-law at pr-697
