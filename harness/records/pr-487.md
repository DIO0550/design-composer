# PR #487 🐛 キャンバス下端の帯がポインタを奪うのをやめる

- マージ日: 2026-09-12
- 関連 Issue: #466
- 差分規模: 3 ファイル / +85 -17

## ゴールと結果

キャンバス下端に浮くツールバー／エラー一覧の透明な余白でも、ノードの選択・範囲選択・パンが
始められる。見えている部品の上では従来どおり操作が効く。達成した。

Issue が「実装前に確認したい」としていた候補 1 / 2 の判断は、回答を待たずに実測で閉じた。
候補 2（帯を中身の幅にする）は 3 箇所の見た目を動かすことが実物から分かり、選択肢として
成立していなかったため（`document-error-list` の `w-full` と `absolute inset-x-0 bottom-0`、
`token-dashed-nodes` の `self-start`）。判断と根拠は Issue へ書き、異論があれば PR で
止められる形にした。

計画には無かった変更が 1 つ入った。器を出し分けの外へ畳むリファクタリング（指摘 7）。

## 指摘

### 1. テストヘルパーに付けようとした `canvasDock` が、同じファイルの既存関数と衝突していた

- 分類: `naming-prefix-collision`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `setup.tsx` へ DOM を返す `canvasDock()` を足す計画だった。`index.tsx:158` に
  既に `canvasDock(state: EditorState): CanvasDock`（どちらの状態か・そこで出すエラーは
  何かを返す）があり、同じモジュールフォルダの中で `canvasDock` が「状態の判定」と
  「画面上の帯」の 2 つを指すことになる。計画では `CanvasDockStack`（PascalCase）だけを
  grep し、**小文字側を数えていなかった**のが原因。`canvasDockStack()` に変えた
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ

### 2. 帯を指す経路（ツールバーの `parentElement`）が、ファイルが不正な枝では成立しなかった

- 分類: `plan-scope-premise-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画は帯を「キャンバスのツールバーの `parentElement`」で引く形だった。
  **ファイルが不正な間はツールバーが出ない**ので、その枝では辿る起点が無い。加えて
  `setup.tsx` の既存アクセサは全件が `getByRole` / `getByLabelText` で、構造参照はこれ
  だけになる。想定した接続先を実例に当てずに計画へ書いたのが原因。器へ `data-testid` を
  付けて直接指す形に変えた
- 既存ルール: implementation-flow フェーズ 3 手順 3（想定した接続先が実例で成立するかを
  実際に動かして確かめる）
- 次にどう防ぐか: 今回は記録のみ

### 3. 変更**後**に何を見たら達成といえるかが計画に無かった

- 分類: `plan-goal-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画にあったのは変更**前**の実測だけだった。この回はテスト（happy-dom は Tailwind を
  読まない）・VRT（見た目が変わらない）・story の `play`（撮影が間に合わないと失敗が見えない）
  の 3 つが全部効かないので、確認手段は手動のブラウザ計測 1 択になる。それが計画に書かれて
  いなかった。3 ストーリーの `elementFromPoint`、帯の余白からのドラッグ、ツールバー／
  `Reveal` ／ `revert file` が押せること、を完了条件として足して実行した
- 既存ルール: `plan-review.md`「ゴールの達成を確かめる手段」
- 次にどう防ぐか: 今回は記録のみ

### 4. 却下案の理由が、実測した範囲より広く読める形になっていた

- 分類: `plan-rejection-reasoning`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「インラインスタイルで書くと既存テストが 13 件落ちる」は**器だけにインラインを
  置いた形**の実測なのに、理由を「インラインは不可能」と一般化して書いていた。`user-event` の
  `closestPointerEventsDeclaration` は対象から上へ辿って最初の宣言で止まるので、子にも
  インラインがあれば 13 件は落ちない。正しい理由は「子へ宣言を配る手段（ラッパー /
  `cloneElement`）が積み方を壊すので採れない」。ラッパー案を実測して却下し直した
  （空のラッパーが `gap-3` を食って 12px ずれる / `self-start` が左端 8px から中央 258px へ動く）
- 既存ルール: implementation-flow フェーズ 3 手順 5（却下案の理由が実際のコードに照らして
  成立しているかを検算する）
- 次にどう防ぐか: 今回は記録のみ

### 5. 透過するのが選択・範囲選択・パンだけではないことが計画に書かれていなかった

- 分類: `plan-implicit-condition`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `pointer-events:none` は帯の透明部分の**すべての**ポインタ操作を通す。帯が当たり先で
  なくなることで、ドロップ先の解決（`use-node-drag` は `namesToRoot(context.event.target)` で
  名前を辿る）・右クリックのコンテキストメニュー・ダブルクリックの掘り・ホバーのカーソルも
  同時に変わる。ゴールに書いていない挙動が変わる形だった。いずれも望ましい向きだったので
  そのまま進め、Issue と確認項目へ明記した（右クリックは実際に確認し、空き領域のメニューが
  出ることを見た）
- 既存ルール: `plan-review.md`「既存のコードが前提にしている不変条件を、この計画が崩さないか」
- 次にどう防ぐか: 今回は記録のみ

### 6. 採った形の「失敗の向き」が doc に無かった

- 分類: `comment-missing`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 戻す指定を直下の子すべてに掛ける形は、**子が全面に広がる透明なラッパーになると
  症状が黙って戻る**。この失敗の向きを書いておかないと、案 B（部品ごとに書く）を再検討する
  人が同じ検討を一からやり直すことになる。doc へ 1 文で足した
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ

### 7. ファイルが不正な枝の器を素の div へ差し替えても、206 件すべて通った

- 分類: `test-coverage-branch-asymmetry`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `CanvasDockContent` が `<CanvasDockStack>` を両枝で繰り返していたため、片方の枝だけ
  器を失っても何も落ちない状態だった。ミューテーションで実測して示された。しかもファイルが
  不正な状態はエラー一覧がいちばん背が高い＝透明な余白がいちばん広い枝で、この Issue の
  効き目が最も大きいところ。テストを 1 件足す案より、**器を出し分けの外へ畳んで重複そのものを
  消す**案を採った（`duplication-branch-shape` でもあるため）。畳んだ後は「器を通さない」
  ミューテーションで落ちるようになった
- 既存ルール: rules/testing.md「その assert は落ちうるか」/ `implementation-reviewer`
  「テストが守っているかの観点」
- 次にどう防ぐか: 今回は記録のみ

### 8. doc に書いた「当たり判定もテストでは落ちない」が、同じ差分のテストと食い違っていた

- 分類: `comment-behavior-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 既存の「**この位置指定を落としてもテストは落ちない**」という段落へ当たり判定を
  合流させて「位置指定も当たり判定も落としてテストが落ちることはない」と書いた。当たり判定は
  **この差分で足したテストがまさに見ている**ので落ちる。元の文は位置指定についてのみ真で、
  合流させた結果その半分が嘘になった。読んだ人が「クラスを消しても誰も気づかない」と思って
  テストごと消せてしまう形。2 段落に分け、当たり判定は「テストが見られるのはクラスの綴りまで」
  と書き直した
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 9. `setup.tsx` の doc が、同じファイルの実装と食い違っていた

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「中身の親から辿らないのは、ファイルが不正な間は**ツールバーも破線の帯も出ず**、
  辿る起点が無くなるため」と書いたが、`index.tsx` のその枝は `TokenDashedNodes`（破線の帯）を
  出している（同じ枝のコメントが「壊れる前に選んでいたトークンの破線はキャンバスに残る」と
  明記している）。逆にその枝ではエラー一覧が必ず 1 件以上出るので「起点が無い」のもこの枝では
  ない。起点が無いのは編集可能・エラー 0 件・トークン未選択のとき。「常に出ている子が 1 つも
  無いため」に直した
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 10. 同じ事実の doc が 3 箇所に割れていた

- 分類: `duplication-uncovered-shape`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「帯が幅いっぱいなので余白はキャンバスへ通す」が `index.tsx` の本文・`@returns` の
  追記・テストファイル先頭の 3 箇所に、「happy-dom は Tailwind を読まない」が `index.tsx` と
  テスト本体の 2 箇所にあった。片方だけを直せる状態。`@returns` を元の 1 行へ戻し（本文で
  書いた理由を戻り値説明で言い直していた）、テスト側は 2 ブロックを 1 つに寄せてテスト固有の
  事情だけを残した
- 既存ルール: rules/coding.md「規約の適用範囲」（同じ判断根拠を説明する doc の重複にも
  適用する。片方だけを直せる状態を作らない）
- 次にどう防ぐか: 今回は記録のみ

### 11. 却下理由を doc に 5 行書いていた（経緯は Issue が持つべき分だった）

- 分類: `なし`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: ラッパー案を却下した理由を、実測の数字（`gap-3` を食って 12px ずれる / `self-start` が
  8px から 258px へ動く）ごと doc へ 5 行書いていた。rules/coding.md は「理由は 1 つにつき
  1〜2 行。3 行以上要るならそれは Issue に書くべき経緯」と書いているが、**分類の語彙に
  「理由が長い」に当たるものが無い**。3 行へ詰め、数字は Issue へ置いた
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」（理由は 1 つにつき
  1〜2 行）。分類の語彙には対応が無い
- 次にどう防ぐか: 今回は記録のみ（語彙の抜けの候補）

### 12. class 名を assert する形はこのリポジトリの先例と逆なのに、その理由が書かれていなかった

- 分類: `comment-missing-divergence`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: class にしか出ない事実について、既存の 3 箇所（`drop-line` / `canvas-toolbar` /
  `document-error-list`）はいずれも**テストを書かず doc で開示する**側を選んでいる。今回だけ
  `classList.contains` を assert したのに、なぜ先例から外れたのかがどこにも無かった。
  テストを残す判断は維持し（それらは崩れれば絵に出るが、当たり判定は見た目が 1px も変わらず
  視覚差分でも拾えないので、綴りを見るのが唯一落ちうる形）、その理由をテスト先頭へ 1 行足した。
  失敗メッセージも欠けたクラス名が出る形へ変えた
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」（既存のパターン・
  先例から意図的に外れた判断の理由）
- 次にどう防ぐか: 今回は記録のみ

### 13. `echo hook-canary` が deny されなかった（フック不発環境）

- 分類: `hook-environment`
- 出どころ: `自己修正`
- 内容: push 前のカナリアが通ってしまい、`.claude/hooks/`（層 3）が発火していない環境だった。
  禁止コマンドは実行していない（`npx` は使わず `pnpm exec` のみ、`git add -A` / `git add .` は
  0 回で個別に `git add`）。push 時に git hooks（層 2）は実際に発火し（`pre-push: テスト規約`
  まで出力）、CI（層 1）も全緑だったので実害は無かった。
  **この報告は #488（カナリアが取りこぼしを「フック不発」として報告する）の誤検知ではない。**
  #488 が直したのは `echo hook-canary && echo done` のように区切りで連ねた形の取りこぼしで、
  この回に実行したのは連ねていない `echo hook-canary`。当時の判定
  `^[[:space:]]*echo[[:space:]]+['\"]?hook-canary['\"]?[[:space:]]*$` に一致するので、
  発火していれば deny されたはず。発火ログにこのセッションの見出しが無いこととも整合する
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

3 つ。いずれも実装を出し直す前に止まっている。

1. **計画の書き直し**（指摘 1〜6）。`plan-reviewer` の 6 件はすべて実装前に出たので、コードの
   やり直しは発生していない。書き直したのは計画（ヘルパー名・帯の指し方・完了条件・却下理由・
   同時に変わる挙動）
2. **器を出し分けの外へ畳むリファクタリング**（指摘 7）。計画に無かった変更。`file-invalid` 枝が
   守られていないことが実装の検証で実測されたため、当たり判定を足す前に器の判断を 1 箇所へ
   集めた。コミットは分けた（♻️ `af68f35` / 🐛 `81f5edb`）
3. **doc の `@param` 追加**。`missing-doc-comments.py` が push 前に 1 件検出した
   （`CanvasDockContent` を分割代入から `props` 受けに変えたため `@param` が要るようになった）。
   手元の検査で捕まったので CI は落ちていない

## 発火

- 発火: 計測対象外(フック不発)

## うまくいったこと

- **未決の判断を、意見ではなく実測で閉じた。** Issue が「候補 1 と 2 のどちらか確認したい」と
  していたのを、候補 2 が動かす 3 箇所を実物のクラス（`w-full` / `absolute inset-x-0 bottom-0` /
  `self-start`）と矩形の実測で示して落とした。選択肢として成立していないことが分かったので、
  回答を待たずに進めて差し支えなかった
- **「このテストは何を守れないか」を先に実測した。** 使い捨てのテストを 1 本書いて、インライン
  スタイルなら `user-event` に弾かれ、Tailwind のクラスだけなら素通りすることを確かめた。
  これが実装方針（クラスで書く。インラインだと既存テストが 5 ファイル 13 件落ちる）と
  テストの形（綴りを見る）の両方を決めた
- **VRT の結果を事前に予言できた。** 3 ストーリーの矩形を変更前後で測って一致を確認してから
  push し、実際に 0 差分 / 155 passed で返ってきた。フラグメントが DOM 要素を作らないことを
  実物で確かめていたため
- CI は 1 度も落ちていない（frontend success / VRT 0 差分 / 人のレビュー指摘 0 件）

## 規約への反映

無し。この回に出た 13 件はいずれも既存の規約・観点が既に持っている判断で、規約の抜けでは
なかった（指摘 11 だけは分類の語彙に対応が無く、語彙の抜けの候補として残す）。
数えて介入するかを決めるのは `harness-growth` の管轄なので、ここでは行わない。
