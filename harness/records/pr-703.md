# PR #703 story の title がフォルダ階層から剥がれたら落とす

- マージ日: 2026-09-22
- 関連 Issue: #701
- 差分規模: 14 ファイル / +425 -78

## ゴールと結果

`*.stories.tsx` の `title` がフォルダ階層から剥がれたときに、人が気づく前に落ちるように
する。達成した。検出器（`story-title-violations.py`）と判定表（`story-title-cases.sh`・
22 ケース）を層 1・層 2 へ、検出器だけを層 3 へ置いた。導入時点の違反は 0 件 / 49 本。

この Issue は #669 の「スコープ外」から切り出したもので、#669 自体は綴りを揃える話。
そちらは着手時点で PR #679 が既に達成済みだったため、差分を出さずに判断だけ残して閉じた。

## 指摘

### 1. 同じ事実を列挙している doc が、ファイル表から 4 つ漏れていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `harness/githooks/README.md` の `pre-push` 行と「判定表 3 本」の列挙、
  `implementation-flow/SKILL.md` の「11 個 / 8 つ」、`harness/githooks/pre-push` の
  `else` メッセージ、`.claude/hooks/README.md` の配線表。どれも直さないと嘘になる。
- 既存ルール: なし

### 2. ファイル表を「ファイル名」で書いていて、直す記述を名指ししていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `.claude/hooks/README.md` の「使う側」列は人が列挙している列なので `import` の
  grep では引っかからない。ファイル名だけの表では、同じファイルの中の直すべき記述に
  辿り着けない。
- 既存ルール: なし

### 3. 却下理由に書いた件数が、どの数え方とも一致しなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 層 3 を省かない理由を「既存検出器 5 つがすべて層 3 の入口を持つ」と書いたが、
  `pre-push-*.sh` は 6 本、`lib/` の検出器を包むのは 4 本で、さらに層 1+2 にあって層 3 を
  持たない反例が 6 つある。結論は妥当だったが根拠が事実と食い違っていた。
- 既存ルール: なし

### 4. 採用案を他案より採った根拠を突く deny ケースが判定表に無かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「中間の PascalCase セグメントも見る」を採用の根拠にしながら、そのセグメントを
  落とした title の deny が無かった。実装が実質「小文字部分だけ比べる」へ退化しても
  判定表は全件緑のまま通る。
- 既存ルール: rules/testing.md「その assert は落ちうるか」

### 5. 導出規則が対象にしていない置き場所の挙動が決まっていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `src/domains/` 等の層、feature 内の `components` 以外のフォルダ、`components` が
  2 回出る場合。いずれも現状 0 件なので、黙って通っても黙って落ちても実データからは
  気づけない。
- 既存ルール: なし

### 6. 「未決」に挙げたものが計画の中で既に決まっていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `story-title-missing` を未決欄に置きながら、同じ計画で違反種別の一覧・判定表・
  落とす理由まで書いていた。一方で本当の未決（指摘 5）は欄に無かった。
- 既存ルール: なし

### 7. probe が置ける形かを確かめずに「probe を足す」とだけ書いていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: README は probe に「doc 付き・整形済みで、モジュールフォルダの外に置く」を課して
  いる。story の probe は置き場所そのものが期待出力を決めるので、パス・中身・期待出力まで
  計画に書く必要があった。
- 既存ルール: なし

### 8. glob の綴りを検出器へ写すと、片側だけが古くなる形だった

- 分類: `duplication`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `.storybook/main.ts` の `@(ts|tsx)` を Python 側へ写しても、`main.ts` を変えた
  ときに検出器は追随せず誰も気づかない。`main.ts` 側へ検出器を指すコメントを置いて
  両方向から辿れる形にした。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 9. 新しい識別子名の grep を計画に書いていなかった

- 分類: `naming`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 衝突は実際には 0 件だったが、探した事実が計画に無かった。あわせて違反種別
  `story-title-path` は「title が path である」とも読め、判定しているのがツリー上の位置
  だと綴りから読めない。`story-title-tree` へ改めた。
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」

### 10. 検出器がファイル全体の最初の `title:` を拾い、既定 export の外の値と取り違えた

- 分類: `harness`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: meta より前にモジュールレベルの `title:` があると、Storybook が実際に使う綴りを
  一度も見ないまま通る。`left-pane-panel` が `title: string` の props を持つので、その
  ストーリーが `args` を定数へ切り出した瞬間に踏む。`title:` が 2 本以上なら
  `story-title-missing` に倒した。
- 既存ルール: なし

### 11. 判定表が落とさないミューテーションが 4 つあった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 層セグメントを比較しない / tree が空なら検査しない / `.stories.tsx` だけ走査する /
  `title:` が 2 本以上でも先頭を採る。検出器へ実際に変異を当てて実測された。対になる
  ケースを足して 14 → 22 ケースにし、4 つとも落ちることを確認した。
- 既存ルール: rules/testing.md「その assert は落ちうるか」

### 12. 実装中に足した早期 return が no-op だった

- 分類: `over-guard`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `if not rest: return segments` は、下の `rest[:-1]` が同じ値を返すので通っても
  通らなくても結果が同じ。削除しても判定表 16 ケースすべて ok のままだった。
- 既存ルール: なし

### 13. 段数チェックが論理的に冗長で、付けたコメントの帰属も誤っていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `title[:-1] == tree` が成り立てば `len(title) == len(tree) + 1` は必ず成り立つ。
  「段数は数えるので、足しても落ちる」と書いたが、その deny を落としているのはリスト比較の
  ほうで、段数チェックを消しても判定表は全件 ok。
- 既存ルール: rules/coding.md「Why と Why not は別物として書く」

### 14. 判定表の報告部分を、既存の判定表から一字一句コピーしていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `report()` と判定の読み取り、`miss` の正規化が `result-option-read-cases.sh` と
  同一。既存の Why not（判定表が 3 つのフォルダに割れているので共有先を作らない）は、
  同じ `.claude/hooks/lib/` に並ぶこの 2 本には当たらない。`cases-report.sh` へ切り出した。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 15. 判断根拠の段落を、既存の判定表から写していた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「判定は終了コードで見る」の説明が `result-option-read-cases.sh` と一字一句同じ。
  正典は `.claude/hooks/README.md` なので、両方を 1 行の参照へ縮めた。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 16. 移設で `src` の出どころが 2 箇所に割れた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 移設前は `FEATURES_ROOT` も `DOMAINS_ROOT` も `ALIAS_ROOT` 由来だったが、移設後は
  前者が `ts_sources.DEFAULT_ROOT` 由来になり、同じ事実を独立に 2 つ持つ形になった。
  `ALIAS_ROOT` を `DEFAULT_ROOT` から引く形へ寄せた。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 17. 検出器を 1 本挟んだことで、既存コメントの「直前のステップ」が偽になった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `harness/githooks/pre-push` の「直前のステップと同じ検出器の判定表」は、間に
  「story の title」を入れたことで指す先がずれた。「上の 2 つの検出器の判定表」へ直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 18. `harness/githooks/README.md` の列が pre-push の実行順と食い違った

- 分類: `harness`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: この表は従来 pre-push の実行順と 1:1 だったが、足した 2 項目を検出器・判定表の
  順ではなく並べてしまった。実行順へ直した。あわせて同 README の「`python3` が無いとき
  飛ぶ検査」の表にも足りていないことに気づいて直した。
- 既存ルール: なし

### 19. push をブロックする検査が持つはずの「エスケープハッチ」の節が無かった

- 分類: `harness`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 既存の 4 つはすべて「ブロックするか / 見る範囲 / 逃げ道を置かない理由 / 導入時点の
  違反件数」を持っている。今回だけ無かった。`title` が story の id でもあるため、検査に
  合わせてフォルダを動かすと VRT のベースラインが総入れ替えになる点も 1 行足した。
- 既存ルール: なし

### 20. 「対応する規約:」が `rules/` のどこも指していなかった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 兄弟の層 3 フックは `rules/` の節まで名指ししているが、story の title に対応する
  規範は `rules/` に無い（機械判定なのでフックだけが持つ）。その旨を明示して、読み手が
  `rules/` を探しに行かないようにした。
- 既存ルール: なし

### 21. 関数名が返すものと一致していなかった

- 分類: `naming`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `derive_tree` が返すのは tree ではなくセグメントの並び。`structural_segments` と
  語形も揃っていなかった。`tree_segments` へ改めた。
- 既存ルール: rules/naming.md「名前と実体を一致させる」

### 22. 層 3 のフックの案内文に書いたバッククォートを bash が実行していた

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `deny_on_violations` へ渡す案内文に `` `components/` `` と書いたところ、
  コマンド置換として実行され `components/: No such file or directory` を出したうえ、
  案内文からその語が消えていた。probe を実際に走らせて気づいた。読むだけの確認では
  PR まで残る形。他の `pre-push-*.sh` に同じ形が無いことも grep で確認した（0 件）。
- 既存ルール: rules/coding.md「外部の挙動は動かして確かめる」

### 23. 規則の適用漏れで、feature フォルダ直置きの story がどんな title でも通った

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: 葉を差し出すフォルダが無いと導出の最後が feature 名になり、それが「見ない葉」の
  位置へ落ちる。`features/editor/features/<何でも>` が通る、という #669 と同じ形だった。
  `claim-verification` で probe を走らせて発見し、規則を変えずに適用を直した。
- 既存ルール: なし

### 24. 判定表のヘッダに書いた理由が、実測すると逆だった

- 分類: `comment`
- 出どころ: `自己修正`
- 内容: 「絶対パスで渡すと feature の連なりが取れず表が丸ごと pass に倒れる」と書いたが、
  実際は走査ルートより上のフォルダまで導出に混ざり、pass の行まで deny になる。
  `claim-verification` で動かして確かめ、理由の側を実物へ合わせた。
- 既存ルール: rules/coding.md「外部の挙動は動かして確かめる」

### 25. PreToolUse フックが不発の環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されず、検証エージェントのマーカーとこのセッション分の
  発火ログの見出しも両方無し（README の表の 4 行目）。代替の無い `block-npx.sh` /
  `block-git-during-verification-agent.sh` が止めるはずだった操作を手で確認した
  （`npx` 0 件 / `git add -A` 未使用 / 検証エージェント実行中の git 書き込み無し）。
- 既存ルール: .claude/hooks/README.md「発火しているかを確かめる(カナリア)」

## 手戻り

- 計画から外れた 3 件（指摘 22・23・24）。実装中に Issue #701 へ追記済み
- `implementation-reviewer` の指摘による実装の直し 5 件（指摘 10・11・12・13・21）と
  doc の直し 5 件（指摘 15・17・18・19・20）、共通化 2 件（指摘 14・16）。
  マージ後に #701 へ追記済み
- 判定表のケース数は 14 → 16 →（指摘 11 を受けて）22 と 2 度増えた
- `result-option-read-cases.sh`（既存のゲート）を共通化のために触ったため、前後の出力が
  完全に同一であることを diff で確認してから入れた
- CI の失敗は 0 件。4 ワークフローすべて初回で success、レビュー指摘（人・bot とも）も 0 件

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。介入の判断は `harness-growth` の管轄で、このスキルは記録のみを残す。
