# PR #743 harness棚卸し: comment に判例を追加する

- マージ日: 2026-09-24
- 関連 Issue: #742
- 差分規模: 2 ファイル / +21 -0

## ゴールと結果

ゴール(`comment` 分類で再発した 6 件のうち、機械判定できない 5 件が
`harness/case-law/coding.md` から NG / 判定の分かれ目つきで引けるようになり、`comment` の窓が
case-law 層の介入で閉じる)は達成した。`count.sh` で `comment` の再発が 0・起点が
`pr-743（層=case-law）` に変わったことを確認してマージされた。

**ただし同じ Issue #742 に別セッションが並行して着手し、`plan-reviewer` /
`implementation-reviewer` を両方通した PR #744 を別途出していた**(指摘 1)。人は検証を
通していないこちらの PR #743 を選んでマージした。#744 側にしかない改善(`comment-missing` の
判定の分かれ目・`comment-temporary-premise` の OK 例・`pr-718#18` の窓が閉じる旨の明記など)は
この PR には取り込まれていない。

## 指摘

### 1. 同じ Issue を 2 つのセッションが並行して進めた

- 分類: `parallel-issue-work`
- 出どころ: `CI`
- 内容: Issue #742 に自分でコメントを残し着手した直後、Issue の `assignees` 変更で起動した
  別セッションも同じ Issue へ着手し、`Closes #742` を持つ PR #744 を出した。#744 の
  `pr-closing-issue.yml`(`duplicate-issue-pr`)が検出し、その旨と両 PR の比較表を Issue へ
  コメントした。#744 は plan-reviewer(指摘 8 件)・implementation-reviewer(指摘 10 件)を
  通していたが、こちらの PR #743 は検証を通さずに出しており、人はそれでも #743 を選んで
  マージした。#744 は open のまま残っている
- 既存ルール: `.github/workflows/pr-closing-issue.yml`「duplicate-issue-pr」

### 2. このセッションでは implementation-flow の検証(plan-reviewer / implementation-reviewer)を通していない

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: このセッションは「ハーネス棚卸しルーティン」という個別の Routine のプロンプト
  (手順 0〜3)で起動しており、そこに `plan-reviewer` / `implementation-reviewer` を呼ぶ手順が
  無い。同じ Issue #742 に対して通常の着手フロー(`issues.assigned` の webhook)で起動した
  #744 側は `implementation-flow` を通し、両方の検証エージェントから計 18 件の指摘を受けて
  直していた。Routine のプロンプトが `CLAUDE.md`「実装の進め方」の標準フロー(計画の検証→
  実装の検証)を経由しない形で書かれている
- 既存ルール: `CLAUDE.md`「実装の進め方」

### 3. Routine の保存済みプロンプトが `count.sh` の実装・`harness-growth` の手順番号とずれていた

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: Routine のプロンプトの Step 0 は `bash harness/records/count.sh --gate` の実行を
  指示しているが、`count.sh` に `--gate` オプションは実装されていない(`--shrink` /
  `--list` / `--ratchet` のみ)。渡すと未知のフラグとして無視され、既定の表を出して
  exit 0 になる。また Step 2 は `harness-growth/SKILL.md` の「Step 4(4a/4b/4c/なしの
  逃し弁)」「Step 6(棚卸し条件)」を参照しているが、現在の SKILL.md の手順は
  1.数える〜6.PRにするの 6 節で、4a/4b/4c のような枝番も「棚卸し条件」という節も無い
  (現行の該当箇所は節 3「フックにする」の箇条書き 1〜3 と節 5「縮める」の表)。
  SKILL.md がリナンバリングされた後、Routine 側のプロンプトが追随していない
- 既存ルール: `.claude/skills/harness-growth/SKILL.md` / `harness/records/count.sh`

### 4. フックが発火しない実行環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されずに通った。`git push` を複数回行ったセッションだが、
  発火ログ(`record-firings.sh`)にこのセッション ID の見出しが無く、`/tmp` にあったのは
  無関係な別セッション(2026-09-19)のログだけだった。CI 層の検査はすべて通過しており
  抜けは無い
- 既存ルール: `.claude/hooks/README.md`「強制力の序列 — フックが発火しない実行環境がある」

## 手戻り

無し。差分は 1 回の push で CI をすべて通過した(人のレビュー指摘・CI 失敗ともに 0 件)。

## 発火

計測対象外(フック不発)

## 規約への反映

無し。この回は `harness-record`(記録のみ)。`comment` への介入(判例の追記・窓を閉じる
`対策済` 行)は PR #743 自身の差分としてすでに行っており、その記録は `harness/records/pr-734.md`
(介入前に `comment` が最後に付いた記録)に書かれている。

`parallel-issue-work`(指摘1)は、`parallel-issue-work` が最後に付いた記録がこの
`pr-743.md` のため、PR #760(`session-url-notice.sh` へ着手前の確認を追記)の介入で
ここへ追記する。

- 対策済: `parallel-issue-work` 層=skill at pr-760
