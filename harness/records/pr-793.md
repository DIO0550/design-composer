# PR #793 background が colors と gradients の両方を指せるようにする（参照と検証）

- マージ日: 2026-09-26
- 関連 Issue: #608
- 差分規模: 37 ファイル / +1043 -126

## ゴールと結果

ゴールは達成した。`background` は colors と gradients の両方を指せる。dangling にならず、参照元にも数える。パネル用のデータ(`paintToken`)は候補を種別ごとに分けて持つ。
colors と gradients の同名は、開いた時点で `conflicting-token-name` になる。追加・改名・採番でも作れない。
パネルで gradients を選べるようにするのは後続 #791 に切り出した。CSS 出力は #609 のスコープで、この PR では変えていない。
人のレビュー指摘は 0 件(`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも確認。コメントは bot の投稿と自分のテストケース表だけ)。CI は初回で緑で、VRT の差分も 0 件だった。

## 指摘

### 1. 衝突したドキュメントで参照元・エラー位置・失敗の優先順位が決まっていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 同名が両方にあるとき、`background` をどちらの参照元に出すかが計画に無かった。`conflicting-token-name` の `nodeName` と、invalid / duplicate / conflicting が同時に当たるときの順も同じく無かった。3 つとも決めて Issue とテストに入れた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「未決の判断」

### 2. docs が未決としているパネルの見せ方を、計画が実装側で決めていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `colorToken` に colors と gradients の名前を混ぜる計画だった。docs/03「塗り」は、どちらの一覧から選ばせるかを未決としている。#609 までは gradients を選んでも描かれない。
  種別ごとに分けて持つ `paintToken` に変え、見せ方は後続 #791 に切り出した。
- 既存ルール: rules/ui-verification.md「UIの拠り所は `docs/Design Composer.html`」

### 3. #609 までは gradients を選んでも描かれない期間があることを書いていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: compiled-element は先頭の種別(colors)で var を綴る。そのため gradients を指すと参照先の無い var になる。この期間とそれを受け入れる理由を計画・PR に書き、パネルから選べない形にした。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3

### 4. 先頭の種別で var を出す理由をコードに残す予定が無かった

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `kindsOf(prop)[0]` は結果として colors を優先する形で、docs/03 の「片方を優先しない」と逆向きに読める。一時的な事実を根拠にせず、挙動として doc に書いた。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 5. 変更で嘘になるコメント・doc がファイル表から抜けていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「一意性は種別の中だけ」「同じ種別の名前と衝突しない」などの記述が、token・prop-definition・token-props・prop-control・editor-state・use-editor-state・token-editor・token-template・design-document・validation に 15 箇所ほどあった。dangling メッセージの綴り(並びの join)も決まっていなかった。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 7

### 6. 「相手の種別」を 3 箇所で別々に求める計画だった

- 分類: `duplication`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: checkWritableName・uniqueName・collectPaintNameConflicts が、それぞれ PaintTokenKinds から相手の種別を導く形だった。`paintCounterpart` 1 つに寄せた。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 7. 却下理由(任意長にしない)を型で守るテストが無かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `TokenKindList` に `[spacing, colors]` や長さ 3 の並びを代入できないことを `expectTypeOf` で固定した。`readonly TokenKind[]` に広げると型チェックが落ちることも確かめた。
  和の型に `.includes` を呼べない問題は、注釈で受ける関数 1 つで解いた(`as` は使わない)。
- 既存ルール: rules/coding.md「不正な状態を型で表現できなくする」

### 8. 入力の選び方のせいで、壊しても落ちないテストケースが 2 件あった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 1 件目は「別名の gradients を指す background は colors の参照元に出ない」で、名前が違うので種別を見ない実装でも通る。2 件目は「gradients を指すと見本が none」で、変更前の実装でも通る。
  同名を使った対照と、見本が some になる対に差し替えた。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 9. 改名と採番のテストが片方向だけだった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: add は両方向あったが、改名(colors → gradients の名前)と採番(colors 側)は片方向だけだった。逆向きを足した。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 10. パネルの通しテストの要否を書いていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: ゴールの「パネル候補に出る」に対して、prop-control 単体のテストしか無かった。見た目を変えないので通しテストは足さない、と判断して Issue に書いた(実装の検証で「出さない」側のパネルのテストは足した / 指摘 20)。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3

### 11. エラー種別の語形が検証側の既存の語彙と割れていた

- 分類: `naming`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 検証側を `token-name-conflict` にする計画だった。既存は形容詞 + 名詞(`duplicate-name` / `dangling-ref`)で、編集側の `conflicting-token-name` とも割れる。両方を `conflicting-token-name` に揃えた。
- 既存ルール: rules/naming.md「メソッド名を既存のドメインに揃える」

### 12. Issue が確認を求めていた点(スキーマデフォルト経由の参照)に計画が答えていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 既定値を持つ token prop は 1 種別だけなので、今は崩れない。そのことを確認済みとして Issue に書き、将来 `background` に既定値が付いたら何を見直すかも添えた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 7

### 13. 同名衝突時にパネルの色見本が黙って colors を採っていた

- 分類: `over-guard`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「どちらの種別の名前か」を 4 箇所が別々に決めていて、見本の `colorOf` は colors を優先していた。docs/03「片方を優先しない」に反する。
  `TokenSet.findPaintKind`(両方にあれば none)を足し、見本はそれが colors のときだけ出す形にした。
- 既存ルール: rules/architecture.md「ロジックの帰属先」

### 14. isRefTo の doc が、docs が決めていない根拠を docs/03 に帰していた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: docs/03 が決めているのは CSS の宣言を出さないことだけで、参照元の数え方は決めていない。「どちらを消しても残る参照を隠さないため」を今回の判断として書き直した。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 15. gradients のテストフィクスチャが 3 ファイルに重複していた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `Fade` 定数が token と design-document のテストに同文で書かれていた。フォルダをまたぐので機械判定の対象外だった。`src/domains/__tests__/gradient-tokens.ts` へ寄せた。
- 既存ルール: rules/testing.md「テスト用ヘルパーの置き場所」

### 16. 型を広げる同じ代入が 2 箇所にあった

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `const kinds: readonly TokenKind[] = definition.tokenKind` が canRefer と collectErrors にあった。ファイル内の `referableKinds` にまとめた。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 17. 改名の規則をコメント 2 箇所に書き写していた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: token-editor と use-editor-state に、規則そのものを同文で書いていた。「`TokenSet.rename` が弾く名前では改名しない」と参照だけにした。
- 既存ルール: rules/coding.md「規約の適用範囲」(doc の重複)

### 18. 利用者の居ない公開 API を 2 つ足していた

- 分類: `dependency`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: primitive-schema からの `TokenKindList` の export と、公開メソッド `PropDefinition.canRefer` は、どちらも外から呼ばれていなかった。export を外し、`canRefer` はローカル関数にした。
- 既存ルール: rules/architecture.md「モジュールの公開API」

### 19. `paintToken.colors` の名前と中身が食い違っていた

- 分類: `naming`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 今の値を先頭へ足すので、gradients の名前も `colors` に入りうる。他の欄と同じ `names` に改名した。
- 既存ルール: rules/naming.md「名前と実体を一致させる」

### 20. 壊しても落ちない箇所が 3 つあった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: ミューテーションで見つかった。
  - paint の枝で `withCurrentValue` を外しても落ちない
  - パネルが gradients を出しても落ちない
  - 衝突の文言の `conflictsWith` をすり替えても落ちない
  それぞれテストを足した。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 21. Claude Code のフックが不発の環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: 単独の `echo hook-canary` が deny されずに通った。検証エージェントを 2 回通した後も、このセッションのマーカーと発火ログの見出しが無かった(README の「本当に不発」の枝)。
  セッションは webhook(issues.assigned)で起動し、起動ディレクトリはリポジトリの親(`/home/user`)だった。禁止コマンドを使っていないことは見直した。PR 本文・Issue・Discord に記載した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

### 22. マージ後の main で、#792 が消した関数を新規テストが呼んでいて typecheck が落ちた

- 分類: `なし`
- 出どころ: `フック`
- 内容: #792(#771)が `TokenReferrer.toText` を移した後に、この PR がマージされた。新規テストはその `toText` を呼んでいた。どちらも単独では CI が緑だった。
  記録 PR の git pre-push(typecheck)で気づき、#798 / #799 で直した。ブランチを最新 main へ追随させずにマージされた組み合わせで、対応する規範は無い。
- 既存ルール: なし

## 手戻り

- 計画段階: plan-reviewer の指摘(1〜12)で計画を直した。パネルの形は `colorToken` へ混ぜる案から `paintToken` へ変え、後続 #791 を起票した。実装前なので作業の手戻りは無い
- 実装段階: implementation-reviewer の指摘(13〜20)で次を直した。push 前なので CI の手戻りは無い
  - `TokenSet.findPaintKind` を追加した
  - `paintToken.colors` を `names` に改名した
  - フィクスチャを共有した
  - テストを 5 件足した
- マージ後: main の typecheck 失敗(22)を #799 で直した
- どれも Issue #608 / #798 に追記済み

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。記録のみ(集計と介入の判断は `harness-growth` の管轄)。
