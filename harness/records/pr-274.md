# PR #274 ツリーの UI 機構を src/components/nested-row-list/ へ移す

- マージ日: 2026-08-22
- 関連 Issue: #252
- 差分規模: 9 ファイル / +773 -456

## ゴールと結果

ゴールは「`document-tree`（473 行）を開いたときに、そこにあるのが『このリポジトリのノードをどう見せるか』だけになっている」「開閉・字下げ・並べ替えの当たり判定は `src/components/` の汎用ツリーが持つ」「`DocumentTree` の公開 API と画面の見た目は変わらない」。達成した（feature 側 236 行 / 器 315 行、`DocumentTree` の props は不変、視覚差分は 0 Changed / 105 Passed）。

human レビューは 0 件（レビュースレッド 0・PR のコメント 3 件はすべて bot）。CI は 6 チェックすべて 1 回目で success。**指摘はすべてサブエージェント（`plan-reviewer` 8 件 / `implementation-reviewer` 12 件）から出た。**

Issue 本文の見立てからは 3 つずれた。**行の中身を render prop ではなく `NestedRow.content: ReactNode` で受ける**（器は名前しか持たないので、render prop にすると呼び出し側が名前から元を引き直す対応表を持つことになる）、**器の名前を `TreeView` にしない**（`docs/06-ui.md` の「ツリービュー」は *1 枚の artboard の中身を映すパネル* を指す語で、同じ語が 2 つを指す）、**`type-glyph` の昇格は決着済みだったので触らない**（#250 で移動済み）。ずれはすべてマージ前に Issue へ書いた。

## 指摘

### 1. 既存テストの観点 2 件が、数え合わせから落ちていた

- 分類: `plan-integration-coverage`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `document-tree.disclosure.test.tsx` の 15 件を計画の行き先へ写像すると 14 件にしかならず、「開いた直後はすべての枝が開いている」（畳んだ**側**の名前を持つという state の設計を固定する唯一のテスト）と「枝を畳んでも選択は動かない」（開閉を名前のボタンと分けた理由を固定する）がどこにも現れていなかった。前者が無いと、初期値を「全部畳む」に壊しても器のテストは全部通る（畳む系はどれも自分でクリックしてから見るため）
- 既存ルール: rules/testing.md「API を作り替えるときは、既存テストの観点を先に数える」
- 次にどう防ぐか: 今回は記録のみ（両方を器側へ引き継いだ。実測で、初期値を壊すと 12 件落ちる）

### 2. 新設した公開 props を確かめるテストが、一覧に 1 件も無かった

- 分類: `plan-integration-coverage`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `NestedRow.isSelected` は今回新設する公開 props なのに、器の一覧にも feature に残す 2 件にも現れない。`document-tree.normal.test.tsx` が見ているのは行の中身側の `aria-current` なので、**器が `isSelected` を丸ごと無視しても全テストが緑**になる。字下げも同じで、ゴールが「字下げを汎用側が持つ」と言っている以上、誰が守るのかを計画に出す必要があった
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ（実測で確認したうえで、字下げはインラインスタイルなので読める → テストを 2 件追加、選択の色は class にしか出ないので VRT だけが守る旨をコードに明記、と分けた。指摘 20・21 に続く）

### 3. 行の並び順を器側で固定するテストが無かった

- 分類: `plan-integration-coverage`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 「親の直後にその子」を見ているのは `document-tree.normal.test.tsx` の 1 件だけで、器の一覧に置いた「子の行も並びに出る」は存在確認しかしていなかった
- 既存ルール: rules/testing.md「古典学派のテスト」
- 次にどう防ぐか: 今回は記録のみ（器側に「行は親の直後にその子が来る順で並ぶ」を置き、指摘 1 の観点と同時に埋まる形にした）

### 4. 命名の衝突確認が、未決に挙げた 1 語で止まっていた

- 分類: `naming-prefix-collision`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画は `content`（Text の文言 prop と重なる）だけを未決に挙げていたが、実際には 3 つ残っていた。**`TreeView`**: `docs/06-ui.md` 11 / 19 / 32 行の「ツリービュー」は *1 枚の artboard の中身を映すパネル* を指し、`DocumentTree` の doc とテスト名も同じ語を使っている。**`TreeItemNote` / `TreeItemMarks`**: 実体は Node の印なのに、公開する `TreeItem` と接頭辞を共有したまま層をまたいで別のものを指す。**`rootName`**: 実体は「最上段の行の**親**の名前」で、その行は描かれない
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」「名前と実体を一致させる」
- 次にどう防ぐか: 今回は記録のみ（`NestedRowList` / `NodeNote` / `NodeMarks` / `parentName` に変えた。**未決に 1 語挙げた時点で「他は確認済み」と読める形になっていた**のが原因で、確認したのは 1 語だけだった）

### 5. 却下案の理由が、移設後の型では成り立たなかった

- 分類: `ownership-reasoning`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 「`moveTargetIndex` を `ArrayEx` へ上げない」の理由を「位置と兄弟の並びの対（`SiblingPlacement`）から求めるのが要点だから」と書いたが、実際に読んでいるのは `position.index` と `siblings` の長さだけで **`parentName` は使わない**。移設後は `(index, siblings)` に落ちるので、対であることは理由にならない
- 既存ルール: rules/architecture.md「ロジックの帰属先」/ rules/coding.md「Why not の根拠に、いま直されつつある一時的な事実を置かない」
- 次にどう防ぐか: 今回は記録のみ（理由を「中身は `ArrayEx.isIndexInRange` の 1 行で、足すと既存操作の言い換えが増える。返す `Option` は『その向きのボタンを出さない』に直結していて並べ替えの出し分けそのもの」に書き直した）

### 6. 層をまたぐと誰の保証でもなくなる前提を、doc に書いていなかった

- 分類: `comment-missing`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `name` の一意性を保証しているのはドキュメントの単一名前空間（`docs/06-ui.md`「自動リネームで名前の一意性を保つ」）で、これはドメイン知識なので横断層へは運べない。移設前の Why（畳んだ名前は消えたノードのぶんも残る / 同じ名前で作り直すと畳んだ状態で現れる）はその前提の上に書かれており、state ごと `src/components/` へ移すと根拠が消える
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ（`NestedRow.name` の doc に**崩れたときに何が起きるか**（同じ名前の枝がまとめて畳まれる / React の `key` が重なる）まで書き、移設前の Why を器と feature のどちらへ置くかを表にして分けた）

### 7. UI の表示確認が計画に入っていなかった

- 分類: `plan`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画は「マークアップと className を変えないので VRT は差分ゼロの想定」と書いていたが、これは主張であって確認ではない。**差分ゼロを狙う変更ほど Storybook を開いて確かめる意味がある**のに、確認する story id が 1 行も無かった
- 既存ルール: rules/ui-verification.md「表示確認」
- 次にどう防ぐか: 今回は記録のみ（story id 4 つと通し確認の手順を計画へ足し、実際に撮って確認した。**「見た目を変えない変更だから確認は要らない」と暗黙に外していた**、という形）

### 8. 残す並べ替えテストの入力が、既定値と同じ答えになる形だった

- 分類: `test-default-input`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 「子を下へ動かすと…」の期待値 `{ parentName: "home", index: 0 }` の `home` は**先頭の artboard** で、`parentName` を「先頭 artboard の名前」に壊しても `EditorState.currentArtboard` の既定（選択なし → 先頭）と同じ答えになるので落ちない。6 件 → 2 件へ絞るなら、残す側は 2 枚目を見ている状態で確かめる必要があった
- 既存ルール: rules/testing.md「既定値・フォールバックがある処理では、既定値と違う答えになる入力を選ぶ」
- 次にどう防ぐか: 今回は記録のみ（ドキュメントを 2 枚組に変え `EditorState.select(state, "settings")` で見る形にした。**実測で、1 枚組では先頭固定に壊しても落ちず、2 枚組にして初めて落ちる**ことを確認している）

### 9. 移設前から書かれていたコメントが、事実と違っていた

- 分類: `comment-false-claim`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 「開閉の三角を置く枠（UI 案 docs/Design Composer.html の実測値は 10px）」が誤り。UI 案を展開して数え直すと Layers の三角スロットは **8 箇所すべて `width:9px`**（三角側 4・空スロット 4）で、`width:10px` は 2 箇所だけ・どちらも `height:2px; background:#111` のリサイズハンドルの棒でこの枠とは無関係。横断層へ移ったことで**この 1 行が値の唯一の根拠**になり、放置できなくなった
- 既存ルール: rules/coding.md「コメントは実装と一致させる」/ rules/ui-verification.md「数えるときは大文字小文字を無視する」（数え直しの手順は規約にある）
- 次にどう防ぐか: 今回は記録のみ（9px へ揃えると視覚差分が出て今回の狙いと衝突するため、**直したのはコメント側**にして「UI 案は 9px、実装は 10px でずれている。揃えるのは別の機会」と書き分けた（rules/ui-verification.md「乖離の解消は個別の issue で行う」）。**乖離そのものは残っている**）

### 10. 字下げがテストで 1 件も守られていなかった

- 分類: `test-coverage-wiring`
- 出どころ: レビュー（`implementation-reviewer` / ミューテーション実測）
- 内容: `depth + 1` を `depth` に潰しても、`paddingInlineStart` を丸ごと削除しても、**どちらも 0 件しか落ちない**。ゴールが「字下げを汎用側が持つ」と言っている当の値が無防備だった
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ（字下げは選択の色と違って**インラインスタイルなので happy-dom で読める**。「深い行ほど字下げが大きくなる」「同じ深さの行の字下げは揃う」の 2 件を足し、追加後に上の 2 つの壊し方を再現して落ちることを確かめた。2 件目は、1 件目だけだと深さと無関係に増やす実装でも通るために置いた）

### 11. 選択の色と空スロットは、テストで守る経路が無かった

- 分類: `test-coverage-wiring`
- 出どころ: レビュー（`implementation-reviewer` / ミューテーション実測）
- 内容: `isSelected` を `false` 固定に壊しても、子を持たない行の空スロットを `null` にしても 0 件しか落ちない。どちらも class にしか出ないため happy-dom では観測できず、`toHaveClass` / `classList` を使ったテストはリポジトリ全体で 0 件（見た目の担保を VRT に置くのが既存の流儀）
- 既存ルール: rules/testing.md（happy-dom がレイアウトを解決しない形は既存記録 pr-173#13・pr-269#20 と同じ）
- 次にどう防ぐか: 今回は記録のみ（**気づく手段が Storybook の視覚差分（`NodeSelected` / `Nested`）だけである**旨を、`NestedRow.isSelected` の doc と空スロットの行にコメントで残した）

### 12. 横断層へ入った出し分けの Why が無かった

- 分類: `comment-missing`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 並べ替えボタンが横断層へ移ったが、「器は出し分けを持たない（どの行にも上下が付く）」という判断がどこにも書かれていなかった
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ（`NestedRowList` の doc に追記）

### 13. テストのヘルパーが、既存のものと同名・同シグネチャで逆の意味だった

- 分類: `naming-prefix-collision`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 器のテストに置いた `rowNames(container): readonly string[]` は、`src/features/editor/__tests__/row-names.ts` の `rowNames` と同名・同シグネチャなのに**引き方が逆**（`aria-label` を持た**ない**ボタンの `textContent` ⇔ `aria-current` を持つボタンの `aria-label`）。同じ DOM に対して違う答えを返す
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ（`contentTexts` へ改名し、doc に違いを書いた。**計画では「`rowNames` の写しを作らない」と決めていたのに、写しではなく別物を同じ名前で作っていた**、という形）

### 14. 綴り直した型の Why が、実装ファイルに無かった

- 分類: `comment-missing`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `NestedRowPosition` を `ChildPosition` と別に綴った理由（横断層から `domains/` を import できない）は Issue と型テストにはあったが、型の doc に無かった
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ（doc へ移した）

### 15. 器と重複するテストを残した理由が書かれていなかった

- 分類: `comment-missing`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: feature 側に残した「孫ノードの並べ替えはその親の中の位置として伝わる」は、器側の「子の行の並べ替えは…」と一見同じことを見ている
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ（残す理由（器が見ているのは描画をたどって作る位置、feature 側は `rowFromNode` が Node の木を再帰で移すときに親子関係を保っていること）を 1 行書いた）

### 16. 移設で生まれた挙動差が doc に無かった

- 分類: `comment-missing`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 開閉の state を器へ移したことで、**artboard が 0 枚になると畳んだ状態が消える**（移設前は `DocumentTree` が描画を止めても state を保っていた）。行が 1 つも無い状態を挟むので見え方は変わらないが、挙動差ではある
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ（`DocumentTree` の doc に明記した）

### 17. fixture と doc が、もう使っていない場合分けを説明していた

- 分類: `comment-stale-edit`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: feature 側の disclosure を 15 件 → 2 件へ絞ったのに、fixture（`title` / `deep` を含む 3 段の木）と doc（開閉の場合分けを 1 つの木で見られるようにする、という説明）は 15 件時点のまま残っていた
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ（fixture を枝 1 つ + 子 1 つへ詰め、doc も揃えた。`title` / `deep` を削っても 24 件通ることは実測済みだった）

### 18. doc が引いた規約が、実際の形に当てはまっていなかった

- 分類: `comment-false-claim`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `BranchControl` の doc が rules/components.md「props が概ね5個を超える」を引いていたが、この型は 3 フィールドしかない（移設元の `TreeControl` は 5 つだったので、引用だけが持ち越された）
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ（引用を落とし「どの行でも同じ値なので行ごとに配らない」に書き換えた）

### 19. 判断の Why not が無かった

- 分類: `comment-missing`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `NestedRow.content` に ReactNode を持たせたのは Issue 本文の「render prop で受ける」から外れた判断なのに、その Why not がコードに無かった
- 既存ルール: rules/coding.md「Why と Why not は別物として書く」
- 次にどう防ぐか: 今回は記録のみ（「木が再帰するので `children` では入れ子を表せない / render prop だと呼び出し側が名前から元を引き直す対応表を持つ」を追記）

### 20. テストに書いたコメントの主張が、ライブラリの挙動と違っていた

- 分類: `comment-false-claim`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 「畳んだ側を持つ設計を壊すとここが 0 になる」と書いたが、`getAllByRole` は 0 件のとき例外を投げるので `0` にはならない
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ（「この引き方自体が失敗する」に直した）

### 21. 戻り値の型注釈が無く、直和へ枝を足しても typecheck が通る形だった

- 分類: `illegal-state`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `NoteText` は `default` の無い `switch` で `NodeNote` を出し分けているのに戻り値の型注釈が無く、`NodeNote` に枝を足しても typecheck が通る。HEAD からの持ち越しだが、この差分で `TreeItemNote` → `NodeNote` に改名して触っており、同じ差分の `NestedRowList` には注釈が付いている（片方だけ抜けている）状態だった
- 既存ルール: rules/coding.md「列挙した状態の網羅を型で強制する」
- 次にどう防ぐか: 今回は記録のみ（`: ReactElement` を付け、器の内部コンポーネント 5 つにも付けて `src/components/` の既存 2 件（`ColorSwatch` / `SegmentedControl`）に揃えた）

### 22. 検証エージェントが実装を書き換えた状態で、コミット作業が重なった

- 分類: `subagent-control`
- 出どころ: フック（Stop hook の通知）
- 内容: `implementation-reviewer` がミューテーション実測の最中に実装ファイルを書き換えていた（`depth + 1` → `depth`）タイミングで、Stop hook の push 促しを受けてコミット作業へ進みかけた。コミットされた blob はミューテーション前と一致しており混入は無かった
- 既存ルール: `implementation-flow`「返ってきたら、指摘を読む前に `git status` を見る」
- 次にどう防ぐか: 今回は記録のみ（pr-269#25 と同じ形の再発。規約は「返ってきたら」を前提にしているが、2 回とも**返ってくる前**に別の理由（Stop hook）で push へ進みかけている）

### 23. `.claude/hooks/` が発火しない実行環境だった

- 分類: `hook-environment`
- 出どころ: フック（カナリア）
- 内容: `echo hook-canary` が deny されずに通った。PreToolUse フックはこのセッション中 1 度も発火していない。push 前検査の enforcement は git hooks（`harness/githooks/pre-push`）と CI が担っており、実際に pre-push で lint / biome / doc コメント / テスト規約が走ったので、手順そのものは変わっていない
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ（PR 本文と Issue に記載済み）

## 手戻り

- **並べ替えのテストが計画の 7 件 → 6 件になった。** 「最上段の行の並べ替えは根の名前の子の位置として伝わる」を独立させる計画だったが、`parentName` が効いているかは「行を上へ / 下へ」の 2 件が期待値 `{ parentName: "root", … }` で既に見ており、同じことを 3 度確かめる形になるのでやめた
- **feature 側に残した disclosure の 1 件目を改名した**（「枝を畳むとその枝の子が並びから消える」→「…その枝の**子のノード**が…」）。器側に同名のテストがあり、名前だけではどちらの層を見ているか分からなくなるため
- **戻り値の型注釈を足したことで doc コメントのフックが `@returns` を 5 件要求し、1 往復した。** 注釈と `@returns` はセットで足す必要がある

## うまくいったこと

- **「見た目を変えていない」を主張ではなく実測にできた。** マークアップと className を 1 文字も変えない方針を保ち、VRT が 0 Changed / 105 Passed で返した。Storybook でも 4 ストーリー + 通し操作（並べ替え → キャンバスも入れ替わる → 選択 → インスペクタが変わる / 開閉 → 子が消える → 開き直すと戻る）を確認している
- **疑わしい assert を頭で判断せず、壊して走らせた。** 開閉の初期値・`parentName`・選択の色の 3 つを自分で実測し、うち 1 つ（選択の色は 929 件すべて通る）が指摘 2 の答えになった。検証エージェント側も 11 件実測し、守られていた 7 件と守られていなかった 4 件を分けられた
- **減らしたテスト（disclosure 15 → 2、reorder 6 → 2）で落ちた観点が 0 件だった。** 既存 40 件を先に数え、「機構へ移す / ドメイン側に残す / 新規」へ振り分けてから書いた
- **消費者が 1 つしか無い汎用化の理由を、再利用ではなく「UI の機構とドメインの語彙を分ける」として PR 本文の先頭に置いた。** Issue が warn していた「過度な抽象化として読まれる」形を避けられた

## 規約への反映

無し。今回出た 23 件はいずれも既存の規約・観点で説明でき、**この回の指摘だけでは「最後の介入以降 2 回以上の再発」を判定できない**（数えるのは `harness-growth` の管轄）。

気づいた点として次の 4 つを記録に残す。

- **`comment-missing` が 6 件（6・12・14・15・19）と `comment-false-claim` が 3 件（9・18・20）で、コメント系が 23 件中 9 件を占める。** 内訳を見ると、6・14・19 は「層をまたいだことで根拠がコードから消えた」形、18 は「移設元の引用がそのまま持ち越された」形で、**層をまたぐ移動そのものがコメントの根拠をずらす**という共通点がある
- **`plan-integration-coverage` が 3 件（1・2・3）で、いずれも「移設後に誰がその観点を持つか」を計画が決めていなかった形。** 1 は既存の観点の引き継ぎ漏れ、2 と 3 は新設した公開 props / 器側の観点の欠落
- **`subagent-control`（22）は pr-269#25 と同じ形の再発。** 2 回とも「検証エージェントが返る前に、Stop hook の push 促しでコミット作業へ進みかけた」で、規約の「返ってきたら `git status` を見る」が想定している順序と違うところで起きている
- **指摘 9 の乖離（三角スロットが UI 案 9px / 実装 10px）は未解消のまま残っている。** rules/ui-verification.md「乖離の解消は個別の issue で行う」に従い、この PR では直していない
