# PR #749 src/libs/ のコンパニオンオブジェクトのメソッドの doc の抜けを埋める（#720 層 3）

- マージ日: 2026-09-24
- 関連 Issue: #737
- 差分規模: 18 ファイル / +136 -14

## ゴールと結果

ゴール（`missing-doc-comments.py --all src/libs --include-methods` が 0 件）は達成した。着手時点は
doc 無し 17 件 / 項目の欠け 4 件 / 17 ファイル（Issue 本文の概算と同じ）。引数ごとの `@param <名前>` も使い捨ての
スクリプトで 0 件にした。doc だけの差分で挙動は変えていない。書く中で見つけた実装側の問題は #748 に切り出した。
CI は 1 回目で全チェック通過、VRT の差分 0 件、人のレビュー指摘 0 件でマージされた。

## 指摘

### 1. `CanvasDom.elementOf` の既存の概要文が実装と食い違っていた

- 分類: `comment`
- 出どころ: `自己修正`
- 内容: 「部品インスタンスの中身も展開時に自動リネームされる」と書いていたが、展開は部品の中のノードの名前を付け替えない。
  部品を 2 回置いた正常なドキュメントで同じ `data-name` が 2 回描かれることを使い捨ての vitest で確かめて直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 2. `elementOf` の「名前は一意」を誤りとした前提が不正確だった

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: docs/01 は部品定義の中も含めて名前を一意と決めている。誤りは「一意だから DOM で 1 つに決まる」という推論のほうで、
  計画の「アートボードのツリーのノード名は一意」は範囲を狭めすぎていた。2 つを分けて書いた。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 3. 同じ推論が `src/features` の `nameSelector` にも残っていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: CSS の選択子なので部品の中のノードを指すと全インスタンスに効く。層 4（#738）の範囲なので #748 に載せて据え置いた。
- 既存ルール: `AGENTS.md`「着手した Issue は、その回で閉じる」

### 4. `@returns` に書く中身が 1 件ずつ計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 14 件の `create` などで `@returns` の中身が決まっていなかった。表にした。pr-730 の 5 / pr-741 の 3 と同じ形の再発。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 5

### 5. 同じ事実を 2 箇所に書く計画になっていた

- 分類: `duplication`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「購読前に起こしたものは届かない」は 2 つの代役ではなく `SingleEventIpcFake` の性質、「テストは fake/ に差し替え」は
  型 doc の写し、`parse` の字句スキャンの中身は `scan` の `@returns` と重なる。持ち主の 1 箇所に寄せた。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 6. `DocumentIpc.create` の詰め替えの記述が購読の経路を覆っていなかった

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 購読は `TauriIpc.caller` を通らず自前の `try/catch` で同じ失敗へ詰め替える。両方の経路を書いた。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 7. 計画の文がコードの値と既定値を写していた

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `String(Error)`、「省くと 0」「省くと空」。見える形・意味で書き直した。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 8. 分けない理由が「独立してマージできるか」に答えていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 独立マージは可能と自分で書きながら分けない結論にしていた。分ける目的に照らした理由に書き直した。pr-741 の 7 と同じ形の再発。
- 既存ルール: `AGENTS.md`「タスクの分割」

### 9. 代役の `@returns` が型のフィールド doc を写していた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `AppMenuFake` / `FileDropFake` / `DocumentIpcFake` / `ClockFake`。フィールドを足すと列挙だけ古くなる。
  先例の `SingleEventIpcFake.create` に揃え、フィールドを名指ししない形にした。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 10. `toCurrent` の `@returns` が同じ doc の箇条書きを言い直していた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `@returns` は素通しの参照とエラーの kind 名、箇条書きに無い条件だけにした。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 11. `parse` の `@returns` が非公開の変換関数の doc を写していた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 段ごとのエラーの位置を 4 行で並べ直していた。「最初に失敗した段の分だけ」の 2 行に縮めた。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 12. `DialogChoice.failed` が別モジュールの綴り規則を写していた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「`Error: ` が前に付く」は `DocumentDialog` の `choose` の規則。持ち主の `@returns` に 1 回だけ書き、代役は参照にした。
  pr-741 の 14 と同じ形。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 13. `@returns` が戻り値の型・型 doc を言い直していた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `DocumentDialog.create` / `TauriDialog.create` / `AppStateIpc.create` / `DocumentIpcFake.create` の 4 件。消した。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 14. `TauriIpc.create` の「そのまま呼ぶ」が不正確だった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `listen` はイベントオブジェクトではなく payload だけを渡す。そう書いた。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 15. 移した表に「major が同じで」が抜けていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 既存の文の誤り（ファイル 0.5 とアプリ 1.1 はマイグレーションになる）。`toCurrent` へ移して `@returns` と並べたので直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 16. フックが発火しない環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されず、検証エージェントのマーカーもこのセッションの発火ログの見出しも無かった（README の表で「本当に不発」）。
  起動時の作業ディレクトリがリポジトリの親（`/home/user`）で、層 1・2 と同じ構成。`npx` / `git add -A` の実行と、検証エージェント実行中の git 操作が無いことを履歴で確認した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- **計画を plan-reviewer の指摘 7 件 + 判断を仰ぐ 4 件で直した**（指摘 2〜8）。うち 2 件（4・8）は層 1・2 の plan-reviewer 指摘と同じ形の再発
- **実装を implementation-reviewer の指摘 7 件で直した**（指摘 9〜15）。重複 4 件は pr-741 の 13〜16 と同じ形
- push 前検査 21 項目（typecheck / lint / biome / test / ハーネスの検査 17 本）と Storybook の撮影範囲（180 枚）、CI は 1 回目で通過した

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。この回は Issue #737 の実装で、記録を数えた結果の介入ではない。`対策済` を書けるのは
`harness-growth` の棚卸しだけ。
