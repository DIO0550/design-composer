# PR #819 SelectionState / renameMap / Placement.fromProps / FormatVersion.parse の疑わしい挙動を直す

- マージ日: 2026-09-26
- 関連 Issue: #774
- 差分規模: 17 ファイル / +225 -99

## ゴールと結果

ゴールは達成した。4 つの値が、重複・不正な綴り・決められない配置を受けても、それぞれ 1 つの答えを返す。
- 選択は名前ごとに 1 件と数える
- 付け替えの対応表は、同じ名前へ最初に割り当てた名前を残す
- 決められない配置は `Option` で返す
- 形式版は、正確に表せる整数を正規の綴りで書いたときだけ読める

`renameMap` の `__proto__` は #768 で既に直っていたので対象から外した。同じ形の `Size.fromProps` は #817 に、detach で同名ノードが残る件（修正前からあり、修正後も残る）は #818 に分けた。
GitHub 上の人のレビューは 0 件だった。`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも確認し、コメントは bot の投稿と自分のテストケース表だけだった。CI は初回から緑で、VRT は差分なし（179 件）。

## 指摘

### 1. isAbsolute が死んだ API になることと、declarations の引数型を決めていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `isAbsolute` の呼び出しは 3 箇所とも `absoluteFromProps` へ移るので、残すと呼び出しの無い API になる。計画は型だけを直す予定だった。
  `isAbsolute` を消し、`declarations` は `AbsolutePlacement` を受け、`absoluteFromProps` は `fromProps` から `flatMap` で導く形に決め直した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 1

### 2. 直す doc を行単位で特定しておらず、compiled-element の placementDeclarations が漏れていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `placementDeclarations` の `@param` にある「決まらないときは `undefined`」がファイル表に無かった。placement 本体の 3 か所も行を特定して表に書いた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 7

### 3. 分けると書いた Size の Issue を起票していなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画に「別 Issue に分ける」とだけあり、番号もリンクも無かった。#817 を起票してリンクした。
- 既存ルール: AGENTS.md「タスクの分割」

### 4. renameMap の修正が、画面から届く重複を直すかを確かめていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: detach は部品を深く展開するので、同じ部品のインスタンスを 2 つ含む部品では同名のノードが並ぶ。対応表は「名前 → 名前」なので、修正後も同名が残る。
  対応表の意味だけを直し、一意性は #818 に分けた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 3

### 5. FormatVersion の綴りの規則に仕様の根拠が無く、既存の doc は逆の振る舞いを明記していた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: docs/01 は `"major.minor"` としか書いておらず、parse の doc は「`"01.02"` は 1.2」と明記していた。仕様変更として docs/01 に追記した。
- 既存ルール: rules/testing.md「テストケースの根拠は仕様・設計ドキュメントに求める」

### 6. FormatVersion の大きさの境界を片側しか置いていなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「読めない大きな数」だけでは、桁数を極端に絞った壊れた実装でも通る。`MAX_SAFE_INTEGER` は読める・2^53 は読めない、を major と minor の両側に置いた。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 7. minor 側の 0 と "00.1" のケースが無かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `"0.9"` は major 側の `0|` しか守らない。`"1.0"` が読める・`"00.1"` が読めない、を足した。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 8. compiled-element で「決まらない」ときの出力を守るテストが無かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 呼び出し側を書き換えるのに、Box / Text の配線を守るテストが無かった。`compiled-element.placement.test.ts` を足した。
- 既存ルール: rules/testing.md「1. 振る舞い(仕様)をテストする」

### 9. selection-state で重複の単一選択を確かめる入力が弱かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `["a","a"]` だけでは「先頭 1 件だけ取る」ような壊れた実装でも通る。`["a","b","a"]` で並びと件数を見るケースを足した。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 10. document-names のケースが空の名前空間だけだった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 最初と最後のどちらの割り当てが残るかは、衝突があるときにしか区別できない。`title` がある名前空間のケース（`title-2` と `title-3` の違い）を足した。
- 既存ルール: rules/testing.md「既定値・フォールバックがある処理では、既定値と違う答えになる入力を選ぶ」

### 11. 「未決: なし」が成り立っていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 1・4・5 が決まっていないのに、計画は未決を「なし」と書いていた。3 つとも決めて Issue に追記した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「未決の判断」

### 12. DocumentSelection.fromNames の doc の前提が崩れ、SelectionState.create の doc を写していた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「作り分けは並びの長さで決まる」は、重複を畳んだ後では成り立たない。`@param` も `create` の doc の写しだった。どちらも `SelectionState.create` を参照するだけの形に直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 13. テストに setupBoxStyle とほぼ同じヘルパーを書いた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `setupBoxStyleInRow` は、既存の `setupBoxStyle` と第 2 引数以外が同じだった。`setupBoxStyle` に既定値付きの引数を足し、そこから呼ぶ形にした。
- 既存ルール: rules/testing.md「テスト用ヘルパーの置き場所」

### 14. 「座標で置いた Box は伸びない」が別テストの対照行 1 行でしか守られていなかった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: ミューテーション（flex の親を常に残す）で落ちたのは、別の仕様を名乗るテストの対照 assert だけだった。独立したテストに分けた。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 15. 本番で fromProps を呼ぶのが absoluteFromProps だけになった（任意）

- 分類: `dependency`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 公開をやめられるかの指摘だったが、直さなかった。ゴールが `fromProps` の `Option` 化で、`Constraint.fromProps` の doc もこれを指しているため。
- 既存ルール: rules/architecture.md「モジュールの公開API」

### 16. Claude Code のフックが不発の環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されずに通った。検証エージェントを 2 回通した後もこのセッションのマーカーが無く、発火ログの見出しも別のセッション ID の 1 件だけだった。
  `npx`・一括の `git add`・検証中のコミットが無いことを見直した。git の pre-push は手で走らせて全件通過した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- 計画段階: plan-reviewer の指摘（1〜11）で計画を直した。実装前なので、作業の手戻りは無い
- 実装段階: implementation-reviewer の指摘（12〜14）で doc・テストヘルパー・テストの分割を直した
- PR 後: 無し

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。記録だけを残す（集計と介入の判断は harness-growth の管轄）。
