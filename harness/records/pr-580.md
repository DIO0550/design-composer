# PR #580 ♻️ [Refactoring]: #569 use-node-drag の boundsOf を DrawnBounds.measure へ寄せる

- マージ日: 2026-09-18
- 関連 Issue: #569
- 差分規模: 2 ファイル / +6 -13

## ゴールと結果

ゴールは「『名前だけを受け取って矩形を返す関数』が `DrawnBounds.measure` 以外に存在しなくなる」。
達成した。`use-node-drag` の非公開ヘルパー `boundsOf` を doc ごと削除し、`measureReposition` の
呼び出し 2 箇所を `DrawnBounds.measure` へ差し替えた。`grep -rn "boundsOf" src/` は 0 件、
`grep -rn "CanvasBounds.ofElement" src/` は 8 件 → 7 件になり、`CanvasDom.elementOf` と直に
合成しているのは `DrawnBounds.ts:20` の 1 件だけになった。

**ゴールの文言は着手時のものから変わっている。** 当初は「実測が `DrawnBounds.measure` の 1 箇所
だけになる」と書いたが、実装後も落とし先の `dropped.name` → 矩形は 162 / 172 行の 2 行に分かれて
残るため、その文言では達成を判定できなかった(指摘 1)。

## 指摘

### 1. ゴールの判定基準と、実装後にそれを確かめる手段が計画に無い

- 分類: `plan-goal-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: ゴールを「実測が 1 箇所だけになる」と書いたが、実装後も `CanvasDom.elementOf` は
  production に 3 件残る(`DrawnBounds.ts` / `use-node-drag` の 100・162)。落とし先を寄せない
  判断自体は却下案に書いてあったのに、**その結論をゴールの判定基準として書き直していなかった**ため、
  「1 箇所になった」を確認するコマンドが計画に無かった。判定基準を「名前だけを受け取って矩形を
  返す関数が `DrawnBounds.measure` 以外に存在しないこと」へ言い換え、`grep -rn
  "CanvasBounds.ofElement" src/` の件数(8 → 7、うち `elementOf` と直に合成しているのは 1 件)を
  完了条件へ足した。**この不変条件は typecheck も lint もテストも守らない**(`boundsOf` が再び
  生えても全部緑のまま)ので、この `grep` が唯一の確認手段になる。
- 既存ルール: `plan-review.md`「ゴールの達成を確かめる手段」
- 次にどう防ぐか: 今回は記録のみ

### 2. 完了条件に、既に取ったミューテーション実測の再実行が書かれていない

- 分類: `plan-goal-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画には「`boundsOf` を `Option.none` へ壊すと 3 ファイルで 17 件落ちる」という実測を
  書いていたが、**差し替え後に同じ 3 ファイルが緑であること**が完了条件に無かった。壊れて
  いないことの根拠が計画の中で閉じていない形。完了条件へ 1 行足した(実装後 46/46 緑を確認)。
- 既存ルール: `plan-review.md`「ゴールの達成を確かめる手段」
- 次にどう防ぐか: 今回は記録のみ

### 3. 「残す import」の数え上げに漏れがある

- 分類: `plan-scope-count-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画は「`CanvasBounds` は 103 / 104 / 172 / 177 行が使い続ける」としていたが、実際は
  `grep -n "CanvasBounds" src/features/canvas/hooks/use-node-drag/index.ts` が 12 件で、
  **183 行(`originShift`)・209 行(`placedAt`)と型としての参照(136 / 138 / 140)が漏れていた**。
  `ofElement` の呼び出しだけを数えて「使い続ける箇所」を網羅したつもりになっていた形。結論
  (import は残る)は変わらないが、計画が「数えた結果」として提示した内訳が実例と合っていない。
  内訳を表へ数え直した。`CanvasDom` の 100 / 162 は実例と一致していた。
- 既存ルール: `implementation-flow` フェーズ 3 手順 2
- 次にどう防ぐか: 今回は記録のみ

### 4. 却下案に「消費側の列挙そのものをやめる」が挙がっていない

- 分類: `plan-rejection-coverage`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 案 B として「`DrawnBounds.measure` の doc の列挙を据え置く」を却下していたが、もう一つの
  近い代替である「**列挙そのものを落とす**」を検討していなかった。`rules/coding.md`「doc に
  書く項目」の判断軸(落とすと入力・出力・画面のどれかが変わるなら列挙、変わらないなら例示)を
  当てると、消費側の列挙は落としても挙動が変わらない側に読める。加えて案 B の却下理由が
  「#397 がそう決めたから」で終わっており、**規約の判断軸を当て直した理由になっていなかった**。
  案 D(列挙を落とす)を追加し、「落とす変更は doc だけで単独マージできるので別 Issue」という
  理由で据え置く判断へ書き直したうえで #581 を起票した。**`implementation-reviewer` も参考として
  独立に同じ構造(`comment-enumeration-drift` の温床。`enclosing` / `collectOverlappingNames`
  経由の `use-canvas-view` / `use-range-select` が既に列挙から漏れている)を挙げている。**
- 既存ルール: `implementation-flow` フェーズ 3 手順 6
- 次にどう防ぐか: 今回は記録のみ

### 5. 「未決の判断: 無し」の裏に、列挙の数え方の基準が決まっていない

- 分類: `plan-open-decision-declaration`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画を「未決の判断: 無し」で締めたが、`DrawnBounds.measure` の消費側の列挙が
  「直接の外部呼び出し側だけ」を数えるのか、`enclosing` / `collectOverlappingNames` 経由の
  間接の 2 つも含むのかが決まっていなかった。**4 つ目を足す作業そのものがこの基準を要求している**
  のに、欄を「無し」で通していた。基準(モジュールの外から `measure` を直接呼ぶ側だけを数える。
  根拠は #397 の `54c870d` が下ろした狙い)を Issue #569 のコメントへ明記して解消した。
- 既存ルール: `plan-review.md`「未決の判断が残っていないか」
- 次にどう防ぐか: 今回は記録のみ

### 6. 計画へ渡した内容に「Issue へ追記済み」が含まれず、済んでいる項目が指摘として返った

- 分類: `なし`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `plan-reviewer` から「分けない理由・却下案を Issue へ追記する旨が計画に書かれていない」
  という指摘が出たが、**セッション URL のコメントも計画のコメントも投稿済み**だった。
  `plan-reviewer` へは計画の本文だけを渡し、それが既に Issue のコメントとして存在することを
  伝えていなかったのが原因。計画側の不足ではないので計画は変えていない。エージェントへの
  受け渡しに「どこまで済んでいるか」を含める手順は `implementation-flow` フェーズ 4 の
  渡し方(`ゴール:` / `計画:` の 2 項目)に無い。**1 件目なので介入は提案しない。**
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ(`harness-growth` が数える材料)

### 7. doc へ足した列挙の「位置」が実物と食い違う

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `DrawnBounds.measure` の doc へ「座標の置き直しで今の親と運んで**いるものを測る位置**
  (`use-node-drag`)」と書いた。2 つ問題があった。(a)「位置」が 1 文の中で 2 通りに割れている
  — 先行 3 項目の「位置」は画面上の位置(ハンドルを重ねる位置・入力欄を重ねる位置)だが、
  新項目の「〜を測る位置」は読み下すと「測っているコード上の箇所」になる。(b) 画面上の位置と
  読むと実物と食い違う — **運んでいるものについては位置を使っていない**
  (`RepositionMeasure.dragged` の doc「使うのは大きさだけ(位置は運んでいる間ずれている)」)。
  同じ差分で触ったファイルの 20 行ほど下にその doc がある。`snapAt` が既に使っている語彙
  「寄せの原点」「運んでいるものの大きさ」へ揃えて書き直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」/
  `.claude/skills/claim-verification/SKILL.md` 手順 1〜5
- 次にどう防ぐか: 今回は記録のみ(`comment-structure-claim` は pr-544 で層=skill
  (`claim-verification`)へ介入済みで、今回はその手順を doc へ書いた 1 文に対して踏まなかった形)

### 8. 同じ関数の中に残した非対称に理由が無い

- 分類: `comment-missing-divergence`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: この差分は `measureReposition` の 3 つの実測のうち 2 つを `DrawnBounds.measure` へ寄せ、
  3 つ目(落とし先)だけ `CanvasDom.elementOf` → `CanvasBounds.ofElement` の直接合成のまま残した。
  **この非対称はこの差分が作ったもの**で、理由がコードのどこにも無かった(Issue の却下案 A と
  PR 本文にはあった)。ゴールが「名前だけを受け取って矩形を返す関数が `DrawnBounds.measure`
  以外に存在しなくなる」である以上、次に読む人は「ここだけ寄せ忘れている」と見て `measure` を
  足しに来る(そうすると `elementOf` を 2 回引くことになる)。153 行のそばへ 1 行足した。
  `measureZone` 側には足していない(戻り値が `DropZone` で「矩形を返す関数」に見えず、同じ誤読が
  起きる形になっていない。同じ理由を 2 箇所に書くと rules/coding.md「規約の適用範囲」の
  「片方だけを直せる状態を作らない」に当たる)。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ

### 9. `.claude/hooks/` が発火しない実行環境だった

- 分類: `hook-environment`
- 出どころ: `自己修正`
- 内容: push 前に `echo hook-canary` を素で実行したところ **deny されずに通った**。
  `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」の表で切り分けると、
  検証エージェントのマーカー(`/tmp/design-composer-verification-agents-<session>`)も
  発火ログの見出し(`/tmp/design-composer-firings-<session>.log`)も無く、**4 行目 = 本当に不発**。
  `/tmp` にあった `design-composer-firings-35db4c64-….log` は見出しの時刻が 2026-09-11 で
  別セッションの残骸だった。同 README「『代替不能』が実際に不発だったとき、手動で肩代わりする」に
  従い、CI に代替の無い 2 つを手で確認した — `block-npx.sh`: このセッションで `npx` は 1 度も
  実行していない(すべて `pnpm exec` / `pnpm run`)。`block-git-during-verification-agent.sh`:
  検証エージェント実行中に `git add` / `commit` / `push` を挟んでいない(読み取りの `git diff` /
  `git status` のみ。コミットは検証がすべて終わってから 1 本だけ)。`git add -A` も未使用。
  **素通りした操作・欠陥は無かった**ので `hook-environment-guard-miss` ではない。
  `git push` 時の pre-push フック(層 2)は実際に走って全項目を通過している。
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

- **計画の書き直しが 1 回。** `plan-reviewer` の指摘 5 件を受け、ゴールの判定基準・完了条件・
  import の内訳・却下案 D・列挙の数え方の基準を Issue #569 のコメントで直した(指摘 1〜5)。
  実装には入っていない段階なのでコードの手戻りは無い
- **実装後の書き直しが 2 箇所。** `implementation-reviewer` の指摘を受け、`DrawnBounds.ts` の
  doc の文言を書き直し(指摘 7)、`use-node-drag/index.ts` へコメントを 1 行追加(指摘 8)。
  どちらも 1 コミットへ含めてから push しているので、CI は 1 回も赤になっていない
- **計画から外れた箇所は無し。** 触ったファイルは計画のとおり 2 つで、テストの追加・変更も
  計画どおり 0 件

## 発火

- 発火: 計測対象外(フック不発)

指摘 9 のとおり、カナリアが通り、発火ログのファイル自体が無い。記憶では
`implementation-flow` / `claim-verification` / `plan-reviewer` / `implementation-reviewer` を
各 1 回通しているが、**ログから取れないので発火の行には書かない**(推測で埋めない)。

## うまくいったこと

- **テストを足さない判断を、実測で裏取りしてから出した。** `boundsOf` と `DrawnBounds.measure` は
  本体が逐語で同一なので**この 2 つを区別できるミューテーションは原理的に存在しない**。
  代わりに既存テストが差分の中心を守っているかを 3 通りのミューテーションで測り
  (各呼び出しを `Option.none` へ → 17 件 / 17 件、2 つの名前を入れ替え → 6 件)、
  「どちらの名前がどちらのスロットへ行くか」まで落ちることを確認してから 0 件と書いた
- **計画の段階でミューテーションを 1 回撮っておいたのが、あとの判断をすべて速くした。**
  差し替え前に `boundsOf` を壊して 17 件落ちることを見ていたので、`plan-reviewer` の
  「完了条件へ写せ」(指摘 2)も `implementation-reviewer` のテスト方針の検証も、
  同じ数字を突き合わせるだけで済んだ
- **却下案が別 Issue の起票まで届いた。** 指摘 4 で挙がった「列挙を落とす」案を、この PR へ
  畳まず #581 として切り、**据え置く側の理由も規約の判断軸で書き直した**。#397 → #569 と
  2 回続けて同じ行を人手で伸ばしている構造そのものが Issue に残った

## 規約への反映

無し。このスキルの管轄は記録を 1 ファイル足すところまでで、集計と介入は `harness-growth` が行う。

今回の 9 件のうち `plan-goal-verification`(指摘 1・2)・`plan-scope-count-verification`(指摘 3)・
`plan-rejection-coverage`(指摘 4)・`plan-open-decision-declaration`(指摘 5)・
`comment-structure-claim`(指摘 7)・`comment-missing-divergence`(指摘 8)・`hook-environment`(指摘 9)は
いずれも既存の分類で、**今回はその観点・手順が踏まれなかった形**。`分類: なし`(指摘 6 /
検証エージェントへの受け渡しに「どこまで済んでいるか」が含まれない)は初出で、1 件では介入しない。
`対策済` / `見送り` の行は書いていないので、`count.sh` が数える再発数の起点は動かない。
