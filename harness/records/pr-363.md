# PR #363 [harness] comment-structure-claim / comment-behavior-claim を skill 層へ引き上げる

- マージ日: 2026-08-30
- 関連 Issue: なし(`ハーネス棚卸しルーティン` からの直接介入。Issue を介さない
  harness-growth の定型作業のため、フェーズ 1〜3 は行っていない)
- 差分規模: 5 ファイル / +75 -0

## ゴールと結果

`harness/records/count.sh` の再発集計で最上位だった `comment-structure-claim`(再発 26・
pr-294 で観点層まで対応済みだが締めの `対策済` 行が書き漏れており「未介入」表示になっていた)/
`comment-behavior-claim`(同 10 件)へ、`harness-growth` スキル Step 2 の 2b(層を1つ上げる)で
介入すること。`pr-294` の記録自身が「どちらかが観点のまま 2 回再発したら skill 化を検討する」と
予告しており、両分類とも閾値を大きく超えていた。

**結果: `.claude/skills/claim-verification/SKILL.md` を新設し、`implementation-flow`
フェーズ 5 から呼ぶよう配線して達成した。** ずれは無い。

## 指摘

該当なし(`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも人のレビュー
指摘は 0 件。`get_comments` の 3 件はいずれも bot(VRT レポート・Storybook プレビュー・
カバレッジレポート)。CI は全チェック green。セッション中に受けた口頭の指示も無し)。

## 手戻り

無し。

## 発火

- 発火: 計測対象外(フック不発)

## うまくいったこと

- `pr-294` の記録本文が「`comment-structure-claim` / `comment-behavior-claim` を
  `implementation-reviewer.md` の対応表へ新しい行として追加した」とすでに述べていたのに、
  締めの `対策済` 行は `comment-false-claim` にしか付いていなかった。`count.sh` の
  「未介入」表示を鵜呑みにせず記録本文まで読んだことで、この書き漏れに気づけた
- 新設したスキルが既存の対応表(`implementation-reviewer.md`)を複製せず参照だけに
  留めたことで、同じ判断根拠が 2 箇所に分かれる形を避けられた

## 規約への反映

`comment-structure-claim`(再発 26・pr-294 で観点層まで対応済み)/ `comment-behavior-claim`
(再発 10・同)は、いずれもすり抜け 0 件(`implementation-reviewer` の観点は機能していた)だが、
観点のまま再発が続き、その都度差分を出し直す手戻りが生じていた。

- 層1(hook): 見送り。主張がコード・外部ドキュメント・実行結果と一致するかは自然言語同士の
  意味照合であり、機械判定できない(`pr-294` の記録が既に同じ結論に達している)
- 層2(skill): 採用。実装フェーズで書いた直後に確かめるチェックリストとして
  `claim-verification` スキルを新設した。確かめ方の対応表そのものは複製せず
  `implementation-reviewer.md` を参照する
- 層3(観点): 見送り。2b の明文規則により、前回と同じ層への追記は選択肢に入れない
- 層4(rules): 変更なし。`rules/` は触っていない(`wc -l AGENTS.md rules/*.md` = 828 行、
  900 行以内で変更なし)

`comment-structure-claim` の締めは `harness/records/pr-356.md`、`comment-behavior-claim`
の締めは `harness/records/pr-359.md` へそれぞれ `対策済` 行を追記済み(この記録では再掲のみ)。

**この回の介入(`count.sh` が数える行):**

- 対策済: `comment-structure-claim` 層=skill at pr-363
- 対策済: `comment-behavior-claim` 層=skill at pr-363
