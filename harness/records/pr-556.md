# PR #556 単体テストを持たない非コンポーネントのモジュール 3 件にテストを足す

- マージ日: 2026-09-15
- 関連 Issue: #551
- 差分規模: 5 ファイル / +217 -1

## ゴールと結果

ゴールは「非コンポーネントで自分の `__tests__/` を持たないモジュールのうち、生成・判定・変換の
ロジックを持つものが、公開 API の振る舞いを固定するテストを持っている状態」。

洗い出しで 44 件（うち再 export のみ 9・型のみ 9・ロジックあり 26）を数え、この回はテストの器が
同じ 3 件（`token-template` / `DrawnBounds` / `use-rename-shortcut`）を扱った。残る 9 件は
#554 / #555 へ分割し、14 件は理由付きで却下して #551 に記録した。production の変更は 0 件。

ゴールとのずれ: 無し。ただし**26 件のうち 3 件しか扱っていない**ので、Issue のタイトル
（「単体テストがないファイルの単体テスト追加」）に対しては #554 / #555 が残っている。

## 指摘

### 1. 却下の件数が実測と合わない（14 と書いて 15 件並べた）

- 分類: `plan-scope-count-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画に「却下したのは 14 件」と書いたが、並べた却下リストは 15 件だった。`node-drop` を
  却下側に置いたまま数に入れていなかった（分類し直すと「再 export のみ」に入るので、
  ロジックあり 26 件の中の却下は 14 件で正しかった）。項目を 1 つずつ突き合わせていなかった。
- 既存ルール: `implementation-flow` フェーズ 3 手順 2
- 次にどう防ぐか: 今回は記録のみ

### 2. 走査の基準が計画の文と違ううえ、除外の適用が一貫していない

- 分類: `plan-scope-premise-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画に「`src/**/index.ts` と `utils/*.ts` を機械的に数えた」と書いたが、その文どおりに
  走査すると feature のバレル 8 件が入り、`features/canvas/utils/DrawnBounds.ts` は `utils/*.ts`
  に当たらないので入らない。**実際に対象にした基準が文に書かれておらず、読み手が再現できない**。
  型だけのモジュールも、`unit/axis` は却下として列挙しているのに `types/*.ts` 9 件は一覧に出て
  いなかった。走査コマンドをそのまま計画へ貼り、除外規則と除外件数を明示して直した。
- 既存ルール: `implementation-flow` フェーズ 3 手順 3
- 次にどう防ぐか: 今回は記録のみ

### 3. 既存テストが何を固定しているかの裏取りが不正確

- 分類: `plan-scope-premise-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画に「`editor-state.token.test.ts` が colors（採番）と spacing（値 0）しか固定して
  いない」と書いたが、実際には「同じ種別に続けて追加すると連番が付く」が `["radius", "radius-2"]`
  を固定しており、**radius の採番元も連番も既に守られていた**。この文のままだと次に読む人が
  「radius は誰も守っていない」と誤解する。固定済み / 未固定を表にして直した。
- 既存ルール: `implementation-flow` フェーズ 3 手順 3
- 次にどう防ぐか: 今回は記録のみ

### 4. 面積 0 のテストが、置き方次第で何も守らない

- 分類: `test-assert-unfalsifiable`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「面積を持たないものは拾わない」を確かめる計画だったが、`CanvasBounds.overlaps` は辺が
  接するだけでも真を返すので、面積 0 の要素を選択矩形から離して置くと `hasArea` を外しても
  結果が変わらない。面積 0 の要素を矩形の内側に置き、拾われる側を 1 件混ぜて期待値を
  「その 1 件だけの並び」にする、と計画へ書き足した。
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ

### 5. `collectOverlappingNames` の「描かれていない名前」の枝がケース一覧に無い

- 分類: `test-coverage-branch-asymmetry`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: フィルタは `Option.isSome` / `hasArea` / `overlaps` の 3 つの連言なのに、計画のケース
  一覧は後ろ 2 つしか持っていなかった。`enclosing` 側では「描かれていない名前は飛ばす」を
  対として挙げているのに、同じ枝が `collectOverlappingNames` 側だけ抜けていた。1 件足した。
- 既存ルール: rules/testing.md「その assert は落ちうるか」
- 次にどう防ぐか: 今回は記録のみ

### 6. 「名前の並びが空なら囲む矩形は無い」は `DrawnBounds` 側では落ちない

- 分類: `test-assert-unfalsifiable`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `enclosing([])` の答えを決めているのは `CanvasBounds.enclosing` の `length === 0` 分岐で、
  `DrawnBounds` 側の `flatMap` をどう壊しても空入力では結果が変わらない。計画段階では
  「`@returns` が仕様として書いているので残すが、落ちうる根拠には数えない」としたが、
  実装の検証（指摘 14）で削ることになった。
- 既存ルール: rules/testing.md「壊し方が思いつかないなら、そのテストは何も守っていない」
- 次にどう防ぐか: 今回は記録のみ

### 7. ケース 6 の assert が `value` だけを比べていた

- 分類: `test-default-input`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「間隔・角丸を足すと 0 から始まる」を `value` だけで比べる計画だったが、spacing と radius
  はどちらも 0 なので、`InitialValues` の引き先を取り違える壊し方が両方 0 のまま通る。期待値を
  トークン全体（`kind` / `name` / `value`）に直した。実装後に `kind: "spacing"` を固定する
  ミューテーションを当て、4 件落ちることを確認した。
- 既存ルール: rules/testing.md「既定値・フォールバックがある処理では、既定値と違う答えになる入力を選ぶ」
- 次にどう防ぐか: 今回は記録のみ

### 8. `use-rename-shortcut` を対象に選んだ基準が、却下の基準と揃っていない

- 分類: `plan-split-followup-omission`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `use-rename-shortcut` は定数 + 1 行委譲なのに対象へ入れ、同じく「定数だけ」を理由に
  `label-width` と `unit/axis` を却下していた。線引きが計画に書かれていないので、次に残り 9 件を
  扱う回で同じ判断ができない。「その定数が仕様のどの分岐に当たるかを決めているなら対象に数える」
  という基準を計画へ明記した。
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### 9. どの Issue を閉じるのかが計画に無い（「未決の判断: 無し」で締めていた）

- 分類: `plan-open-decision-declaration`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: #551 を `Closes` で閉じるのか、親として open に残すのかが計画に無かった。
  `AGENTS.md` の「着手した Issue はその回で閉じる」と「子へ分割した親 Issue は open のままでよい」
  が両方かかる形。後者は**閉じる起点（PR）が来ないもの**を指すと読んで、`Closes #551` に決めた。
  **計画の「未決の判断」欄は「無し」で締めていた**が、`plan-reviewer` が「8・9 は計画の時点で
  決めるべき判断として残っている」と指摘した。欄は埋めたが、その記載を疑っていなかった。
- 既存ルール: AGENTS.md「着手した Issue は、その回で閉じる」
- 次にどう防ぐか: 今回は記録のみ

### 10. テストの器の名前と、既存の器との重複回避が計画に無い

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: フックを呼ぶ器が要るのに、器の名前も既存 `KeyShortcutHarness` との衝突確認も計画に
  書いていなかった。`RenameShortcutHarness` に決め、`grep` で衝突 0 件を確かめた旨を添えた。
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 今回は記録のみ

### 11. `clearDrawn` の呼び方が計画に無い

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `drawNamed` が `document.body` へ直に足す要素は `src/test-setup.ts` の
  `afterEach(cleanup)` では外れず、テストをまたいで残る。`afterEach(clearDrawn)` を置くことを
  計画へ書き足した。
- 既存ルール: rules/testing.md「クリーンアップが必要な場合のみ `afterEach` を許可」
- 次にどう防ぐか: 今回は記録のみ

### 12. Ctrl+R と Cmd+R が同じ経路を 2 本確かめている（受け入れず）

- 分類: `duplication-uncovered-shape`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `CommandKey.isHeld` は `ctrlKey || metaKey` の 1 本で、同値性は
  `CommandKey.normal.test.ts` が固定済み。このフックの中では 2 本が同じ 1 つの事実を確かめている。
  **受け入れなかった**。既存の単発ショートカットフックのテスト 11 件がすべて 2 本置いており、
  ここだけ 1 本にすると「両 OS で確かめたのか」が読み手に分からなくなるため。減らすなら
  12 件まとめて変える話として理由を Issue に書いた。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 今回は記録のみ

### 13. `baseName` の直接テストが `toToken` のテストと完全重複

- 分類: `duplication-uncovered-shape`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `baseName` を直接呼ぶテスト 2 件が押さえる 5 種別の綴りは、`toToken` のテストがすべて
  `name` として持っており、**片方だけが落ちるミューテーションを 1 つも作れなかった**。
  兄弟の `NodeTemplate` のテストも `baseName` を直接呼ばず `toNode(...).name` で確かめている。
  2 件を削り、削除後に `BaseNames.radius → "radiu"` / `BaseNames.shadows → "shadows"` を
  当て直して、それぞれ 1 件落ちることを確認した。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する（実装・テストとも）」
- 次にどう防ぐか: 今回は記録のみ

### 14. 「名前の並びが空なら囲む矩形は無い」を削らずに残していた

- 分類: `test-assert-unfalsifiable`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 指摘 6 で「落ちうる根拠には数えないが残す」と判断したテストについて、実装の検証で
  ミューテーションを当てた結果、`DrawnBounds` 側を壊して落とす手段が無いことと、
  `canvas-bounds.normal.test.ts` が同じ事実を既に固定していることが確認された。削った。
- 既存ルール: rules/testing.md「壊し方が思いつかないなら、そのテストは何も守っていない」
- 次にどう防ぐか: 今回は記録のみ

### 15. 面積 0 のテストのコメントが主張している理由が成り立たない

- 分類: `comment-behavior-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「辺が接するだけでも重なりとみなすので、外して残る側を 1 件置かないと面積の判定を
  外しても同じ答えになる」と書いたが、実際に対照なしのテストを作って `hasArea` を消すと
  落ちた。対照 1 件が効くのは逆向き（何も拾わない実装）に対してで、理由の文が違っていた。
  計画の指摘 4 を受けて書いたコメントだが、**書いた理由そのものは動かして確かめていなかった**。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 16. 同じ 3 行コメントが 2 ファイルへ書き写された

- 分類: `duplication-rationale`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `afterEach(clearDrawn)` の注記を `DrawnBounds` の 2 ファイルへ一字一句同じで書いた。
  同じ状況の既存 2 ファイル（`use-canvas-view.fit` / `use-drawn-bounds.normal`）は 1 行参照
  だけで、この注記を持たない。「Testing Library の後片付けでは外れない」は `clearDrawn` 自身の
  性質なので、`canvas-measure.ts` の doc へ 1 回書き、テスト側は既存と同じ 1 行参照に揃えた。
- 既存ルール: rules/coding.md「規約の適用範囲」（同じ判断根拠を説明する doc の重複）
- 次にどう防ぐか: 今回は記録のみ

### 17. Shift のテストだけ、先例が持っている理由が無い

- 分類: `comment-missing-divergence`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「Shift を足した ⌘R では伝わらない」を置いたが、既存の兄弟で Shift の否定テストを
  置いているのは Shift 付きが**別の割り当てに当たる**ケース（undo / redo / グループ解除）だけで、
  Shift+R に当たる割り当ては無い。実測でもこのテストだけが落ちるミューテーションは作れなかった
  （守っているのは `KeyShortcut.matches` の Shift 判定で、`use-key-shortcut` が固定済み）。削った。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ

### 18. 計画の裏取りが 1 箇所、実物と食い違っていた（typography は `s` で終わらない）

- 分類: `plan-scope-premise-verification`
- 出どころ: `自己修正`
- 内容: 計画の裏取りに「末尾の `s` を落とす壊し方を単独で捕まえるのは `typography` と `shadows`」
  と書いたが、`"typography"` は `s` で終わらないので `replace(/s$/, "")` では変わらない。
  実際にミューテーションを当てて捕まえたのは `radius` の側だけだった。ケース 2 で
  `radius` / `typography` / `spacing` の 3 つとも見ているので結果は変わらないが、
  **`plan-reviewer` の指摘 3 を受けて書き直した文が、また動かさずに書いた主張だった**。
- 既存ルール: `implementation-flow` フェーズ 3 手順 3 / `claim-verification` スキル
- 次にどう防ぐか: 今回は記録のみ

### 19. `.claude/hooks/` が発火しない実行環境だった

- 分類: `hook-environment`
- 出どころ: `自己修正`
- 内容: push 前の `echo hook-canary` が deny されず通った。`.claude/hooks/README.md`
  「発火しているかを確かめる（カナリア）」の表で切り分けたところ、このセッション ID の
  検証エージェントのマーカーも発火ログも存在せず（`/tmp` にあるのは別セッション・9/11 のログ
  だけ）、取りこぼしではなく本当の不発だった。CI の代替が無い 2 つは手動で肩代わりし、
  どちらも該当する操作が無いことを確認した（`block-git-during-verification-agent.sh` =
  検証エージェント実行中のコミット 0 件 / `block-npx.sh` = `npx` 未実行）。
  git hooks（層 2）は push で動作しており、CI（層 1）も全チェック通過したので実害は無い。
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

- **計画の書き直し 1 回**（`plan-reviewer` の 12 指摘。10 件受け入れ・2 件は理由を書いて据え置き）。
  件数の不整合・走査の基準が再現できない・既存テストの裏取りの誤り・落ちない assert の置き方・
  枝の非対称・器の名前と後片付けの欠落・閉じる Issue の未決
- **実装の書き直し 1 回**（`implementation-reviewer` の 5 指摘。全件受け入れ）。
  テスト 4 件削除（`baseName` 直接 2 件 / 空配列 1 件 / Shift 1 件）・コメント 2 箇所修正・
  `clearDrawn` の理由を `canvas-measure.ts` の doc へ移設
- **CI 失敗 0 件。** push 前に 10 検査をすべて通してあり、CI は初回で全チェック通過した
  （VRT も差分 0 / 165 件 passed）
- **人のレビュー指摘 0 件。** `get_review_comments` / `get_reviews` / `get_comments` の 3 つとも
  引いた。`get_comments` の 4 件はすべて bot（Storybook プレビュー・カバレッジレポート・VRT）と
  自分が貼ったテストケース表

## 発火

- 発火: 計測対象外(フック不発)

## うまくいったこと

- **計画で「落ちうる」と書いた assert を、push 前に実際に壊して確かめた。** 6 通りの
  ミューテーションを当てて、どれが何件落ちるかを Issue と PR 本文へ書いた。指摘 13 の
  「完全重複」も、テストを削ったあとに同じミューテーションを当て直して守りが残ることを確認できた
- **却下 14 件の理由を Issue に 1 件ずつ残した。** CI のカバレッジレポートが 80% 未満として
  挙げた 2 ファイル（`tauri-dialog` 28.57% / `tauri-ipc` 75%）は、まさにこの回で
  「`vi.mock` が要るので覆う手段が無い」として却下したものだった。次に同じ数字を見る人が
  理由へ辿れる
- **テストの器を軸に 3 つへ分けた。** 「独立してマージできるか」だけだと 26 件すべてが該当して
  しまうので、器（React 不要 / `EditorProvider` / ポインタ操作）で切った

## 規約への反映

無し。この回の指摘はすべて既存の規約・スキルの手順に対応があり、新しい判断基準は
示されていない。数えて介入するかを決めるのは `harness-growth` の管轄なので、この記録では
集計も介入も行わない。棚卸しの材料として次の 2 点だけ書き添える。

- **`plan` に残したのは 2 件（指摘 10・11）** で、どちらも「計画に書く項目の抜け」。器の名前と
  後片付けの呼び方で、既存の `plan-*` 語彙のどれにも当たらない。指摘 8・9 は pr-547 で新設された
  `plan-split-followup-omission` / `plan-open-decision-declaration` に当てた
- **`duplication-uncovered-shape` の 2 件は、pr-530 の `対策済`（層=観点）より後の再発**
