# PR #267 [architecture] features/document-sync を切り出す（自動保存・外部編集の追従）

- マージ日: 2026-08-20
- 関連 Issue: #249
- 差分規模: 45 ファイル / +247 -76

## ゴールと結果

ゴールは「自動保存・外部編集の追従・書き戻しの一式が `features/document-sync/` に独立し、`editor` → `document-sync` の一方向になっている。`document-sync` から `EditorState` / `EditorProvider` への import は 0 件のまま」。達成した（`grep -rn "@/features/editor" src/features/document-sync` = 0 件）。

移したのは 5 モジュール（`document-sync-failure-list` / `use-auto-save` / `use-document-reload` / `use-file-revert` / `use-elapsed`）、`src/domains/` へ昇格したのは 2 つ（`document-save-state` / `elapsed`）、共有フィクスチャ 2 つを層直下へ寄せた（`instants.ts` → `src/domains/__tests__/`、`document-change.ts` → `src/libs/__tests__/`）。`opened-document` の昇格は #248（PR #264）で先行して済んでいたため、この回の作業から減った。

human レビューは 0 件（レビュー・レビューコメントとも 0）。CI は 6 チェックすべて 1 回目で success（視覚差分も差分 0 / passed 105）。**指摘はすべてサブエージェント（`plan-reviewer` 9 件 / `implementation-reviewer` 9 件）から出た。**

テストは 341 ファイル / 2326 テスト。移動前（340 / 2320）との差は `use-file-revert` の 1 ファイル / 6 ケースだけで、`origin/main` と HEAD の全テスト表題を sort / diff して 2279 行が完全一致することも確認した（観点の欠落なし）。

## 指摘

### 1. `elapsed` を昇格させる根拠が、計画自身の別の決定に依存していた

- 分類: `domain-scope-promotion`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `elapsed` の消費側は 5 ファイルで、4 つが `editor-top-bar`（`editor` に残る）、1 つが `use-elapsed`。つまり 2 番目の消費者 feature は「`use-elapsed` を `document-sync` へ移す」という計画自身の決定によってのみ生まれる。しかも計画は `use-elapsed` がなぜ `document-sync` に属するのかを書いておらず、ゴールが名指しする 3 つ（自動保存・外部編集の追従・書き戻し）のどれでもなかった。**pr-261 の指摘 1 と同じ形**（2 番目の消費者を名指しできるかどうか）
- 既存ルール: rules/architecture.md「配置の判断基準」
- 次にどう防ぐか: 今回は記録のみ（`elapsed` に固有の理由 —「起点が `FileValidity.since`（外部編集でファイルが不正になった時刻）で、数えているのは今映っているものがファイルからどれだけ古いか」= 追従が失敗している側の状態そのもの — を書いて昇格を維持し、その Why を `document-sync/index.ts` の doc に置いた）

### 2. 直前の PR がゴールとして潰した辺を、申し送りメモとして扱っていた

- 分類: `layer-dependency`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `DocumentSaveState` の昇格で、本番コードの `src/domains/**` から `@/libs` への import が初めて 1 件生まれる（`DocumentIpcError` の型 import）。pr-261 のゴールは「**`src/domains/` から `@/libs` への辺を作らずに**」で、1 つ前の PR がわざわざ避けた辺を次の PR が作ることになる。計画はこれを「#257 への申し送り」として書いていたが、CLAUDE.md「設計判断の確認」が言う**未決の判断**として扱う対象だった。却下案に「`features/document-sync/domains/` に置いて `index.ts` から re-export する案」と「`DocumentIpcError` をドメインへ移して依存を逆転させる案」も挙がっていなかった
- 既存ルール: rules/architecture.md「依存方向のルール」/ CLAUDE.md「設計判断の確認」
- 次にどう防ぐか: 今回は記録のみ（未決の判断として Issue と Discord へ選択肢を出し、実装は Issue の決定どおり A で進めた。差し戻しは `git mv` + re-export 1 行で済むことも併記した）

### 3. 共有フィクスチャの帰属の理由が、その値に固有でなかった

- 分類: `ownership-reasoning`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画は `instants.ts` を「中身が `Instant.create(0)` だから `@/domains/instant` と同じ層へ」、`document-change.ts` を「対象が属する `libs` 層へ」と書いていたが、前者の理由は `elapsed/__tests__/setup.ts`（同じく `Instant`）にも、後者は `use-auto-save/__tests__/setup.ts`（同じく `DocumentIpc` を包む）にも同じだけ成り立つ。**中身の型は置き場所を決めていない。** 実際に効いている理由は先例（`src/domains/__tests__/document-errors.ts` の doc）が書いているとおり「消費側が 2 つ以上の層／feature にまたがるため」
- 既存ルール: rules/architecture.md「ロジックの帰属先」
- 次にどう防ぐか: 今回は記録のみ（理由を「共有する理由」と「置き場所の理由」に分けて書く形。前者は消費側の割れ方、後者は値の出どころの層）

### 4. 却下案に、実在するいちばん近い候補が入っていなかった

- 分類: `plan`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `document-change.ts` の却下案は `src/libs/document-ipc/fake/`（本番フォルダ）だけを潰していたが、実在するもっと近い候補は `src/libs/document-ipc/__tests__/`（既存フォルダ）だった。却下の理由も `@testing-library` ではなく「モジュールフォルダの内部になるので外部からの import が deep import になる」が正しい
- 既存ルール: rules/coding.md「Why not を書く相手は、実際に検討して捨てた案だけ」
- 次にどう防ぐか: 今回は記録のみ（新しいフォルダを作る計画では、**同じ役割の実在フォルダ**を先に列挙してから却下案を書く形。結論は変えず理由だけを載せ替えた）

### 5. 「grep で数えた実数」と銘打った表に、実測していない行が混ざっていた

- 分類: `plan-scope-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画は `src/` の外で `features/editor/domains/elapsed` を含む記録として `harness/records/pr-205.md` / `pr-210.md` / `pr-261.md` の 3 本を挙げていたが、実際に含むのは `pr-205.md:48` と `pr-210.md:23` の 2 本だけだった。`pr-261.md` は含まない
- 既存ルール: implementation-flow フェーズ 3「立てた収集条件を実際にコードへ当てて件数を検算する」
- 次にどう防ぐか: 今回は記録のみ（`src/` の外を数えるときも `grep -rl` を全体に当てて、ファイル名を 1 本ずつ突き合わせる）

### 6. 件数表の基数（ファイル数か import 文数か）を書いていなかった

- 分類: `plan-scope-verification`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画の書き換え件数表は、`opened-document-editor/index.tsx` の行を「1」と数えていた。ファイル数としては 1 だが import 文は 5 本（10 / 34 / 39 / 41 / 43-45 行）で、文で数えると 5 になる。基数が書かれていないと、読む側が別の数え方で照合して食い違う。**pr-261 の指摘 4 と同じ形**
- 既存ルール: implementation-flow フェーズ 3「呼び出し側を grep で数える」
- 次にどう防ぐか: 今回は記録のみ（件数表の見出しに「ファイル数」と明記する）

### 7. ゴールの不変条件を確認する手順が計画に無かった

- 分類: `plan-integration-coverage`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: ゴールは「`document-sync` から `EditorState` / `EditorProvider` への import は 0 件のまま」だったが、計画に書いた検証手順は `pnpm run test:run` の件数突き合わせ（340 ファイル / 2320 テスト）だけだった。**テスト件数はこの不変条件を一切守らない。** 完了条件として `grep -rn "@/features/editor" src/features/document-sync` が 0 件になることを書くべきだった
- 既存ルール: `plan-review.md`「ゴールが『画面から届く経路を通すこと』なら」
- 次にどう防ぐか: 今回は記録のみ（ゴールに測れる不変条件を書いたなら、**それを測るコマンド**を完了条件に併記する形。今回は実装後に grep で確認して PR 本文に載せた）

### 8. 観点が落ちないことの確認が、件数比較だけになっていた

- 分類: `plan-integration-coverage`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 移すテストファイルの一覧は実測と完全に一致していたが、次の 2 行が無かった。(a) `use-file-revert` には `__tests__/` が無く、その振る舞いを守っているのは `editor` 側に残る `opened-document-editor/__tests__/` の 5 ファイル（`sync-failure` / `file-invalid` / `frozen` / `last-valid-render` / `top-bar`）であること。(b) 配線の通しを新規に足さない判断（純粋な移動で配線切れは typecheck が落とすため「要らない」）。書かれていないと、挙げ忘れなのか要らないと確定したのかが区別できない
- 既存ルール: `plan-review.md`「単体しか無いなら、通しが要らないと確定したのか挙げ忘れなのかを 1 行で書かせる」
- 次にどう防ぐか: 今回は記録のみ（移動の計画でも「移さないテストが何を守っているか」を 1 行書く）

### 9. 新しい feature の `index.ts` に doc を置く計画が無かった

- 分類: `comment-missing`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: 計画のファイル表は `document-sync/index.ts` の「置くもの」欄を export の羅列だけにしていた。先例の `document-start/index.ts` は「何がこの feature に属し、何が属さないか」の doc と Why not（`editor-screen` をここに置かない理由）を持っている。指摘 1 の答え（`use-elapsed` がなぜここか）と却下案の答え（`editor-top-bar` がなぜここでないか）は、まさにこの doc に書かれるべき Why / Why not だった
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 今回は記録のみ（新しいモジュールフォルダを作る計画では、`index.ts` の「置くもの」に doc も書く）

### 10. 移す doc が、移動後の層から見て成り立たない名指しを含んでいた

- 分類: `comment-stale-edit`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `instants.ts` の doc は `EditorState.applyReload` を名指ししており、`src/domains/` へ移すと「domains のフィクスチャが feature の型を名指ししている」形になる。計画のファイル表に「doc を直す行」として挙がっていなかった。同じ形が `elapsed/index.ts`（綴りの持ち主を `editor-top-bar` と名指し）にもあり、こちらは実装中に自分で見つけて直した
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ（層をまたぐ移動では、移すファイルの doc が**移動先の層から参照してよい語彙だけで書かれているか**を確認する。**pr-261 の指摘 3 と同じ形**で、あちらは「変更で嘘になるコメント」、今回は「層が変わって参照できなくなる名指し」）

### 11. 移動後に別 feature を指すことになるコメントの扱いを決めていなかった

- 分類: `comment-stale-edit`
- 出どころ: レビュー（`plan-reviewer`）
- 内容: `editor` 側に残る 3 箇所（`opened-document-editor.frozen.test.tsx:162` が `use-auto-save.file-invalid.test.tsx` を、`opened-document-editor.last-valid-render.test.tsx:34` が `use-elapsed` を、`editor-state.frozen.test.ts:13` が `useAutoSave` を名指し）が、移動後は別 feature を指すコメントになる。パスが壊れるわけではないので直す義務は無いが、ファイル表に挙げるかどうかの判断が書かれていなかった
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ（3 箇所とも feature 名（`document-sync` の）を前置して、読み手が辿れる形にした）

### 12. `index.ts` の Why not が、依存の向きを逆に書いていた

- 分類: `comment-false-claim`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `document-sync/index.ts` の Why not に「バーは保存状態とズームの両方を映すので、移すとキャンバス側からこの feature への依存が生まれる」と書いた。**向きが逆。** `editor-top-bar/index.tsx:11` が `@/features/editor/domains/canvas-view` を import しているので、バーを移すと `document-sync` → `editor` の domains への直接 import になる（規約が禁じている向き）。しかもコメントが書いていた向き（組み立て点 → `document-sync`）は**今まさに存在していて、公開 API 経由なので規約が許している**。結論（バーは `editor` に残す）は変わらないが、根拠が成立していなかった
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ（「移すと依存が生まれる」と書く前に、**移す対象の import 文を実際に読んで**どちらからどちらへの辺かを確認する。今回は `editor-top-bar` の import を読んでいれば書けなかった）

### 13. 置き場所の doc に、事実でない「同じ形」の主張を書いた

- 分類: `comment-false-claim`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `src/libs/__tests__/document-change.ts` の doc に「（`src/domains/__tests__/` と同じ形）」と書いたが、同じ形ではない。`instants.ts` はドメイン型 1 つしか引かないのに対し、こちらは `@testing-library/react` を引いており、`src/domains/` `src/libs/` `src/utils/` 配下で `@testing-library` を import している唯一のファイルになる（本番の `libs/` は React 依存禁止の層）。置き場所自体は擁護できるが理由が別だった
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ（先例を引くときは「同じ形」と書く前に、**引いている依存が実際に同じか**を確認する。今回は理由を「組み立てているのが `DocumentIpcFake` なので値の出どころの層に置く」へ載せ替え、React 依存が入る点を Why として明示した）

### 14. ゴールが作った不変条件を、何も強制していない

- 分類: `layer-dependency`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: レビュアーが実際に壊して確認した。`use-elapsed/index.ts` に `import { EditorState } from "@/features/editor/domains/editor-state"` を足しても、`opened-document-editor` の公開 API 1 経路を 5 本の deep import に書き換えても、typecheck / oxlint / biome / doc コメント検査 / テスト規約検査がすべて通り、`document-sync` のテストも全通過する。**この不変条件はいまレビューの目だけが担保している**（`.oxlintrc.json` は `curly` / `max-depth` の 2 ルールだけで import 制限は未設定）
- 既存ルール: rules/architecture.md「依存方向のルール」
- 次にどう防ぐか: 今回は記録のみ（**#257「依存方向を CI / lint で強制する」がまさにこのスコープ**なので、この PR では強制を足さず、拾うべき検査 3 つ — `document-sync` 内の `@/features/editor` / feature 間の deep import / `src/domains/**` の本番コードからの `@/libs`（`__tests__` は対象外） — を #249 のコメントへ書き出した）

### 15. 公開 API になったモジュールに、モジュール直下のテストが無かった

- 分類: `なし`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `use-file-revert` は移動前から `__tests__/` を持たず（`origin/main` でも同様なので新規の欠落ではない）、成功・失敗の各経路を守っているのは `editor` 側の通しテスト 1 件ずつだけだった。この PR で `useFileRevert` / `FileRevertControl` が `document-sync` の公開 API になったので、モジュール直下にテストを置く価値が生まれた。**「公開 API になったモジュールはテストを持つ」という規則は `rules/` に無い**（rules/testing.md は「テスト対象は公開 API」とは書いているが、公開 API 化に伴ってテストを足す、とは書いていない）
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ（6 ケースを追加し、すべてがミューテーションで落ちることを実測した。`onReverted()` の削除 / 失敗分岐の `Saved` 化 / `Saving` への遷移の削除 / 成功後の復帰の削除でそれぞれ狙った 1 件が落ち、`ipc.save` を呼ばない形では 4 件が落ちる）

### 16. 追加したテストの 1 件が、初期値と同じ答えを確かめていた

- 分類: `test-default-input`
- 出どころ: 自己修正
- 内容: 「書き込みが終わると書き出し済みに戻る」を、最初は `holdWrites` を使わずに「`revert()` して `Saved` を確かめる」形で書いた。保存状態の初期値が `Saved` なので、**書き戻しが何もしなくても通る**。書いた直後の自己チェック（`rules/testing.md`「その assert を通すために実装をどう壊せるか」）で気づいた
- 既存ルール: rules/testing.md「既定値・フォールバックがある処理では、既定値と違う答えになる入力を選ぶ」
- 次にどう防ぐか: 今回は記録のみ（書き込みを止めて状態を一度 `Saving` へ離させる形に変え、成功後の復帰を消すと落ちることを実測した）

### 17. コメントの折り返し幅が、同じブロックの幅から外れた

- 分類: `comment-stale-edit`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: 指摘 11 の対応で「`document-sync` の」を挿し込んだ 2 行だけが、同じブロックの折り返し幅（80〜87 桁）を超えた（`editor-state.frozen.test.ts:13` が 99 桁、`opened-document-editor.last-valid-render.test.tsx:34` が 94 桁）。biome は日本語コメントの折り返しを見ないので誰も教えてくれない
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ（コメントへ語を挿し込んだら、その**ブロック全体**を折り返し直す。桁数はバイト数ではなく表示幅で数える — 日本語は 2 桁）

### 18. `sync` という語が docs / UI 案に 1 件も無い

- 分類: `naming-absent-metaphor`
- 出どころ: レビュー（`implementation-reviewer`）
- 内容: `docs/*.md` 7 ファイルと UI 案（展開後 HTML）で「sync / 同期」を `grep -i` すると **0 件**。それでもこの feature 名を採ったのは、`DocumentSyncFailureList` が main に既にあり、コード内の前例に揃うため。`index.ts` の doc は `docs/05-architecture.md` の実在する 3 見出し（「保存モデル: 自動保存」「外部編集の検知」「競合の解決」）へ紐づけてある
- 既存ルール: rules/naming.md「比喩・抽象語は `docs/06-ui.md` と `docs/Design Composer.html` に `grep -i` で数える」
- 次にどう防ぐか: 今回は記録のみ（受け入れ可と判断した理由 — docs の語彙には無いが既存コンポーネント名に合わせた — を #249 に残した。この判断が再燃したときに前回の理由を辿れるようにするため）

### 19. `.claude/hooks/` が発火しない実行環境だった

- 分類: `hook-environment`
- 出どころ: フック
- 内容: push 前の手順どおり `echo hook-canary` を実行したが **deny されずに通った**。`.claude/settings.json` の配線が読まれないリモート実行環境だった
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」
- 次にどう防ぐか: 今回は記録のみ（`pnpm install` の `prepare` が `core.hooksPath = harness/githooks` を配線しており、`git push` では pre-push が実際に発火して oxlint / biome / doc コメント / テスト規約すべて通過した。CI も 6 チェック全 success。ゲートは効いているので手順は変えず、PR 本文と Discord 通知に記録した）

## 手戻り

**相対 import を数え漏らして typecheck が 1 度落ちた。** フィクスチャの移動で、呼び出し側を `@/features/editor/__tests__/instants` の綴りだけで grep したため、`src/features/editor/__tests__/frozen-state.ts:3` の `from "./instants"`（同フォルダ内の相対 import）を数え落とした。`git mv` 後の `pnpm run typecheck` で TS2307 が出て気づき、その場で直した。**指摘 5 / 6 と同じ「収集条件を実例に当てて検算する」の系統**だが、こちらはレビューではなく自分の検査で捕まえた。

**追加テスト 1 件を書き直した**（指摘 16）。

上記 2 件以外は計画どおり。計画から外れた判断が 1 つあり、それは**「新しいテストケースは足さない」としていた計画に対して `use-file-revert` の 6 ケースを足したこと**（指摘 15）。理由は「この PR で公開 API になったのでモジュール単体の観点が要る」で、Issue へ追記した。

## うまくいったこと

- **層をまたぐ移動を 3 コミットに分けた**（共有フィクスチャ / 昇格 / 切り出し）。切り出しの差分から「どのモジュールが層をまたいだか」が読め、レビュアーも各コミットの範囲で確認できた
- **移動前にテストの実数を取っておいた**（340 ファイル / 2320 テスト）。移動後との突き合わせで観点の欠落を確認でき、さらにレビュアーが全テスト表題の sort / diff（2279 行が完全一致）で裏取りできた
- **追加したテストをミューテーションで実測した。** 6 ケースすべてが個別の壊し方で落ちることを確認したので、「守っているつもり」で終わらなかった（指摘 16 に気づけたのも同じ流れ）
- **未決の判断を止めずに扱った。** 指摘 2 の設計判断は Issue と Discord へ選択肢と根拠を出し、差し戻しが機械的な変更で済むことを併記したうえで実装を進めた。回答待ちで止めると何も出来上がらず、かつ差し戻しコストが小さいことが事前に分かっていたため

## 規約への反映

**無し。** 今回の 19 件は、次の理由でいずれもこの回では介入しない。

- **`plan-scope-verification`（指摘 5・6）と `plan-integration-coverage`（指摘 7・8）** は pr-227 で観点層へ介入したばかりで、そこからの再発数は各 2 件。`harness-growth` の「同じ層で再発したら層を 1 つ上げる」は数えたうえで判断する話なので、集計はそちらへ回す
- **`comment-false-claim`（指摘 12・13）** は 2 件とも「主張を実測に照らさずに書いた」形で揃っており、pr-219 で分割した直後の分類。数え直しは `harness-growth` の管轄
- **`comment-stale-edit`（指摘 10・11・17）** は 3 件出たが、うち 2 件（11・17）は同じ対応の中で連鎖したもの
- **`layer-dependency`（指摘 2・14）** は #257 が引き取る前提で、この PR では強制を足さないと決めた。フック / CI 層の介入先はそちらにある
- **`分類: なし`（指摘 15）** は 1 件。語彙への昇格は 10 件溜まってから（`harness-growth` の逃し弁）

集計と介入の判断は `harness-growth` の管轄なので、ここでは記録を残すまでとする。
