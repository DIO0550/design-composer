# PR #639 🐛 [Bug Fix]: #646 duplicate-issue-pr の検査を、失敗が見える形に固める

- マージ日: 2026-09-20
- 関連 Issue: #646（当初は #635。経緯は下記）
- 差分規模: 4 ファイル / +213 -17（最終形。作業中は 6 ファイルだったが、下記のとおり検出器と判定表が丸ごと破棄になった）

## ゴールと結果

当初のゴールは #635「PR が開かれた / 更新されたときに、同じ Issue を閉じる別の open な PR があればその PR のコメントで分かる」。**達成したのは PR #636 のほう。**

**同じ Issue #635 を PR #636 が並行して実装し、06:47Z に先にマージされた。** 衝突箇所はすべて main の版を採り（`pr-533` / `pr-605` の前例）、こちらが作った `check-parallel-issue-pr.sh` と 23 ケースの判定表は破棄した。残したのは main に無いものだけで、ゴールを #646「マージ済みの `duplicate-issue-pr` が黙って効かなくなる経路を持たない状態にする」へ置き直して達成した（ページング / 赤にする条件 / 判定表 13 ケース追加 / 分類の語彙）。

**この回の並行実装は、この PR が入れた検査自身が最初の実行で検出した**（`重複: Issue #635 → #636`）。

## 指摘

### 1. 同じ Issue を 2 つのセッションが並行して実装し、後発が作り直しになった

- 分類: `parallel-issue-work`
- 出どころ: `CI`
- 内容: 棚卸しルーティンのセッション（#636）と、その Issue のアサインで起動したこちらの
  セッション（#639）が、ほぼ同時刻に同じ #635 の実装を始めた（`pr-573` 指摘3 と同じ経路）。
  どちらも相手に気づかないまま PR まで進み、**気づいたのは PR を出した後、入れたばかりの検査が
  `重複: Issue #635 → #636` を出したとき**。後発のこちらが実装を丸ごと捨てて再スコープした。
- 既存ルール: なし（この回で `duplicate-issue-pr` が入るまで、着手前に同じ Issue の PR を
  探す手順はどこにも無かった）
- 次にどう防ぐか: 検査は入った。残る穴は「PR を出す前」で、`pr-573` 指摘3 と同じくイベントが
  発生しない区間。着手時の確認は人（セッション）の手順に留まる

### 2. 着手時に引いた open な PR の一覧に、1 分前にできた PR が出てこなかった

- 分類: `parallel-issue-work`
- 出どころ: `自己修正`
- 内容: 着手時に `list_pull_requests`(state=OPEN) を引き、結果は #631 / #634 の 2 本だった。
  **#636 の作成は 00:18:29Z で、この呼び出しはその直後**。一覧に現れなかった理由は、
  作成直後の PR が反映されるまでの遅れか、呼び出しが先行したかのどちらかで、**断定できない**
  （どちらであれ、着手時の一覧だけでは同じ分に立つ PR を捕まえられない）。
- 既存ルール: なし
- 次にどう防ぐか: 着手時の一覧は「その時点のもの」でしかない。PR を出した後に走る検査
  （この回で入った `duplicate-issue-pr`）が最後の網になる

### 3. 採用前の再生（捕捉率と偽陽性）が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `harness-growth`「採用前の再生」は「捕まえた件数と偽陽性 0 を PR に書く」を求めるが、
  計画には「実例を JSON で再現する」しか書いていなかった。実測したところ 4 件中 3 件
  （`pr-533` / `pr-604` / `pr-605`）を捕まえ、`pr-573` は 2 本目の PR が無いので対象外だった。
- 既存ルール: `.claude/skills/harness-growth/SKILL.md`「採用前の再生」
- 次にどう防ぐか: 介入の計画では、捕捉率の実測を計画の項目として先に置く

### 4. `count.sh --shrink` 節 2 に該当があるのに、計画が縮める側に触れていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `harness-growth`「5. 縮める」は「該当があるのに飛ばさない」と定めているが、計画には
  縮める項目が 1 つも無かった。`naming`（内部=124 / 以降=97）と `over-guard`（内部=23 / 以降=91）
  の 2 候補があり、#637 として分離した（`rules/` と行数のラチェットを動かす別の差分のため）。
- 既存ルール: `.claude/skills/harness-growth/SKILL.md`「5. 縮める」
- 次にどう防ぐか: 棚卸しの計画では `--shrink` の出力を計画に貼ってから分岐を決める

### 5. 「CI は赤にしない」を壊す経路が 2 つ、計画で塞がれていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: コメントの投稿が失敗すると `set -euo pipefail` でそのまま赤になる経路と、
  `permissions` に `contents: read` が無く `actions/checkout` が落ちる経路。どちらも
  ゴール（判定では赤にしない）を壊すが、計画は「問い合わせの失敗」しか扱っていなかった。
- 既存ルール: なし
- 次にどう防ぐか: 「◯◯のときだけ赤」を掲げたら、赤になりうる経路を**列挙してから**計画に書く

### 6. sticky コメントの探索が 1 ページ目で切れていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 既存 2 実装（`frontend.yml` / `storybook-visual-regression.yml`）は PR の早い段階で必ず
  1 回貼るのでページ 1 に載るが、**この検査は重複が出たときにしか貼らない**。コメントが
  積もった後に貼られると marker を見失い二重投稿になる。`--paginate` で直した。
- 既存ルール: `rules/coding.md`「外部の挙動は動かして確かめる」
- 次にどう防ぐか: 既存実装を踏襲するときは、**踏襲元と自分で前提が違わないか**を 1 つずつ見る

### 7. コメントの投稿経路がテストケースに 1 件も無かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画のケースは判定行と終了コードしか見ておらず、**コメントを 1 度も投稿しない実装でも
  全ケースが緑**になる形だった。`gh` を差し替えて貼る / 差し替える / 貼らないの 4 分岐を
  固定するケースを足した。
- 既存ルール: `rules/testing.md`「その assert は落ちうるか」
- 次にどう防ぐか: ゴールの文に出てくる動詞（「コメントで分かる」）が、ケースの期待結果に
  1 つも現れていなければ、その観点は抜けている

### 8. 本番に存在しない入力をケースが固定していた

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: open な PR の一覧には必ず自分自身が入るのに、「他に open な PR が無い」を空配列で
  表していた。空配列では自己除外を壊しても落ちない。全ケースの `nodes` に自分を含める形へ直した。
- 既存ルール: `rules/testing.md`「その assert は落ちうるか」
- 次にどう防ぐか: フィクスチャを書いたら「この入力は本番で起きるか」を 1 度問う

### 9. 新しい分類語彙の置き方が未決のまま計画に書かれていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「分類の語彙へ 1 行」とだけ書いており、トップレベルの分類にするのか `harness` の
  旧語彙にするのかで**観測される結果が変わる**（後者は `count.sh` の `fold_tag` も要る）。
  `over-guard` を前例にトップレベルへ置くと決め、集計側の変更を不要にした。
- 既存ルール: `.claude/skills/harness-record/templates/record.md`「分類の語彙」
- 次にどう防ぐか: 語彙を足す計画では、足した後に `count.sh` の出力がどう変わるかまで書く

### 10. ワークフローの「何行目の何を直すか」が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「ジョブが 1 つである前提で書かれたコメントも直す」とだけ書き、対象の記述を挙げて
  いなかった（`pr-627` 指摘8 と同じ形）。実際の対象は冒頭の説明・`ci-trace` に載せない理由・
  concurrency・必須チェックの注記の 4 つだった。
- 既存ルール: なし
- 次にどう防ぐか: ファイル名だけでなく、直す記述を計画のファイル表に列挙する

### 11. sticky コメントの upsert が 3 箇所目になることに触れていなかった

- 分類: `duplication`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 同じ「marker で探して PATCH / 無ければ POST」が既に 2 箇所（yml のインライン）に
  あり、この差分で 3 つ目になる。共通化は無関係な 2 ワークフローの書き換えを伴うので
  #638 として分離した。
- 既存ルール: `rules/coding.md`「同じ処理が2箇所に現れたら共通化する」「規約の適用範囲」
- 次にどう防ぐか: 既存実装を踏襲するときは、踏襲＝重複の追加であることを計画に書く

### 12. `if !` の条件位置では関数内の `set -e` が効かず、二重投稿になっていた

- 分類: `type`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `existing_comment_id` を `if ! existing="$(existing_comment_id)"` で呼んでいたため、
  関数の中で `gh` が 5xx で落ちても最後の `printf` の 0 が返り、**「コメントは無い」と読んで
  新規投稿していた**（実測で再現: `GET-FAILED` のあと `POST`）。`|| return 1` を明示して直した。
- 既存ルール: `rules/coding.md`「外部の挙動は動かして確かめる」
- 次にどう防ぐか: シェルの `set -e` の抑止は、書いた形をそのまま動かして確かめる

### 13. 差し替えた `gh` が本文を捨てており、marker も表の行も守られていなかった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: スタブが `-f body=` を見ないので、marker を別の綴りにしても表の行を出さなくても
  全ケースが緑だった（実測）。本文を `$STUB_BODY_FILE` へ落とし、marker で始まることと
  相手の PR の行が入ることを 2 ケースで固定した。
- 既存ルール: `rules/testing.md`「その assert は落ちうるか」
- 次にどう防ぐか: スタブを書いたら、**スタブが捨てている引数**が仕様に含まれていないかを見る

### 14. 「失敗しても赤にしない」側が 1 ケースも踏まれていなかった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: スタブの POST / PATCH が必ず成功するので、冒頭コメントが明言している判断
  （コメントの失敗は緑・問い合わせの失敗は赤）の片側だけが表にあった。`FAIL_ON` で
  失敗を注入するケースを 3 つ足した。
- 既存ルール: `rules/testing.md`（対の片側しか無い形）
- 次にどう防ぐか: doc に「◯◯は赤 / △△は緑」と書いたら、両側をケースにする

### 15. 括弧内に書いた理由が、実物と食い違っていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「自分の番号も問い合わせ結果から読む（判定表が PR 番号を別に渡さずに済む）」と
  書いたが、判定表は `PR_NUMBER` を渡しており、コメントの URL がそれを必須にしていた。
  クエリから `pullRequest.number` を落とし、`--argjson self "$PR_NUMBER"` の 1 経路にして
  理由ごと消した。
- 既存ルール: `rules/coding.md`「コメントは doc と Why / Why not に絞る」
- 次にどう防ぐか: 括弧内の「〜で済む」は、その状態を実際に作れるか確かめてから書く

### 16. 同じ値を 2 経路から導いていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: この PR の番号を `$repo.pullRequest.number`（JSON）と `PR_NUMBER`（env）の両方から
  導いており、食い違う入力を判定表で作れるのに何も落ちなかった。1 経路に寄せた。
- 既存ルール: `rules/coding.md`「同じ処理が2箇所に現れたら共通化する」
- 次にどう防ぐか: 同じ意味の値が 2 つの出どころを持ったら、どちらかを落とす

### 17. `duplicate` がこのリポジトリで既に別の意味を持っていた

- 分類: `naming`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `duplicat*` は一貫してコードの重複を指す（分類 `duplication` /
  `check-added-test-helper-duplication.sh` / `duplicate-test-helpers.py`）。一度は
  `check-parallel-issue-pr.sh` へ改名したが、**main の版を採る再スコープで元に戻った**。
  分類の語彙だけ `parallel-issue-work` として残っている。
- 既存ルール: `rules/naming.md`「その名前が既に別の意味を持っていないか確認する」
- 次にどう防ぐか: 接頭辞の grep は計画の段階で行う（この回は実装の検証まで出なかった）

### 18. concurrency の注記が、打ち切りの効果を言い過ぎていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「コメントが古い実行と競合しない」と書いたが、打ち切りは投稿済みのコメントを
  戻さない。重ね貼りを防いでいるのは marker による差し替えの側。
- 既存ルール: `rules/coding.md`「コメントは実装と一致させる」
- 次にどう防ぐか: 仕組みの効果を書くときは、効かない範囲も 1 つ挙げて確かめる

### 19. main の `visual-regression` が、どの PR でも赤になる状態だった

- 分類: `ui`
- 出どころ: `CI`
- 内容: PR #634 が Size 節に 4 行足した結果、PropertyPanel の 6 ストーリーが 1280x720 に
  収まらず**撮影ステップ**で落ちる。#634 は本文にこれを「判断待ち」と書いたままマージされ、
  `visual-regression` は `pull_request` でしか走らないので main 単体では見えない。
  この PR の差分は `src/` に 1 行も触れていない。#650 として切り出した。
- 既存ルール: `rules/ui-verification.md`「差分ではなく撮影で落ちることがある」
- 次にどう防ぐか: 判断待ちのまま赤を残してマージすると、後続の PR 全部が同じ赤を踏む

### 20. 判定表のフィクスチャが `totalCount` と `nodes` の数を取り違えていた

- 分類: `test`
- 出どころ: `自己修正`
- 内容: 最初に書いたケースは `totalCount` を `nodes` の数と無関係に置いたため、切り詰めて
  いないはずのケースで注記が出て 8 件が NG になった。`nodes` に自分を含める形へ直し、
  `totalCount` は省略時に `nodes` の数へ落ちるようにした。
- 既存ルール: なし
- 次にどう防ぐか: フィクスチャは「本番でありうる形」を先に書いてから、崩す側を足す

### 21. 再試行のケースが、コメント側の呼び出しまで数えていた

- 分類: `test`
- 出どころ: `自己修正`
- 内容: 差し替えた `gh` が呼び出しを一律に数えていたため、コメントの投稿まで再試行回数に
  入り、1 回で通るケースが「3 回」と出た。スタブを `graphql` の呼び出しだけ数える形へ直した。
- 既存ルール: なし
- 次にどう防ぐか: スタブが 2 種類の呼び出しを受けるなら、数える対象を先に決める

### 22. 検証エージェントの実行中に、コミットを促す停止時フィードバックが来た

- 分類: `harness`
- 出どころ: `フック`
- 内容: `implementation-reviewer` がミューテーションで実装を書き換えている最中に、停止時の
  未コミット検査が「コミットして push してください」と促した。従うとミューテーションが
  コミットに載るので、完了を待ってからコミットした（`pr-542` 指摘26 / `pr-543` 指摘44 と同じ形）。
  このセッションはフック不発環境で、`block-git-during-verification-agent.sh` は働いていない。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md`「実行中に git add / commit / push を挟まない」
- 次にどう防ぐか: 記録のみ（3 回目の形。数えるのは `harness-growth` の管轄）

## 手戻り

- **PR を 1 度再スコープした。** #636 が先にマージされたため、衝突した 3 ファイルをすべて
  main の版へ寄せ、自作の検出器（208 行）と判定表（23 ケース・266 行）を破棄した。
  タイトル・本文・閉じる Issue（#635 → #646）も差し替えた
- 判定表のフィクスチャを 1 度書き直した（`totalCount` と自分自身の扱い）
- 検出器を 1 度改名して、再スコープで元の名前へ戻した（`duplicate` → `parallel` → `duplicate`）
- 計画を 1 度書き直した（`plan-reviewer` の 14 件のうち 12 件を受け入れ）

## 発火

- 発火: 計測対象外(フック不発)

`echo hook-canary` が deny されず、検証エージェントのマーカー
（`/tmp/design-composer-verification-agents-<セッション ID>`）も、このセッション ID の
発火ログも存在しなかった（`.claude/hooks/README.md`「発火しているかを確かめる」の 4 行目）。
禁止コマンド（`npx` / `pnpm dlx` / `git add -A`）は実行していない。

## 規約への反映

無し。この回は記録を残すところまでで、介入の判断は `harness-growth` の管轄。

分類 `parallel-issue-work` の語彙はこの PR が追加したが、`対策済` の行は #636 が
`at pr-636` で既に書いている（同じ形への介入は 1 本）。スコープ外に残した 3 件は
#637（`--shrink` 節 2）/ #638（sticky コメント upsert の共通化）/ #650（main の VRT）。
