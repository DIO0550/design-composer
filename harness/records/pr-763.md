# PR #763 doc コメント検査からファイル単位のハッチ @doc-comments-ok を外す

- マージ日: 2026-09-25
- 関連 Issue: #756
- 差分規模: 4 ファイル / +7 -23

## ゴールと結果

ゴールは達成した。`src` の doc コメント検査に、ファイル単位で外す手段(`// @doc-comments-ok`)が無い状態になり、
Claude Code のフック・git hooks・CI が同じ判定を返す。報告文・README もハッチを案内しなくなった。
Issue の未決(全層で効かせる A / やめる B)はオーナーに確認して B に決まった。
push 前フックの走査を `--all src` の 1 回呼び出しに揃えたのは計画外で、実装の検証で足した。
人のレビュー指摘は GitHub 上では 0 件(`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも確認。
コメントは bot の投稿と自分の報告だけ)。セッション中の確認 1 件を下の 1 に記録した。

## 指摘

### 1. 選択肢を「実装の形」で書き、「何を許すか」で書いていなかった

- 分類: `なし`
- 出どころ: `レビュー（人）`
- 内容: A(検出器に読ませて全層で揃える)/ B(ハッチを外す)で提示し、オーナーは一度 A と答えた。
  直後に「そもそも何だっけ? コメントが無いのを許容するかって感じ?」と聞き返され、「ファイル単位で doc の欠落を許すか」と
  説明し直したところ B に変わった。判断待ちで出す選択肢を、判断する人の語彙で書く規範は無い。
- 既存ルール: なし

### 2. 判定表に足す予定の 2 行が、変更の前後どちらでも通る assert だった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 判定表は検出器を直接呼び、ハッチを読んでいたのはフック側の `grep` だけだった。行は足さず、完了条件を grep 0 件にした。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 3. ゴールを確かめる手段が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: ゴールは grep で確かめられる条件なのに、コマンドが書かれていなかった。
  `grep -rn 'doc-comments-ok' ... | grep -v harness/records` が 0 件を完了条件にした。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 3

### 4. README に書く理由の文を決めずに「未決: 無し」としていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「314 行以降と同じ書き方」とだけ書いていたが、その理由(規則の側に免除がある)は doc コメント検査には成り立たない。
  2 か所に書く文を計画の時点で決めた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「未決の判断」

### 5. フック層のハッチ除去がどのテストにも守られていなかった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 両フックに `grep` を戻しても判定表は緑のままだった(ミューテーションで実測)。走査を 1 本にし(6)、除外を差し込む場所をフックから無くした。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 6. push 前フックのファイルごとの走査が `--all` と重複していた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: ハッチを外すと `find src` のループは `--all` と同じ対象を別の道筋で回るだけになる。#756 の食い違いはこの 2 系統の片方にだけ
  除外が載っていたことが原因なので、`--all src` の 1 回呼び出しへ置き換えた。未使用の `command -v git` も外した。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 7. README の例示が 1 件になり、網羅した列挙に読めた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「他の push ブロック系(`@test-rules-disable`)」から `@doc-comments-ok` を外すと 1 件になるが、`@lint-suppress-ok` も push を止める検査のハッチ。並べた。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 8. 挙動を確かめるプローブで、ハッチ行を宣言の直上に置いていた

- 分類: `test`
- 出どころ: `自己修正`
- 内容: 検出器は直上の `//` 行を doc と読むので、変更後も無出力になり「効いていない」ように見えた。ハッチを宣言から離して取り直し、
  変更前は免除・変更後は報告 / deny を確かめた。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 9. Claude Code のフックが不発の環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されずに通り、検証エージェントのマーカーも発火ログの見出しも無かった(README の「本当に不発」の枝)。
  セッションの起動ディレクトリがリポジトリの親(`/home/user`)だった。禁止コマンドは使っていないことを見直した。PR 本文・Issue・Discord に記載。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

### 10. `check-added-*` が浅いクローンで merge base を取れず落ちた

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: 2 つとも `fatal: origin/main...HEAD: no merge base`(exit 128)。`git fetch --unshallow origin main` の後に再実行して通過した。
- 既存ルール: なし

## 手戻り

- ゴールの確定で A → B に変わった(1)。実装前なので作業の手戻りは無い
- 計画の判定表 2 行を取りやめた(2)
- push 前フックの走査の置き換えと README の例示の追加は、実装の検証を受けて計画外で足した(6・7)。Issue に追記済み

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。記録のみ(集計と介入の判断は `harness-growth` の管轄)。
