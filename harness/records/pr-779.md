# PR #779 ファイル由来の名前で辞書を引くとき Object.prototype 上の名前を「ある」と判定しない

- マージ日: 2026-09-25
- 関連 Issue: #768
- 差分規模: 26 ファイル / +496 -81

## ゴールと結果

ゴールは達成した。部品名・トークン名・prop 名・ノード名で辞書を引く経路は、すべて `RecordEx` を通るようになった。
`constructor` / `toString` / `__proto__` はもう「ある」と判定されない。`ComponentSet.get` は `Option` を返す。
計画検証で `__proto__` が対象に加わり、`NameSpace.renameMap` の組み立ても直した。
既存の重複(`names` → `get` の引き直し・スキーマからの prop 定義の引き当て)は #777 に分けた。
人のレビュー指摘は 0 件。`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも確認し、
コメントは bot の投稿と自分のテスト表だけだった。CI の失敗も 0 件。

## 指摘

### 1. `__proto__` が対象か決めておらず、`NameSpace.renameMap` の代入で対応が消える経路を取りこぼしていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画は `constructor` / `toString` だけを想定していた。`renameMap[name] = newName` では `__proto__` の代入が無視されるので、
  `Node.rename` 側を直しても付け替わらない。`Object.fromEntries` で組み立てる変更を計画に足した。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 7

### 2. 「どの経路でも」を確かめるコマンドが計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 引き当てを列挙する grep を計画に入れ、全ヒットを「変更 / 対象外(理由)」に分類して Issue に貼った。
  再発防止のフックはこの回では足さないと決め、その理由も Issue に書いた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 2

### 3. `RecordEx.get` の戻り値を `Option<V>` と書いていた

- 分類: `type`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `Option.some` は `NonNullable<T>` しか受けないので、戻り値は `Option<NonNullable<V>>` になる。
  値が null / undefined のときに none とすることを doc に書いた。
- 既存ルール: rules/coding.md「エラーと不在の表現(Result / Option)」

### 4. `ComponentSet.get` の名前を残す理由を書いていなかった

- 分類: `naming`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 戻り値を `Option` に変えるのに、近い引き当て(`find*`)と名前が揃っていない理由が計画に無かった。
  「キーでの引き当てで、探索していない」を理由として書いた。
- 既存ルール: rules/naming.md「名前と実体を一致させる」

### 5. `Object.hasOwn` を却下した理由が、設定値の事実だけだった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「lib が ES2020」は、今コンパイルが通らない事実でしかなかった。lib を上げない理由として、
  WebView の対応状況の確認が範囲に入ってくることを補った。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 5

### 6. テストで守れない書き換えを明記していなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `directRefs` / `resolveOverrides` は、`constructor` でも修正前と結果が変わらない。
  形を揃えるためだけの書き換えで、テストでは守れないと Issue に書いた。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 7. キャンバスの経路(`ExpandedNode`)にテストを予定していなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `ref` が `constructor` の参照ノードを展開すると `component-not-found` になるテストを足した。
- 既存ルール: rules/testing.md「振る舞い(仕様)をテストする」

### 8. 挙動の変わる書き換え 3 件がテストで守られていなかった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 修正前の形に戻しても、1959 件がすべて通った(ミューテーションで実測)。3 件は `component-binding` のスキーマ引き当て・
  `targetThroughRef` の上書き引き当て・入れ子参照の `dangling-binding-prop`。テストを 3 件足し、修正前の形で落ちることを確かめた。
- 既存ルール: rules/testing.md「assert は「落ちうるか」と「1つの仕様か」で見る」

### 9. `NameSpace.renameMap` のコメントが事実と違っていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「`__proto__` への代入はプロトタイプを差し替える」と書いていた。実際は、文字列の代入は無視されるだけ。
  node で確かめ、「無視され、対応が残らない」に直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 10. 名前を列挙してから引き直す 3 か所に、到達しない none の分岐がある

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `ComponentSet.names` で得た名前で `ComponentSet.get` を引き直しており、`Option` 化で握りつぶしに見える分岐になった。
  #768 より前からある形なので、#777 に分けた。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 11. スキーマから prop 定義を引く処理が 3 か所で重複している

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `component` / `component-binding` / `validation` の 3 か所。`PrimitiveSchema.findProp` のような形に寄せる案とともに、#777 に分けた。
- 既存ルール: rules/coding.md「同じ処理が2箇所に現れたら共通化する」

### 12. `RecordEx.has` のコメントに What の言い換えが混ざっていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「記述子の有無は自身のキーだけを見る」の 1 行を消し、理由の 2 行だけを残した。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 13. Biome の自動修正が、`tsc` の通らない `Object.hasOwn` を入れた

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `hasOwnProperty.call` で書いた。ところが `biome check --write` の `useObjectHasOwn` が `Object.hasOwn` に書き換え、
  tsc が TS2550(lib ES2020)で落ちた。`Object.getOwnPropertyDescriptor(...) !== undefined` に変えた。
- 既存ルール: rules/coding.md「外部の挙動は動かして確かめる」

### 14. PR 本文に書いた新テストの件数が誤っていた

- 分類: `comment`
- 出どころ: `自己修正`
- 内容: PR 作成時に「新しいテストは 24 件」と書いた。ファイルごとに `test(` を数え直すと 33 件(ドメイン 23 件 + `RecordEx` 10 件)だったので、本文を直した。
- 既存ルール: `.claude/skills/claim-verification/SKILL.md`

### 15. Claude Code のフックが不発の環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されずに通った。検証エージェントのマーカーも発火ログの見出しも無く
  (`/tmp` の発火ログは別のセッション ID のもの)、README の「本当に不発」の枝に当たる。
  `npx` / `git add -A` と、検証エージェント実行中の git 操作をしていないことを履歴で確かめた。PR 本文・Discord に記載した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- 計画検証の後に `__proto__` を対象に加え、`NameSpace.renameMap` の変更とテスト 2 件を足した(指摘 1)
- `RecordEx.has` の実装を `hasOwnProperty.call` から `getOwnPropertyDescriptor` に変えた(指摘 13)
- `collectBindingTargetErrors` の入れ子参照は、`||` で組んだ条件に名前を付けず、`if` を 2 つに分けた
- 実装検証の後にテストを 3 件足した(指摘 8)
- コミットを 1 変更ずつに分けるため、作業ツリーを退避してから積み直した。最終状態が退避と一致することを確かめた

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。記録のみ(集計と介入の判断は `harness-growth` の管轄)。
