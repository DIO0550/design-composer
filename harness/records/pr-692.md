# PR #692 LeftPane の器化で死んだ受け口・公開口と、前提が変わった doc を落とす

- マージ日: 2026-09-21
- 関連 Issue: #688
- 差分規模: 14 ファイル / +55 -56

## ゴールと結果

#678 の器化で呼ぶ側が消えた受け口・公開口を無くし、前提が変わった doc を実態へ揃え、
`Assets` の検索語の配線をテストで守る。達成した。配線テストは `query={""}` へ固定する
変異を入れ、`src/features/editor` 配下 1810 件のうち追加した 1 件だけが落ちることを実測した。

同じ Issue を別セッションが並行実装しており、#691 と重複した。#692 がマージされ #691 は
unmerged で閉じられている。

## 指摘

### 1. 同じ Issue #688 を 2 つのセッションが並行実装し、PR が 2 本になった

- 分類: `parallel-issue-work`
- 出どころ: `CI`
- 内容: 着手前に open PR と相手ブランチを確認した時点では #688 の作業は 0 件（#686 の
  コミットのみ）で PR も無く、Issue の最新コメントは「判断待ちで停止」だった。その後
  #686 が閉じられて相手セッションが再開し、5 分違いで 2 本が出た。
- 既存ルール: なし

### 2. 計画が削除するファイルを名指ししている doc が、ファイル表に入っていなかった

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `__stories__/index.ts` と `IdleGrab` を消すと `__stories__/asset-grab.ts` を
  ストーリーから直接読む経路が 0 になり、「`__stories__/` にありながらストーリーが 1 つも
  読まない中継ファイル」が残る。この Issue が消している形と同じものを作るところだった。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 3. 却下理由が、却下した案のコストだけを数えて採用案のコストを数えていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「先例が名指ししているので崩さない」を却下理由にしたが、採用案でも doc を 1 つ
  直す必要があることを数えていなかった。どちらを採っても直す量は同じだった。
- 既存ルール: なし

### 4. doc に書いた「パネル」が、リポジトリ内で別のものを指す語だった

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `AssetGrab` の通り道から「レール」だけを落とすと「パネル」が残るが、この語は器の
  `LeftPanePanel` を指して使われている。実際に `grab` を通すのは `AssetsPanel` で、器は
  通さない。`AssetsPanel` と明示する形へ直した。
- 既存ルール: rules/naming.md「その名前が既に別の意味を持っていないか確認する」

### 5. doc を書き換えると、同じ文が兼ねていた別の理由が消える形になっていた

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `sample-sidebar-document.ts` の doc を Layers 限定へ直すと、`tokens` /
  `components` の 2 行が要る理由も一緒に消える、という指摘。これを受けて理由を書こうとして
  指摘 9 の誤りに繋がった。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 6. タスクを分割しない判断の理由が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: doc の片付けと配線テストは独立してマージできるが、分けない理由を計画に書いて
  いなかった。AGENTS.md は分けない判断も記録の対象としている。
- 既存ルール: AGENTS.md「タスクの分割」

### 7. 「無くなった」を確かめるコマンドが計画の完了条件に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 受け口・公開口の削除も doc の書き換えも typecheck / lint / テストのどれも捕まえない
  ため、grep を完了条件に置く必要があった。
- 既存ルール: なし

### 8. 計画に書いた件数の指し方と行番号がずれていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「消費者は 4 ストーリー」は正しくは 4 ストーリーファイル（1 ファイルに 4 本の
  story がある）。反証手段の行番号も 103 ではなく 104 だった。件数の陳腐化を直す回で、
  計画側の件数の指し方がぶれていた。
- 既存ルール: なし

### 9. doc に書いた理由が実測で反証された

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「雛形のトークンと部品を落とすと行が参照を解決できなくなる」と書いたが、左ペインの
  `document-tree` / `layers-panel` / `artboard-list` はどれも `document.tokens` /
  `components` を読まず、外してもマークアップが完全に一致する。理由の文ごと落とした。
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」

### 10. 誤った doc を削っただけで、残った非対称の説明が無くなった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「`setupAssetGrab` はストーリー側は要らない」が事実と違うので削ったが、削るだけだと
  同じ概念の半分ずつを 2 フォルダから引く形の説明が消える。`setupAssetGrab` も
  `__stories__/` へ移して非対称そのものを無くした。
- 既存ルール: rules/architecture.md「配置の判断基準」

### 11. `@param` が 2 行上の文の言い換えになっていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「省くと何も掴んでいない状態になる」を足したが、直前の行が既に書いており、しかも
  既定のもう半分（掴んでも何も起きない）を落としていた。元の形へ戻した。
- 既存ルール: rules/coding.md「消すのは … What の言い換え」

### 12. テストのコメントが規約の文面の転記になっていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「`queryBy*` が null になる理由が 2 通り」の説明を書いたが、これは
  rules/testing.md の文面の写し。規約が守っている理由はコメントへ写さない。
- 既存ルール: rules/coding.md「Why と Why not は別物として書く」

### 13. PreToolUse フックが不発の環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されず、検証エージェントのマーカーと発火ログの見出しも
  このセッション分は両方無し（README の表の 4 行目）。代替の無い `block-npx.sh` /
  `block-git-during-verification-agent.sh` は手で確認した。git hooks（層 2）は発火している。
- 既存ルール: .claude/hooks/README.md「発火しているかを確かめる(カナリア)」

### 14. git の staging 挙動を確かめずにコミットし、既に staged だった削除が載った

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: 対象 4 ファイルだけを `git add` してコミットしたが、先に `git rm` してあった
  `__stories__/index.ts` の削除が staged のままで同じコミットに入った。コミット出力の
  ファイル数で気づき `git reset --mixed` でやり直した。
- 既存ルール: rules/coding.md「外部の挙動は動かして確かめる」

## 手戻り

- 計画から外れた 2 件（指摘 2 を受けて `__tests__/asset-grab.ts` の doc 修正と、
  ストーリーの import 元の変更を追加）。どちらも Issue #688 へ追記済み
- `implementation-reviewer` の指摘による doc の書き直し 4 件（F1・F2 必須 / F3・F4 軽微）
- `pnpm exec biome check` が import の並び順で 1 度落ちた。`--write` で修正し再確認して
  からコミットした（push 前検査を 1 つずつ実行していたため CI へは出ていない）
- コミット 1 のやり直し 1 回（指摘 14）
- マージ後に #693 を起票（`spyRenameActions` の `@returns` に件数が残り、双子の
  `sampleRenameActions` と食い違った）

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。介入の判断は `harness-growth` の管轄で、このスキルは記録のみを残す。
