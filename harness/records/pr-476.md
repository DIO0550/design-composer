# PR #476 [ui] コンテキストメニューの見せ方を UI 案と docs/06-ui.md に載せる

- マージ日: 2026-09-11
- 関連 Issue: #469（親 #418）
- 差分規模: 7 ファイル / +49 -13

## ゴールと結果

右クリックのコンテキストメニューの「見せ方」を UI 案（`docs/Design Composer.html`）と
`docs/06-ui.md` に載せ、器の実装（#470）がそれを拠り所にできる状態にする。達成した。

親 #418 では「メニューを持つか」の判断そのものを扱い、案 B（持つ）に決めた。#418 の時点で
私は案 A（持たない）を推していたが、その推奨の根拠に「Figma が持っているか」が含まれて
おらず、オーナーの指摘で撤回している（下記 指摘 1）。

## 指摘

### 1. 案 A を推した根拠に、Figma が持っているかどうかが入っていなかった

- 分類: `plan-rejection-reasoning`
- 出どころ: `レビュー（人）`
- 内容: #418 で「コンテキストメニューを持たない（案 A）」を推奨として出したが、推奨理由は
  「案 B は UI 案と `docs/06-ui.md` の仕様変更 2 件を伴う」だけで、**#370「基本のベースは
  Figma」に照らしたときに Figma が右クリックメニューを操作の主な入口として持っている**
  ことを書いていなかった。オーナーから「Figma って持ってないなら A でもいいけど」と
  条件付きの返しがあり、条件が偽（#373 の A9 自身が「操作の主な入口」と記録している）
  だったため案 B へ倒した。選択肢の提示としては、判断軸である #370 に照らした事実が
  片方の案からだけ抜けていた
- 既存ルール: implementation-flow フェーズ 3 手順 5「却下案に書いた理由が事実を主張して
  いるなら、その事実を実際に開いて / `grep` して確かめてから書く」
- 次にどう防ぐか: 今回は記録のみ

### 2. plan-reviewer の指摘を、その推論が成立しないまま受け入れて品目を絞った

- 分類: `plan-rejection-reasoning`
- 出どころ: `レビュー（人）`
- 内容: `plan-reviewer` が「複製 / 名前を変更 / グループ化・解除は `docs/*.md`・UI 案・
  実装のいずれにも 0 件」という事実確認から「この回で編集操作を 4 つ新設することになる」
  と結論し、私はそれを受け入れてメニューの品目を 13 行から 5 行へ絞った。**事実確認は
  正しいが結論は成立しない** — 3 操作はいずれも既に立っている Issue（#415 / #416 / #421）で、
  入れると決まっていて実装が未了なだけだった。オーナーの「基本は Figma ベースでいいん
  だけど？」で撤回し、品目を戻した。加えて私が却下理由に書いた「描かないと見せ方の判断が
  3 回に割れる」も誤りで（見せ方が決まれば後から 1 行足すのは判断のやり直しではない）、
  絞る側・戻す側の両方で理由が事実と食い違っていた
- 既存ルール: implementation-flow フェーズ 3 手順 5（同上）
- 次にどう防ぐか: 今回は記録のみ（`plan-rejection-reasoning` として 2 件目）

### 3. 崩れる前提を探す grep が同一行しか拾えていなかった

- 分類: `plan-scope-premise-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: UI 案を根拠にしている Why を洗い出すのに
  `grep -rn "UI 案" src/ | grep -E "無|持たない|存在しない|描いていない"` を使い 24 件を
  数えたが、**同一行に両方ある場合しか拾えない**。doc コメントは折り返すので、
  `opened-document-editor.componentize.test.tsx` の「部品化の入口は `Assets` の下端にしか
  無く」（16 行目と 18 行目に分かれている）を取り逃していた。計画に「残り 21 件は崩れない」
  と書いた根拠が、検証コマンドの覆う範囲より広い結論になっていた
- 既存ルール: implementation-flow フェーズ 3 手順 3「検証コマンド自体が意図した範囲を
  覆っているか」
- 次にどう防ぐか: 今回は記録のみ

### 4. `json.dumps` が原文を再現せず、そのまま書き戻すとファイルが壊れる

- 分類: `tool-behavior-unverified`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `docs/Design Composer.html` の編集手順を「展開 → 編集 → `json.dumps` で再
  エンコード → 書き戻し」と計画に書いたが、動かして確かめていなかった。原文は `</` を
  931 箇所で `</` に退避しており、**ペイロード自身が `</script>` を 1 つ含む**ため、
  素の `json.dumps` はそれをリテラルで書き戻して外側の `<script type="__bundler/template">`
  をそこで閉じ、500KB のファイルが壊れる。指摘を受けて
  `json.dumps(payload, ensure_ascii=False).replace("</", "<\\u002F")` がバイト完全一致する
  ことを実測し、**編集前に往復一致を assert する**手順へ変えた
- 既存ルール: rules/coding.md「外部の挙動は動かして確かめる」
- 次にどう防ぐか: 今回は記録のみ

### 5. ゴールを確かめる手段が計画に無かった

- 分類: `plan-goal-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 差分が docs だけなので typecheck / lint / test / VRT はすべて緑のままで、
  **6 画面目が描画されていなくても誰も気づけない**。`.github/workflows` `.github/scripts` に
  `docs/` や `Design Composer.html` を読むものは 0 件。指摘を受けて playwright で
  `docs/Design Composer.html` を開いて撮影する手順を計画へ入れた。この追加が指摘 11 を
  実際に捕まえている
- 既存ルール: `plan-reviewer.md`「ゴールの達成を確かめる手段」
- 次にどう防ぐか: 今回は記録のみ

### 6. 変更箇所の列挙に、同じファイル内の Design notes が挙がっていなかった

- 分類: `plan-file-omission`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画のファイル表には `id` / `data-screen-label` / 見出し / ナビ / `5 states` を
  挙げたが、UI 案の末尾にある Design notes の `<ul>`（画面ごとに 1 項目ずつ並ぶ）へ
  6 画面目の行を足すかが書かれていなかった。同じファイルの中でも、変更が要る箇所は
  別に数える必要があった
- 既存ルール: `plan-reviewer.md`「変更で壊れる既存の資産を洗い出しているか」
- 次にどう防ぐか: 今回は記録のみ

### 7. 「未決の判断: 無し」と書いたが、#470 が実装に入れない穴が 8 点残っていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画に「未決の判断: 無し」と書いたが、右クリックが選択を変えるか・複数選択中の
  扱い・押せない項目の見せ方・空き領域のペーストの貼り先・Esc の優先順位・出す位置・
  出る場所・部品名の入力場所、の 8 点が決まっていなかった。どれも「見せ方」そのもので、
  この Issue が決めると宣言した範囲に入る。品目を確定させたことで 4 点は消え、残り 4 点は
  オーナー判断（Figma を既定にする）で倒し、器の振る舞いに属する 4 点は
  `docs/06-ui.md` に「決めていない（#470）」として明記した
- 既存ルール: implementation-flow フェーズ 3「未決の判断（あれば）」
- 次にどう防ぐか: 今回は記録のみ

### 8. `⇧` がリポジトリに前例 0 件の綴りだった

- 分類: `naming-absent-metaphor`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: メニューの割り当てを `⌘⇧G` / `⌘⇧Z` と書いたが、数え直すと **`⇧` は UI 案 0 件・
  `docs/06-ui.md` 0 件**で、このリポジトリに無い綴りだった（`docs/06-ui.md` は `⌘` 5 件・
  `Ctrl` 3 件・`Shift` 6 件で `Shift+` と綴る）。`docs/06-ui.md` の流儀に合わせて
  `Shift+⌘G` にした。比喩・抽象語ではなく記号の綴りだが、`grep -i` で 0 回なら
  リポジトリの語彙に無いという同じ形
- 既存ルール: rules/naming.md「比喩・抽象語は `grep -i` で数える」
- 次にどう防ぐか: 今回は記録のみ

### 9. `docs/06-ui.md` の書き換え範囲が 1 文だけでは足りなかった

- 分類: `plan-implicit-condition`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 書き換えるのは「キーボードだけで行う操作」の 1 文だけと計画したが、メニューを
  持つことで前提が変わる記述が他にもあった — `:64`「編集操作は複数選択中は行えない」、
  並べ替えの行（入口を「ドラッグ・ツリービュー・キーボード」と列挙）、ポインタ操作の節
  （右ボタンの扱いが一言も無い）。同じファイルの中で、この変更が暗黙に崩す条件を
  数えていなかった
- 既存ルール: `plan-reviewer.md`「既存のコードが前提にしている不変条件を、この計画が
  崩さないか」
- 次にどう防ぐか: 今回は記録のみ

### 10. 却下案の一覧に、実在するいちばん近い代替案が挙がっていなかった

- 分類: `plan-rejection-coverage`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 却下案に 5 件を挙げたが、「UI 案には触れず `docs/06-ui.md` だけで規定する」
  「窓全体ではなくメニュー単体の切り出しを並べる」の 2 つが入っていなかった。どちらも
  `rules/ui-verification.md`・既存 5 画面の形から却下になるが、記録が無いと次に再浮上する
- 既存ルール: implementation-flow フェーズ 3 手順 6
- 次にどう防ぐか: 今回は記録のみ

### 11. メニューを描いたが、artboard に隠れて絵に出ていなかった

- 分類: `ui-fidelity`
- 出どころ: `自己修正`
- 内容: メニューをキャンバスの器の**先頭**へ差し込んだため、後続の artboard（白背景）が
  上に重なり、6 画面目にメニューが 1 行も描かれていなかった。`data-screen-label` は 6 件に
  増え、スクリプトの検算（再パース・往復一致）もすべて通っており、**マークアップを読む
  だけでは気づけない**。playwright で撮影して初めて分かった。器の**末尾**へ置く形に直した
- 既存ルール: rules/ui-verification.md「表示確認」
- 次にどう防ぐか: 今回は記録のみ（指摘 5 で入れた撮影手順が実際に効いた形）

### 12. UI 案の画面数を数えている記述 3 件を取り逃していた

- 分類: `comment-enumeration-drift`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「今回偽になる」に挙げたのは `use-edit-shortcuts` と `rules/ui-verification.md` の
  2 件だけだったが、**UI 案の画面数を数えている記述**が 3 件残っていた —
  `left-pane-shell.tsx`「左パネルを 5 画面とも 248px」/ `right-pane-shell.tsx`「右ペインを
  5 画面とも 264px」/ `document-start/index.tsx`「Default / … / Error の 5 画面」。
  画面を 1 枚足すと `width:248px` `width:264px` はいずれも 5 件から 6 件になるので、
  どれも偽になる。指摘 3 で grep の範囲は直したが、**「画面数を数える記述」という観点を
  持っていなかった**ため同じ洗い出しで再び漏れた
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ

### 13. `document-start` の画面名の列挙が `rules/ui-verification.md` と二重に置かれていた

- 分類: `duplication-uncovered-shape`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 指摘 12 の 3 件目は、`rules/ui-verification.md:8` と**同じ画面名の列挙を 2 箇所に
  持っている**形で、私は先に `rules/` 側だけを直していた。片方だけが古くなる状態を作って
  いたので、`document-start` 側は列挙をやめて「画面の数と名前は `rules/ui-verification.md`
  が持つ」に変え、重複そのものを解消した
- 既存ルール: rules/coding.md「同じ判断根拠を説明する doc・docstring・README の重複にも
  適用する。片方だけを直せる状態を作らない」
- 次にどう防ぐか: 今回は記録のみ

### 14. 「キーボードからしか行えないのは 3 つ」が偽だった

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `docs/06-ui.md` に「キーボードからしか行えないのは選択解除・座標の移動・収める
  ズームの 3 つ」と書いたが偽。**選択解除はインスペクタにボタンがあり**（同じファイルの
  `:65` 自身が「選択の解除だけを残す」と書いている）、**座標の移動はドラッグと
  プロパティパネルの x / y 直打ちがある**。言いたかったのは「メニューに入口が無いのは
  この 3 つ」で、数は合っていたが主語が違った。同じファイルを読めば分かる主張を、
  読まずに書いていた
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 15. `use-edit-shortcuts` の「ここが今のところ唯一の入口」が偽だった

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 前提が崩れた Why を直すつもりで「ここが今のところ唯一の入口」と書き換えたが、
  選択解除と並べ替えはツリーとインスペクタからも呼ばれる（`use-node-actions` が
  「ツリーとインスペクタから呼ぶ相手が別に居る」と真逆を書いていた）。**書き換え前の文
  （「UI 案が対応するボタンを持たないため画面にも置いていない」）は範囲が狭くて真だった**
  ので、直そうとして偽にした形。「削除・コピー & ペースト・undo / redo は画面にこれ以外の
  入口を持たない」まで絞った
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 16. 「部品にするはインスペクタにも入口がある」が偽だった

- 分類: `comment-structure-claim`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `docs/06-ui.md` に「部品にする / インスタンスを解除はインスペクタにも入口が
  ある」と書いたが、**部品にするの入口は `Assets` パネルの下端**で、UI 案でも
  `Create component` は 2 箇所とも `LEFT PANEL · ASSETS` の内側にある（`Detach instance`
  だけが `RIGHT PANEL · instance`）。添えた「名前を打つ欄を伴う」も UI 案には無く、
  `create-component/index.tsx` 自身が「UI 案は入力欄を描いていない」と書いている。
  両方に置くという結論は変えず、置き場所を直した
- 既存ルール: rules/coding.md「コメントは実装と一致させる」
- 次にどう防ぐか: 今回は記録のみ

### 17. 絵が、同じ差分で書いた自分の規則に反していた

- 分類: `ui-fidelity`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `docs/06-ui.md` に「隣がいない向きへの前面へ / 背面へは押せない状態で並べる」と
  書きながら、UI 案では両方を有効な色で描いていた。新画面の `login-form` は **artboard
  直下の唯一の子**（Layers の該当行が 1 本・キャンバスのカードも 1 枚）なので、自分の
  規則に従えば両方とも押せない状態が正しい絵だった。しかも `:148` が名指しした唯一の例が
  絵として存在しないことになっていた。両方を淡色にして揃えた
- 既存ルール: rules/ui-verification.md「UIの拠り所は `docs/Design Composer.html`」
- 次にどう防ぐか: 今回は記録のみ

### 18. UI 案の流儀から外れた 3 点（`z-index` 初出・影の種類・`code` の等幅）

- 分類: `ui-fidelity`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: (a) 指摘 11 を直すときに `z-index:10` を足したが、**UI 案は重なりをすべて DOM 順で
  解いていて `z-index` を 1 つも持たない**（base 0 件）。器の末尾へ置けば不要だったので
  外した。(b) 影に `0 6px 18px rgba(0,0,0,0.07)` を使ったが、これは**キャンバスに描かれた
  ドキュメントの中身**（login-form カード）の影で、浮いている chrome の影は浮動ツールバーの
  `0 5px 18px …/0.18, 0 0 0 0.5px …/0.06`。メニューは chrome なので後者へ寄せた。
  (c) Design notes に足した `<code>` 2 個に `font-family:'IBM Plex Mono',monospace` が
  無く、既存 4 個は全部持っている（ページの `<style>` に `code` の規則は無いので、
  ここだけブラウザ既定で描かれる）
- 既存ルール: rules/ui-verification.md「単一の画面・単一の状態だけを見て断定しない。
  同じ要素を全画面・全状態で並べて属性を比較する」
- 次にどう防ぐか: 今回は記録のみ

### 19. 押せない行の指定が、UI 案の何を指すのか読めない書き方だった

- 分類: `ui-fidelity-misread`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「押せない行の見せ方は UI 案が `Create component` で既に持っている形に揃える」と
  書いたが、(a) 画面名が無いため読み手は新しい `Context menu` 画面の `Create component`
  **行**を見るが、そこは有効（`#1e1e1e`）で押せないのは `Detach instance` 側 —
  **色が逆に読める**。(b) その形は `background:#fafafa` + 枠 + 文字色 + `cursor:default` +
  理由の一文の組で、メニュー行が借りているのは文字色だけ。(c) 理由の一文をメニューでは
  持たないことが書かれていない。「UI 案 `Assets · Instance` の `Create component` と同じ
  文字色 `#c4c4c4`。ボタンの背景・枠と理由の一文はメニューでは持たない」まで具体化した
- 既存ルール: rules/ui-verification.md「単一の画面・単一の状態だけを見て断定しない」
- 次にどう防ぐか: 今回は記録のみ

### 20. `docs/06-ui.md` に足した記述が、同じファイルの他の節と噛み合っていなかった

- 分類: `なし`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 新しく足した「コンテキストメニュー」節の中と、既存の節との間で 4 点が食い違って
  いた。(a)「ポインタの下のものが選択になる」を空き領域に当てると選択は「なし」になり、
  「選択が無い空き領域では押せない」（選択が残る場合がある前提）と噛み合わない。
  (b)「編集の行はどれも押せない」が `:64` の列挙より広く、ペースト・複製・名前を変更・
  グループ化 / 解除・undo / redo が `:64` に入っていない。(c)「artboard の枠の背景」と
  節を根拠に引きながら、`:92` の掴み口は見出しと枠の背景の 2 つで片方しか指していない。
  (d)「並び」の語は このファイルでは順序の意味で使われている。**`rules-consistency` は
  `rules/` への追記を対象にした分類で、仕様書（`docs/`）に足した記述が同じファイルの
  前の節と矛盾する形に当たる語彙が無い**
- 既存ルール: なし（`rules-consistency` は `rules/` が対象）
- 次にどう防ぐか: 今回は記録のみ（`分類: なし` として溜める）

### 21. 「展開後 116,001 字」がバイト数だった

- 分類: `plan-scope-count-verification`
- 出どころ: `自己修正`
- 内容: Issue #469 の本文に「展開後 116,001 字」と書いたが、これは `wc -c` の**バイト数**で、
  文字数は 115,829 だった（差の 172 バイトは `⋯` `×` などのマルチバイト文字）。数えた
  コマンドが答えている単位を確かめずに「字」と書いていた。計画を書く時点で気づいて訂正した
- 既存ルール: implementation-flow フェーズ 3 手順 2「計画に書いた件数・基数を実例へ
  項目ごとに突き合わせて数え直す」
- 次にどう防ぐか: 今回は記録のみ

### 22. Design notes に足した `opens` が、別の doc の「0 件」という根拠を壊した

- 分類: `comment-premise-drift`
- 出どころ: `自己修正`
- 内容: Design notes へ「Right-click **opens** one menu …」と書いたところ、UI 案に
  `open` が 1 件出現した。`document-start/index.tsx` の doc が「UI 案に `open` / `recent` /
  `welcome` はいずれも 0 件」を根拠に「開く前の画面を描いていない」と書いているので、
  この語を入れるとその根拠が崩れる。既存コメントが件数を根拠にしている語を base と
  突き合わせて数えていて気づき、`brings up` へ言い換えた（`open` / `recent` / `welcome` /
  `zoom` / `fit` / `ズーム` / `Legend` / `drag` / `unused` はすべて base と同数を維持）
- 既存ルール: rules/coding.md「実装を変えたらコメントも同時に直す」
- 次にどう防ぐか: 今回は記録のみ

### 23. マージ競合の解消で、コメントが main の新しい規約に反していた

- 分類: `comment-premise-drift`
- 出どころ: `自己修正`
- 内容: PR を出した直後に main へ #472 の doc コメント整理（#473）が入り、`rules/coding.md`
  自体が更新されていた（「消すのは … **Issue 番号**」「doc は 1〜2 文」「理由の根拠に、
  いま直されつつある一時的な事実を置かない」）。私のコメントは `#470` / `#112` / `#316` を
  含んでおり、**マージした時点で新しい規約に反する状態**になった。競合解消時に Issue 番号を
  落とし、「docs がメニューを定めたが器はまだ無い（#470）」の一文も落とした（#470 が入った
  瞬間に嘘になる一時的な事実のため）。`docs/06-ui.md` 側の Issue 参照は、main の
  `06-ui.md` にも 2 件あり段階的に入る行を Issue 番号付きで書くのがこのファイルの流儀
  なので残した
- 既存ルール: rules/coding.md「コメントは doc と Why / Why not に絞る」（この PR の作業中に
  main 側で更新された）
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

- **メニューの品目を 13 行 → 5 行 → 13 行と往復した。** `plan-reviewer` の指摘 1 を受け入れて
  絞り、オーナー判断で戻した（指摘 2）
- **UI 案を 3 回作り直した。** 1 回目は artboard に隠れて描画されず（指摘 11）、2 回目は
  `z-index` / 影 / 品目の差し替え（指摘 17・18）、3 回目は `opens` の言い換え（指摘 22）。
  いずれもスクリプトを直して `git checkout --` で戻してから再生成した
- **マージ競合で 4 ファイルのコメントを書き直した。** main の #473 が同じファイルの doc を
  整理していたため（指摘 23）
- 案 A / B の判断そのものは #418 で 2 往復した（指摘 1）

## 発火

- 発火: 計測対象外(フック不発)

`echo hook-canary` が deny されず通過し（`分類: hook-environment`）、このセッションの
発火ログ（`record-firings.sh` が書くもの）も存在しなかった（`/tmp` にあったのは 1 週間前の
別セッションのもので、見出し行だけを持ち発火行は 0）。`.claude/hooks/` が発火しない環境
だったため、スキル・エージェントの発火回数は計測できていない。**git hooks は動いている**
（push 時に `pre-push: biome` / `doc コメント` / `import 規約` / `テスト規約` が走った）。

## うまくいったこと

- **`plan-reviewer` の指摘 4（`json.dumps` がファイルを壊す）が、実際に壊す前に止まった。**
  計画に書いた手順をそのまま実行すれば 500KB の UI 案が壊れていた
- **指摘 5 で入れた撮影手順が、その回のうちに指摘 11 を捕まえた。** 検算（再パース・
  往復一致・`data-screen-label` の件数）はすべて通っていたので、絵を見る手順が無ければ
  「メニューが 1 行も描かれていない 6 画面目」がマージされていた
- **既存コメントが件数を根拠にしている語を base と突き合わせて数える形が効いた**（指摘 22）。
  `open` / `zoom` / `fit` / `Legend` などを一覧で base と比較したので、意図した
  `menu` / `context` 以外が動いていないことを確認できた
- 器の振る舞いに属する未決 4 点を `docs/06-ui.md` へ「決めていない（#470）」として明記した
  ので、#470 が同じ判断をやり直さずに済む

## 規約への反映

無し。**この回で介入はしていない。** 指摘 23 件のうち 22 件は既存ルールに対応があり、
どれも既存の観点・手順が捕まえている（`plan-reviewer` 8 件・`implementation-reviewer` 9 件）。
残り 1 件（指摘 20）は `分類: なし` として溜める。

すり抜け（`レビュー（人）`）は 2 件（指摘 1・2）で、どちらも `plan-rejection-reasoning`。
**却下・推奨の理由が事実に照らして成立しているかを、判断軸（ここでは #370）に照らして
確かめていなかった**という同じ形。層を上げるかの判断は集計（`count.sh`）を見てから
行うため、このスキルの管轄では行わない（→ `harness-growth`）。
