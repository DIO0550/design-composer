# PR #199 📝 #197 ツールバーの doc から UI 案と食い違う ◆ の記述を消す

- マージ日: 2026-08-12
- 関連 Issue: #197
- 差分規模: 1 ファイル / +3 -3

## ゴールと結果

「`NodeInsertToolbar` の doc に UI 案と食い違う記述が残っていない。`◆` を出していない理由が
UI 案の読みではなく未決の判断（#198）を指している」。達成した。

ゴールからのずれは無い。差分は #197 のコメントで確定した `◆` の 1 箇所に収まっており、
ポインタ（#196 の決定 1 の帰結）と `#`（#43 の担当）の理由は正しいので触っていない。

## 指摘

### 1. UI 案の `◆` を「ドラッグ中の状態表示」と読み違えた doc が、マージ済みコードに残っていた

- 分類: `ui-fidelity`
- 出どころ: 自己修正（#137 の調査で発覚。**この PR はその修正そのもの**）
- 内容: doc が `◆` を「ドラッグ中の状態表示でボタンではない」と書いていたが、UI 案では
  ツールバー 5 番目のスロットとして **Default / drag / Assets / Tokens の 4 画面すべてに常在**し、
  他の 4 スロットと同じ `36×32` / `border-radius:6px` のボタン枠だった（展開後 492 / 816 /
  1105 / 1325 行）。読み違いの正体は **drag 画面（816 行）のスロットだけが
  `background:#f3ebff` を持つ**こと。差はこの 1 属性だけで、ドラッグ中に
  そのスロットが**ハイライトされている**のを「ドラッグ中にだけ現れる状態表示」と読んでいた。
  さらに UI 案は **5 スロットとも素の div で `button` 要素が 1 つも無い**ため、
  「ボタンではない」は `◆` を出さない理由として二重に成立していなかった
  （同じ理屈なら実装が現にボタンにしている `□` / `T` も出せなくなる）
- 既存ルール: rules/ui-verification.md「見るのはスクリーンショットではなくマークアップ」
- 次にどう防ぐか: 今回は記録のみ

**この件は他の指摘と性質が違うので、数える人向けに 2 点書き残す。**

1. **この PR で作った誤りではなく、以前の PR で入った誤りをここで直している。** 過去の記録を
   `◆` / ドラッグで検索したが、この読み違いを記録したものは無い（pr-148 の `ui-fidelity` は
   UI 案 `Used by` 行の別件）。**二重計上ではない**
2. **既存の `ui-fidelity` の多くと違い、計画検証をすり抜けてマージ済みコードに残った。**
   pr-148 / pr-152 はいずれも計画検証のサブエージェントがマージ前に捕まえている。
   今回は doc コメントだったため typecheck も lint もテストも落ちず、
   **後の別 Issue（#137）の調査で初めて見つかった**

### 2. doc の Why not が 1 つあたり 1〜2 行の枠を超えていた

- 分類: `comment`
- 出どころ: 自己修正（計画検証のサブエージェント）
- 内容: 計画の書き換え案は `◆` の理由だけが他の 2 つ（「ツールモードの概念が無い」
  「artboard の追加が未実装（#43）」）より長く 5 行に増えていた。詳細は #198 が持っているので
  「押したときの挙動が未決（#198）」まで縮め、4 行版にした
- 既存ルール: rules/coding.md「Why / Why not は1つにつき1〜2行に収める」
- 次にどう防ぐか: 今回は記録のみ

### 3. 参照先を #196 ではなく #198 にした判断の理由が計画に無かった

- 分類: `plan`
- 出どころ: 自己修正（計画検証のサブエージェント）
- 内容: #197 本文のタスク欄は `#196` と書いているが、doc から指すのは #198 にした。
  後続コメントが「#198 を指すように」と上書きしており、「どの部品のインスタンスを挿すかが
  決まらない」を本文で担当しているのが #198 だから。**Issue 本文と食い違う番号を選ぶのは
  判断**なので、理由を残さないと後から必ず疑われる。計画に 1 行足した
- 既存ルール: なし（`plan` は「計画の誤り・不足」だが、「Issue 本文と食い違う選択をしたときに
  理由を残す」を名指しした記述は無い）
- 次にどう防ぐか: 今回は記録のみ

### 4. 却下案に「`rules/` へ反映する案」の検討が無かった

- 分類: `plan`
- 出どころ: 自己修正（計画検証のサブエージェント）
- 内容: 今回の読み違いは rules/ui-verification.md が数えている「過去 2 回」の 3 例目にあたり、
  `AGENTS.md`「規約の更新」の対象になりうる位置にある。それを検討した形跡が計画に無かった。
  却下案として「`rules/ui-verification.md` へ『同じ要素を全画面で並べて差分を取る』を足す →
  数えて介入するのは `harness-growth` の担当なので本 Issue では採らない」を追加した
- 既存ルール: なし
- 次にどう防ぐか: 今回は記録のみ

### 実装の検証（フェーズ 6）は指摘 0 件

差分が doc コメント 3 行・実行コード 0 行のため、過剰なフォールバック / 過剰なブロック /
重複はいずれも該当無し。ミューテーションも**中心の判断がコードに無い**ため対象無しだった。

代わりに implementation-review.md の「コメントが事実を主張していたら、その事実を確かめる」を
適用し、新しい doc が主張する 4 つ（ツールモードが無い / artboard 追加が未実装 /
#198 が未決 / `ArtboardList` の `+` と同じ判断）を 1 つずつ裏取りして、4 件とも真と確認した。

## 手戻り

**実装のやり直しは 0 件。** 指摘 2〜4 はすべて計画検証（フェーズ 4）で、実装に入る前に潰れた。

コミット後に 1 件だけ直しがある。**PR 本文で `<div>` / `<button>` を書いた箇所が GitHub 側で
サニタイズされ、文が欠落した**（「5 スロットすべてが素の `` で、`` は 1 つも無い」）。
`update_pull_request` で「素の div 要素 / button 要素」という書き方へ直した。コードには影響しない。

## フックの発火

**`echo hook-canary` が deny されなかった（フック不発環境）。** `.claude/settings.json:103` に
PreToolUse として設定されているにもかかわらず発火しなかった。pr-185 / pr-192 に続いて 3 回目。

push 前検査は手元で 1 つずつ独立に実行した（`&&` で連ねていない）。

| 検査 | 結果 |
| --- | --- |
| `pnpm run typecheck` | exit 0 |
| `pnpm run lint`（oxlint） | 0 warnings / 0 errors（481 files） |
| `pnpm exec biome check` | 483 files / No fixes applied |
| `bash .claude/hooks/pre-push-doc-comments.sh` | exit 0 |
| `bash .claude/hooks/pre-push-test-rules.sh` | exit 0 |
| `pnpm run test:run` | 299 files / 1990 tests passed |

CI は 5 チェックすべて success。VRT は **差分 0（65 passed）** で、doc のみの変更で描画が
変わらないという主張の裏が取れた。

## うまくいったこと

- **同じ要素を全画面で並べて差分を取ったら、読み違いの原因が特定できた。** 「UI 案を展開して読む」
  だけでは「`◆` はボタン枠である」までしか分からず、**なぜ前回ドラッグと読んだのか**は出てこない。
  4 箇所を並べて `background:#f3ebff` の 1 属性差を見つけたことで、誤りの再発条件まで書けた
- **テストを足さない判断を、規約を根拠に明示できた。** doc のみの変更で観測できる振る舞いが
  変わらないため、コメント文字列の assert は実装詳細のテストになる。「テストが 0 件」を
  黙って通さず、PR 本文とIssueに理由を書いた
- **実装検証に「コメントが主張する事実の裏取り」をやらせたのが効いた。** 差分が 3 行でも
  観点が空振りにならず、**UI 案の 5 スロットが全部 div**（＝旧 doc の「ボタンではない」は
  二重に不成立）という、こちらが持っていなかった事実が出てきた
- **未決の判断（#198）を推測で埋めなかった。** doc は「未決である」ことだけを書き、
  `◆` の挙動そのものには踏み込んでいない

## 規約への反映

**この回は介入しない（記録のみ）。** 集計と介入の判断は `harness-growth` の管轄で、
実装 PR で `rules/` へ手を入れると、介入したことが記録に残らないまま再発数がリセットされる。

数える人向けに、この回で見えた材料だけ残す。

- `ui-fidelity` は既存ルール（rules 層 / pr-152）がありながら再発した。**ただし今回のものは
  「マークアップを見ないで読んだ」ではなく「マークアップを見たうえで意味を取り違えた」型**で、
  既存ルールの「見るのはスクリーンショットではなくマークアップ」では防げない。
  防ぐなら「同じ要素を全画面で並べて差分を取る」が要る
- 指摘 3・4 はどちらも `plan` だが、既存ルールに名指しの記述が無い（`分類: なし` の候補ではなく
  `plan` に入るが、`plan` の中身としては新しい観点）
