# PR #631 githooks / hooks / records の README を、判定表を配線した後の実体に合わせる

- マージ日: 2026-09-20
- 関連 Issue: #628
- 差分規模: 3 ファイル / +7 -2

## ゴールと結果

当初のゴールは #628 の 4 点(`count-cases.sh` の新設 / `pre-push` と CI への配線 /
`implementation-flow` の列挙のずれ / `githooks/README.md` の事実誤り)だった。
**作業中に PR #629 が main へマージされ、1・2・3 が main に入った。** 4 も
「型 / lint / format だけを飛ばす」までは直っていた。

#628 自身が #627 について決めた判断(衝突箇所はすべて main の版を採り、残すのは main に
無いものだけ)をそのまま自分の差分へ当て、doc 3 ファイル・7 行へ再スコープして閉じた。
作った判定表 26 ケースとミューテーション実測 26 通は捨てになり、観点は #640 へ移した。

これは #627 に続き **2 回連続で「作業中に同じ Issue が別セッションで片付いた」**回。

## 指摘

### 1. 着手した Issue のやることが、作業中に main へ入っていた

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: 着手時に見たのは main の HEAD だけで、#627 の PR が open か merged かを確認して
  いなかった。気づいたのは PR 作成後、CI を見に行ったときの `mergeable_state: dirty`。
  **#628 自身が「並行実装」の経緯で立っている Issue なのに、同じ形を踏んだ。**
- 既存ルール: なし

### 2. `CI` で始まる出どころの肯定側がケースに無かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `自己修正（CI の結果）` が内部に入る否定側しか置いておらず、`reached_outside` の
  `CI` の行を丸ごと消しても一覧は全部緑のままだった。
- 既存ルール: `rules/testing.md`「その assert は落ちうるか」

### 3. 窓の境界(対策済と同じ記録番号)を固定していなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 窓の前後 2 点しか置いておらず、`number + 0 <= w` を `<` へ変えても通る書き方だった。
  対策済を書いた回そのものの指摘は再発に入らない、が仕様。
- 既存ルール: `rules/testing.md`「境界と異常系があるか」

### 4. 内部も窓で切られることを固定していなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「サブエージェントは内部に入る」だけを見ていたので、内部だけ全期間で数える壊し方を
  捕まえられない。窓より前のサブエージェント指摘を混ぜて期待値を 0 に固定した。
- 既存ルール: `rules/testing.md`「その assert は落ちうるか」

### 5. 起点の層が、その窓のものであることを誰も見ていなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 片方が未介入の形しか踏んでおらず、2 綴りとも介入済みで層が違うケースが無かった。
  awk の `for (s in spellings)` は順序未定義なので、層を取る位置がずれると実行ごとに違う
  層が出うる。
- 既存ルール: `rules/testing.md`「その assert は落ちうるか」

### 6. `fold_tag` の対応表 10 行のうち 2 行しか踏んでいなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `type` / `react` グループの行を消しても表は緑。`fold_tag` は旧語彙が増えるたびに
  触る箇所なので、ここが手薄だと判定表の主目的が薄くなる。
- 既存ルール: `rules/testing.md`「API を作り替えるときは、既存テストの観点を先に数える」

### 7. 異常系・欠損入力のケースが 1 件も無かった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 指摘 0 件の記録も `以降` に数えられる・`分類` の直後に `出どころ` が無いブロックは
  行すら出ない、が仕様として固定されていなかった。
- 既存ルール: `rules/testing.md`「空 / 1 件 / 見つからない」

### 8. `reached_outside` の別名 7 つのうち 3 つしか置いていなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 記録は書き換えない規約なので別名リストは恒久的に効き続ける。消しても落ちない別名を
  残すと、次に誰かが整理したときに気づけない。
- 既存ルール: `rules/testing.md`「その assert は落ちうるか」

### 9. `--ratchet` の 2 つの予算のうち片方しか壊していなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `check_budget` の第 4 引数は下回りのメッセージにしか出ないので、`growth_skill_cap`
  側で変数名を取り違えても全ケースが緑になる。
- 既存ルール: `rules/testing.md`「その assert は落ちうるか」

### 10. 判定表自身が空振りしない形を計画に書いていなかった

- 分類: `test`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 行が 1 つも出ないときに NG になる前提を書いておらず、`|| true` や `grep -q` の
  組み方次第で緑になる形だった。既存 3 本と同じ `ok` / `NG` + `failed` 集約へ揃えた。
- 既存ルール: なし

### 11. ゴールを確かめる手段が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: ゴールが「ゲートで走る判定表に固定されている状態」なのに、判定表が落ちうること・
  ゲートが実際に止まることを確かめる手順が無かった。判定表は配線されていても中身が
  空振りだと永久に緑になる。
- 既存ルール: `implementation-flow`「計画を渡す前に踏む 8 つの手順」3

### 12. 「未決の判断: 無し」の裏に 4 件が隠れていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: とくに **CI のジョブ名を据え置く**判断が書かれていなかった。今回のテーマが
  「列挙を実体へ合わせる」なので直しにいく動線が強いが、変えるとブランチ保護の必須
  チェックが外れる。
- 既存ルール: なし

### 13. 続きの Issue を起票する約束と、分けない理由が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 却下案に「続きの Issue にする」と書きながら起票とリンクを手順にしていない。
  独立してマージできる修正を 1 つの PR にまとめた理由も書いていなかった。
- 既存ルール: `AGENTS.md`「タスクの分割」

### 14. ファイル表の 1 行が、同じファイルの 4 箇所を指していた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `githooks/README.md` を 1 行で挙げていたが、直すのは検査の列・呼んでいるものの列・
  `lib/` の外の列挙・`pnpm` の文の 4 箇所だった。ファイル名は挙がっていても何行目の何を
  直すかが書かれていない形。
- 既存ルール: `implementation-flow`「計画を渡す前に踏む 8 つの手順」1

### 15. 同じファイルの閉じた列挙が、差分で嘘になることを見落としていた

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「`check-added-*` の 2 つ**だけは** `.github/scripts/` にあり」は閉じた列挙で、
  `harness/records/` の 2 本を足すと 2 つ目の例外になる。
- 既存ルール: `rules/coding.md`「コメントは実装と一致させる」

### 16. 差し替える文に、新設するものを入れ忘れていた

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 事実誤りの修正文案に、新設する判定表が入っていなかった。**この Issue の症状を、
  修正した瞬間に同じファイルで再生産する形**だった。
- 既存ルール: `rules/coding.md`「コメントは実装と一致させる」

### 17. 触る節の別の行も、実体とずれていた

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `records/README.md` の `--ratchet` の説明が「常時ロードの行数」のままで、
  harness-growth スキルの分が漏れていた。同じ節を触る計画なのに対象に数えていなかった。
- 既存ルール: `rules/coding.md`「コメントは実装と一致させる」

### 18. 却下理由「`--shrink` は入力側が覆われている」が事実と違った

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 節 2 は `summary_rows` の上に独自の閾値(再発 0 / 以降 10 以上)と整形を重ねており、
  入力側を固定しても守られない。「覆われている」ではなく「ゴールの外なので据え置く」が実態。
- 既存ルール: `implementation-flow`「計画を渡す前に踏む 8 つの手順」5

### 19. 却下理由が、対象 3 本のうち 1 本にしか当てはまっていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「20 秒かかる」は `check-pr-closing-issue-cases.sh` だけ。残り 2 本は python3 依存で、
  これは自分が「判定表を python3 で書く」を却下した論法と同じだった。
- 既存ルール: `implementation-flow`「計画を渡す前に踏む 8 つの手順」5

### 20. 却下理由が、置き場所に依存した条件でしか成り立っていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「python3 の枝で判定表だけ飛ぶ」は枝の中に置いた場合の話で、外に置けば飛ばない。
  結論は同じだが、理由を置き場所に依らない形へ書き直した。
- 既存ルール: `implementation-flow`「計画を渡す前に踏む 8 つの手順」5

### 21. `--list` 側の窓の境界だけ、対になるケースが無かった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 既定の表には境界のケースを足したのに、同じ境界を別に計算している `--list` 側に
  置いておらず、`>` を `>=` に変えても 24 ケース全部が緑だった。
- 既存ルール: `rules/testing.md`「境界と異常系があるか」

### 22. 並び順のケースが、awk の並びとたまたま一致して空振りしていた

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `sort` を丸ごと落としても `for (tag in tags)` の並びが期待値と一致するので通る。
  再発 2 を持つ分類を差し替えるだけで落ちるようになった。
- 既存ルール: `rules/testing.md`「その assert は落ちうるか」

### 23. 実記録で最頻の綴りが、判定表の入力に 1 件も無かった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: バッククォート付きの `レビュー（人）` は実記録に 46 件あるのに、ケースを書く関数が
  常にバッククォート無しで書き出していた。同じ `` `? `` でも素の `レビュー` と `CI` は
  守られていて、3 本並んだ同型の行のうち 1 本だけ無防備だった。
- 既存ルール: `rules/testing.md`「その assert は落ちうるか」

### 24. 同じガードが、片方の関数でだけ守られていた

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `pending` のガードとリセットは 2 つの関数に同じ形であるが、`summary_rows` 側だけ
  落としても通る入力しか無かった(出どころが 2 行続く記録・分類を持たない出どころ)。
- 既存ルール: `rules/testing.md`「その assert は落ちうるか」

### 25. 出力の 3 列のうち 2 列しか比べていなかった

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `--list` の比較が 1・2 列目だけで、見出しを空にしても出どころのバッククォート除去を
  落としても通る。doc が謳う出力書式の 3 列目が無防備だった。
- 既存ルール: `rules/testing.md`「その assert は落ちうるか」

### 26. 使い方エラーの経路にケースが無い

- 分類: `test`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `--list` を引数なしで呼んだときの `exit 2` を `exit 0` にしても素通りする。
  **受け入れていない。** この判定表のゴール(窓の数え方と `--ratchet` の終了コード)の外なので、
  `--shrink` を据え置いたのと同じ理由を doc に書いて閉じた。
- 既存ルール: なし

### 27. 同じ判断根拠を、既存の判定表から逐語で写していた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 終了コードで見る理由の 2 行が `result-option-read-cases.sh` と 1 文字違わず同じで、
  出典として挙げた README にも原文があった。3 箇所のうち片方だけを直せる状態。
- 既存ルール: `rules/coding.md`「規約の適用範囲」

### 28. 2 つのヘルパーが 5 行中 4 行同じだった

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 違いは「並べ替えるか否か」の 1 行だけ。モードを引数で受ける 1 本へ畳んだ。
- 既存ルール: `rules/coding.md`「同じ処理が2箇所に現れたら共通化する」

### 29. コメントとケース名に Issue 番号を書いていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 番号を落として「現状の答えを固定するため」という理由だけ残した。
- 既存ルール: `rules/coding.md`「コメントは doc と Why / Why not に絞る」

### 30. 事実誤りを直しながら、同じ形の誤りを別の行に作っていた

- 分類: `harness`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「残りは bash だけで走る」に `check-added-*` の 2 つを入れたが、この 2 つは python3 を
  使い、呼び出しが `|| true` なので **python3 が無いと走って何も見ずに緑で通る**。
  PATH を潰して実測して確認し、飛ぶものと走るが見ないものを別の段落に分けた。
- 既存ルール: なし

### 31. 理由に書いた実測値が、測り直すと違っていた

- 分類: `comment`
- 出どころ: `自己修正`
- 内容: `pre-push` のコメントに「1 回 0.2 秒」と書いたが実測は 0.31〜0.32 秒。0.2 秒は
  `plan-reviewer` が測った `count.sh` 単体 25 回分で、判定表はこれを 29 回呼ぶうえ一時ツリーの
  組み立てが乗る。他人の測った数字を自分の対象の数字として書いていた。
- 既存ルール: `rules/coding.md`「外部の挙動は動かして確かめる」

### 32. コミットを分けるために書いた復元スクリプトが、修正を 1 つ落とした

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: 最終状態を退避する辞書へ、同じファイルの 2 回目の書き換えで**退避済みの内容を
  上書き**していた。復元後の `git diff` を読んで気づき、落ちた節を再適用した。
  一度しか使わないスクリプトでも、往復するなら往復を確かめる必要があった。
- 既存ルール: `rules/coding.md`「外部の挙動は動かして確かめる」

## 手戻り

- 計画を 1 度書き直した(`plan-reviewer` の 19 件すべてを受け入れ)
- 判定表を 1 度書き直した(ケース 24 → 26 件。`implementation-reviewer` の 5 系統を反映)
- `pre-push` のコメントの数字を 1 度直した(0.2 秒 → 0.3 秒)
- **PR を 1 度再スコープした。** #629 が先にマージされたため、衝突した 5 ファイルをすべて
  main の版へ寄せ、タイトル・本文を差し替えた。判定表 26 ケースとミューテーション実測
  26 通はこの時点で捨てになった
- 復元スクリプトが落とした修正を 1 度貼り直した

## 発火

- 発火: 計測対象外(フック不発)

`echo hook-canary` が deny されず、`plan-reviewer` を通した後にも検証エージェントの
マーカーが無く、このセッション ID の発火ログも存在しなかった
(`.claude/hooks/README.md`「発火しているかを確かめる」の 4 行目)。
禁止コマンド(`npx` / `git add -A` / 検証エージェント実行中の git 操作)は実行していない。

人のレビューは 0 件(`get_review_comments` / `get_reviews` / `get_comments` の 3 つとも引いた。
PR コメント 3 件はすべて `github-actions[bot]`)。CI の失敗も 0 件。

## 規約への反映

無し。この回は記録を残すところまでで、介入の判断は `harness-growth` の管轄。
捨てた判定表の観点は #640、`check-added-*` の偽の緑は #641、既存 3 本の配線は #643、
#628 のスコープ外 6 点は #644、**着手した Issue がまだ有効かを確かめる手順**は #642 へ
それぞれ残した。
