# PR #760 harness棚卸し: parallel-issue-work(再発4)へ介入する

- マージ日: 2026-09-25
- 関連 Issue: #759
- 差分規模: 3 ファイル / +15 -2

## ゴールと結果

ゴール(`count.sh` の再発トップの同数(`harness` / `parallel-issue-work`、ともに4)から、
内部が少ない `parallel-issue-work` を選び、着手前に他セッションの着手表明を確認する
手順を届けること)は達成した。`session-url-notice.sh` の SessionStart メッセージへ、
URL をコメントする前に他セッションの URL コメント・自分以外の assignee を確認する
一文を追記し、`harness/records/pr-743.md`「規約への反映」に
`対策済: parallel-issue-work 層=skill at pr-760` を追記した。CI は 10 チェックすべて
green、レビュー指摘(人・bot)は 0 件。

## 指摘

### 1. フックが発火しない実行環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されずに通った。発火ログ
  (`/tmp/design-composer-firings-<session_id>.log`)にこのセッション ID の見出しが無く、
  `/tmp` にあったのは無関係な別セッション(2026-09-19)のログだけだった。CI 層の検査は
  すべて通過しており抜けは無い。`pr-743.md` 指摘4 と同じ形が今回も出ている
- 既存ルール: `.claude/hooks/README.md`「強制力の序列 — フックが発火しない実行環境がある」

### 2. このセッションでは implementation-flow の検証(plan-reviewer / implementation-reviewer)を通していない

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: このセッションは「ハーネス棚卸しルーティン」という個別の Routine のプロンプトで
  起動しており、そこに `plan-reviewer` / `implementation-reviewer` を呼ぶ手順が無い。
  `pr-743.md` 指摘2 と同じ形(Routine のプロンプトが `CLAUDE.md`「実装の進め方」の標準フロー
  を経由しない)が今回も出ている
- 既存ルール: `CLAUDE.md`「実装の進め方」

### 3. Routine の保存済みプロンプトが `count.sh` の実装・`harness-growth` の手順番号とずれたままだった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `pr-743.md` 指摘3 で報告された `count.sh --gate`(未実装オプション)・
  `harness-growth/SKILL.md` の旧手順番号(4a/4b/4c・Step 6「棚卸し条件」)への参照が、
  このセッションでもそのまま渡ってきた。`--gate` は渡しても無視され既定の表が出て
  exit 0 になるため、Step 0 の「gate が exit 1 なら該当なしで終了」は実行できず、
  今回は `count.sh`(既定表)の出力から `harness-growth` Step 2 の基準(再発最大・同数は
  内部が少ないほう)を手作業で適用して該当を判定した。指摘から 1 回(pr-760)経っても
  Routine 側のプロンプト自体は直っていない
- 既存ルール: `.claude/skills/harness-growth/SKILL.md` / `harness/records/count.sh`

### 4. 過去の記録(pr-193)が示す「反映は記録のみで止め、オーナー承認を待つ」という先例と、今回の Routine プロンプトの契約が矛盾していた

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `harness/records/pr-193.md`「見送りを解除した経緯」は、棚卸しルーティンによる
  `rules/`・スキルへの書き込みはまず記録のみで PR を出し、オーナーの会話上の承認を経てから
  反映する、という運用を残している。一方、今回の Routine プロンプトの「成果物の契約」は
  「該当ありで差分ゼロの終了は失敗である」と明記し、記録のみでの終了を明示的に禁じている。
  今回は Routine プロンプトの明文の指示を優先し、承認を待たずに差分を含む PR を出した。
  どちらが現在の運用かを Routine のプロンプト・スキルのどちらにも書いていないため、
  次に読むセッションが同じ判断を迷う可能性がある
- 既存ルール: なし(`harness/records/pr-193.md`「見送りを解除した経緯」と今回の Routine
  プロンプトの間)

## 手戻り

無し。差分は 2 回の push(本体 → 対策済行の追記)で構成したが、いずれも CI を通過した。

## 発火

計測対象外(フック不発)

## 規約への反映

`parallel-issue-work` への介入(手順の追記・窓を閉じる `対策済` 行)は PR #760 自身の差分として
すでに行っており、`harness/records/pr-743.md`「規約への反映」に記載済み。それ以外の介入は無し。
