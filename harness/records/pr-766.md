# PR #766 🔥 [Remove]: #665 rules/naming.md の実例を判例へ落とす

- マージ日: 2026-09-25
- 関連 Issue: #665
- 差分規模: 4 ファイル / +20 -8

## ゴールと結果

ゴールは、`harness-growth`「5. 縮める」節 2 で naming を扱うこと。`rules/naming.md` から、実例として挙げていた
このリポジトリ固有のシンボル・ファイル名を外し、判例から読めるようにする。規範の対象を指す
パスと規範の文は残す。縮んだ行数ぶん `always_loaded_cap` を下げる。このゴールは達成した。
常時ロードは 814 → 813 行になった。判例に無かった `DocumentTemplate.DEFAULT` は、`harness/case-law/naming.md` に
「定数は PascalCase」節を新設して置いた。`pr-744.md` へ
`対策済: naming 層=case-law at pr-766` を足した。

CI は現在のコミット(`44da63c`)で 10 チェックすべて green だった(VRT は差分 0 / 179 件一致)。
人・bot のレビュー指摘は 0 件で、`get_reviews` / `get_review_comments` / `get_comments` の 3 つとも
確認した。`get_comments` にあるのは `github-actions[bot]` のレポートと、自分で書いた
テストケース表だけだった。

## 指摘

### 1. ゴールが「パスが残らない」と読め、規範の対象として残すパスと食い違っていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: ゴールを「固有の実例(シンボル名・ファイル名・パス)が残らない」と書いていた。一方で計画は
  `src/utils/` などのパスを残す前提だった。ゴールを「実例として挙げた綴りを消し、規範の対象を指す
  パスは残す」に言い直し、残る綴りを `grep -o` で出して 1 つずつ分類した
- 既存ルール: `implementation-flow` フェーズ 1「ゴールの確定」

### 2. 判例 naming-precedent に「`docs/` 全体」を補うと、数える範囲が 2 通りになる計画だった

- 分類: `harness`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 判例の確かめ方 3 と `implementation-reviewer.md` は、数える範囲を `docs/06-ui.md` と
  `Design Composer.html` の 2 ファイルとしている。そこへ「`docs/` 全体」を足すと、同じ節で範囲が
  割れる。判例には手を入れず、規範側をパス無しの文で残す形に変えた
- 既存ルール: `AGENTS.md`「規約を直すときは、同じファイルが既に持っている記述・例と突き合わせる」

### 3. 素の `naming` で対策済を書くと、畳まれる綴りの窓がすべて閉じる副作用が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 閉じる窓は naming-precedent / prefix-collision / vocabulary-gap / vocabulary-alignment / pr-211 の
  すべて。加えて、次に naming が再発すると case-law が置き場所の候補から外れる。この 2 つの帰結と
  許容する理由を Issue とコミット本文に書いた
- 既存ルール: `implementation-flow` フェーズ 3 手順 7(前提を崩さないか)

### 4. pr-744.md の既存文「対策済は書かない」と、足す行が並ぶと読み手が混乱する

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 既存文は書き換えず、pr-654 の前例にならって行の後に説明の段落を添えた。段落には、この行が
  節 2 の縮めによることと、どの窓を閉じるかを書いた
- 既存ルール: なし(`harness/case-law/README.md`「過去の判例を書き換えない」の記録版にあたる運用)

### 5. `at pr-<番号>` を PR 作成前には埋められないのに、埋める段取りが計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: pr-760 と同じく「PR 作成 → 番号を確認 → 対策済の行を足して 2 回目の push」を手順にした
- 既存ルール: なし

### 6. 同じ作業を扱う Issue(#637 / #675)の状態が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 確認すると、両方とも 2026-09-22 に duplicate として閉じ済みだった。#665 が残っている側
  なので対応は不要と Issue に記録した
- 既存ルール: なし

### 7. `withLocation` を消すだけだと、「find* / locate* を使わない」の OK 側が無くなる

- 分類: `naming`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 規範の語彙で、OK 側を `with*` の形にして残した
- 既存ルール: `rules/naming.md`「名前と実体を一致させる」

### 8. 却下案の理由「over-guard に順番が来ない」が SKILL.md から導けなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 節 2 には、候補から最大を選ぶ規定が無い。理由を「縮めは介入(SKILL.md 5)なので、
  SKILL.md 6 に従い起点を置く」に置き換えた
- 既存ルール: `implementation-flow` フェーズ 3 手順 5(却下理由の事実を確かめる)

### 9. 比喩・抽象語の確かめ方を縮めたとき、UI 案と「0 回なら語彙に無い」の基準まで消した

- 分類: `harness`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 指摘 2 への対応で「仕様書に何回出るかを数える」まで縮めた。すると UI 案が数える範囲から
  外れて読め、判断基準も消えていた。「UI 案・仕様書に何回出るかを数える
  (0 回ならこのリポジトリの語彙に無い)」として、1 行に収めた
- 既存ルール: `harness-growth`「5. 縮める」節 2(規範の 1 文は残す)

### 10. `with*` の説明が、同じファイルの定義とずれていた

- 分類: `harness`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 最初は「値を付け足した新しい値」と書いた。同じファイルの `with*` の定義は「その値を持つ
  新しい値」で、判例の `ColorToken.withRgb` は置き換えの操作にあたる。定義と同じ語に揃えた
- 既存ルール: `AGENTS.md`「規約を直すときは、同じファイルが既に持っている記述・例と突き合わせる」

### 11. 判例の NG 列に、このリポジトリで一度も使われていない `DocumentTemplate.default` を書いた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `git log -S` で 0 件だった。実在した `DocumentTemplate.DEFAULT`(pr-148 の記録・`7c60984` 以前)
  だけにした
- 既存ルール: `claim-verification`(comment-structure-claim)

### 12. 判例に新設した節のリード文と理由列が、規範の書き写しだった

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: リード文を消し、列を「何だったか」(実物の説明)にした。理由は規範側だけが持つ
- 既存ルール: `rules/coding.md`「規約の適用範囲」(同じ判断根拠の doc の重複)

### 13. 判例に「`DocumentTemplate.createXxx()` と並ぶ」と、実物を見ずに書いた

- 分類: `comment`
- 出どころ: `自己修正`
- 内容: 書いた直後に `src/domains/dcmp/design-document/template/index.ts` を開いた。メンバーは `Default`
  だけでメソッドは無かったので、実物に合わせて書き直した
- 既存ルール: `claim-verification`(comment-structure-claim)

### 14. フックが発火しない実行環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されずに通った。plan-reviewer / implementation-reviewer を通した後も
  検証エージェントのマーカーが無く、このセッションの発火ログも無かった(`/tmp` にあったのは別
  セッション `50f8c937…` のログだけ)。README の表では「本当に不発」に当たる。禁止コマンドは実行して
  いない。push 前の検査は手元で 1 つずつ実行して、すべて通った
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- 計画からの外れは 1 件。plan-reviewer への対応で比喩の数え方を削りすぎ、implementation-reviewer の
  指摘で基準を戻した(指摘 9)。外れた時点で Issue に追記した
- 2 回目の push(対策済の行)で、1 回目のコミット `111d204` の `frontend` / `Storybook Visual Regression`
  が打ち切られた。VRT の sticky コメントは一時的に「レポートが見つかりませんでした(verify: failure)」
  になったが、現在のコミットの実行で「差分なし」に上書きされた。失敗ではなく打ち切りなので、CI の
  指摘としては数えない

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。この回の縮め(`naming` 層=case-law)は、PR #766 自身の差分として
`harness/records/pr-744.md` の `at pr-766` で締めてある。このスキルの管轄は記録を 1 ファイル足す
ところまでで、集計と介入は `harness-growth` が行う。
