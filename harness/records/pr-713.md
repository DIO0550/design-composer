# PR #713 撮影範囲の検査を push 前手順(層 4)へ載せる

- マージ日: 2026-09-22
- 関連 Issue: #664
- 差分規模: 2 ファイル / +52 -1

## ゴールと結果

ゴール（撮影範囲からはみ出したストーリーを push の前に手元で検出できるようにし、残る穴を
`.claude/hooks/README.md` に明記する）は達成した。Issue が判断を求めていた 3 点のうち、
「載せないなら『なぜ CI だけで足りるのか』を書く」は**足りていない実例がある**ため採らず、
載せたうえで層 4 に残る穴を書く形にした。`rules/ui-verification.md` のビューポートの行は
#712 へ分離し、常時ロードは 1 行も触っていない。

## 指摘

### 1. 走らせる条件が、この Issue の発端そのものを捕まえない

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 条件を「`src/` の `.tsx` / `.css`・`.storybook/`」と拡張子で絞っていたが、はみ出しを
  main へ入れた PR #634 の差分は `docs/*.md` 2 件 + `src/**/*.ts` 12 件で `.tsx` / `.css` は 0 件。
  パネルの行はスキーマの `.ts` が決めるので、この条件では発端の push で 1 度も走らない。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「計画を渡す前に踏む 8 つの手順」

### 2. 同じ判断根拠を SKILL.md と README の 2 箇所に書く計画だった

- 分類: `duplication`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「重いから無条件にしない」を両方が独立に説明する形。既存の先例（SKILL.md の
  `check-pr-closing-issue-cases.sh` の行）は理由を書かず参照 1 行にしている。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 3. 層 2 の却下理由に「環境で割れる」を挙げていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `harness/githooks/README.md`「道具が無い環境では、その道具を使う検査だけを飛ばす」が
  まさにその形を許容しており、`pre-push` は 3 段で飛ばしている。却下理由にならない。
- 既存ルール: `harness/githooks/README.md`「道具が無い環境では…飛ばす」

### 4. 層 3 の却下理由が、層 3 全体の性質だった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「フェイルオープンかつサイレント」は既存の `pre-push-*.sh` すべてに当てはまるのに
  却下理由へ置いていた。所要時間と位置づけだけに絞り直した。
- 既存ルール: `.claude/hooks/README.md`「強制力の序列」

### 5. `rules/` の 1 行書き換えについて、分割の判断を書いていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: Issue の決定 3 は「載せないなら」の条件付きで、載せる今回は必須ではない。独立して
  マージできる別の変更なので、分ける／分けないの判断が要る。#712 として分離した。
- 既存ルール: AGENTS.md「タスクの分割」

### 6. 撮影サイズを `rules/` へ写すと実測値の二重管理になる

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 値の出どころは `storybook-visual-regression.mjs` の `--width` / `--height` の既定。
  「撮影と同じ大きさ」の意味に付け替えた瞬間に既定値の写しになる（pr-343 指摘 11 と同じ形）。
- 既存ルール: rules/coding.md「コード内の値の再掲」

### 7. 「1280x720 以上」から「以上」を落とすと、Storybook 以外の表示確認まで狭まる

- 分類: `ui`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: その行は `## ルール` の下にあり、`pnpm dev` でのブラウザ確認にも効く。アプリ全体を
  縦長のビューポートで一度に見る確認ができなくなる。#712 の判断材料へ移した。
- 既存ルール: rules/ui-verification.md「表示確認」

### 8. 「未決の判断: 無し」と書いたが、実装中に手が止まる判断が 3 件残っていた

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: フェーズ 7 のどこへ置くか（件数「下の 15 個」を崩さない位置）、README のどの見出しの
  下へ置くか（既存の表は層 3 のフック用で足せない）、「以上」を落としてよいか、の 3 件。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「未決の判断」

### 9. 残る穴の列挙が薄かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: ゴールの後半が「穴が明記されている」ことなので、列挙が薄いとゴール未達になる。
  道具・条件の外・`visual:compare` を含む候補を挙げ直した。
- 既存ルール: `.claude/hooks/README.md`「カバー範囲と残る穴」

### 10. 手順に書く綴りが CI と同じかを確かめる手順が計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 綴りがずれると「CI と同じ検査」という前提が黙って嘘になる。`grep -nF` 2 本で
  `storybook-visual-regression.yml` の 95 行 / 107 行と突き合わせる手順を計画へ入れた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 3

### 11. 却下案に、いちばん近い代替（表示確認の手順 / フェーズ 5 へ載せる）が無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: UI を触ったときの確認手順は `rules/ui-verification.md` に集約されており、そちらへ
  載せる案が一覧から漏れていた。探して却下したのか挙げ忘れかが読めない状態だった。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 6

### 12. 据え置いた `--only` の後続をどうするかが書かれていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 条件を広げると 3 分半を払う回が増えるので、縮める手段として再浮上しうる。起票せず
  据え置く理由（痛みが出ていない／絞ると共有部品経由の伸びを取りこぼす）を Issue へ書いた。
- 既存ルール: AGENTS.md「タスクの分割」

### 13. 「Chrome の有無で黙って飛ぶ」と書いたが、飛ばしたことは出力に残る

- 分類: `comment`
- 出どころ: `自己修正`
- 内容: `harness/githooks/README.md` は「飛ばしたことを出力に『飛ばします』と残す」と書いて
  いる。`claim-verification` で計画の主張を当て直したときに気づき、Issue で訂正した。
- 既存ルール: `.claude/skills/claim-verification/SKILL.md`

### 14. 「3 本が無関係な差分で赤を踏んだ」は 1 本について事実と違った

- 分類: `comment`
- 出どころ: `自己修正`
- 内容: `pr-651.md` 指摘 19 は「この PR で行が 1 段高くなって閾値を越えた」と書いており、
  自分の差分も効いている。Issue の記述を「pr-639 / pr-654 が無関係な側」に訂正した。
- 既存ルール: `.claude/skills/claim-verification/SKILL.md`

### 15. 「`documentElement.scrollHeight` しか見ない」は実装と違う

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: プローブは `scrollWidth` も読み、判定も `scrollWidth > width || scrollHeight > height`
  なので横のはみ出しでも落ちる。「`documentElement` の実寸だけ」へ書き換えた。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 16. 「クリップされた中身は数えない」を README へ写していた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: これは `storybook-visual-regression.mjs` の doc の主張で、`pr-634.md` 指摘 26 が
  前提の崩れを実測済み（#645）。写すと崩れている前提が 2 箇所へ増える。
- 既存ルール: rules/coding.md「理由の根拠に、いま直されつつある一時的な事実を置かない」

### 17. 残る穴の列挙から、いちばん重い「手元で通っても CI と等価ではない」が落ちていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `pr-651.md` 指摘 20 は、手元で 180 本すべて撮れたのに CI では 6 本が枠外だった形
  （CI は `fonts-noto-cjk`、手元は書体が違い折り返しが変わる）。README と手順の両方へ足した。
- 既存ルール: rules/ui-verification.md「表示確認」

### 18. 「条件の外」の例示が、起こりえない機序を挙げていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `.storybook/main.ts` の `stories` は `../src/**/*.stories.@(ts|tsx)` だけなので、
  `docs/` の差分でストーリーの高さは動かない。実際の形（main が壊れている間に踏む）へ替えた。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 19. 動機として挙げた「踏んだ側」2 件は、決めた条件では走らない

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `pr-639.md` 指摘 19 / `pr-654.md` 指摘 21 はどちらも `src/` を触っていない。読む人が
  「踏んだ側もこの手順で防げる」と読む書き方だったので、動機は入れる側に一本化した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 20. 同じ実測値と同じ物差しを、同じファイルの 2 箇所に書いていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 35.9 秒 / 20.3 秒を新しい節が再掲していた。加えて「1.5 倍」が何に対する比かが元の
  記述と入れ替わっていた。数値の再掲をやめ「`pre-push` 全体の 6 倍」だけにした。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 21. SKILL.md の bullet が「何を見る検査か」の 3 箇所目になっていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `rules/ui-verification.md` と README 新節に既にある説明を 3 度書いていた。計画の方針
  （理由は README に一本化）に照らして参照へ寄せた。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 22. 新しいコードブロックで「下の 15 個」の指す先が読み違えやすくなった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: bullet 群の直後にブロックが付いたため、「下の」が新ブロック（どちらも `pnpm` の
  スクリプトに**ある** 2 コマンド）と読める。「一覧の下 15 個」へ書き換えた。件数 15 は不変。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 23. 「`visual:compare` は手元では走らせられない」は言い過ぎだった

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: CI も gh-pages から 2 行で baseline を取っているだけなので、手元でも取れる。
  「手間があるので手順に入れていない」へ直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 24. フックが発火しない環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されず、検証エージェントのマーカーも発火ログの見出しも
  このセッションのものは無し（`/tmp` にあったのは別セッションの残骸）。README の表の
  「本当に不発」の枝。`npx` の実行と検証エージェント実行中のコミットが無いことを手で確認した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- **走らせる条件を計画の途中で広げた。** 拡張子で絞る案（`.tsx` / `.css`）から「`src/` 配下
  すべてと `.storybook/`」へ。PR #634 のマージコミットの差分を数え直して決めた（指摘 1）
- **`rules/ui-verification.md` を触る計画を取り下げた。** 3 つの問題（分割の判断・実測値の
  二重管理・表示確認が狭まる）が重なったため、#712 を起票して分離した（指摘 5・6・7）
- **README の新節と SKILL.md の bullet を、実装の検証後に書き直した。** 事実の誤り 5 件と
  重複 2 件（指摘 15〜23）。書いた時点で `claim-verification` を通していたのは計画側だけで、
  実装側の記述は検証エージェントに拾われた

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。この回は Issue #664 の実装（手順とハーネスの doc への追加）で、記録を数えた結果の
介入ではない。`対策済` を書けるのは `harness-growth` の棚卸しだけ。
