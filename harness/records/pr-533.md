# PR #533 📝 [Doc]: #531 hook-environment の積み残しを解消し、なし の逃し弁で 4 語彙を昇格する

- マージ日: 2026-09-14
- 関連 Issue: #531
- 差分規模: 6 ファイル / +52 -9（最終形。作業中は 10 ファイルだったが、下記のとおり 4 ファイル分が重複で破棄になった）

## ゴールと結果

ゴールは「`count.sh` の最上位が実測を表すようになり（`hook-environment` の締めの `対策済` 行の積み残しを解消）、`分類: なし` に 2 件以上まとまっていた亜種が語彙・観点・数え直しの 3 箇所で使える状態になる」。**達成したのは PR #532 のほう。**

**同じ棚卸しを PR #532（`claude/gifted-fermi-ayisir`）が並行して実装し、先にマージされた。** main を取り込んだ時点で 4 ファイルが衝突して発覚し、重複部分（語彙表 4 行 / `implementation-reviewer` の観点 3 節 / `hook-environment` の `対策済` 1 行 / `なし` の `対策済` 5 行）はすべて main 側を採った。この PR に残ったのは main に無い 2 つだけ。

- `harness-process-drift` を層 1 へ下ろす配線（CI 限定だった `check-added-lint-suppressions.sh` / `check-added-test-helper-duplication.sh` を `harness/githooks/pre-push` と `implementation-flow` フェーズ 7 の push 前コマンド一覧へ足し、`harness-growth` の配線の行と 2 つの README を実体に合わせた）
- 判例 `harness/case-law/process.md`「単発」の 12 行

`hook-environment` の積み残し解消と 4 語彙の昇格そのものは main（#532）が入れたので、この記録はそちらの成果を数えない。

## 指摘

### 1. `harness-process-drift` の却下 2 件が、それ自体で 2 件の組になっていた

- 分類: `plan-rejection-reasoning`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画は pr-478#2・#3（起動プロンプトと SKILL.md のずれ）を「対象がルーティン起動プロンプトで `implementation-reviewer` の観測範囲外」を理由に `harness-process-drift` から外していた。これは**置き場所を先に決めてから逆算した理由**で、グルーピングの理由になっていない（逃し弁は「グルーピング → 層の選択」の順）。さらに `harness/records/pr-478.md` 自身が「`なし` が今後まとまって出るようであれば、この 2 件も候補に含めて数えてほしい」と申し送っており、計画はそれに触れていなかった。4 件で 1 語彙にする形へ直した（なお main は 2 件のまま昇格させており、最終的には main の判断が残っている）
- 既存ルール: implementation-flow フェーズ 3 手順 5
- 次にどう防ぐか: 今回は記録のみ

### 2. 層の選び方が `rules/` との比較でしか書かれていなかった

- 分類: `plan-rejection-coverage`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画の却下案は「4 語彙を `rules/` へ足す案 → 機械判定できないため観点層」の 1 行だけだった。`rules/` は観点より**下**の層なので、`harness-growth` 2a が要求する層 1（linter / 自前スクリプト）・層 2（新しいスキル）の Why not になっていない。実際に確かめたところ `check-added-*` の 2 本は引数なしで `origin/main` を base に走り、**層 1 が成立した**（手元で実行して 2 本とも exit 0）。pr-502 の指摘 17 自身が「push 前コマンドへ足すかを判断する材料にする」と申し送っていたのも見落としていた
- 既存ルール: `harness-growth`「Step 2a」/ `harness/case-law/process.md`「層を選ぶときの実測」
- 次にどう防ぐか: 今回は記録のみ

### 3. 語彙ごとの「すり抜け」を数えていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `harness-growth` Step 1 は「すり抜けが 0 の分類へ観点を厚くしても再発は止まらない。どこが漏れているかはすり抜け列にしか出ない」と書いているのに、計画は語彙ごとのすり抜けを出していなかった。数え直すと `comment-excess` が 2、他 3 語彙は 0（全件 `implementation-reviewer` 由来）で、後者へ観点を足す目的は再発を減らすことではなく数えられるようにすることだと分かった
- 既存ルール: `harness-growth`「Step 1」
- 次にどう防ぐか: 今回は記録のみ

### 4. `comment-excess` の定義が昇格根拠の 3 件目を覆っていなかった

- 分類: `rules-consistency`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 計画の定義は「型・テスト・lint が守っている理由や規約の写しを削らず書き残した形」だったが、pr-487#11 は「却下理由を doc に 5 行書いた（理由は 1 つにつき 1〜2 行）」で、写しでも型/テストが守っている理由でもない。3 件で昇格する根拠と定義が 2 件分しか噛み合っていなかった。定義に「理由 1 つが 1〜2 行を超えて実測の数字・経緯まで書かれている形」を含めた
- 既存ルール: AGENTS.md「規約に足すときは、同じファイルが既に持っている記述・例と突き合わせる」
- 次にどう防ぐか: 今回は記録のみ

### 5. 既存段落を完了形へ直す変更がファイル表に無かった

- 分類: `plan-comment-reference`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `templates/record.md` の末尾が「**後続の棚卸しルーティンが `harness/records/pr-467.md` へこの 1 行を追記し、積み残しを解消することを想定する。**」で終わっており、この計画を実行した瞬間に古くなる。pr-301・pr-352 の段落はどちらも「解消した。」と完了形に直してあるのが前例。計画のファイル表は record.md について「語彙表 4 行」「書き方の例へ 1 段落」しか挙げていなかった
- 既存ルール: `plan-review.md`「ファイル表に挙がっているか」
- 次にどう防ぐか: 今回は記録のみ

### 6. `comment-excess` の観点の挿入位置が、既存の表と受け皿文を割っていた

- 分類: `ownership-reasoning`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「書くべき Why / Why not がそもそもコードに無い」表の**直後**へ入れる計画だったが、その位置は表と「上のどれにも当てはまらない…単発の形は `comment-missing`」という受け皿文の間だった。さらにその表が属する節の前提は「実際に壊して確かめる」で、コメントの書きすぎはミューテーションでは確かめられない。既に「過剰なフォールバックの観点」「過剰なブロックの観点」が並んでいるので、そちらへ「過剰なコメントの観点」として置き直した
- 既存ルール: rules/architecture.md「ロジックの帰属先」（rules/coding.md「規約の適用範囲」によりハーネス自身の doc にも及ぶ）
- 次にどう防ぐか: 今回は記録のみ

### 7. 新設する節の位置と、毎回の「無し」のコストが書かれていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `implementation-reviewer.md` は「該当が無い観点も『無し』と書く」ので、節を 3 つ足すとすべてのレビューで 3 行の「無し」が増える。計画は挿入位置も、常時コストを払ってでも節として立てる理由も書いていなかった
- 既存ルール: `plan-review.md`（変更の帰属先）
- 次にどう防ぐか: 今回は記録のみ

### 8. 単発 12 件を既存の単発表と突き合わせていなかった

- 分類: `plan-rejection-coverage`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `harness/case-law/process.md`「単発」の表自身が「2 件目が出たら語彙へ昇格する候補」と宣言しているのに、計画には「22 − 10 = 12 を単発へ」としか無く、既存 16 行との突き合わせも 12 件同士の組の有無も書かれていなかった。突き合わせた結果、pr-459#23 と既存の pr-258 の行は別物（隣と揃っていない形 / 書式から外れた形）、pr-459#23 と pr-467#46 は対象がコメントとコードの綴りで別、と判断して組にしなかった
- 既存ルール: implementation-flow フェーズ 3 手順 6
- 次にどう防ぐか: 今回は記録のみ

### 9. 検算の期待値が「最上位から消える」だけで、番号を打ち間違えても通った

- 分類: `plan-goal-verification`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `hook-environment` の検算が「最上位から消えること」だけだったので、`at` の番号を打ち間違えても（例: pr-476）条件が成立してしまう。「再発 8 / 以降 13 / 最終介入 pr-467（層=観点）」まで数字で書き、実行して一致を確かめる形に直した
- 既存ルール: `plan-review.md`「ゴールの達成を確かめる手段」
- 次にどう防ぐか: 今回は記録のみ

### 10. `対策済` を足す書式が「1 行」「5 行」としか書かれていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `pr-467.md` も `pr-516.md` も「規約への反映」本文が「無し。」で始まるので、行だけ足すと本文と矛盾する。前例（pr-442・pr-352・pr-438）は例外なく `---` ＋ 太字見出し ＋ 経緯 1 段落を伴っている
- 既存ルール: `harness-record/templates/record.md`「書き方の例」
- 次にどう防ぐか: 今回は記録のみ

### 11. 誰の権限でこの変更を行うかが計画に無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `対策済` 行・判例・語彙表はいずれも「書けるのは `harness-growth` だけ」と明記されている。この回が `harness-growth` の Step 4 として走っていることを計画に書いていなかった
- 既存ルール: `harness-record/templates/record.md` / `case-law/README.md` / `harness-growth`「Step 2c」
- 次にどう防ぐか: 今回は記録のみ

### 12. 追記後に新たに発火する分岐を、スコープ外と明示していなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `hook-environment` の `対策済` を足すと再発 8 で 2c に、`hook-environment-guard-miss` は未介入・再発 2 で 2a に該当する。今回扱わないなら理由を残さないと、次に数える人が同じ判断をやり直す
- 既存ルール: AGENTS.md「分けないと決めた場合も、その理由を Issue に書く」
- 次にどう防ぐか: 今回は記録のみ

### 13. 同じ判断根拠を doc の 3 箇所に書いた

- 分類: `duplication-uncovered-shape`
- 出どころ: `自己修正`
- 内容: 「CI だけが走らせていて 2 回落ちた」という同じ根拠を、`harness/githooks/pre-push` のコメント・`implementation-flow` の push 前コマンド一覧の注記・`templates/record.md`「書き方の例」の 3 箇所に書いていた。`implementation-reviewer` が起動できず自分で差分を読み直したときに見つけ、`pre-push` 側を参照 1 行へ縮めた
- 既存ルール: rules/coding.md「規約の適用範囲」（同じ判断根拠を説明する doc の重複）
- 次にどう防ぐか: 今回は記録のみ

### 14. 同じ棚卸しを 2 つの PR が並行して実装した

- 分類: `なし`
- 出どころ: `自己修正`
- 内容: Issue #531 は Step 0 で「open な `harness-inventory` ラベルの PR も無いため続行」と判断していたが、実際には同じ棚卸しを実装する PR #532 が並行して存在し、先にマージされた。main を取り込むまで気づかず、語彙表 4 行・観点 3 節・`対策済` 6 行の作業がすべて破棄になった。**ラベルで探す前提が機能していない**（#532 側もラベルを付けていなかったか、Issue を立てる前だった）
- 既存ルール: なし（並行する同種の作業を着手時に検出する手順が無い）
- 次にどう防ぐか: 今回は記録のみ（`harness-growth` の Step 0 の探し方が対象）

### 15. `implementation-reviewer` が 2 回とも起動直後から進捗せず停止した

- 分類: `なし`
- 出どころ: `自己修正`
- 内容: フェーズ 6 で 2 回起動したが、どちらも最初のステップ（差分を読む）から進捗せず、transcript が 110 バイトのまま 14 分 / 11 分止まった。同じセッションで `plan-reviewer` は完走しているので断続的な実行環境の問題。検証を飛ばさず、機械的に検算できる部分（昇格根拠 22 件の突き合わせ・README と `pre-push` の一致・`count.sh` の期待値・綴りの一致）を自分で実施し、その過程で指摘 13 を見つけた。**意味理解が要る観点の重複判定とミューテーション実測は未実施のまま PR を出した**
- 既存ルール: なし（`subagent-control` は「指示に反して書き換えた / 結果を取り逃した」形で、エージェントが起動しない形を指していない）
- 次にどう防ぐか: 今回は記録のみ

### 16. Issue 本文の「残り 14 件」が実数と違った

- 分類: `plan-scope-count-verification`
- 出どころ: `自己修正`
- 内容: Issue #531（棚卸しルーティンが書いた）は「22 件のうち 4 亜種を昇格し、残り 14 件は単発」と書いていたが、昇格 10 件を引いた実数は 12 件。計画段階で全記録を機械走査して気づき、Issue へ訂正を書いてから進めた。**main（#532）は訂正せずマージされている**ので、`harness/records/pr-516.md` には「14 件」が残り、判例側に差の理由を 1 行添えて整合させた
- 既存ルール: implementation-flow フェーズ 3 手順 2
- 次にどう防ぐか: 今回は記録のみ

## 手戻り

- **重複の発覚で 4 ファイル分の作業が破棄になった。** 語彙表 4 行・`implementation-reviewer` の観点 3 節・`pr-467.md` の `対策済` 1 行 + 経緯・`pr-516.md` の `対策済` 5 行 + 経緯。すべて main 側を採用したので、この PR の差分には 1 行も残っていない
- **単発の件数を 2 度組み替えた。** 12 件（当初）→ 10 件（`harness-process-drift` を 4 件にした結果）→ 12 件（main が 2 件で昇格させていたのに合わせて戻した）
- **計画を `plan-reviewer` へ渡してから 12 件の指摘で書き直した。** とくに `harness-process-drift` のグルーピング（2 件 → 4 件）と層（観点 → hook）は計画の骨格が変わる修正で、Issue に修正版の計画を書き直している
- **`implementation-reviewer` の停止で約 25 分を待ちに使った。** 2 回目の停止を確認してから自分で機械的検算に切り替えた

## 発火

- 発火: 計測対象外(フック不発)

## うまくいったこと

- **Issue の記述を鵜呑みにせず、全記録を機械走査して数え直した。** pr-444 より後の 19 記録から `分類: なし` を抽出するスクリプトを書き、22 件・指摘番号・出どころまで実データと突き合わせた。「残り 14 件」の誤り（指摘 16）はこれで見つかり、後に判例の整合にも使えた
- **層 1 が成立するかを、読むだけでなく実行して確かめた。** `check-added-*` の 2 本を引数なしで走らせて `origin/main` を base に exit 0 になることを実測してから、層 1 へ下ろす判断をした。push の出力にも実際に 2 検査が走っていることが出ている
- **検証エージェントが動かなくても検証を飛ばさなかった。** 機械的に検算できる部分を自分で実施し、その過程で見つけた doc 重複 1 件を直して push した。**できなかった部分（意味理解が要る観点の重複判定・ミューテーション実測）は PR 本文と Issue に「未実施」と明記した**
- **衝突の解消で main 側を採る判断の理由を書いた。** `対策済` の `at` 番号を書き換えると `count.sh` の数え直しの起点が動く、という機械的な理由をマージコミットに残している

## 規約への反映

無し。このスキルの管轄は記録を 1 ファイル足すところまでで、集計と介入は `harness-growth` が行う。

**数える人へ 3 つ申し送る。**

1. **指摘 14（並行する棚卸し PR に気づかなかった）と指摘 15（検証エージェントが起動しない）は、どちらも `分類: なし` だが性質が違う。** 前者は `harness-growth` の Step 0 の探し方（ラベルで探す）が機能していない形で、手順の側に穴がある。後者は実行環境の問題で、手順では防げない。同じ `なし` として数えるときはこの 2 つを混ぜないでほしい
2. **`harness-process-drift` の層の表記と実際の置き場所がずれている。** main（#532）は層=観点 at pr-502 で締めているが、この PR が `harness/githooks/pre-push` と `implementation-flow` の push 前コマンド一覧へ下ろしたので、実際の置き場所は層 1（hook）。層を上げ直す `対策済` 行を足すかは次の棚卸しの判断
3. **`hook-environment-guard-miss` は未介入・再発 2 のまま。** 2a（未介入で 2 回以上）に該当する。#532 も #533 も `対策済` 行を足していないので、カウンタは一度も数え直されていない
