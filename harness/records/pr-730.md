# PR #730 src/utils/ のコンパニオンオブジェクトのメソッドの doc の抜けを埋める（#720 層 1）

- マージ日: 2026-09-23
- 関連 Issue: #727
- 差分規模: 14 ファイル / +406 -20

## ゴールと結果

ゴール（`missing-doc-comments.py --all src/utils --include-methods` が 0 件になる）は達成した。着手時点は
doc 無し 35 件 / 項目の欠け 26 件 / 14 ファイル。doc だけの差分で挙動は変えていない。検出器は `@param` が
1 つあれば通すので、引数ごとの `@param` の有無は使い捨てのスクリプトで別に突き合わせた。型で閉じていない
前提と規約との食い違い 5 件は #729 に切り出した。CI は 1 回目で全チェック通過、VRT の差分 0 件、
人のレビュー指摘 0 件でマージされた。

## 指摘

### 1. `StringEx.isDigit` を「半角 0〜9 のみ」と計画に書いていた

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 実装は文字列の大小比較なので `isDigit("10")` / `isDigit("5abc")` も true。`@param ch` に 1 文字の
  前提と、2 文字以上で起きることを書いた。前提が型で閉じていない点は #729 へ。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 2. `toCapitalCase` の PascalCase 入力の挙動が計画から抜けていた

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `toCapitalCase("PositionX")` は `" Position X"`（先頭に空白）。`@param camelCase` に先頭が小文字で
  あることと、PascalCase を渡したときの結果を書いた。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 3. `ArrayEx.distinct` を「=== 比較」とだけ書くと NaN の扱いが誤る

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `indexOf` は NaN を見つけられないので NaN は全部消える（`prependIfAbsent` は `includes` なので
  見つける）。`@returns` に「NaN は残らない」を書き、基準の食い違いは #729 へ。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 4. ゴールの確かめ方が検出器 0 件だけだった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 検出器は doc に `@param` が 1 つあれば通すので、「doc に書く項目に沿う」を確かめられない。
  引数ごとに `@param <名前>` を数える使い捨てのスクリプトを走らせることにした。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3「計画を渡す前に踏む 8 つの手順」

### 5. 却下案「検出器側で自明なメソッドを外す」の理由を実例で確かめていなかった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 「全件に書く中身があった」と書いたが、`Result.ok` / `Option.some` など薄い 8 件に何を書くかが
  計画に無かった。1 件ずつ書く中身を計画に足し、#719 へ持ち帰る対象は無いと確かめた。
- 既存ルール: `.claude/skills/implementation-flow/SKILL.md` フェーズ 3 手順 5

### 6. `combine3`〜`6` の `@param` を一部の引数だけにする判断が規約の表から外れていた

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: 前例を探すと、引数の一部だけに `@param` を書いた doc はリポジトリに 0 件だった。全引数に書き、
  `b` 以降は「`build` の何番目の引数になるか」を書く方針へ変えた。
- 既存ルール: rules/coding.md「doc に書く項目」

### 7. 既存の概要文が実装と食い違っていたのを直す対象に入れていなかった

- 分類: `comment`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `ElementEx.isTextEditable` はチェックボックスを含むすべての `input` で真、`Css.escapeQuotedString`
  は改行を escape しない。項目を足すだけでなく概要文も書き換える対象にした。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 8. doc に書いて据え置く規約違反の後続が無かった

- 分類: `plan`
- 出どころ: `レビュー（plan-reviewer）`
- 内容: `Json.optional` の `undefined` 返し、`Json.parseText` の `try/catch` を doc で説明した時点で
  据え置きを決めたことになる。1〜3 と合わせて 5 件を #729 に立てた。
- 既存ルール: `AGENTS.md`「着手した Issue は、その回で閉じる」

### 9. `combine` の `@returns` が `ok` に包んで返すことを落としていた

- 分類: `comment`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「すべて成功なら `build` の戻り値」と書いたが、実装は `Result.ok(build(...))`。`Option.map` /
  `Result.map` の書き方と揃えて、5 箇所とも「`build` の戻り値の `ok`」に直した。
- 既存ルール: rules/coding.md「コメントは実装と一致させる」

### 10. `Result.unwrap` と `Option.unwrap` に同じ理由を二重に書いていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「テストコードだけ」の理由を `Result.unwrap` に足したが、`Option.unwrap` に同じ文があり、
  しかも `Result.unwrap` を参照していた。`Result.unwrap` 側の 2 行を消した。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 11. `isKebabCase` の `@returns` が定数の doc の規則を言い換えていた

- 分類: `duplication`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: `KebabCasePattern` の doc が既に規則を定めていた。`@returns` は「`KebabCasePattern` の規則どおりの
  綴りなら true」だけにした。
- 既存ルール: rules/coding.md「規約の適用範囲」

### 12. `Font.systemStack` の doc に唯一の呼び出し元の用途を書いていた

- 分類: `ownership`
- 出どころ: `レビュー（implementation-reviewer）`
- 内容: 「フォントファミリ未指定のときに書き出す値」は `src/domains/dcmp/token/typography` 側の用途で、
  utils に用途が入り込む。概要を「OS 標準フォントへ順にフォールバックするフォントスタック」に直した。
- 既存ルール: rules/architecture.md「utils の形式」

### 13. フックが発火しない環境だった

- 分類: `harness`
- 出どころ: `自己修正`
- 内容: `echo hook-canary` が deny されず、検証エージェントのマーカーもこのセッションの発火ログの見出しも
  無かった（README の表で「本当に不発」）。起動時の作業ディレクトリがリポジトリの親（`/home/user`）。
  `npx` / `git add -A` の実行と、検証エージェント実行中の git 操作が無いことを履歴で確認した。
- 既存ルール: `.claude/hooks/README.md`「発火しているかを確かめる(カナリア)」

## 手戻り

- **計画を plan-reviewer の指摘 8 件で直した**（指摘 1〜8）。計画に書いた挙動のうち 3 件が、実際に動かすと
  実装と食い違っていた（計画前の確認スクリプトは、書いた主張しか確かめていなかった）
- **実装を implementation-reviewer の指摘 4 件で直した**（指摘 9〜12）。`Result.unwrap` の理由の削除は
  計画に無かった変更で、Issue に追記した
- push 前検査 21 項目（typecheck / lint / biome / test / ハーネスの検査 17 本）と Storybook の撮影範囲、
  CI は 1 回目で通過した

## 発火

- 発火: 計測対象外(フック不発)

## 規約への反映

無し。この回は Issue #727 の実装で、記録を数えた結果の介入ではない。`対策済` を書けるのは
`harness-growth` の棚卸しだけ。
