# design-composer 仕様書: 概要

ローカルで動く、宣言的データ駆動のデザインツール。部品も画面も同じデータモデルで扱い、両方を主役にする。

## コンセプト

スキーマで縛った宣言的データ（JSON）から、実UIをそのまま組み立てるデザインツール。

- **Figma との違い**: Figma はベクターで描き直すため実コードと切れる。design-composer は実HTML/CSSをそのまま構成する。
- **pattern-lab 系との違い**: パターンをドキュメントとして見せるのではなく、データから部品・画面を組み立てて編集できる。
- **AI フレンドリー**: source of truth は JSON。可読性の受益者は AI と Git diff のレビューであり、人間の閲覧はアプリが担う（人間はファイルを直接読まない想定）。

## 形態・スタック

- デスクトップアプリ（Tauri v2 + React + TypeScript）
- レンダリングは webview 上の実HTML/CSS

## 仕様書の構成

| ファイル | 内容 | 状態 |
|---|---|---|
| 00-overview.md | 概要・コンセプト | 決定済み |
| 01-file-format.md | ドキュメントフォーマット | ほぼ確定（バージョニング・ノード識別が未定） |
| 02-data-model.md | ノード・props・部品参照 | ほぼ確定（overrides範囲・ノード識別が未定） |
| 03-schema.md | プリミティブ語彙・スキーマ・コンパイル・検証 | 確定 |
| 04-tokens.md | デザイントークン・初期テンプレート | 確定（初期セットは叩き台） |
| 05-architecture.md | core/UI分離・モジュール・IPC・保存モデル | 確定 |
| 06-ui.md | 画面構成・キャンバス操作・部品化/解除 | 確定 |
